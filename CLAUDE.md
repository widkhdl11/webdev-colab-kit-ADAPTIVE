# CLAUDE.md — 협업 규칙 (핵심만, 상세는 라우팅)

이 프로젝트는 사용자와 함께 웹사이트를 만드는 협업 세션이다.
오케스트레이터는 사용자다. 단계를 임의로 넘어가지 않는다.

## 세션 시작 시

1. SessionStart 훅의 브리핑을 확인한다 (대기 결정·게이트·멈춘 지점·다음 할 일·프론티어)
2. kickoff은 **무엇을 만들지가 아직 합의되지 않은 상태**를 푸는 인터뷰다. 활성 프로젝트가 없거나
   PRODUCT.md가 비어 있는 것이 그 신호다. 단, 사용자가 만들 것을 이미 구체적으로 말했으면 인터뷰를
   다시 돌리지 말고 받아적어 PRODUCT.md로 만들고 확인만 받는다 — 아는 걸 다시 묻는 것이 인터뷰의 실패다.
   만들 물건이 **이미 동작하는 코드로 존재하면 kickoff이 아니라 intake다** — 합의문의 원본이 사람
   머릿속이 아니라 코드에 있으니, 물어서 만들지 말고 읽어서 만든 뒤 확인받는다. 골격만 있고 동작하는
   코드가 없으면 여전히 kickoff이다
3. 브리핑의 `◆ 프론티어(지금 작업할 노드)`가 "다음 할 일"의 1차 근거다 — dirty인 최상류 노드부터 작업한다 (실행 그래프: 아래 아키텍처)

## 항상 지키는 규칙 (이 파일에만 존재)

- 결정마다 등급으로 가른다. 되돌리기 싼 것 — 간격·문구·정렬·네이밍·기본값, 그리고 커밋 시점과
  분리·push·폴더 정리·파일 위치와 이름·검사 항목 구성·기존 의존성 안의 구현 선택 같은 **살림 판단** —
  은 실무 표준이나 정책 규칙을 근거로 결정하고 로그에 남기고 진행한다. 묻지 않는다.
  근거가 될 규칙이 없거나 되돌리기 비싼 표면이면 결정하지 말고 보류로 올린다 —
  **올리되 실행은 멈추지 않는다.** 올린 항목이 막는 노드만 세워 두고 나머지 일은 계속 한다.
  **진행을 실제로 막는 것은 게이트뿐이다**(위험 표면 스펙 부재·검사 실패 등) — 보류를 열었다고
  게이트가 풀리지 않는다. 어느 영역이 어느 등급인지의 정본은 `gates/lib/cycle-policy.mjs` 의
  `DECISION_GRADES` 다 (등급·보류·로그 상세: docs/references/decision-layer.md)
- 구현 전 한 문단 합의문은 **escalate 등급 결정이 하나라도 있는 작업에만** 받는다. 전부
  auto-decide 인 작업은 합의문 없이 시작하고 리포트로 보고한다 — 모든 작업에 합의문을 받으면
  등급을 나눈 의미가 사라진다. 사이클은 그 동의 시점에 열리고, 합의문을 안 받는 작업은 첫 결정을
  기록하는 턴에 열린다. 사이클 안의 개별 결정에는 다시 동의를 받지 않는다 —
  등급에 따라 로그로 가거나 보류로 올라간다
- **escalate 등급 결정은 결정 카드를 같이 쓴다.** 스펙 승인·사인오프·범위 결정·새 시각 방향을
  채팅으로 올리는 바로 그 턴에 `node scripts/report-decision.mjs --ask …` 로 카드와
  근거 본문을 쌍으로 기록하고, 같은 턴에 `report-note.mjs --blocker` 로 대기를 건다.
  답이 오면 같은 턴에 `--answer` 로 닫는다. 채팅의 긴 합의문은 근거 자료로 남고 결정은
  카드에서 한다 — 읽고 결정하는 데 드는 시간을 줄이는 것이 목적이다. 칸은 **사용자 말로**
  채운다(경로·규칙 번호·단계 이름·라이브러리 이름 금지, 기계가 판정한다).
  **카드를 열어 두고도 그 답에 의존하지 않는 일은 계속 한다.** auto-decide 결정에는 쓰지 않는다
  (규약: docs/references/report-contract.md 12절)
- PRODUCT.md의 비범위 기능은 요청받아도 먼저 지적. 필수에 없는 기능 임의 추가 금지
- 결정이 내려지면 즉시 문서 갱신. 대화와 문서가 다르면 문서가 우선
- **문서를 만들거나 고치기 전에 두 가지를 묻는다.** ① 하네스를 다른 것으로 바꿔도 이 내용이
  표기만 바꿔 살아남나(→ `projects/<이름>/docs/`) 버리고 새 하네스가 다시 만드나(→ `workspace/` 또는 킷)
  ② 기계가 값으로 읽나(정해진 자리에만) 사람이 산문으로 읽나(본문 아무 데나).
  **하네스 자신에 대한 사정(게이트 이름·정규식·파싱 순서·노드 글롭)은 프로젝트 docs 에 적지 않는다** —
  킷의 docs/LESSONS.md·docs/references/ 로 간다. 결정의 근거로 필요하면 구현 이름 없이
  무슨 일이 일어나는지로 적는다("승인된 스펙은 불변식마다 테스트가 요구된다").
  기계가 읽는 자리의 전체 목록은 docs/references/docs-contract.md 이고, 거기 없는 파싱을
  게이트에 추가하려면 그 문서에 먼저 등재한다
- 재작업 범위를 선언하지 마라. 무엇을 바꾸면 무엇을 다시 하는지는 dirty 전파가 정한다(graph.mjs).
  되돌아갈 때 경로를 임의로 정하지 말고 프론티어를 따른다 (상세: docs/references/graph-engine.md)
- 통과했던 판정을 거부할 때는 사유와 함께 mark한다: `node gates/graph-stop.mjs --mark <노드> "<사유 한 줄>"`.
  clean이던 노드를 mark하면 `rework`가 된다(아직 승인 못 받은 노드는 그냥 dirty). rework는 하류를 막는 것도
  프론티어에 뜨는 것도 dirty와 같고, 다른 건 하나뿐이다 — 재승인처럼 **턴을 끝내야 풀리는 게이트 실패가
  턴을 막지 않는다**. `design`처럼 자식이 있는 노드는 거부된 자식만 찍는다
  (`--mark design/page-designer "<사유>"`) — 부모로 찍으면 사유가 자식 전부에 복사돼 손대지 않은
  `schema-designer`까지 재승인을 기다린다. 상세: docs/references/graph-engine.md
- 이번 작업에 해당 없는 노드는 사유와 함께 n/a로 선언한다:
  `node gates/graph-stop.mjs --na <노드> "<사유 한 줄>"`. n/a는 프론티어에 뜨지 않아 매 턴 지적받지 않는다.
  판단이 틀리면 기계가 취소한다 — 산출물이 생기거나, 상류가 바뀌거나, risk-surface 게이트가 위험 패턴을
  잡으면 dirty로 돌아온다. review는 n/a 불가(graph.mjs가 거부한다)
- "테스트/검증했다"는 보고는 실행한 명령과 출력을 근거로만
- 보호 파일(gates·graph.mjs·.claude/hooks·settings.json)은 사용자가 직접 적용한다. 패치를 넘길 때는
  **적용 전에 실패하고 적용 후에 통과하는 검사**를 같이 준다 — 없으면 만들고, 못 만들면 없다고 밝힌다.
  사용자가 코드를 안 읽어도 판정할 수 있어야 한다(2026-08-16: 절반만 고쳐진 훅 패치를 믿고 적용했다)
  **검사에는 위반을 일부러 심어 잡히는 것을 보는 항목을 넣는다** — "위반 0건"이라는 보고와 검사가
  아예 안 돈 것은 겉이 같다(2026-09-02: 표면 패턴을 스캔 대상 밖 파일에 심고 "방벽이 열렸다"로 읽을 뻔했다)
- 패치가 파일 둘 이상을 고치면 **부분 적용 조합을 하나씩 확인해 패치 문서에 적는다.** 둘 다 붙은
  상태만 검사하면 한쪽만 붙은 상태는 아무도 안 본다. **어느 조합에서도 안전하게 실패하도록 설계를
  고치는 것이 먼저**고, 못 고치면 위험한 조합을 문서에 밝힌다
  (2026-08-30: 감지 줄에 위치를 섞었더니 한쪽만 붙은 상태에서 security-reviewer 요구가 사라졌다)
- 사람이 읽을 문서(PRODUCT·design-rules·PROGRESS 서사·docs/references·UI 카피 등)는 자연스러운 한국어로:
  번역투 금지(영어 직역식 어색한 표현 대신 일상어), 코드에서 쓸 고유명사
  (변수·파일명·`status: draft` 등)는 원어 유지. AI가 읽는 문서(CLAUDE.md·.claude/rules·에이전트/스킬 프롬프트)는
  전달력 위해 예외 — 밀도 허용
- **코드는 그 반대다. 식별자는 ASCII 로 쓴다** — 변수·함수·객체 키·구조분해 이름 전부.
  한국어는 주석·문자열·문서에만. 한글 이름은 세 번 사고를 냈다(2026-09-16 한글 이름이 이메일
  앞부분에 들어가 통합 테스트 열한 개가 `beforeAll` 에서 전부 죽음 · 09-17 보호 파일에 붙여
  넣은 줄이 깨짐 · 09-17 그것을 되돌리는 치환에서 이름 셋을 빠뜨려 게이트가 실패).
  **한 파일의 스타일이 아니라 레포 전체가 기준이다** — 옆 파일을 따라 쓰기 전에 세어 본다
  (`grep -oE '\b(function|const|let)\s+[가-힣]' scripts/*.mjs`). 세 번째 사고가 난 이유가
  그것이다: 40여 개 파일이 ASCII 인데 하루 전에 생긴 파일 하나를 보고 따라 썼다

### 설명은 비유 말고 사실로 — 문서와 답변 둘 다 (2026-08-09 사용자 지시)

어려운 개념은 **비유를 지어내지 말고 "실제로 무슨 일이 일어나는지"로** 푼다. 비유는 멋있게 들리지만
읽는 사람이 그 비유를 다시 해석해야 해서 오히려 멀어진다.

| 쓰지 마라 | 이렇게 |
|---|---|
| "그 구멍이 닫힙니다" | "설정이 레포에 저장돼 다른 곳에서도 Playwright MCP를 쓸 수 있습니다" |
| "그래프가 거짓말이 된다" | "있지도 않은 경로가 있는 것처럼 보인다" |
| "리뷰 빚을 프론티어에 표시" | "밀린 리뷰가 프론티어에 그대로 보인다" |
| "판정 방식 세 종류가 이 그래프의 뼈대다" | "clean 을 내리는 방법은 세 가지뿐이다" |

**논리학·전산 용어 직역도 같이 금지.** 공진리·술어·논리곱·공집합·판정기·포획 →
"검사할 파일이 없으면 그냥 통과한다", "조건 두 개를 모두 만족해야 한다" 식으로 풀어 쓴다.
단 레포가 이미 쓰는 말(dirty/clean·프론티어·전파·귀속·집계 노드·결정론 게이트)은 그대로 둔다 —
문서와 코드가 다른 말을 쓰면 대조가 안 된다.

**답변(대화)에도 똑같이 적용한다.** 추상적으로 요약하지 말고 무엇이 어떻게 바뀌는지 구체적으로.
결과를 먼저 쓰고 근거를 뒤에 붙인다.

### 보고·설명의 순서 (2026-08-14 사용자 지시 — "왜 너의 말은 이해하기가 어려운걸까?")

Phase별·파일별로 정리한 보고서는 **내가 한 일의 구조**지 읽는 사람의 구조가 아니다.
"사용자가 무엇을 결정해야 하나" 순서로 쓴다.

- **이름 대신 실물을 붙인다.** "signal RLS 블록(authz)" 이 아니라 그 SQL 세 줄. 이름은 그 파일을
  이미 아는 사람에게만 정보다.
- **결론이 아니라 작동 방식을 준다.** "안전하다"가 아니라 "RLS 를 켜면 정책 없는 동작은 전부 거부되고,
  secret 키는 RLS 를 우회한다". 결론만 주면 사용자는 믿거나 마는 것 외에 할 수 있는 게 없다 —
  그건 결정을 맡긴 게 아니라 떠넘긴 것이다.
- **한 문단에 한 가지.** 배경·판단·근거·다음 단계를 한 문장에 욱여넣지 않는다. 밀도가 높으면
  짧아 보이지만 읽는 시간은 더 걸린다.
- **선택지는 각각의 비용과 함께.** 추천을 먼저 말하고 이유를 뒤에.
- 사용자가 "쉽게 풀어서 설명해줘"라고 말해야 문체가 바뀌면 그건 실패다.

## 아키텍처 (요약 — 게이트가 자동 강제하므로 상세 암기 불필요)

- FSD 6레이어, import는 아래 방향만. TypeScript strict, any 금지
- 편집 시 훅이 게이트를 실행한다. 실패 메시지가 오면 새 기능 없이 위반만 수정
- 레이어별 상세 규칙은 .claude/rules/ 가 해당 경로 작업 시 자동 로드된다
- 프로젝트 코드는 projects/<이름>/ 아래(각자 src/ + 설정파일). 킷(.claude·gates·scripts, 그리고 루트 docs/의 LESSONS·references)은 루트 상주
- 프로젝트 문서는 projects/<이름>/ 하위(workspace/=과정 기록, docs/=정의·참고). 활성 프로젝트는 루트 ACTIVE 파일이 가리킨다
- 실행 흐름은 의존성 그래프로 관리된다: `product→spec·design→implement→qa→review→deploy`.
  Stop 훅(graph-stop)이 턴마다 dirty 상태(HANDOFF.md)를 갱신하고 프론티어를 파생한다. 상세: docs/references/graph-engine.md

## 표면별 판단 — 절차는 이번 diff가 닿는 표면마다 따로 정한다 (구현 전)

작업에 등급을 매기지 않는다. 등급은 위험 기능 하나 때문에 작업 전체를 최대 강도로 만들고,
반대로 "빠른 경로"라는 이름이 실제로 닿은 위험 표면을 덮는다. 대신 **이번 diff가 무엇에 닿는지**를
먼저 열거하고, 표면마다 필요한 절차를 독립적으로 정한다.

구현 전 한 문단으로 선언하고 동의를 받는다 — 무엇을 어디에 만들지 + 닿는 표면 + 표면마다 걸 절차.
(예: "카드 컴포넌트를 widgets에 추가합니다. 닿는 표면은 시각 방향뿐이고 승인된 design-rules 안이라
시안은 생략, 끝나고 ui-reviewer만 돌립니다.")

| 이번 diff가 닿는 것 | 붙는 절차 | 왜 이 절차인가 |
|---|---|---|
| 인증·세션·결제·권한·시크릿 | 구현 전 spec(불변식) + security-reviewer | 틀리면 되돌릴 수 없다. **이 하한선은 risk-surface 게이트가 기계로 강제한다** |
| 동시성·시변/파생 상태 | 구현 전 spec | 사고가 나중에, 조용히 난다 — 테스트로 잡으려면 불변식이 먼저 있어야 한다 |
| 새 시각 방향 | design-interview → 시안 → checkpoint | 시각은 말로 합의되지 않고, 크게 만든 뒤 뒤집으면 비싸다 |
| 승인된 방향의 반복 UI | 바로 구현 + ui-reviewer | design-rules가 이미 기준이다. 다시 물으면 같은 답이 나온다 |
| 도메인 로직·상태 관리 | code-reviewer | 게이트는 문법을 보고 이건 의미를 본다 |
| 데이터 모델(엔티티·필드) | modeling-checklist 훑기, 걸리는 게 있으면 spec | 잘못 잡은 모델은 마이그레이션으로만 고쳐진다 |
| 카피·이미지·토큰 조정 | 자동 게이트만 | 되돌리는 비용이 거의 0이다 |

- **시각 방향 표면 상세**: 국면을 얼마나 깊게 돌지의 판단 기준(뒤집는 비용)과 순서는
  .claude/rules/design-drafting.md. UI 구현 전 design-rules `status: approved` 하한선은 게이트가 지킨다.
- **닿지 않은 표면의 절차는 걸지 않는다.** 결제 화면의 문구 하나를 고치는 diff는 결제 표면에 닿지 않는다.
- **애매할 때의 기준은 되돌리는 비용이다.** 사고가 나는 표면(보안·동시성·데이터 모델)은 애매하면 절차를
  건다. 취향 표면은 애매하면 싼 확인(시안 1장)으로 끝낸다.
- 사용자가 더 두껍게/얇게 가자고 하면 그 판단이 이긴다. 다만 위 표의 1행(위험 표면)은 게이트가 막으므로
  얇게 갈 수 없고, 얇게 가려면 예외 주석에 사유를 남겨야 한다(예외는 stderr에 계속 남는다).
- 그래프 진행: 프론티어를 따라 자동 진행하되, spec-level·위험표면 실패는 항상 사용자에게 보고한다.
  qa/리뷰 실패 분류(qa-classifier)는 파견해도 되고, 사용자가 "내가 볼게" 하면 멈춘다.

## 필요할 때만 읽는 문서 (라우팅)

- UI/페이지 작업 시작 전: projects/<이름>/docs/design/design-rules.md (checkpoint로 승인된 기준)
- 새 시각 방향(첫 화면·새 레이아웃) 시작 전: .claude/rules/design-drafting.md — 시안 먼저 절차(design-interview 스킬이 진입점).
  **새 시각 방향은 언제나 보류로 올린다** — 스스로 결정하지 않고, 리포트의 「디자인 방향 빈칸」도 아니다.
  빈칸은 승인된 방향 안에서 그 문서가 다루지 않은 세부만이다
- 사이클·결정 등급·결정 로그·리포트·규칙 승격: docs/references/decision-layer.md
- 커밋을 언제 끊고 무엇을 한 덩어리로 묶을지·push 시점: docs/references/commit-policy.md
  (전부 auto-decide 다 — 묻지 않고 그 기준대로 하고 보고한다)
- 위험 기능(결제·인증·권한·동시성·시변/파생 상태) 구현 전: projects/<이름>/docs/specs/ 의 해당 스펙 (없으면 /spec 먼저).
  이 하한선은 risk-surface 게이트가 기계로 강제한다 — 스펙 frontmatter의 `surfaces: [auth·payment·authz·concurrency]`
  가 있어야 게이트가 커버로 인정하고, 없으면 편집 즉시 차단된다. 오탐이면 파일에 `risk-surface-exempt: <표면> <사유>`.
  예외는 영구가 아니다 — 사유의 전제가 깨지면 게이트가 예외를 만료시킨다. ("인가 모델이 없다"고 적은 authz
  예외는 그 프로젝트에 쓰기 정책이 하나라도 생기는 순간 무효가 되고, 주석을 고쳐 달아도 통과하지 않는다)
- 데이터 모델(엔티티·필드) 설계 시: docs/references/modeling-checklist.md (얕게=kickoff, 깊게=/spec)
- 과거 결정의 이유: projects/<이름>/docs/DECISIONS.md / 반복 실수 패턴: docs/LESSONS.md
- 실행 그래프·프론티어·재작업 전파·검증 실패 분류: docs/references/graph-engine.md
- 하네스의 목표 구조·생성 루프 조건(①~⑤)·작업 순서: docs/references/harness-design.md — **정본이다.** 백로그·대기열이 쓰는 ①~⑤·「필터」·「책임 표」·「판단 지점」은 여기 정의를 가리킨다. 구조나 순서를 바꾸는 결정이 있을 때만 사람이 갱신한다 — 모델이 고치지 않는다
- 프로젝트 docs 에서 기계가 읽는 것의 전체 목록(스펙 frontmatter 세 필드와 그 어휘·INV 정의 앵커·
  design-rules status·경로 규약): docs/references/docs-contract.md

## 세션 종료 시

- wrap-up 스킬로 projects/<이름>/workspace/PROGRESS.md "현재 상태"를 갱신한다 (5개 필드, 10줄 이내)
- **미룬 작업은 projects/<이름>/docs/BACKLOG.md 로 뺀다** — PROGRESS의 "대기 중인 결정"은 사용자 승인 대기 전용이다.
  미룬 것을 거기 쌓으면 블록이 세션마다 길어져 브리핑이 읽어 주는 다섯 줄이 뭉개진다. 브리핑은 미결 건수만 띄운다(0건이면 침묵)
- 큰 진전이 있었으면 retro 스킬을 제안한다

## 기능 완성 시 — review 노드 사인오프

리뷰어 통과 = 그래프의 `review` 노드를 clean으로 만드는 것. 통과하면 `workspace/review.md`에
`project: <프로젝트명>` + `status: passed` + `basis: <해시>` + `reviewers: [실제로 돌린 리뷰어]`를 기록한다(무엇이 모자란지는
graph-stop이 매 턴 알려준다). 구현이 바뀌면 basis 불일치로 review가 자동으로 낡는다(재리뷰 강제).
review가 dirty인 동안 `deploy`는 차단된다.

리뷰어는 개수가 아니라 **닿은 표면**으로 정한다. 닿지 않은 표면의 리뷰어는 읽을 것이 없어
지적도 못 하므로 파견하지 않는다. 어느 리뷰어를 왜 뺐는지는 한 줄로 알린다(뺀 판단도 보고 대상이다).
→ 표면별 리뷰어 넷과 `reviewers:` 규약: docs/references/review-signoff.md

## 검증 실패 시 (qa 테스트·리뷰어 지적)

- 분류의 목적은 **어느 층이 틀렸는지 귀속**하는 것이다 — 실패한 자리를 고치면 원인이 그대로 남아 같은 실패가
  다시 난다. `qa-classifier` 서브에이전트가 실패를 `impl / design / spec` 레벨로 귀속한다(판정만, 라우팅 X).
  실패 원인이 한눈에 명백하면(오타·널 체크 누락 등) 분류기 없이 바로 고쳐도 된다 — 분류기는 "어느 층인지
  헷갈릴 때" 쓰는 도구지 통과 의례가 아니다.
- 판정 후 행동: **impl** = evidence 위치 코드 수정(실패한 qa/review가 이미 dirty로 잡음) /
  **design** = design-rules.md `status: draft`(거부) / **spec** = 해당 스펙 `status: draft`(거부).
  **거부는 파일만 고치고 끝내지 말고 `--mark <노드> "<사유>"`까지 찍는다** — 마크를 빠뜨려 HANDOFF가 옛
  프론티어를 들고 있었고, 그 상태에서 재작업 순서를 손으로 추론해 거꾸로 간 적이 있다(2026-08-06).
  마크가 있어야 rework로 잡혀서 재승인을 받을 턴도 끝낼 수 있다.
  거부는 재작업+재승인 전까지 dirty를 유지하고, 재작업 범위는 전파가 파생한다.
- 강제 승격: `spec-level`이거나 위험 표면(인증·결제·권한·격리 INV·security)에 닿으면 사용자에게 먼저 보고. (하한선)

  ## 참고

- 라이브러리/API 문서, 코드 생성, 설정 또는 구성 단계가 필요할 때 내가 명시적으로 요청하지 않아도 항상 Context7 MCP를 사용하세요.
  (사용자 지시 2026-08. 이유: 기억에 의존한 API 시그니처·설정 키는 조용히 틀리고, 틀린 채로 돌아가는 코드가 나온다)

## Terminology

The user communicates in Korean using aliases for canonical terms.
Read GLOSSARY.md at session start to resolve Korean paraphrases
to canonical terms. When a mapping is ambiguous, ask instead of guessing.
