---
name: design-interview
description: 말로는 수렴하지 않는 시각 취향을, 선택지·예시가 채워진 양식의 반복으로 좁히는 상담 절차. 결론은 시안(design-drafter)의 입력이 되고 승인 후 design-rules.md 로 확정된다. 신호는 design-rules 로 커버되지 않는 새 레이아웃 언어. 레퍼런스가 이미 있거나 "알아서 무난하게"면 상담을 압축하거나 건너뛰고 시안부터 뽑는 편이 빠르고, 승인된 방향의 반복 화면에는 쓰지 않는다
---

# 디자인 인터뷰 — 양식 반복 방식

빈 질문을 던지지 않는다. **선택지·예시·추천 기본값이 채워진 양식**을 만들어 사용자가
편하게 채우게 하고, 답변을 읽고 양식을 갱신하는 반복으로 수렴시킨다.
(이유: 사용자가 디자인 지식이 없어도 답할 수 있어야 한다 — 심사는 선택보다 쉽다)

## 언제 쓰나 (경계)
이 상담의 값은 **취향이 갈릴 수 있을 때**만 나온다. 갈릴 게 없으면 사용자가 이미 아는 것을 다시 묻는 일이 된다.
- **깊게 돈다**: 새 시각 방향이 필요한 화면(첫 메인 페이지, 홈과 구조가 다른 게시판·지갑·대시보드)이고
  원하는 그림이 아직 말로 안 나온 상태. rules/design-drafting.md 의 "새 방향" 판단과 같은 기준.
- **압축한다**: 레퍼런스 URL·스크린샷이 이미 있거나 사용자가 "알아서 무난하게"라고 할 때 —
  양식을 한 바퀴만 돌리거나, 조사로 채운 양식을 확인만 받고 시안으로 넘어간다.
- **안 쓴다**: `projects/<이름>/docs/design/design-rules.md` 로 이미 승인된 방향의 반복 화면. 그건 상담 없이 바로 구현.

## 절차
1. 아래 템플릿으로 `projects/<이름>/docs/design/INTERVIEW.md` 생성 → 사용자에게 채워달라고 요청
2. 사용자가 채우면 읽는다. 레퍼런스 URL 은 style-scout 파견(Playwright 연결 시),
   스크린샷은 직접 분석 → 결과를 양식의 해당 칸에 반영
3. 양식 갱신: 모순 지적, 빈칸에 조사 기반 추천값 제안, 후속 질문 삽입
4. 2~3을 반복. **3회차부터는 매 회 "이 정도면 시안에 충분합니다. 마무리할까요?" 를 명시**
5. 마무리 전 최종 확인: "더 추가하거나 뒤집을 것은 없습니까?"
6. **[게이트 A]** 승인되면 결정 사항만 추출한다. 그다음:
   - **첫 시각 방향이면**(design-rules.md 가 비었거나 이 화면이 새 언어를 정의하면):
     결론을 다음 단계 시안의 근거로 삼아 `design-drafter` 에 넘긴다.
   - 상담 결과 자체는 `projects/<이름>/docs/design/INTERVIEW.md` 에 남는다(과정 보관). 확정 기준은
     시안 checkpoint 승인 후에야 `projects/<이름>/docs/design/design-rules.md` 에 기록된다(승인의 결과물).

## 다음 단계로 연결 (v2 흐름)
```
design-interview (이 스킬, 상담)
  → design-drafter (projects/<이름>/docs/design/mockups/<화면>.html 시안)
  → [checkpoint] 시안 승인/수정 반복
  → projects/<이름>/docs/design/design-rules.md 에 확정 기준 기록 + tokens.css 갱신
  → 구현
```
이 스킬은 위 흐름의 **첫 칸(상담)**만 담당한다. 시안 생성부터는 design-drafter,
전체 순서는 rules/design-drafting.md 를 따른다.

## 갱신 규약 (반복 오염 방지)
- 사용자가 쓴 답은 **절대 지우거나 고쳐 쓰지 않는다**
- 에이전트의 해석·추천·질문은 반드시 `> 🤖:` 인용 블록으로만 추가한다
- 해소된 질문 블록은 다음 갱신 때 제거해 양식을 얇게 유지한다

## 기존 기준과의 관계
`projects/<이름>/docs/design/design-rules.md` 에 이미 승인된 전역 기준이 있으면, 그것과 **충돌하지 않는 범위**에서
이 화면 고유의 디자인만 상담한다. 전역 톤(예: 인디고 단일 강조)을 뒤집는 제안이 나오면
사용자에게 "전역 방향을 바꾸는 결정입니다" 라고 명시하고 확인받는다.

## INTERVIEW.md 템플릿

```markdown
# 디자인 인터뷰 — <프로젝트/화면명>
회차: 1        (모르는 항목은 비워두거나 "모름, 추천해줘" 라고 적으셔도 됩니다)

## 1. 무엇을 만드나
- 화면/페이지:
- 이 화면의 단 하나의 목표 (예: 펀딩 참여 버튼 클릭 / 프로젝트 열람):
- 보는 사람과 상황 (예: 후원자가 모바일로 훑다가 마음에 들면 참여):

## 2. 원하는 느낌 (해당되는 것에 O, 복수 가능)
- 무드: [ ]미니멀 [ ]클래식·신뢰 [ ]플레이풀 [ ]테크·미래적 [ ]따뜻함 [ ]고급스러움 [ ]기타:
- 절대 아니어야 할 느낌 1개:
- 밀도: [ ]정보 빽빽 (대시보드형)  [ ]여백 위주 (랜딩형)
- 톤: [ ]라이트 [ ]다크 [ ]모름
- 모서리: [ ]각짐 [ ]살짝 라운드 [ ]둥글둥글 [ ]모름
- 폰트 느낌: [ ]깔끔한 고딕 [ ]세리프 포인트 [ ]모노(코드 느낌) 포인트 [ ]모름

## 3. 레퍼런스 (여러 개 가능 — URL 또는 스크린샷 첨부)
| 출처 (URL/스크린샷) | 여기서 가져올 것 | 여기서 버릴 것 |
|---|---|---|
|  |  |  |

## 4. 색
- 확정 브랜드/포인트 컬러 (있으면 hex 나 이름):
- 없으면 비워두세요 → 무드 기준으로 2~3안 제안드립니다
  (projects/<이름>/docs/design/design-rules.md 에 전역 브랜드색이 있으면 그것을 기본 전제로 합니다)

## 5. 구성
- 반드시 들어가야 할 섹션/요소:
- 절대 넣지 말 것:
- 반응형 우선순위: [ ]데스크톱 우선 [ ]모바일 우선

## 6. 자유 메모 (형식 무시하고 하고 싶은 말)
-
```
