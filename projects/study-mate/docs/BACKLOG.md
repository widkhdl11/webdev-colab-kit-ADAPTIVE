# 백로그 — 미룬 제품 작업

> 기존 문서와 코드에 "아직 안 했다"고 적혀 있던 것을 옮겼다. **사람의 결정을 기다리는 것은
> 여기 없다** — 그건 들여오기 질문 목록에 있다.

      스트리밍까지는 코드에 들어갔다. 2단계 랭킹 + 추천 이유, 캐시·폴백·호출 제한이
      계획으로만 있다. `[문서에서 확인]`
      타이밍은 사람이 직접 봐야 하는 항목으로 남아 있다. `[문서에서 확인]`
      확인되지 않았다고 적혀 있다. `[문서에서 확인]`
      화면은 맞지만 프리미티브 기본값에 옛 토큰이 남아 있다. `[문서에서 확인]`
      각자 관리한다. 기존 문서가 "누가 주인인지 정리 대상"이라고 적어 두었다.
      `[문서에서 확인]` (참가·정원 스펙 INV-P6)
      다르다. `[코드에서 확인]`
      `[코드에서 확인]`
      필요하면 정리하기로 적혀 있다. `[문서에서 확인]`

## 미결

- [ ] **강퇴·거절·탈퇴에는 확인 단계가 없다** (2026-09-06, ui-reviewer) — 승인된 「파괴적
  행동」 규칙은 **지우기에만** 걸려 있고, `features/manage-participants/ui/ParticipantActions.tsx`
  의 셋은 한 번 눌러 끝난다 · 지우는 것이 아니라 상태가 바뀌는 것이고 되돌리는 길(다시 신청)이
  있어서 같은 무게는 아니다 · 다만 호스트가 실수로 누르면 상대에게 알림이 나가고, 그 알림은
  못 되돌린다 · 되살릴 조건: 참여자 관리 화면을 다시 손댈 때

- [ ] **좋아요 cascade 는 `likes_del_self` 정책을 한 번도 안 지난다** (2026-09-06, security-reviewer) —
  `likes_del_self` 는 「자기 좋아요만 뺀다」고 적혀 있는데, 외래 키 연쇄 삭제는 참조 무결성
  트리거가 테이블 소유자 권한으로 실행하고 이 레포에는 `force row level security` 가 없다 ·
  **그래서 정책 목록을 아무리 읽어도 「남의 좋아요가 지워지는 문」이 안 보인다** · 지금은
  좋아요가 카운트뿐이라 잃을 것이 없다. 뜻이 붙는 순간(담아 둔 글 목록 · 좋아요 알림 ·
  좋아요 정렬) 달라진다 · **보류 P15 와 같은 자리이고, 그쪽을 「좋아요에 뜻을 붙일 계획이
  있는가」로 물으면 한 번에 닫힌다** · 되살릴 조건: 좋아요를 누를 수 있게 만들 때

- [ ] **폼이 제어 컴포넌트인 것을 자동 검사가 안 붙든다** (2026-09-06, test-auditor P1-2) —
  「실패해도 적은 것이 남는다」는 브라우저 실측으로만 확인했다. `value={content}` 를
  `defaultValue={post.content}` 로 되돌려도 전 스위트가 초록불이다 · **만들 수 있다**:
  `vitest.config.mts` 가 이미 jsdom + react 플러그인이고 `src/**/*.test.tsx` 를 덮는다.
  없는 것은 `@testing-library/react` 하나다 · 변이도 같이 등록할 수 있다 · 되살릴 조건:
  폼 컴포넌트 검사를 처음 붙일 때

- [ ] **폼의 `maxLength` 를 아무도 안 붙든다** (2026-09-06, test-auditor P2-3) —
  길이 상한의 강제 위치는 넷이다: 작성 액션 · 수정 액션 · 작성 폼의 `maxLength` · 수정 폼의
  `maxLength`. 앞의 둘은 이제 `validatePostText` 한 자리로 합쳐져 검사가 붙들지만, 폼 둘은
  검사가 0개다 · 위 항목(컴포넌트 검사)이 생기면 같이 닫힌다 · 되살릴 조건: 같음

- [ ] **저장된 본문이 상한을 넘으면 수정 화면이 막다른 길이 된다** (2026-09-06, code-reviewer 10) —
  `maxLength` 는 **이미 들어 있는 값을 자르지 않는다**. 스키마에 길이 제약이 없어서(서버
  액션이 유일한 강제 위치라고 `limits.ts` 가 적어 두었다) 상한이 생기기 전에 쓴 4000자 초과
  본문이 있으면, 사용자는 저장을 눌러야 「4000자까지」를 읽고 어디를 얼마나 지워야 하는지는
  화면 어디에도 없다 · **지금 데이터에는 그런 행이 없다**(시드 본문은 전부 짧다) ·
  싼 개선: 남은 글자 수를 칸 아래 표시 · 되살릴 조건: 길이 제약을 스키마로 올릴 때

- [ ] **`posts_update_author` 정책을 무력화하는 변이가 없다** (2026-09-06, test-auditor P3-9) —
  수정 액션의 주석 둘이 「인가의 주인은 갱신 정책」이라고 적어 두었는데, 그 정책을
  `using(true) with check(true)` 로 여는 변이가 등록소에 없다. `posts_insert_author`·
  `posts_read` 는 있다 · 통합 검사는 있으므로 「검사가 없다」가 아니라 「그 검사가 붙드는지
  확인된 적이 없다」다 · 되살릴 조건: 정책 변이를 다음에 손볼 때

- [ ] **`posts` 의 `author_id` 열 권한을 붙드는 검사가 없다** (2026-09-06, test-auditor P3-10) —
  `z8-posts-columns` 변이는 `grant update on public.posts` 로 열 권한을 통째로 여는데
  `holds` 가 INV-Z9(스터디 이동)라고 적혀 있고, 통합 검사 전체에 `update({ author_id: … })`
  가 한 번도 없다. 정책의 `with check` 가 받치고 있어 사고는 안 나지만 그 방벽을 확인하는
  검사가 0개다 · 되살릴 조건: INV-Z8 검사를 다음에 손볼 때

- [ ] **`canonicalUuid` 가 수정 경로에서 안 붙들린다** (2026-09-06, test-auditor P3-11) —
  `canonicalUuid(form.get("postId"))` 를 `String(form.get("postId") ?? "")` 로 바꿔도 전부
  초록불이다. 검사가 넣는 값이 전부 이미 정규 표기라서다 · 하이픈 없는 32글자를 넣고
  좁힌 조건과 캐시 경로가 정규 표기인지 보는 단언 한 줄이면 닫힌다 · 되살릴 조건: 표기
  정규화를 다음에 손볼 때

- [ ] **변이 이름표 셋이 겹친다** (2026-09-06, test-auditor P3-8) — `0행`(change-status·edit-post) ·
  `정확히 세 칸`(셋) · `길이 상한`(send-message 의 「길이 상한 2000」이 부분일치로 물린다).
  지금 오판이 안 나는 이유는 변이가 파일 하나만 건드려 실패가 같은 파일에 머물기 때문이지
  이름표가 갈래를 가려서가 아니다 · 기능 이름을 이름표에 넣으면(`0행(수정)`) 그 우연에 안 기댄다 ·
  되살릴 조건: 이름표가 실제로 오판을 낼 때, 또는 등록소를 정리하는 국면

- [ ] **스펙에 INV-Z3·INV-Z4 의 시나리오가 없다** (2026-09-06, test-auditor P3-13) —
  `write-authorization.md` 의 S1~S21d 어디에도 그 둘을 가리키는 것이 없다. 그래서 이번에 쓴
  검사들이 「스펙 시나리오와 어긋나는가」를 **대조할 대상이 없다** — 어긋난 것을 못 찾은 게
  아니라 비교 기준이 없다 · 모집글 수정이 이제 제품 표면이 됐으니 S22(남의 글 수정 → 0행) ·
  S22b(작성자의 수정은 들어간다) · S23(폼의 작성자 필드는 안 쓴다)를 더할 만하다 ·
  **스펙 편집이라 `status: draft` 재승인이 붙는다 — 사람 결정** · 되살릴 조건: 다음에 스펙을 열 때

- [ ] **지워진 스터디의 모집글 수정을 액션은 막지 않는다** (2026-09-06, security-reviewer) —
  수정 화면은 404 로 막는데(`read-post-for-edit.ts`) 액션에는 그 조건이 없어서 액션을 직접
  부르면 고쳐진다. **불변식 위반은 아니다** — INV-Z10 이 막는 것은 새 신청·새 수락·새
  모집글이고 기존 글의 수정은 아무도 정한 적이 없다. 피해도 없다(그 글은 작성자 말고 아무에게도
  안 보인다) · 정할 것: 「기존 글 수정은 지워진 스터디에서도 허용한다」를 INV-Z10 에 적을지,
  액션과 정책에 조건을 붙일지 · 되살릴 조건: INV-Z10 을 다음에 열 때

- [ ] **퍼센트 인코딩된 보호 경로를 실측 안 했다** (2026-09-06, security-reviewer · 추측) —
  `/posts/x/%65dit` 처럼 인코딩하면 `PROTECTED_PATTERNS` 의 정규식에 안 걸릴 가능성이 있다.
  이번 diff 가 만든 것이 아니라 `/studies` 같은 기존 경로에도 같이 해당한다 · **데이터는 안
  샌다** — 미들웨어를 건너뛰어도 수정 화면이 세션 없으면 판독기를 안 부르고 404 로 닫힌다.
  새는 것은 「로그인 화면 대신 404 를 본다」는 흐름뿐이다 · 확인하려면 그 경로를 한 번 열어
  보는 것이 가장 싸다 · 되살릴 조건: 첫 배포

- [ ] **인증 두 장의 페이지 껍데기를 `FormPage` 가 안 덮는다** (2026-09-06, ui-reviewer info) —
  `auth-form.module.css` 의 `.title`·`.sub` 가 `form-page.module.css` 와 값까지 같은데
  본문 폭만 460px 라 못 물려받는다. 카드는 2026-09-06 에 같이 물려받았다 ·
  후보: `FormPage` 에 폭만 좁히는 변형을 열기 · 되살릴 조건: 인증 화면을 다시 손댈 때

- [ ] **모집글 작성 액션에 등록된 변이가 없다** (2026-09-06, 사이클 6 에서 드러남) —
  액션 다섯에 검사를 붙인 사이클 5 가 `apply`·`manage`·`chat`·`create-study` 넷을 등록했는데
  `create-post` 는 그 전 사이클에 검사가 생겨서 목록에서 빠졌다. 검사는 있지만 **그 검사가
  무엇을 붙드는지 판정된 적이 없다** · 사이클 6 이 등록한 `edit-post-limits-unbounded` 가
  작성 쪽 검사도 함께 빨간불로 만들어서 길이 상한 한 자리는 덮였다. 남은 것은 가드·작성자
  출처·보내는 다섯 칸이다 · 되살릴 조건: 작성 액션을 다음에 만질 때

- [ ] **모집글을 화면에서 지울 수 없다** (2026-09-06, 사이클 6) — 제품 정의는 삭제를 필수로
  적어 두었고 데이터베이스에는 `posts_delete_author` 정책이 있는데, 누를 자리가 어디에도 없다 ·
  막힌 것은 기능이 아니라 **시각 결정**이다(보류 P14: 파괴적 행동을 무엇으로 그릴 것인가) ·
  되살릴 조건: P14 가 정해질 때

- [ ] **검증에 실패하면 사용자가 적은 것이 사라진다** (2026-09-06, code-reviewer 5 · **2026-09-06 실측함**) —
  `<form action={submit}>` 은 액션이 끝나면 React 19 가 제어되지 않는 입력칸을 초기화한다.
  액션이 실패를 **반환**하면(던지지 않는다) 정상 종료로 처리되므로 오류 문구만 남고 제목·본문이
  빈칸이 된다. 모집글 본문은 4000자까지라 잃는 양이 크다 · 자주 나는 경로는 아니다 — 길이는
  `maxLength` 가, 필수는 브라우저 검증이 앞에서 막는다. 남는 것은 세션 만료와 데이터베이스
  거부인데, **하필 그 둘은 사용자가 다시 적을 이유가 없는 실패다** · `create-study` 도 같은
  모양이라 갈라진 것이 아니라 물려받은 것이다 ·
  **실측 결과(2026-09-06, 사이클 6, 프로덕션 빌드 + 브라우저)**: 그대로 일어난다. 수정 폼에서
  본문에 새 문장을 적고 제목을 비워 제출하니 오류 문구는 떴는데 **본문이 저장돼 있던 옛 문장으로
  되돌아가 있었다**. 작성 폼에는 `defaultValue` 가 없으므로 되돌아가는 것이 아니라 **빈칸**이 된다 —
  그쪽이 더 나쁘다 ·
  **고치는 법도 확인했다**: 입력칸을 `useState` 로 React 가 들고 있으면 실패해도 적은 것이 남는다.
  `EditPostForm` 은 그렇게 고쳤고 같은 방법으로 다시 실측해 남는 것을 봤다 ·
  **남은 것은 폼 다섯 장**(모집글 작성 · 스터디 개설 · 프로필 수정 · 비밀번호 변경 · 인증) ·
  되살릴 조건: 그 폼들을 다음에 만질 때

- [ ] **푸터의 「서비스 소개」가 없는 화면을 가리킨다** (2026-09-06, 사이클 6 브라우저 확인) —
  `/about` 이 아직 없는데 푸터가 모든 화면에서 그 링크를 그린다. Next 가 그 링크를 미리
  받아 두려 해서 **화면마다 콘솔에 404 가 하나씩 찍힌다**(브라우저 확인 중 유일하게 나온 오류였다) ·
  누르면 404 화면으로 간다 · 화면 목록에 있는 화면이라 지우는 것이 아니라 만드는 것이 답이다 ·
  되살릴 조건: `/about` 을 만들 때, 또는 그 전에 콘솔이 조용해야 할 때
- [x] **작업 화면 두 장의 껍데기 CSS 가 바이트까지 같다** (2026-09-06, ui-reviewer 상) —
  `app/posts/create/page.module.css` 와 `app/studies/create/page.module.css` 가 15줄 전체
  동일하고(720px + 44/64 여백 + `.title` + `.sub`), `.card { padding: 28px var(--space-24) }` 도
  두 벌이다. `28px` 은 토큰에 없는 값인데 이제 반복되므로 「일회성은 px 허용」 예외가 더는 안
  걸린다 · 제출 자리(`.actions`)는 2026-09-06 에 `FormActions` 로 합쳤다 — 남은 것은 페이지
  껍데기다 · 되살릴 조건: 세 번째 작업 화면(모집글 수정·스터디 수정)을 만들 때 ·
  **닫힘** — 2026-09-06(사이클 6): 작성 두 장이 `FormPage` 를 쓰고 각자의 `page.module.css` 를
  지웠다. 카드 껍데기는 `shared/ui/form-page/FormCard` 하나가 됐다 — 폼 여섯 장이 그걸 쓴다
- [ ] **`HostedStudy` 는 이름이 담은 것보다 적게 들고 있다** (2026-09-06, code-reviewer 9) —
  `{ id, title }` 인데 이름에는 「호스트」가 들어 있어서, 다음 사람이 「내가 호스트인 스터디」가
  필요할 때 이걸 집어 쓰고 필드를 더하게 된다. 그 순간 「고르는 데 필요한 만큼만 읽는다」는
  근거가 깨진다 · 스터디를 가리키는 타입이 이미 셋이다(`StudyPage`·`StudySummary`·`HostedStudy`) ·
  후보: `StudyOption` 으로 바꿔 용도를 이름이 말하게 · 되살릴 조건: 이 타입을 두 번째로 쓸 때
- [ ] **모집글이 이미 있는 스터디인지 작성 화면이 모른다** (2026-09-06, code-reviewer 4) —
  한 스터디에 모집글을 여러 장 허용하기로 정했는데(사이클 20260906-1 D3), 선택 목록은 그
  스터디에 이미 모집글이 몇 장인지 안 보여 준다. 목록(`/posts`)에는 같은 스터디를 가리키는
  카드가 여러 장 뜨고 정원·자리 표시가 전부 같다 · 싼 개선: `readMyHostedStudies` 의 select 를
  `"id, title, posts(count)"` 로 바꾸면 왕복 수가 안 늘고 옵션 라벨에 「(모집글 2개)」를 붙일 수
  있다 · **2026-09-06 에 절반이 좁혀졌다** — 목록에 모집 중인 스터디만 나오므로(INV-Z14) 마감했거나 정원이 찬 스터디는 애초에 안 뜬다. 남은 것은 모집 중인 스터디에 이미 모집글이 몇 장인지 안 보이는 것뿐이다 · 되살릴 조건: 같은 스터디에 두 번째 모집글이 실제로 생길 때

- [ ] **시드가 프로필을 다시 만들려다 죽는다** (2026-09-06) — `npm run db:seed` 가 `jiwon` 에서
  `duplicate key value violates unique constraint "profiles_pkey"` 로 멈춘다. 0010 이 가입 트리거로
  프로필을 자동으로 만들게 되면서(INV-A7), 시드가 사용자를 만든 뒤 프로필을 **또** 넣는 자리가
  부딪힌다. 그 뒤 단계(스터디·모집글·메시지)가 통째로 안 돌아서 **데모 계정으로는 아무것도 확인할 수
  없다** — 2026-09-06 에 화면을 확인하려고 새 계정을 손으로 가입해야 했다 · 고치는 방향: 프로필 삽입을
  `upsert`(또는 트리거가 넣은 행을 갱신)로 · 되살릴 조건: 다음에 시드로 화면을 확인할 때

- [ ] **`private` 이 노출 스키마가 아니라는 전제를 배포처에서는 아무도 안 지킨다** (2026-09-06, security 2) — INV-Z12 가 성립하는 이유는 `private` 이 PostgREST 에 안 열려 있어서인데, 그 값을 정하는 것은 Supabase Cloud 의 대시보드 설정이고 레포에는 로컬용 `config.toml` 한 줄뿐이다. 판정 함수 아홉에는 anon 실행 권한이 이미 있으므로 **누가 노출 목록에 `private` 을 더하는 순간 2026-09-05 에 닫은 구멍이 그대로 돌아온다.** 로컬 검사는 계속 초록불이다 · 후보 둘: 마이그레이션에서 `alter role authenticator set pgrst.db_schemas = ...` 로 못 박기(대시보드보다 이기는지 확인 필요) · 배포 URL 에 대고 INV-Z12 검사를 한 번 돌리기 · **기존 항목(security 1, 프로덕션 노출 스키마)과 같은 자리다** — 그쪽이 「확인하자」면 이건 「강제하자」다 · 되살릴 조건: 첫 배포
- [ ] **아바타 버킷을 아무나 통째로 나열할 수 있다** (2026-09-06, security · diff 밖) — `0001` 의 버킷이 `public = true` 이고 `avatars_read` 가 `using (bucket_id = 'avatars')` 라, 비로그인이 storage API 로 목록을 받으면 객체 이름이 다 나온다. 경로 규약이 `<user_id>/...` 라 **그 목록이 곧 사용자 id 명부**다. 지금은 업로드 화면이 없어 데이터가 0건이지만, INV-Z13 이 방금 프로필을 좁힌 것과 방향이 반대인 자리다 · 후보: 목록만 막고 파일 URL 은 열어 두기 · 되살릴 조건: 아바타 업로드를 붙이기 전
- [ ] **보호 경로를 통과한 응답의 캐시 지시를 아무도 안 붙든다** (2026-09-06, security 4) — 지금은 안 샌다. 쿠키가 갱신되는 응답은 `@supabase/ssr` 이 캐시 금지를 달아 주고, 나머지는 `src/app` 의 페이지 아홉이 전부 `force-dynamic` 이라 Next 가 달아 준다. **문제는 두 번째를 붙드는 것이 없다는 것** — 새 페이지에서 `force-dynamic` 을 빼면 세션에 따라 다른 내용을 담은 응답이 공유 캐시에 저장될 수 있고, 미들웨어의 `no-store` 는 리다이렉트에만 붙는다 · 후보 둘: 통과 응답에도 `private, no-store` 붙이기 · 페이지 모듈을 import 해서 `dynamic` 익스포트를 읽는 유닛 검사 · 되살릴 조건: 페이지를 새로 추가할 때
- [ ] **`/profile` 이 보호 경로 목록에 있는데 그 페이지가 없다** (2026-09-06, security · diff 밖) — `route-access.ts` 에 있지만 `src/app/profile/` 이 없어서 로그인한 사람에게 404 가 나간다. 지금은 무해하지만, 그 페이지를 만들 때 「이미 보호된다」로 읽힐 자리다 · 되살릴 조건: 프로필 화면을 만들 때
- [ ] **`safePattern` 이 따옴표와 역슬래시를 안 지운다** (2026-09-06, security 8 · 추측) — 검색어는 `.or()` 에 문자열로 이어 붙는 유일한 값이고 `%_,()` 는 지운다. 남은 것은 PostgREST 의 논리 트리 파서가 값 안의 `"` 와 `\` 를 문법으로 읽을 가능성이다 · **아직 실측 안 했다** — `/posts?q=a%22` 와 `/posts?q=a%5C` 를 열어 200 이면 이 항목은 버린다 · 되살릴 조건: 목록 화면을 다음에 만질 때
- [ ] **이메일 확인을 켜면 가입 뒤에 "메일함을 확인하세요"를 띄울 자리가 없다** (2026-09-05, security L10 · code 7) — `signUpAction` 은 세션 유무와 무관하게 `redirect(next)` 한다. 확인이 켜지면 그 시점에 세션이 없어 보호 경로에서 프록시가 곧바로 로그인 화면으로 되돌리고, 사용자는 "가입했는데 로그인 화면"만 본다. **프로필은 이제 생기지만(INV-A7) 사람에게는 여전히 막힌 것으로 보인다** · 고치려면 `ActionResult` 의 성공 갈래에 문구를 싣고 폼이 그것을 그려야 한다(지금 폼은 실패만 그린다) — **시각 표면이라 ui-reviewer 가 붙는다** · 되살릴 조건: 이메일 확인을 켤 때
- [ ] **가입 오류가 "이미 가입된 이메일"을 그대로 알려 준다** (2026-09-05, S6 재확인 · security 3) — 로그인은 한 문구로 잘 묶었는데(`SIGN_IN_FAILED`) 가입은 `error.message` 를 그대로 낸다. 확인 메일이 꺼져 있으면 GoTrue 가 `user_already_exists` 를 돌려주므로 **가입 폼이 이메일 존재 여부를 요청 한 번에 답하는 오라클이 된다** · 고치는 법: `error.code` 로 고칠 수 있는 것(`weak_password` 등)만 통과시키고 나머지는 로그인처럼 고정 문구로 · 되살릴 조건: 인증 화면을 다시 만질 때(이번 diff 가 이 함수를 만졌지만 시각 표면이 붙어 미뤘다)
- [ ] **폭 없는 글자와 양방향 텍스트 제어가 이름에 그대로 들어간다** (2026-09-05, security 6) — 0010 이 제어문자와 공백은 씻지만 U+200B~U+200F · U+202A~U+202E · U+2066~U+2069 는 통과한다. 폼의 `trim()` 도 안 지우므로 **앱과 데이터베이스가 어긋나는 자리는 아니다** — 새 판단이라 여기로 뺐다 · 남의 이름처럼 보이게 만들거나 멤버 목록에서 줄을 밀 수 있다 · 되살릴 조건: 프로필 표시 이름을 다듬을 때
- [ ] **`private.study_accepted_count`·`study_is_recruiting` 을 아무도 안 부른다** (2026-09-05, security 5) — 정책 열둘 어디에도 없고 앱에도 없다. 화면이 쓰는 것은 계산 컬럼 쌍이다. **검사와 변이만이 소비자**라, 옮길 것이 아니라 지울 것이었다는 지적 · 다만 지우면 INV-P6 S7-1 의 「함수 쌍」 검사와 변이 넷이 같이 사라지므로, 지우는 판단은 그 커버리지를 어디로 옮길지와 같이 정해야 한다 · 되살릴 조건: 파생값 계산을 한 벌로 합칠 때
- [ ] **변이 넷이 판정 함수 본문을 통째로 다시 적는다** (2026-09-05, code 6) — `p6-rpc-adds-deadline` 등이 `study_is_recruiting` 본문 전체를 심는다. 0010 의 본문이 나중에 조건을 하나 더 얻으면 이 변이들은 「낡은 본문 + 의도한 한 줄」을 심고, 그때 빨간불이 변이 때문인지 되돌아간 본문 때문인지 안 갈린다 · 싼 방어: 심기 전에 대상 함수의 `md5(prosrc)` 를 기준선과 대조하고 다르면 멈춘다(`FINGERPRINT_SQL` 이 이미 그 값을 뽑는다) · 되살릴 조건: 판정 함수 본문을 다음에 고칠 때
- [ ] **프로덕션의 노출 스키마는 레포 밖에서 정해진다** (2026-09-05, security 1) — INV-Z12 를 실제로 강제하는 값은 PostgREST 의 `db-schemas` 이고, Supabase Cloud 에서는 대시보드가 정한다(`config.toml` 은 `config push` 없이는 안 올라간다). 레포의 검사는 로컬만 본다(`helpers.ts` 가 API_URL 을 로컬로 강제한다) · 배포 뒤 프로덕션 URL + anon 키로 `rpc/is_study_member` 가 404 인지 한 번 확인하고, 그 값을 배포 문서에 적는다 · 되살릴 조건: 첫 배포
- [ ] **`increment_post_views` 가 지워진 스터디의 모집글에도 닿는다** (2026-09-05, security 4) — `posts_read` 가 감추는 행인데 조회수는 올라가고, `posts_touch` 트리거가 `updated_at` 을 민다(지금 화면이 안 읽어서 피해는 없다) · P9 를 (다)로 닫았으므로 이건 그 결정 안의 세부다 · 싼 완화: 함수 본문에 `and private.study_is_visible(...)` 한 줄 · 되살릴 조건: P9 의 (가)를 할 때 또는 "수정됨" 표시를 붙일 때

- [ ] **좋아요를 아직 누를 수 없다** (2026-09-05) — 시안은 누를 수 있는 버튼으로 그렸지만 기능이 없어 조회수와 같은 모양의 숫자로 뒀다 · 되살릴 조건: 핵심 흐름 밖 기능을 붙이는 국면
- [ ] **개발 서버(`next dev`)에서 하이드레이션이 안 된다** (2026-09-05) — 이 환경에서 HMR 웹소켓 handshake 가 실패하면서 클라이언트 컴포넌트가 하나도 안 붙는다. 프로덕션 빌드는 정상이라 제품 결함은 아니지만, **개발 중에 화면을 확인하려면 매번 빌드해야 한다** · 되살릴 조건: 개발 편의가 아쉬워질 때
- [ ] **채팅방 목록이 방마다 질의를 두 번 더 던진다** (2026-09-05) — 마지막 메시지와 안 읽은 수를 방별로 센다. 사람당 방이 몇 개라 지금은 안 아프다 · 되살릴 조건: 방 수가 늘거나 목록이 느려질 때
- [ ] **`study_sessions` 라는 이름이 인증 세션과 겹친다** (2026-09-05) — 도메인 언어로는 「모임 일정」이다. 지금은 소비자가 적어 바꾸기 싼 시점 · 되살릴 조건: 이름 정리 국면
- [ ] **모집글은 하드 삭제인데 스터디는 소프트 삭제다** (2026-09-05) — 모집글에 남들이 누른 좋아요가 딸려 있는데 같이 사라진다. INV-Z6 이 세운 기준과 어긋나는데 근거가 문서에 없다 · 되살릴 조건: 좋아요를 지표로 쓰기 시작할 때
- [ ] **인증 두 장이 아직 자기 페이지 껍데기를 갖고 있다** (2026-09-06, ui H1·M3·L3 · 2026-09-06 갱신) — 본문 폭 720px 과 제목 38px 을 `shared/ui/form-page` 로 모았는데, 인증 폼만 아직 자기 것을 쓴다(`auth-form.module.css` 의 `.page`·`.title`·`.sub`) · **모집글 작성·스터디 개설은 2026-09-06(사이클 6)에 옮겼다.** 남은 것이 인증뿐인 이유는 폭이 다르기 때문이다 — 460px 이라 720px 을 그대로 물려받을 수 없다 · 후보: `FormPage` 에 좁은 변형을 열거나, 인증만 예외로 두고 그 사실을 시각 기준에 적기 · 되살릴 조건: 인증 화면을 다시 손댈 때
- [x] **카드 안쪽 여백 28px 이 다섯 파일에 하드코딩** (2026-09-06, ui L2) — `--space-28` 토큰이 없어서 폼 다섯 장이 각각 적는다. 기존 「반복되는데 토큰이 없는 간격 다섯」과 같은 건이다 · 되살릴 조건: 토큰을 정리하는 국면 · **닫힘** — 2026-09-06(사이클 6): `FormCard` 한 곳으로 모았다. 토큰이 없다는 사실은 그대로지만 적히는 자리가 하나라, 토큰이 생기는 날 고칠 곳도 하나다
- [ ] **프로필 사진이 프로필 화면에만 나온다** (2026-09-06) — 아바타를 올릴 수 있게 됐는데(`/profile/edit`) 그것을 그리는 곳은 `/profile` 뿐이다. 모집글 상세의 작성자·호스트, 스터디 상세의 멤버 명단, 채팅의 보낸 사람은 여전히 이니셜만 그린다 — 그 자리들이 프로필 행을 읽을 때 `avatar_url` 을 안 고르기 때문이다 · 되살릴 조건: 화면을 다시 다듬는 국면
- [ ] **임베드 모양 확인이 슬라이스마다 복사된다** (2026-09-06, code L4) — 「PostgREST 는 못 만든 키를 오류 없이 빼고 돌려준다」에 대한 방어가 `read-chats.ts`·`read-my-participations.ts`·`read-my-studies.ts`·`read-my-posts.ts` 넷에 같은 모양으로 있다. 엔티티끼리 import 를 게이트가 막아서 타입은 못 합치지만 **모양 검사 자체는 도메인이 아니라 `shared/lib` 로 뺄 수 있다** · 되살릴 조건: 다섯째 복사본이 생길 때
- [ ] **화면 네 장이 아직 없다** (2026-09-05 · 2026-09-06 갱신) — 스터디 수정, 소개, 알림 패널, AI 추천. 모집글 작성·프로필 수정·비밀번호 변경·`/profile` 은 2026-09-06 에, **모집글 수정은 같은 날 사이클 6 에서** 만들었다 · 스터디 수정은 authz 표면인데 **승인된 스펙에 「스터디를 고치는 것은 호스트만」이 불변식으로 없다**(INV-Z1 은 삭제만 정한다) — 데이터베이스는 `studies_update_host` 로 이미 막고 있으니 계약과 강제 장치가 어긋난 것이 아니라 **계약이 그 자리를 안 적은 것**이다. 만들기 전에 그 불변식을 스펙에 올려 승인받는다 · 되살릴 조건: 스터디 수정을 만들 때
- [ ] **정렬 화이트리스트를 「부르는 자리」에서는 아무도 안 붙든다** (2026-09-05, test-auditor M6) — `toSort` 함수 자체는 검사와 변이가 붙들지만, `src/app/posts/page.tsx:35` 의 `sort: toSort(one(params.sort))` 를 `one(params.sort) as Sort` 로 바꿔도 전 스위트가 초록불이다. 서버 컴포넌트라 통합 검사가 부를 수 없다 · **일부러 변이를 등록하지 않았다** — 붙들 검사가 없는 변이를 등록하면 「빠져나간 것 1」이 영구히 남아 그 숫자가 뜻을 잃는다 · 되살릴 조건: 서버 컴포넌트를 부를 수 있는 검사 방식이 생길 때
- [ ] **정렬의 마지막 키(id)를 아무도 안 붙든다** (2026-09-05, code-reviewer 9) — 준비물 여덟의 순위가 전부 달라서 `.order("id")` 를 지워도 초록불이다. 이것이 막는 것(쪽을 넘길 때 같은 글이 두 번)은 동점이 있을 때만 드러나므로, 「기한 없음」 준비물을 하나 더 넣고 **쪽을 나눠 읽어 중복이 없다**로 단언해야 실제로 붙들린다 · 되살릴 조건: 쪽 나눔을 손볼 때
- [ ] **안 읽음 알약이 두 벌 따로 선언되고 치수가 다르다** (2026-09-05, ui-reviewer L-2) — `app/chats/page.module.css`(24px/12px, sr-only 「안 읽음 N」)와 `widgets/site-header`(21px/11px, 링크 라벨 「알림, 안 읽음 N개」). 형태 규칙은 둘 다 지키지만 같은 물건이 두 벌이라 다음 화면이 세 번째 치수를 부른다. 21px 이 승인된 시안 값이다 · `shared/ui` 에 `UnreadPill` 하나로 합치고 읽어 주는 문구도 거기서 · 되살릴 조건: 화면을 다시 다듬는 국면
- [ ] **마감일의 "오늘"이 서버 시간대다** (2026-09-05) — 「마감 임박순」의 구간(0008)은 `current_date`, 화면의 남은 날짜는 서버에서 도는 Node 의 날짜를 본다. 지금 배포에서는 둘이 같은 시간대라 안 갈리지만, 사용자는 한국 시간으로 읽는다. 경계 날짜에 목록 순서와 화면 문구가 하루 어긋날 수 있다 · 되살릴 조건: 배포 시간대를 정할 때
- [ ] **지난 회차 모집글에 「최신 모집글 보기」가 없다** (2026-09-05) — 화면 목록이 그렇게 정했는데 상세 화면에 아직 안 붙였다 · 되살릴 조건: 한 스터디에 모집글이 둘 이상 생길 때

- [ ] **AI 추천 고도화의 남은 단계.** 구조화 출력과 인젝션 방어, 모집글 작성 도우미
- [ ] **채팅 개편 이후의 수동 검증.** 빌드·타입·라우팅까지만 확인됐고 로그인 뒤 화면·실시간·
- [ ] **공용 UI 프리미티브의 옛 색 토큰 정리.** 사용처마다 잉크 클래스로 덮어쓰고 있어
- [ ] **모집 상태 값의 주인 정하기.** 데이터베이스 트리거와 도메인 규칙이 같은 값을
- [ ] **읽히지 않는 문서 손보기.** 기존 문서의 폴더 설명과 AI 제공자 표기가 실제 코드와
- [ ] **쓰이지 않는 의존성 정리.** AI 패키지 하나가 의존성에 남아 있는데 부르는 코드가 없다.
- [ ] **알림 정리 정책.** 스터디·모집글이 지워져도 관련 알림은 남는다. 방치가 기본이지만
- [ ] **클레임의 role 을 함께 보기** (2026-09-04) — 지금은 sub 만 본다. 지금 뚫리지는 않지만(공개 키 토큰에는 sub 자체가 없다) 서비스 토큰류가 들어오는 경로가 생기면 방어가 된다 · 잘못 걸면 정상 사용자가 로그아웃되므로 실제 토큰을 보고 넣는다 (security-reviewer L4)
- [ ] **이 프로젝트의 서명키가 대칭인지 비대칭인지 확인** (2026-09-04) — 대칭이면 세션 검증이 요청마다 Auth 서버 왕복이 되고, 일시적 네트워크 장애가 전원 로그아웃으로 보인다 · 로컬 Supabase 를 띄울 때 확인해서 코드 주석의 "아직 확인되지 않았다"를 지운다 (security-reviewer L5 · code-reviewer L4)
- [ ] **라우트 핸들러가 인증 스펙의 빈칸이다 (INV-A5)** (2026-09-05) — `app/api/**` 가 INV-A1(페이지)과 INV-A4(서버 액션) 사이에 빈다. 페이지처럼 로그인 화면으로 보내면 부르는 쪽이 그 HTML 을 데이터로 파싱하므로 401 이 맞다 · 지금 핸들러가 하나도 없어 테스트가 가리킬 코드가 없다 · 되살릴 조건: 첫 라우트 핸들러 파일이 생기는 순간
- [ ] **도메인 모델(`src/entities/*/model.ts`)이 비어 있다** (2026-09-05) — 스키마가 계약을 지키고 있고, 도메인은 그 계약을 읽는 쪽이라 부를 곳이 생겨야 모양이 정해진다 · 소비자 없는 모델은 테스트도 실행도 안 된 채 남는다 · 되살릴 조건: 화면이나 서버 액션이 데이터를 읽기 시작할 때

## 닫힘

- [x] **입력 검증이 얇은 자리 넷** (2026-09-05 · 2026-09-06 닫음, S10·S11·S5b·C1) — 폼 값이 검증 없이 `revalidatePath` 로 감 · 목록 필터 값이 PostgREST 필터 문자열에 들어감(데이터는 안 새고 500 만 남) · 채팅 주제 정규식이 uuid 모양을 안 봄 · `hasControlChar` 가 U+0080~U+009F 를 통과시킴 · **셋을 닫았고 하나는 재현되지 않았다**: 폼 값은 `shared/lib/uuid.ts` 의 모양 검사를 지나고, 주제 정규식은 `0012` 가 8-4-4-4-12 로 좁혔고(변이 `p8-topic-shape-loose` 가 빨간불), C1 구역을 제어문자에 더했다(지우면 빨간불) · **목록 필터 값은 재현되지 않았다** — supabase-js 가 `.in()`·`.eq()` 값을 감싸서 넘겨서 `it)` · `it,design` · `%` · `*` 를 넣어도 오류 없이 빈 결과다(2026-09-06 실측). 실제로 깨지는 자리는 `or()` 에 이어 붙는 검색어 하나뿐이고 그건 `safePattern` 이 이미 막고 있었다. 넣었던 검증은 **아무 검사도 붙들지 않아서**(변이가 빠져나갔다) 도로 뺐고, 회귀 검사만 남겼다

- [x] **서버 액션 다섯 묶음에 테스트가 0개다** (2026-09-05 → 2026-09-06 닫음, 사이클 `study-mate-20260906-5`) — 네 파일을 「본체·조립」과 「액션」으로 갈라(`insert-application` · `change-status` · `send-message`/`mark-read` · `insert-study`) 액션 파일을 직접 부르는 검사 43건을 붙였다. `requireSession` 을 무력화한 변이 셋과 판정을 지운 변이 여덟, 합쳐 **11건이 전부 빨간불**이 됐다(유닛 스위트로 도는 변이 종류를 `mutation-run` 에 새로 만들었다 — 통합 스위트에는 이 검사가 한 건도 안 들어와서 「빠져나갔다」로 보고됐다). `revalidateEntityPath` 호출도 조립 안으로 옮겨 주입 가능하게 했다 — 그 전에는 호출을 통째로 지워도 전 스위트가 초록불이었다
- [x] **서버 액션 다섯에 검사가 0개다** (2026-09-05, T-H4·T-M6 → 2026-09-06 닫음) — 위 항목과 같은 건이다. test-auditor 가 가장 위험하다고 짚은 둘이 둘 다 붙들렸다: `changeParticipationAction` 의 「0행 갱신은 오류가 아니다」(그 판정을 지우면 검사가 빨간불이 된다)와 `sendMessageAction` 의 길이·빈값 제약(경계 안쪽 짝까지 단언해, 상한을 무한대로 바꾼 변이가 잡힌다)

- [x] **회원 명부 전체가 비로그인에게 열려 있다** (2026-09-05 · 2026-09-06 닫음 — INV-Z13/0011) — `profiles_read` 가 무조건 참이라 이름·소개·지역·관심분야를 통째로 긁을 수 있다. 뚫린 것이 아니라 의도보다 넓은 열림이고, 화면 목록에 공개 프로필 화면이 없다 · **닫았다**: INV-Z13 을 계약으로 삼고 `0011` 의 조회 정책이 강제한다. 검사 14건 · 변이 5건

- [x] **아바타 저장 경로 규약이 강제되지 않는다** (2026-09-05 · 2026-09-06 닫음) — 정책이 버킷과 올린 사람만 보고 폴더를 안 봐서, 남의 아이디 폴더를 미리 차지할 수 있었다. **더 나쁜 쪽은 그다음이었다** — 그렇게 자리를 차지하면 진짜 주인이 자기 폴더에 못 올린다(덮어쓰기가 update 라 소유자가 안 맞는다). `0015` 가 정책 넷에 경로 판정을 더해 닫았고(INV-E3), `avatar_url` 컬럼 쪽도 같이 막았다(INV-E8)

- [x] **`profiles.region` 에 외래 키가 없다** (2026-09-06 · 같은 날 닫음) — `0015` 가 외래 키를 걸었다(INV-E5). 덤으로 프로필 조회가 두 질의에서 한 질의가 됐다

- [x] **리다이렉트 목적지의 호스트를 요청에서 받는다** (2026-09-04 · 2026-09-06 닫음) — 리다이렉트 URL 의 오리진이 요청 Host 헤더에서 나온다. 앞단이 Host 를 고정하지 않는 배포에서 공유 캐시가 그 응답을 저장하면 다른 사용자에게도 나갈 수 있다 · **닫았다**: 리다이렉트에 `Cache-Control: no-store` 를 달아 공유 캐시가 저장하지 못하게 했고(설정 없이도 도는 기본 방어), `NEXT_PUBLIC_SITE_URL` 로 오리진을 못 박을 자리를 뒀다. **상대 경로 Location 은 안 된다** — Next 미들웨어가 `new URL()` 로 파싱해서 500 이 난다(실측) (security-reviewer L2)

- [x] **회원가입이 프로필 없는 계정을 남긴다** (2026-09-05, S1 → 같은 날 닫음) — 프로필을 만드는
  자리를 앱에서 데이터베이스로 옮겼다(0010). 계정 행이 들어오는 그 트랜잭션에서 트리거가
  프로필을 만들므로 세션이 있든 없든, 어느 경로로 가입하든 둘이 함께 생긴다. 이름 길이 규칙도
  폼에서 데이터베이스로 내렸다. 계약은 INV-A7, 검사 넷 + 변이 둘. **이메일 확인을 켜고 실측해
  옛 경로가 거부되는 것과 새 경로가 통과하는 것을 둘 다 봤다.**

- [x] **판정 함수가 RPC 로 열려 있다** (2026-09-05, S4 의 큰 절반 → 같은 날 닫음) — 일곱을
  `private` 스키마로 옮기고 정책 열둘을 다시 걸었다(0010). 비로그인 호출이 200+판정값에서
  404 가 됐다(실측). 계약은 INV-Z12. 남은 절반(`increment_post_views`)은 위 미결 항목.

- [x] **시각 기준에 대비 실측값이 없다** (2026-09-04 → 2026-09-05 닫음) — 시안 3장의 텍스트 295개를 브라우저 computed style 로 훑어 쟀다. 미달 0건 · 최저 5.43:1(약한 잉크/종이). 값은 시각 기준 문서 「2026-09-05 확정」 블록에 있고, 다음 화면이 그 하한을 물려받는다. 옛 항목: — 기존 문서가 "잉크를 얹어 대비를 확보한다"고만 적고 숫자를 안 남겼다 · 첫 화면을 만들 때 재서 시각 기준 문서에 적는다(그 하한을 다음 화면이 물려받는다)
- [x] **`next build` 로 한 번 확인.** 타입 검사는 통과했지만 빌드(린트 + 라우트 검증)는 · **닫힘** — 2026-09-05: 빌드 통과. 그리고 개발 서버에서만 하이드레이션이 죽는 것을 이때 잡았다
- [x] **파비콘이 없다** (2026-09-04) — 워킹 스켈레톤이라 아직 안 만들었다 · 시각 작업을 시작할 때 같이 만든다 · **닫힘** — 2026-09-05: src/app/icon.svg 로 붙였다(종이 위 형광펜 한 줄)
- [x] **로그인 후 원래 가려던 곳으로 돌려보내기** (2026-09-04) — 지금은 보호 경로에서 튕기면 로그인 후 홈으로 떨어진다. 제품 원칙 1("끊김 없는 한 흐름")과 어긋나는 기본값이다 · 넣을 때 되돌아갈 경로 검사(슬래시 하나로 시작하고 둘로는 시작하지 않는 경로만)를 같이 넣지 않으면 그 순간 외부 사이트로 열린다 (code-reviewer L2) · **닫힘** — 2026-09-05: `?next=` 로 붙였고, 열린 리디렉션은 INV-A6 이 막는다
- [x] **모바일 헤더를 아직 안 정했다** (2026-09-05) — 640px 아래에서 네비 둘이 사라지고 로고·알림·로그인만 남는다. 햄버거 / 하단 탭 / 축약 노출 중 미정 · 시안 3장에 없는 화면 상태라 실물을 보고 정하는 편이 낫다 · 되살릴 조건: 헤더를 실제 컴포넌트로 만들 때 · **닫힘** — 2026-09-05: 640px 아래에서 네비를 감추지 않고 헤더 아래 한 줄로 내린다

## 리뷰어 넷의 남은 지적 (2026-09-05)

보안·코드·화면·검사 네 관점의 리뷰에서 나온 것 중, 가장 급한 다섯을 처리하고 나머지를
여기로 옮겼다. 괄호 안의 기호(S1 · A9 · T-H3 …)는 그 리뷰가 항목마다 붙인 번호다.

### 보안 표면


- [ ] **조회수를 조회 기록 행으로 바꾼다** (2026-09-05, S4 의 남은 절반 · P9 에서 (가)로 남긴 것) — 지금 `increment_post_views` 는 아무나 부를 수 있어서 화면을 한 번도 안 열고 숫자만 올릴 수 있다. **이건 알고 열어 둔 것이다**(P9 에서 "그대로 두고 배포한다"로 정했다) — 새는 것은 없고 부풀려지는 것은 숫자 하나다 · 고치는 방향은 `(모집글, 사람, 날짜)` 에 유일 제약을 건 조회 기록 테이블 + 그 행을 센 파생값이다. 그러면 판정 함수가 아예 없어지고 정책이 그대로 지킨다. **대가는 비로그인 조회가 안 세어지는 것**(신원이 없다) · 이걸 하면 INV-Z12 구조 검사의 허용 목록이 비워진다 · 되살릴 조건: 조회수를 지표로 쓰기 시작하거나 데이터 모델을 다시 열 때
- [ ] **이미 열린 채팅 구독이 강퇴로 안 끊긴다** (2026-09-05, S5·일부 추정) — 새 구독은 막히는데 이미 연결된 탭은 토큰 만료까지 새 메시지를 받을 수 있다 · 먼저 실측한다: 탭 둘로 같은 방을 열고 강퇴한 뒤 새 메시지가 뜨는지 · 되살릴 조건: 실시간 대화를 다듬는 국면
- [ ] **가입 오류가 가입 여부를 알려 준다** (2026-09-05, S6) — 로그인은 한 문구로 잘 묶었는데 가입은 `error.message` 를 그대로 낸다. 로그인에서 막은 열거가 가입에서 열린다 · 되살릴 조건: 인증 화면을 다시 만질 때
- [ ] **데이터베이스 오류 문구가 화면으로 그대로 나간다** (2026-09-05, S7) — 알려진 코드 셋은 잘 옮기는데 나머지는 제약 이름·정책 이름이 사용자에게 보인다(여섯 파일) · 되살릴 조건: 오류 문구를 정리하는 국면
- [ ] **서버가 막는다고 적은 것을 데이터베이스는 안 막는다** (2026-09-05, S8·A11) — 호스트가 PostgREST 를 직접 불러 멤버를 `withdrawn` 으로 바꿀 수 있어 「강퇴」가 「스스로 나감」이 된다. 호스트 자신도 탈퇴할 수 있고 INV-P7 때문에 되돌아갈 수 없다 · **먼저 정할 것: "강퇴와 탈퇴는 행위자가 다르다"를 스펙에 넣을 것인가** · 되살릴 조건: 참여 상태 스펙을 다시 열 때
- [ ] **미들웨어가 퍼센트 인코딩을 안 푸는지 확인 못 했다** (2026-09-05) — `curl -i "http://localhost:3000/%73tudies/<id>"` 가 307 `/login` 이 아니라 200 이면 뚫린 것이다 · 되살릴 조건: 다음에 미들웨어를 만질 때(확인만 하면 되는 5분짜리)

### 검사(테스트)의 빈칸

- [ ] **조립 함수 두 개가 아직 옛 모양이다** (2026-09-06, code-reviewer) — `makeCreatePost`·`makeChangePassword` 가 위치 인자를 받는다. 나머지 여섯은 `shared/lib/action-deps.ts` 의 `ActionDeps`/`ReadDeps`/`PathDeps` 로 맞췄다. `makeChangePassword` 는 `createVerifier`/`createSession` 이 나뉜 것 자체가 INV-C2 라 이름을 접으면 안 되고, 별도 타입으로 두되 같은 정본에서 파생시키는 것이 맞다 · 되살릴 조건: 그 둘을 다시 손댈 때
- [ ] **`study_sessions` 의 제약 둘을 통합 검사가 안 본다** (2026-09-06, security-reviewer·test-auditor) — 앱(`create-study/model/slots.ts`)이 시간 순서와 중복을 먼저 거르게 됐지만, **데이터베이스가 실제로 거부하는지**는 아무도 안 본다(`study_sessions_time_order`·`study_sessions_unique`). 앱의 검사가 사라져도 데이터가 안 깨진다는 근거가 없다 · 되살릴 조건: 다음 검사 보강 국면
- [ ] **`db.테이블` 단언이 「쓰기 전에 아무것도 안 읽는다」를 우연처럼 고정한다** (2026-09-06, test-auditor) — 다섯 검사가 `toEqual(["participants"])` 식으로 표 접촉의 전체 순열을 박는다. 지금은 그것이 실제 설계 의도지만(서버 판정을 안 한다) 검사 이름에 안 적혀 있어서, 조회 하나를 앞에 붙이는 리팩터링이 들어오면 계약이 안 깨졌는데 빨간불이 난다 · 되살릴 조건: 그 액션들에 읽기가 필요해질 때
- [ ] **`db-error.ts` 의 `23514` 판정이 문장 모양에 기댄다** (2026-09-06) — PostgreSQL 의 제약 위반 문장(`violates check constraint`)과 우리 트리거의 한국어 문장을 코드로는 못 가르므로 문장으로 가른다. 더 확실한 길은 제약 이름 → 한국어 문구 표를 두는 것이다 · 되살릴 조건: 새 검사 제약을 추가할 때

- [ ] **실시간 대화 경로에 「구독을 실제로 여는」 검사가 없다** (2026-09-05, T-H3 · 2026-09-06 에 절반 닫음) — 0012·0013 과 `participation-capacity.test.ts` 로 **강제 장치를 직접 부르는 검사**는 생겼다(주제 이름을 `private.chat_topic_uuid` 에 넣어 결과를 보고, 정책이 그 함수를 거치는지 따로 본다). 남은 것은 **웹소켓 구독을 실제로 열어 보는 검사**다 — 지금 검사는 정책이 판정에 쓰는 값을 만들 수는 있어도 realtime 이 그 정책을 정말 그 자리에서 평가하는지는 못 본다. 정책 스캔의 스키마 목록에 `realtime` 이 없고 `polcmd` 가 쓰기만 보는 것도 그대로다 · 되살릴 조건: 다음 검사 보강 국면
- [ ] **어휘 검사가 붙드는 범위가 좁다** (2026-09-05, T-M1·A15) — 손으로 적은 목록이 아무것도 안 붙들고, 어휘는 두 벌이 아니라 최소 다섯~여섯 벌이며, 데이터베이스가 실제로 거부하는지는 안 본다(제약식 문자열만 읽는다) · 되살릴 조건: 다음 검사 보강 국면
- [ ] **붙들리지 않은 불변식 넷** (2026-09-05, T-M2·T-M3·T-M4·T-L3) — INV-Z7 의 수정 쪽 · INV-Z3 의 삭제 쪽 · `is_latest_for_study` 전체(본체를 `select true` 로 바꿔도 초록불) · 그리고 등록된 변이가 INV-Z1~Z7 · P1~P7 을 하나도 안 다룬다 · 되살릴 조건: 다음 검사 보강 국면
- [ ] **테스트가 있어야 할 순수 판정 함수들** (2026-09-05, T-L1) — `applyState`(분기 8개, 화면의 유일한 쓰기 버튼이 이 값으로 정해진다) · `safePattern` · `toSort`(단언으로 바꾸면 주소창의 아무 값이 정렬 키로 간다) · `formatSlots`/`formatSlotsLong` (**여기엔 이미 어긋난 자리가 있다** — weekday 가 7이면 뒤쪽이 "undefined 20:00 – 22:00" 을 낸다) · 되살릴 조건: 다음 검사 보강 국면
- [ ] **INV-Z8 의 강제 위치가 스펙에 절반만 적혀 있다** (2026-09-05) — 스펙은 "열 단위 갱신 권한"이라고만 적었는데, S8 은 열 권한을 통째로 열어도 읽기 정책 `chatpart_read_member` 가 새 행을 막는다(2026-09-05 실측) · 되살릴 조건: write-authorization 스펙을 다시 열 때

### 화면·구조

- [ ] **형광펜이 카테고리·모집 상태 밖으로 샜다** (2026-09-05, ui 1·2·4·6·7) — 실패 문구 배경 코랄(다섯 파일이 같은 여섯 줄을 다섯 번 적었다 → `FormError` 하나로) · 호스트 표시가 옐로 알약(아바타처럼 괘선 채움으로) · 안 읽음 배지가 「마감」 배지와 같은 자리·같은 색(**색 자체는 2026-09-05 P3 결정으로 허용됐다** — 남은 것은 같은 자리에 같은 색이 겹쳐 무엇을 뜻하는지 안 갈리는 것뿐이다) · 실시간 연결 점의 민트/코랄 · 모르는 카테고리 대체색이 옐로 · 되살릴 조건: 화면을 다시 다듬는 국면
- [ ] **채팅방 카드에 제목 요소가 없다** (2026-09-05, ui 3) — 사각 형광펜 라벨은 두 화면에서 「카테고리 이름」인데 `/chats` 에서는 같은 기호가 스터디 이름을 뜻하고, 카드에 `<h3>` 가 아예 없다 · 되살릴 조건: 위와 같은 국면
- [ ] **없는 화면을 가리키는 링크 하나** (2026-09-05, ui 9·A4·A5 · 2026-09-06 셋 닫음) — 푸터의 「서비스 소개」가 없는 `/about` 으로 간다. **닫힌 셋**: 모집글을 만드는 화면(2026-09-06) · 종 아이콘과 「내 프로필」이 가리키던 `/profile`(2026-09-06) · 되살릴 조건: 소개 화면을 만들 때
- [ ] **접근성·토큰·중복** (2026-09-05, ui 5·8·10~14) — 힌트 글이 `aria-describedby` 로 안 붙음(개설 폼 힌트 일곱 개) · 확정된 종료 상태가 「아직 확정 아님」의 점선을 씀 · 반복되는데 토큰이 없는 간격 다섯 · 간격 토큰을 둥글기에 씀 · 인증 폼이 공용 폼 조각을 안 씀 · 채팅 목록이 `Card` 를 다시 조립함 · 되살릴 조건: 화면을 다시 다듬는 국면
- [ ] **UI 레이어에 스며든 판단 넷** (2026-09-05, ui 15) — 남은 자리 계산 · 지역 표기 조립 두 벌 · `isHost` 권한 판정이 화면에 · `mondayOf` 날짜 계산이 위젯에 · 되살릴 조건: 다음 구조 정리 국면
- [ ] **오류를 삼켜 「고장」을 「비어 있음」으로 그리는 자리 다섯** (2026-09-05, A10) — 특히 `read-study-detail.ts` 는 읽기 실패를 "멤버 목록은 참여한 사람에게만 보입니다"로 **권한 문제라고 거짓말한다** · 되살릴 조건: 오류 처리를 정리하는 국면
- [ ] **상세 화면마다 같은 질의가 두 번 돌고, 홈이 가장 비싸다** (2026-09-05, A12·A13) — `generateMetadata` 와 본체가 각각 부른다(`/posts/[id]` 한 요청에 6개, `/studies/[id]` 8개) · 홈은 카드 3장을 그리려고 `count: "exact"` 로 모집글 전체를 센다 · 되살릴 조건: 성능이 아쉬워질 때
- [ ] **시드가 화면 상태의 절반을 안 덮는다** (2026-09-05, A14) — 대기 중인 신청 0건(그래서 「승인」 단계를 시드만으로는 볼 수 없다) · 스터디당 모집글이 정확히 1장 · 채팅 메시지 0건 · 소프트 삭제된 스터디 없음 · 그리고 `seed.mjs` 4절이 자기가 넣은 `created_at` 을 uuid 순으로 덮어쓴다 · 되살릴 조건: 화면 확인이 잦아지는 국면
- [ ] **모집글 흐름의 빈 자리들** (2026-09-05, A7·A8·A16·A17·A18·A19·A20) — uuid 모양이 아닌 id 가 404 대신 500 · 일정 저장 실패 시 같은 스터디를 또 만들게 됨 · 「온라인」 필터가 없고 그 코드가 죽어 있음 · 모집글 상세에서 스터디 상세로 가는 길 없음 · 화면에 불변식 ID 가 나오고 접두사가 두 번 붙음 · 강퇴당한 사람에게 "이미 신청한 스터디입니다" · 홈의 예시 일정이 남의 스터디를 「이번 주 스터디」로 띄울 수 있음 · 되살릴 조건: 모집글 화면을 만드는 국면

### 2026-09-05 두 번째 리뷰에서 새로 나온 것

- [ ] **지워진 스터디의 id 가 옛 멤버에게 계속 보인다** (2026-09-05, security) — 대화를 남기려고 바깥 조인으로 바꾸면서 `chats.study_id` 가 오게 됐다. 그 id 는 정책 판정 함수들(`is_study_member`·`study_accepted_count` 등)의 인자 키라, **더는 읽을 수 없는 스터디에 대해 인원수·멤버십을 계속 물을 수 있다.** 새 데이터가 아니라 위 「정책 판정 함수가 RPC 로 열려 있다」 항목의 도달 범위가 넓어진 것이다 · 그 항목을 고칠 때 「지워진 스터디 id 를 쥔 옛 멤버」를 시나리오에 넣는다
- [ ] **`readMyChats(userId)` 가 아무 uuid 나 받는다** (2026-09-05, security) — 그 필터는 인가가 아니라 중복 제거인데(정책이 내가 속한 방의 참여자 행을 전부 보여준다) 시그니처는 그렇게 안 읽힌다. 남의 id 를 넣으면 같은 방 안에서 그 사람의 「마지막으로 읽은 시각」이 나온다. 지금 부르는 곳은 하나뿐이고 세션에서 온 값이다 · 되살릴 조건: 이 함수를 부르는 곳이 둘이 될 때
- [ ] **같은 조건이 두 정책에 따로 적혀 있다** (2026-09-05, security) — 모집글 조회와 스터디 조회가 "지워지지 않았거나 내가 호스트"로 글자까지 같은데 두 파일에 따로 산다. 0007 의 정렬 키가 부르는 사람의 시야로 읽어도 되는 근거가 **그 둘이 같다는 것**이라, 스터디 쪽을 좁히면(예: 비공개 스터디) 조용히 어긋난다. 넓히는 방향은 검사가 잡지만 좁히는 방향은 아무도 안 잡는다 · **2026-09-06 에 이 등식에 기대는 자리가 하나 늘었다** — 「내가 쓴 모집글」이 `studies!inner` 로 읽는다(security 정보). 그쪽은 등식이 깨지면 내 글이 내 프로필에서 사라지는 방향이라 새지는 않는다 · 되살릴 조건: 스터디 조회 조건을 손댈 때
- [ ] **`studies_recruit_until_idx` 가 이제 안 쓰인다** (2026-09-05, code) — 그 인덱스를 만든 이유가 「마감 임박순」이었는데, 정렬이 모집글 쪽 계산 컬럼을 거치면서 쓸 자리가 없어졌다. 남길지 지울지 · 되살릴 조건: 목록 성능을 볼 때
- [ ] **변이 이름표가 시나리오까지는 못 좁힌다** (2026-09-05, code) — INV-P9 를 가리키는 변이가 넷, INV-Z11 이 셋이다. 어느 하나가 실패해도 같은 「빨간불」이 되어, 무엇을 잡았는지가 안 갈린다. `holds` 에 이미 `(S7)`·`(S11)` 이 붙어 있으니 있으면 시나리오 번호까지 요구하고 없으면 INV 로 떨어지게 하면 된다 · 되살릴 조건: 같은 INV 의 변이가 더 늘 때
- [ ] **정리가 실패해 남은 데이터는 지문에 안 잡힌다** (2026-09-05, test-auditor) — 지문이 정책·함수·권한·트리거·제약을 보는데, 뒷정리가 실패해 사용자가 남는 것은 다음 변이의 판정을 바꾸면서도 거기 안 걸린다. 사용자 수 한 줄은 넣었지만 스터디·모집글까지는 안 본다 · 되살릴 조건: 뒷정리 실패가 실제로 한 번 더 날 때
