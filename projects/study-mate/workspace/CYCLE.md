# 사이클 — 열린 것과 닫힌 것

한 줄이 사이클 하나다. 사이클은 **사용자 지시 하나**이고, 계획 승인에서 열려
종료 조건이 성립할 때 닫힌다. 세션과 다른 축이라 세션을 넘어 이어진다.

## 열린 사이클

없다.

## 닫힌 사이클

- `study-mate-20260906-4` — **프로필 수정(`/profile/edit`)과 비밀번호 변경(`/profile/password`).**
  2026-09-06 에 종료 조건이 성립해 닫았다. 위험 표면 둘에 닿아 스펙을 먼저 썼고
  (`profile-editing.md` · `password-change.md`), 사람이 넷을 정했다 — 현재 비밀번호를 다시
  받는다 · 바꾸면 다른 기기를 끊는다 · 사진은 공개 · 닉네임 중복 허용.

  **검사를 먼저 쓰고 빨간불 6건을 본 뒤에 마이그레이션을 썼다.** 그 6건이 스펙이 지목한
  구멍과 정확히 겹쳤다 — 남의 폴더에 올리는 것이 **성공**했고, 그 뒤 진짜 주인이 자기 폴더에
  못 올렸다(덮어쓰기가 update 라 소유자가 안 맞는다).

  **리뷰어 넷이 실질 결함 넷을 더 잡았다.** ① 아바타 버킷을 비로그인이 통째로 나열할 수
  있었다 — 폴더 이름이 곧 회원 id 명부이고 id 하나면 얼굴 사진이 따라 나온다(백로그의
  되살릴 조건이 정확히 이번 diff 였다) ② 비밀번호를 `trim()` 으로 읽어서, 끝에 공백이 있는
  비밀번호를 쓰는 사람은 맞게 쳐도 영영 못 바꾸는 상태였다 ③ 다른 기기 끊기가 실패해도
  「끊었습니다」를 사실로 말했다 ④ 검사가 확인용 클라이언트를 자기가 만들어 끼워서
  프로덕션 팩토리가 한 번도 안 돌았다.

  **변이 판정 자체도 두 번 고쳤다** — `0015` 가 복구 목록에 없어서 그 파일이 건드리는 것을
  무력화한 변이가 안 되돌아왔고, 지문이 버킷 설정을 안 봐서 형식·크기 제한을 지운 변이가
  「강제 장치를 하나도 안 바꿨다」로 넘어갔다. 둘 다 방벽 하나가 사라진 상태인데 조용했다.

  불변식 셋을 새로 세웠다(INV-E7 목록 제한 · E8 `avatar_url` 경로 · E9 이름 제어문자).
  기록: `workspace/logs/DECISION_LOG.study-mate-20260906-4.md`(D1~D38),
  사인오프: `workspace/review.md`(유닛 164 · 통합 153 · 변이 58/58).
  **보류는 P13 하나가 앞 사이클에서 열린 채 그대로다.**


- `study-mate-20260906-3` — **`/profile` 내 프로필 화면 한 장.** 2026-09-06 에 종료 조건이
  성립해 닫았다. 네 구역(내 프로필 카드 · 내가 만든 스터디 · 내 신청 현황 · 내가 쓴 모집글)이
  브라우저에서 그려지고, **헤더의 종 아이콘과 「내 프로필」이 없는 주소로 가던 것이 닫혔다.**
  「내가 쓴 모집글」은 화면 목록에 없던 것을 사용자가 추가한 것이라 `docs/IA.md` 도 같이 고쳤다.

  **리뷰어 셋이 잡은 것 중 큰 것은 둘이다.** 하나는 「모집글을 써 보세요」가 막다른 길을
  가리킨 것 — 조건을 「내 스터디가 있다」로 판단했는데 실제 조건은 「모집 중인 내 스터디가
  있다」(INV-Z14)라, 스터디가 전부 마감인 사람에게 링크가 떴다. 다른 하나는 **내가 지운
  스터디가 한 화면 안에서 두 가지로 처리된 것** — 「내가 만든 스터디」는 빼는데 「내가 쓴
  모집글」에는 그 이름이 남았다(`study_is_visible` 이 호스트에게는 지워진 것도 보여 준다).

  **승인된 형태가 이미 있는 자리를 새 형태로 바꿨던 것도 되돌렸다** — 「참여 중」을 민트 형광
  밑줄로 그렸는데, 형광 밑줄은 「일반 강조」 장치로 이미 배정돼 있어서 그러면 밑줄이 두 가지를
  뜻하고 둘을 가르는 것이 색뿐이 된다. 승인된 「신청 상태 3종」의 값으로 되돌렸다.

  **시드가 다시 돌게 됐다.** 0010 의 가입 트리거와 충돌해 첫 사람에서 죽고 있었다(백로그에
  걸려 있던 항목). `upsert` 로 고치면서 4절이 처음으로 도달 가능해졌고, 그 4절이 표 전체의
  `created_at` 을 다시 쓰고 있어서 대상을 이번 실행이 만든 것으로 좁혔다.

  기록: `workspace/logs/DECISION_LOG.study-mate-20260906-3.md`(D1~D29),
  사인오프: `workspace/review.md`(유닛 132 · 통합 124 · 변이 53/53 · 새 검사의 변이 검증 14/14).
  **보류 하나(P13)를 열어 둔 채 닫는다** — 「내 참여 상태」의 모양과 「거절됨」의 표현이 승인된
  기준에 비어 있다. 지금 화면은 승인된 값 안에 있고 어느 노드도 그것에 안 막힌다.


- `study-mate-20260906-2` — **보류 둘(P11·P12)에 사람 결정을 받아 반영.** 2026-09-06 에 종료
  조건이 성립해 닫았다.

  **P12 가 새 불변식이 됐다(INV-Z14)** — 새 모집글은 「모집 중」인 스터디에만 붙는다. 범위는
  사람이 「화면의 모집중 배지 그대로」로 골랐고(지워짐·닫힘·정원 참), 강제 위치는 모집글 삽입
  정책이다. 조건을 정책에 다시 적지 않고 화면이 쓰는 판정을 그대로 부른다. 이 결정이 앞
  사이클에서 드러난 INV-Z10 의 빈칸도 같이 덮어서, 어제 만든 `0014` 를 얹지 않고 새로 썼다.

  **P11 은 버튼 강조를 모집글 유무로 가르는 것으로 닫혔고**, 같이 정해진 「한 패널에 잉크
  버튼은 하나」가 승인된 시각 기준에 규칙으로 들어갔다.

  기록: `workspace/logs/DECISION_LOG.study-mate-20260906-2.md`(D1~D10),
  사인오프: `workspace/review.md`(변이 53/53 · 통합 124 · 유닛 101).
  **보류 0건.**


- `study-mate-20260906-1` — **`/posts/create` 모집글 작성 화면.** 2026-09-06 에 종료 조건이
  성립해 닫았다. 핵심 흐름의 마지막 빈칸이 닫혔다 — 이제 스터디를 만들고 모집글을 올려
  목록에 나타나는 데까지 브라우저에서 한 번 통과했다.

  **리뷰어 넷 중 셋이 독립적으로 같은 것을 짚었다** — INV-Z10 의 「새 모집글」 갈래에 강제
  위치가 없었다(`is_study_host` 가 `deleted_at` 을 안 본다). 새는 값은 없었지만 계약이
  거짓이었고, 하필 이번 사이클이 쓴 주석이 그것을 지켜진다고 적고 있었다. `0014` 로 닫았다.

  **브라우저에서 온 본문이 문단으로 안 나뉘는 것**도 이번에 처음 드러났다 — 폼이 보내는
  CRLF 를 아무도 정규화하지 않았고, 지금까지 본문이 전부 SQL 시드라 안 보였다.

  기록: `workspace/logs/DECISION_LOG.study-mate-20260906-1.md`(D1~D25),
  사인오프: `workspace/review.md`(변이 53/53 · 통합 121 · 유닛 101).
  **보류 둘(P11·P12)을 열어 둔 채 닫는다** — 둘 다 이 diff 의 결함이 아니라 아직 아무도
  안 정한 시각·제품 판단이고, 프론티어의 어느 노드도 그것에 안 막힌다.


- `study-mate-20260905-5` — **배포 준비 국면으로 예약해 둔 보안 셋.** 2026-09-06 에 종료 조건이
  성립해 닫았다. 셋을 닫았다: 회원 명부 전체가 비로그인에게 열려 있던 것(INV-Z13 · `0011`) ·
  입력 검증이 얇던 자리 넷 · 리다이렉트 목적지의 오리진이 요청 Host 에서 오던 것.

  보류 둘(P10·P6)을 열고 둘 다 사람 결정으로 닫았다. 리뷰어 셋을 파견해 지적을 반영했고,
  그 과정에서 **변이 판정이 거짓이던 자리**가 드러났다 — 갈래 둘을 함께 빼는 변이 하나가
  「잡혔다」를 내는 동안 한쪽 갈래는 검사가 0개였다. 갈래마다 변이를 쪼갰고, 그 교훈은
  회고에서 `.claude/rules/tdd.md` 로 올라갔다.

  **`deploy` 는 n/a 다** — 배포처 결정이 「배포 안 하고 로컬에서만 본다」였다.
  기록: `workspace/logs/DECISION_LOG.study-mate-20260905-5.md`(D1~D21),
  사인오프: `workspace/review.md`(변이 52/52 · 통합 119 · 유닛 86).

- `study-mate-20260905-4` — **배포 준비 국면의 보안 하한선.** 2026-09-05 에 종료 조건이
  성립해 닫았다. 둘을 닫았다: 회원가입이 프로필 없는 계정을 남기는 것(S1)과, 인가 판정
  함수가 그대로 RPC 로 열려 있어 "누가 어느 스터디의 멤버인가"를 비로그인이 재구성할 수
  있는 것(S4). 보류 셋(P7·P8·P9)을 열고 셋 다 사람 결정으로 닫았다.
  기록: `workspace/logs/DECISION_LOG.study-mate-20260905-4.md`,
  리포트: `workspace/reports/CYCLE_REPORT.study-mate-20260905-4.md`,
  사인오프: `workspace/review.md`.

- `study-mate-20260905-3` — **보류 셋(P3·P4·P5)의 답을 반영하고 review 재사인오프.**
  2026-09-05 에 종료 조건이 성립해 닫았다. 기록: `workspace/logs/DECISION_LOG.study-mate-20260905-3.md`,
  사인오프: `workspace/review.md`. **이 줄은 뒤늦게 적었다** — 그 사이클은 CYCLE.md 를
  건드리지 않은 채 커밋됐고(68eb9c7), 다음 사이클을 열면서 빈자리가 드러났다.

- `study-mate-20260905-2` — **핵심 흐름을 화면으로 세운다.** 2026-09-05 에 열렸다
  (사용자 지시: "이어서하자 … 목표는 1차 완성").
  발견 → 신청 → 승인 → 대화가 브라우저에서 한 번 통과했고, 리뷰가 잡은 인가 구멍 셋과
  파생값 결함을 닫았다. **아직 안 닫은 이유는 `review` 사인오프가 남았기 때문이다** —
  리뷰어 넷을 파견했지만 결과를 받기 전에 세션이 끝났다.
  종료 조건은 `review` 사인오프였고, 2026-09-05 에 성립해 닫았다.

- `study-mate-20260905-1` — 데이터 모델. 2026-09-05 에 종료 조건이 성립해 닫았다.
  불변식 14개(INV-P1~P7 · INV-Z1~Z7)가 실제 로컬 Postgres 에 붙는 테스트 37개에 붙들렸고,
  강제 장치를 하나씩 무력화한 변이 9건이 전부 빨간불이 됐다. 들여온 스펙이 "지금 안 지켜진다"고
  스스로 적어 둔 둘이 닫혔다 — 접근 정책 부재(INV-Z5)와 모집 상태의 주인 둘(INV-P6).
  기록: `workspace/logs/DECISION_LOG.study-mate-20260905-1.md`.

- `study-mate-20260904-2` — 세션 가드. 2026-09-05 에 종료 조건이 성립해 닫았다. 불변식 넷이
  테스트로 붙들렸고(변이 13건 전부 빨간불 확인), 실제 요청에서 보호 경로가 로그인으로 갔다.
  리뷰 사인오프는 `workspace/review.md`. 통과와 함께 열어 뒀던 스펙 지적 둘 중 하나
  (보호 경로 목록이 모집글 수정을 안 덮음)는 2026-09-05 에 화면 목록(`docs/IA.md`)이 생기면서
  닫았고, 나머지 하나(라우트 핸들러)는 INV-A5 로 백로그에 걸었다.

- `study-mate-20260904-1` — 워킹 스켈레톤. 2026-09-04 에 종료 조건(화면 한 장이 `localhost:3100`
  에서 뜬다)이 성립해 닫았다. 데이터 계층과 실제 기능은 예정대로 이 사이클에 넣지 않았다.

들여오기 자체는 이 프로젝트의 사이클이 아니라 킷 쪽 사이클(`signal2-20260904-2`)이었다.
한 사이클의 기록이 두 폴더로 갈리지 않게 그쪽에 그대로 두었다.
