---
project: study-mate
status: passed
basis: d7c2578f0283
reviewers: [test-auditor, security-reviewer]
---

# 리뷰 사인오프 — 눈에 안 보이는 글자, 검사 국면 (`study-mate-20260911-1`)

> 앞선 사인오프(AI 국면 `-20260910-3`·`-4`)는 이 파일의 git 이력에 있다.

## 이번 diff 가 무엇인가 — 제품 코드는 한 줄도 안 바뀌었다

직전 리뷰가 낸 스물넷 중 **안 닫은 아홉이 전부 「검사가 안 붙든다」쪽**이었다. 구현은 맞는데
되돌려도 아무도 안 깨진다는 뜻이라, 다음 사람이 조용히 되돌릴 수 있는 자리다. 이번 작업이
그 아홉을 닫았다.

바뀐 파일은 검사 여덟과 검사 도구 하나, 그리고 통합 스위트의 접속 주소 판정 하나다.
`src/**` 의 제품 코드(판정 함수·서버 액션·컴포넌트·마이그레이션)는 전부 그대로다.

## 누구를 파견했고 왜 둘인가

| 리뷰어 | 판단 |
|---|---|
| `test-auditor` | 파견 — 이번 diff 자체가 「검사가 무엇을 붙드나」이고, 그것이 이 리뷰어가 보는 자리다 |
| `security-reviewer` | 파견 — 방벽을 일부러 무력화하는 도구를 열두 개 넘게 늘렸고, 그 도구가 붙는 곳을 정하는 판정을 만졌다. 위험 표면 감지(`auth`·`authz`·`concurrency`)가 이 코드에 걸려 있기도 하다 |
| `code-reviewer` | **안 보냈다** — 도메인 로직과 상태 관리가 한 줄도 안 바뀌었다. 읽을 것이 없어 지적도 못 한다 |
| `ui-reviewer` | **안 보냈다** — 시각 변경이 없다. 스타일 파일은 **읽기만** 하고 안 고쳤다(검사가 그 본문을 읽는다) |

## 아홉을 어떻게 닫았나 — 그리고 리뷰가 넷은 「절반만 닫혔다」고 했다

**리뷰를 돌리기 전에 아홉을 다 닫았다고 말할 뻔했다.** 검사를 새로 쓴 것만으로는 닫히지
않는 것이 넷 있었다.

| 지적 | 처음 닫은 방식 | 리뷰가 짚은 것 | 최종 |
|---|---|---|---|
| `1` 앱 층 호출 자리 셋에 검사 0건 | 세 자리에 이름·제목·본문별로 거부 검사와 반대 절반을 넣었다 | **판정 순서를 아무도 안 잰다** — 두 판정에 동시에 걸리는 값이 목록에 한 건도 없어서 블록 자리를 바꿔도 답이 안 바뀐다 | U+061C 를 넣었다. 지우는 집합과 금지 목록에 **둘 다** 든 글자가 그것뿐이다 |
| `2` 「부류 전체」가 한 열에만 붙들림 | 같은 값 목록(보이지 않는 글자 26 · 서식 문자 12)을 네 열에 다 먹인다 | 닫혔다 | — |
| `3` 알림 열이 제일 얇음 | 같은 목록이 그 열에도 간다 | 닫혔다 | — |
| `4` 네 열의 제약 본문을 아무도 안 봄 | 열 이름만 지우고 넷을 서로 대 본다 | **넷을 똑같이 고치는 diff 가 통과한다** — 서로 같으니까 | 본문에 든 글자 집합이 **절대 기준**과 같은지도 본다 |
| `5` 앱 대조가 제목 한 열에만 감 | 이름 경로를 더하고, 판정별로 나눠 균형을 본다 | **값 목록이 데이터베이스 쪽 목록과 갈려 있다** — 둘 다에 없는 부류가 다섯이고 그 다섯은 앱 쪽을 아무도 안 잰다 | 한 목록에서 파생시킨다 |
| `6` 제어문자 판정을 넓히는 쪽이 안 막힘 | 보통 공백이 제어문자가 아니라는 검사를 넣었다 | 닫혔다 | — |
| `7` 실시간 방송 검사에 변이가 0건 | 쓰기 정책을 여는 변이를 등록했다 | 닫혔다 | — |
| `8` 「안 왔다」를 3초 시계로 판정 | 지어낸 방송 뒤에 진짜를 보내고 그것이 올 때까지 기다린다 | **두 경로의 도착 순서는 보장이 아니다.** 보내기가 서버 확인 없이 끝난다 | 확인을 켜고, 표시 메시지 뒤에 유예 0.5초를 붙였다 |
| `9` 방향 격리가 스타일 한 줄로 꺼짐 | 그 파일의 `.name` 규칙에 금지 속성이 있나 본다 | **빠져나가는 길이 셋** — 다른 파일 · 그 요소를 맞히는 다른 고르개 · 이름이 안 보이는 `all: unset` | 셋 다 막았다 |

## 리뷰어가 잡은 것 — 반영한 것

| 지적 | 누가 | 반영 |
|---|---|---|
| **[High] 붙을 곳을 정하는 판정이 우회된다.** 「문자열 패턴으로는 못 가린다 — 그래서 파싱한다」고 닫았다고 적힌 자리 **바로 아래**에 같은 부류가 남아 있었다. 접속 문자열 파서가 질의 매개변수를 주소의 호스트 **위에 덮어쓴다** — 판정을 통과한 주소가 실제로는 바깥에 붙고, 그 연결에 최고 권한 자격과 **인가를 일부러 무력화하는 문장들**이 실린다 | security | 호스트를 정하는 매개변수가 붙어 있으면 값에 상관없이 거부한다. **판정이 두 자리에 따로 있어서 둘 다 고쳤다** — 통합 스위트와 검사 도구는 서로를 못 부른다. 검사도 둘이다: 함수를 직접 부르는 것과 **도구를 실제로 실행해 거절을 보는 것** |
| **[Medium] 「없음으로 지키는 것」 둘이 복구 확인에 없다.** 실시간 방송의 쓰기 정책과 메시지 표의 갱신·삭제 정책은 어느 마이그레이션도 만들지 않아, 유일한 제거 수단이 손으로 적은 되돌리기 한 줄이다 — 그 줄에 오타가 나면 「복구 완료」가 그대로 찍힌다 | security | 복구 확인에 두 갈래를 더했다. **정책을 손으로 심고 돌려서 거절이 나는 것을 봤다** — 심기 전에는 「복구 완료」였다 |
| **[Medium-High] 판정 순서를 아무도 안 잰다** | test-auditor | 위 표 `1` |
| **[Medium] 값 목록이 두 자리에 따로 적혀 있다** | test-auditor | 위 표 `5` |
| **[Medium] 개수 임계값이 아무것도 안 막는다.** 실제 수가 26·25·15·25 인데 「12 이상」이었고, 좁혀도 옛 제약이 대신 물어서 수가 그대로다 | test-auditor | 개수를 버리고 **부류로 못 박았다** — 어느 옛 제약에도 안 걸리는 폭 없는 글자 열다섯은 `*_visible` 이 막았다고 단언한다 |
| **[Medium] 본문 대조가 상대적이다** | test-auditor | 위 표 `4` |
| **[Medium-Low] 방송 판정이 순서 가정에 올라타 있다** | test-auditor | 위 표 `8` |
| **[Low-Medium] 스타일 쪽 절반이 셋에 열려 있다** | test-auditor | 위 표 `9` |
| **[Low] 「지어낸 행이 저장 안 됐다」가 어떤 잘못된 구현으로도 안 깨진다.** 그 id 를 표에 넣는 경로가 제품에도 검사에도 없다 | test-auditor | **방송이 쏜 값이 표에 닿았나**로 바꿨다 — id 와 본문 둘로 묻고, 요청이 정한 시각을 단 행이 있는지도 본다 |
| **[Low] 한 검사에 갈래 둘이 묶여 있다.** 앞 단언이 실패하면 뒤 갈래를 아예 안 재는데 판정은 「잡혔다」가 된다 | test-auditor | 이름용·본문용으로 쪼개고 **이름표도 갈랐다**. 그러자 옛 이름표 둘이 검사 이름을 못 물어 「엉뚱한 빨간불」로 보고됐다 — **안전한 실패다**. 이름표를 맞췄다 |
| **[Low] 스펙 본문의 글자 목록이 구현보다 좁다**(INV-T1 · INV-T2 둘 다) | test-auditor | **안 고쳤다** — 승인된 스펙이라 재승인이 붙는 사람 결정이다. 백로그에 근거까지 적었다 |

## 반영하지 않고 올린 것 — 사람이 정할 것이 섞여 있다

- **`posts.title` 을 이 스펙의 범위에 넣을지**(`workspace/PENDING.md` P17) — 이번 재리뷰가
  근거 둘을 더했다. `1` 스터디 상세 화면이 **글자까지 같은 조립**을 쓰는데 그쪽만 판정이
  있다 — 이 스펙의 문제 진술이 그대로 재발한 자리다. `2` 방향 쪽뿐 아니라 **보이는 내용**
  쪽 손해도 난다(제목이 채움 문자 한 자인 모집글은 빈 카드로 그려진다).
- **알림 제목만 제어문자 제약이 없다** — 오늘 걸릴 경로는 없지만(원본이 이미 막는다) 알림을
  만드는 자리가 하나라도 늘면 그 줄로 들어간다. 마이그레이션 한 장짜리다.
- **판정이 하나도 없는 사람 글자 넷**(스터디 한 줄 소개·설명 · 모집글 본문 · 프로필 소개) —
  넷 중 「제품 글자 옆에 놓이는」 것은 한 줄 소개뿐이다. 나머지 셋은 길이 상한 쪽 일이다.
- **스펙 본문의 글자 목록이 구현보다 좁다**(위).

넷 다 `docs/BACKLOG.md` 와 `workspace/PENDING.md` 에 되살릴 조건과 함께 있다.

## 실행 근거

- **타입 통과** — `npx tsc --noEmit -p tsconfig.json`
- **유닛 608개 통과** — `npx vitest run` (시작 593 · 새 검사 15건)
- **통합 260개 통과** — `npm run test:integration` (시작 250 · 새 검사 10건)
- **강제 장치를 하나씩 무력화한 것 40건 — 전부 빨간불** (`npm run test:mutation`).
  새로 등록한 것이 스물둘, 기존이 열여덟이다. **되돌려도 안 깨지던 자리를 찾는 것이 이번
  작업의 전부였으므로 이 숫자가 결과 그 자체다.**
  - 그중 넷은 **값으로는 원리상 못 재는 되돌림**이다: 네 열의 제약을 **똑같이** 좁히는 것 셋과,
    태그는 그대로 두고 스타일 한 줄로 방향 격리를 끄는 것. 앞은 제약 본문의 글자 집합을,
    뒤는 스타일 규칙 본문을 읽어서 잡는다.
  - **낡아 있던 기존 것 둘도 고쳤다.** 직전 국면에 판정 줄이 바뀌었는데 그 줄을 가리키는
    문자열이 안 따라가서, 그동안 이 도구를 부르면 스위트 전체가 멈췄다.
- **위반을 일부러 심어 잡히는 것을 봤다** — 「위반 0건」과 「검사가 아예 안 돈 것」은 겉이 같다.
  - 주소 판정: 호스트를 바꾸는 매개변수를 단 주소로 도구를 돌려 **거절(종료 코드 1)** 을 봤고,
    비밀번호가 메시지에 안 나가는 것도 같이 봤다. 고치기 전에는 그 주소가 **통과**했다
    (설치된 파서에서 실측: 판정은 `127.0.0.1`, 실제 접속지는 `evil.example:5432`).
  - 복구 확인: 지우지 않는 이름으로 정책을 심고 복구를 돌려 **거절**을 봤다. 심기 전에는
    「복구 완료」였다.
  - 스타일 격리: 규칙에 한 줄을 심어 빨간불을 봤다.
- **게이트 통과** — `node gates/run-gates.mjs`. 남는 실패 하나는 **열린 보류 항목 신고**이고
  (P17) 막는 노드는 없다.

## 안 돌린 것 — 안 돌린 것과 적을 게 없는 것은 다르다

- **변이 전체(백 건 넘는다)는 안 돌렸다.** 이번에 돌린 마흔은 이 diff 가 닿는 자리
  (글자 판정 · 방송 · 주소 판정 · 스타일 격리)에 붙은 것 전부다. 나머지는 참가·인가·추천에
  붙은 것이고 이 diff 가 그 파일들을 안 건드렸다.
- **돌아가는 앱에서 다시 확인하지 않았다.** 제품 코드가 한 줄도 안 바뀌어서 화면에서 달라질
  것이 없다. 직전 국면의 확인이 그대로 유효하다.
- **탭 제목이 실제로 뒤집히는지는 여전히 안 쟀다** — 브라우저 크롬이라 페이지에서 못 잰다.
  P17 이 그 사실을 안고 있다.
