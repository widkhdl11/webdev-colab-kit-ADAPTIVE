# 결정 로그 — study-mate-20260907-8

사이클: 소개 화면 `/about` 을 붙이고, 사람이 정한 보류 P15 의 근거를 계약에 등재한다.
사람이 정한 것 하나(P15 = 그대로 둔다)와 화면 하나가 이 사이클의 입력이다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 범위 | **모집글 삭제가 좋아요를 같이 지우는 것을 그대로 둔다** | **딸려 있는 것의 무게가 다르다** — 스터디에 딸린 것은 참여 기록과 대화이고, 모집글에 딸린 것은 그 회차에 대한 반응이다. 한 스터디가 기수를 나눠 모집글을 여러 번 쓰므로, 회차가 사라질 때 그 회차의 반응이 같이 사라지는 것은 사람의 기록이 없어지는 것과 다르다 | 모집글도 소프트 삭제(마이그레이션 + 조회 정책 + 새 불변식 + 스펙 재승인 + 모집글을 읽는 자리 전부가 조건을 하나 더 본다) | 사람 결정 2026-09-07(보류 P15) |
| D2 | 문서 | **근거를 쓰기 인가 스펙의 INV-Z6 **바로 아래**에 적는다**(결정 기록이 아니라) | INV-Z6 을 읽는 사람이 「모집글도 그런가」를 묻는 자리가 거기다. 결정 기록에 적으면 찾아가야 알고, 안 찾아가면 근거 없이 다른 상태 그대로다 | `docs/DECISIONS.md` 에만 적기 | CLAUDE.md 「결정이 내려지면 즉시 문서 갱신」 |
| D3 | 문서 | **새 불변식을 만들지 않고 산문 블록으로 적는다** | 지킬 것을 새로 정한 게 아니라 **왜 INV-Z6 의 대상이 아닌지**를 적는 것이다. 불변식으로 올리면 참조 테스트가 요구되는데, 붙들 것이 「지금 그대로」라 그 테스트는 현재 동작을 베끼는 알리바이가 된다 | INV-Z14 신설 | 줄 맨 앞 `- INV-` 만 정의로 세므로 산문은 커버 요구를 안 만든다 |
| D4 | 문서 | **다시 열리는 조건을 같이 적는다** — 좋아요에 뜻이 붙는 것(담아 둔 글 목록·좋아요 알림·좋아요 정렬) | 근거가 「지금 좋아요는 수를 세는 것뿐」이라는 사실 위에 서 있다. 그 전제가 깨지는 시점을 안 적으면, 전제가 깨진 뒤에도 결론만 남아 그대로 인용된다 | 결정만 적기 | 백로그의 `likes_del_self` 항목이 같은 자리 |
| D5 | 시각 | **`/about` 은 시안 없이 바로 구현한다** | 화면 목록이 `/about` 을 홈의 **랜딩 언어**를 물려받는 화면으로 이미 배정해 뒀다. 새 레이아웃 언어가 아니라 뒤집어도 버릴 것이 이 화면 하나다 | design-interview → 시안 → checkpoint | `docs/IA.md` 「화면 언어는 세 종류뿐이다」 · design-drafting 규칙의 「반복」 갈래 |
| D6 | 시각 | **히어로는 1열**(홈은 2열) | 홈의 오른쪽 칸은 주간 플래너이고 그것은 「지금 누구인가」에 달린 홈 전용 장치다. 소개에 빈 칸을 두거나 예시 플래너를 또 그리면, 읽는 화면에 읽을 것이 아닌 것이 절반을 차지한다 | 2열 유지 + 오른쪽에 예시 플래너 | 랜딩 언어는 「히어로 + 섹션 쌓기」이고 2열은 히어로의 한 변형이다 |
| D7 | 시각 | **닫는 칸의 잉크는 「모집글 둘러보기」**(홈은 「스터디 만들기」) | 「잉크 행동 규칙」이 **지금 이 화면에서 해야 할 일**로 잉크를 정하라고 한다. 소개를 다 읽은 사람의 다음 걸음은 무엇이 열려 있는지 보는 것이고, 만드는 쪽은 찾을 스터디가 이미 정해진 사람의 길이라 한 걸음 뒤다 | 홈과 같이 「스터디 만들기」를 잉크로 | design-rules 「잉크 행동 규칙」(2026-09-06 스터디 상세의 같은 판단) |
| D8 | 구조 | **닫는 칸을 `shared/ui/cta-card/` 로 뽑는다** | 홈이 갖고 있던 값(패딩 44/28 · 가운데 정렬 · 제목 clamp · 버튼 줄)을 소개가 그대로 쓴다. **두 벌이 되면 한쪽만 고쳐지고, 그 어긋남은 두 화면을 나란히 놓기 전까지 아무도 못 본다** — 이 레포가 폼 카드 껍데기 다섯 벌에서 겪은 그대로다 | 소개에 같은 값을 다시 적기 | 2026-09-06 「폼 화면의 껍데기」를 모은 판단 |
| D9 | 구조 | **어느 버튼이 잉크인지는 `CtaCard` 가 안 정한다**(부르는 쪽이 넘긴다) | D7 이 말한 대로 잉크는 **그 화면에서 해야 할 일**로 정해지는데, 컴포넌트는 자기가 어느 화면에 있는지 모른다 | `primary` 속성으로 컴포넌트가 정하기 | design-rules 「잉크 행동 규칙」 |
| D10 | 범위 | **카테고리 칩 줄을 소개에 안 넣는다** | 홈의 「무엇을 공부하시나요?」와 같은 섹션이 된다. 두 화면이 같은 데이터를 같은 모양으로 그리면 어느 쪽이 정본인지가 사라진다 | 8색 칩으로 카테고리 8종 보여주기 | 「값이 두 벌인 자리」를 만들지 않는다 |
| D11 | 시각 | **산문 블록에 `word-break: keep-all`** | 안 걸면 한글 어절이 줄 끝에서 글자 단위로 끊긴다 — 실제로 「스터디를 찾다 / 가 이야기를」로 나온 것을 브라우저에서 봤다. 읽는 화면이라 이 화면에서 가장 크게 드러난다 | 그대로 두기 | `home-hero.module.css` 의 플래너 블록이 쓰는 같은 값 |
| D12 | 접근성 | **단계 숫자(1~4)를 읽어 주는 기계에서 숨기지 않는다** | 전역 리셋이 `ul, ol` 의 `list-style` 을 없앤다. 그러면 순서를 말해 주는 표지가 이 숫자뿐이라, `aria-hidden` 을 붙이면 「발견 · 신청 · 승인 · 대화」가 순서 없는 넷이 된다 | `aria-hidden="true"` (장식으로 취급) | `tokens.css` 의 리셋 · 화면이 「네 걸음」이라고 말하는 부제 |
| D13 | 카피 | **소개에 「밝혀 둘 것」 구역을 둔다** — 데모라는 것, 예시 데이터라는 것, 결제가 없다는 것 | 제품 원칙 5(「정직한 데모」)가 가장 먼저 시험받는 자리가 소개 화면이다. 여기가 보통 실적·후기·수치가 들어가는 자리이고, 이 제품에는 그것이 **없다** | 실적 수치·후기 넣기 · 아무 말 안 하기(푸터 한 줄로 충분하다고 보기) | `docs/PRODUCT.md` 제품 원칙 5 · 푸터가 이미 같은 취지의 한 줄을 갖고 있다 |
| D14 | 시각 | **닫는 칸의 세로 여백을 44 → 48 로 되돌린다**(`--space-48` 을 새로 만든다) | 승인된 시안이 48 인데 구현이 44 였다. 이번 사이클이 만든 어긋남은 아니지만 **그 값이 이제 홈 한 곳이 아니라 랜딩 언어 전체의 값이 됐다** — 안 맞추면 다음 랜딩 화면을 만드는 사람이 시안을 보고 48 을 물려받아 두 화면이 갈린다 | 44 를 유지하고 시안·시각 기준의 숫자를 44 로 고치기 | ui-reviewer M1 · code-reviewer 6 · 승인된 시안 `home.html` |
| D15 | 시각 | **소개의 히어로 제목·부제를 랜딩 원본 값으로 되돌린다**(제목 `clamp(34px, 5vw, 54px)` · 행간 1.65) | 물려받는다는 것은 값을 그대로 쓴다는 뜻이다. 내가 고른 값에는 근거가 없었고, 근거 없이 다르면 랜딩 히어로가 두 벌이 된다 | 작은 값을 유지하고 시각 기준에 「읽는 화면의 히어로」를 새로 등재하기 | ui-reviewer M2 |
| D16 | 시각 | **부제 폭 46ch 는 남긴다**(랜딩은 34ch) | 홈은 오른쪽에 플래너가 있어 히어로가 2열이라 34ch 가 그 칸의 폭이다. 소개는 1열이고 이 문단이 화면의 첫 설명이라 34ch 로는 여섯 줄이 된다 | 34ch 로 맞추기 | 승인된 방향이 다루지 않은 세부(auto-decide) |
| D17 | 시각 | **카드 제목·구역 제목을 19px 한 값으로 모은다** | 시안의 카드 제목이 19px 이고 `post-card` 와 `profile-overview` 의 구역 제목이 이미 그 값이다. 내가 18px 을 새로 만들면서 레포의 카드 제목이 19/18/17 세 벌이 될 뻔했다 | 18px 유지 | ui-reviewer M3 · code-reviewer 7 |
| D18 | 시각 | **카드 본문은 16px 로 남긴다**(시안의 곁글은 15px) | 시안의 15px 은 카드에 붙는 곁글이고 소개의 그 자리는 화면의 본문이다. 접근성 하한선이 본문 16px 이라 그 값을 따른다 | 15px 로 맞추기 | ui-layers 규칙 「본문 16px 이상」 |
| D19 | 구조 | **여는 표(eyebrow)도 `shared/ui/eyebrow/` 로 뽑는다** | 닫는 칸을 모은 근거(D8)가 여는 칸에도 그대로 참이다 — 홈과 소개의 히어로가 같은 언어라, 한쪽에서 테두리나 여백을 고치면 다른 쪽이 조용히 어긋난다. 선언 열 개가 바이트까지 같았다 | 소개에 같은 값을 다시 적기 | code-reviewer 1 |
| D20 | 카피 | **「밝혀 둘 것」 첫 줄을 푸터와 다른 문장으로 바꾸고, 「기능은 예시가 아니다」를 더한다** | 푸터 문장을 그대로 옮겨 적어서 **한 화면에 같은 문장이 두 번** 보였다(소개는 푸터를 달고 있다). 소개는 푸터가 못 담는 것을 적을 수 있는 자리다 | 문장을 그대로 두기 · 상수 하나를 두 곳이 나눠 쓰기 | code-reviewer 2 |
| D21 | 카피 | **「신청한 뒤의 상태를 감추지 않습니다」를 「신청이 지금 어디까지 왔는지 늘 보입니다」로** | 앞 제목은 모든 상태를 다 보여 준다고 읽히는데, 탈퇴·강퇴는 프로필 현황에서 빠져 있다(그릴 모양이 승인된 적 없다). 본문이 드는 셋과 제목의 범위를 맞췄다 | 그대로 두기 | security-reviewer 정보 항목 |
| D22 | 카피 | **「로그인이 처음 필요해지는 자리」에 「찾는 길에서」를 붙인다** | 만드는 길은 로그인 벽이 더 앞에 있다(`/studies/create` 가 보호 경로다). 그 섹션이 신청 흐름을 말하는 자리라 틀린 문장은 아니었지만, 한 단어로 정확해진다 | 그대로 두기 | security-reviewer 정보 항목 |
| D23 | 접근성 | **소개의 목록 셋에 `role="list"` 를 붙인다** | 전역 리셋이 `ul, ol` 의 `list-style` 을 없애서 Safari/VoiceOver 가 목록을 목록으로 안 읽는다. 소개는 목록으로 이뤄진 화면이라 여기가 가장 크게 드러난다 | 아무것도 안 붙이기 · 지금 레포 전체를 고치기(리셋 한 곳에서 잡는 것이 맞고, 그건 백로그로) | ui-reviewer L5 |
| D24 | 문서 | **모집글이 왜 소프트 삭제 규칙 밖인지를 그 규칙 문장에도 표시한다** | 스펙 45줄의 「참조가 전혀 없는 것만 하드 삭제한다」에 주어가 없어 규칙 전반처럼 읽혔다. 새 블록이 모집글을 그 밖에 두는데, 두 문단을 따로 읽는 사람은 어긋난 것으로 본다 | 새 블록만 적기 | security-reviewer 정보 항목 |
| D25 | 문서 | **P15 가 다시 열리는 조건을 `likes` 테이블 주석으로도 남긴다**(마이그레이션 0016) | 근거가 「지금 좋아요는 수를 세는 것뿐」이라는 사실 위에 서 있는데, 그 사실이 깨졌다는 것을 알려 줄 자리가 문서뿐이었다. **좋아요에 기능을 붙이는 사람이 반드시 여는 곳은 이 테이블이다** | 문서에만 적기 | security-reviewer 정보 항목 |
| D26 | 검사 | **링크 검사를 「경로 → 라우트」와 「라우트 집합」 두 방향으로 만든다** | 한 방향만으로는 라우트가 **사라지는** 쪽을 못 잡는다 — 동적 칸이 아무 값이나 받아서, `/studies/create` 를 지워도 `/studies/[id]` 가 대신 맞아 준다. 그 상태의 제품은 404 가 아니라 **틀린 화면**을 그린다 | 링크 방향만 검사 | test-auditor 1 |
| D27 | 검사 | **`href=` 라는 이름에 안 걸고 `/` 로 시작하는 문자열 전부를 본다. `.ts` 도 훑는다** | `footHref: "/signup"` · `more={{ href: "/posts" }}` · `redirect("/")` 가 전부 빠져 있었다. **`/about` 이 죽어 있던 것과 글자 하나 안 다른 자리들이다** | `href="` 만 보기 | test-auditor 4·5 |
| D28 | 검사 | **템플릿 문자열의 고정 칸도 판정하되, 한 칸이 글자와 실행 시점 값으로 섞이면 판정하지 않는다** | `` `/posts/${id}/edit` `` 는 앞뒤가 고정이라 오타가 잡힌다. 반면 `` `/posts${질의를 만드는 함수(…)}` `` 는 붙는 값이 질의인지 경로의 뒷글자인지 알 수 없다 — 아는 척하면 없는 경로를 지적한다 | 템플릿을 통째로 빼기(전체 링크의 3분의 1이 빠진다) | test-auditor 2 |
| D29 | 검사 | **개수 대신 정체를 못박는다** — 어느 파일에서 무엇을 뽑았는지 | 「30건 이상 훑었다」는 훑는 범위가 좁아져도 통과한다. 푸터에서 `/about` 이 실제로 나왔다를 못박으면 범위가 좁아지는 순간 빨간불이 된다 | 개수만 보기 | test-auditor 6 |
| D30 | 검사 | **`npm test` 에서 `--passWithNoTests` 를 뗀다** | 그 플래그가 있으면 검사 파일 이름만 바꿔도 조용히 실행 대상에서 빠지고 게이트는 초록불이다 — **검사가 사라진 것과 위반이 0건인 것이 또 같아 보인다.** 검사가 하나도 없던 시절의 잔재다 | 그대로 두기 | test-auditor 9 곁다리 |
| D31 | 검사 | **판정기를 `route-links.helpers.ts` 로 나눈다** | 검사 파일에 경로 예시 문자열이 있어야 하는데, 훑는 범위가 넓어지면 검사가 자기 예시를 죽은 링크로 잡는다. 지금은 `src/` 밖이라 안 훑지만 그 규칙을 파일 나눔으로 보이게 뒀다 | 한 파일에 두기 | test-auditor 8 |
