# 결정 로그 — study-mate-20260908-2

사이클: 헤더 종 아이콘에 **알림 패널**을 단다. 데이터베이스는 이미 다 있었고(표 · 자기 것만
보는 정책 셋 · 참가 사건에서 알림을 만드는 트리거) 없던 것은 **계약 · 화면 · 액션**이다.
승인된 세 언어(랜딩·목록·상세)에 「떠 있는 면」이 하나도 없어서 시각 방향도 새로 정했다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 계약 | **알림 스펙을 세운다 — 불변식 여덟(INV-N1~N8)** | 정책 셋과 트리거가 처음부터 있었는데 **계약이 그 자리를 적은 적이 없다.** 적히지 않은 것은 다음에 정책을 손대는 사람 앞에서 근거가 못 되고, 지워도 아무 검사가 안 난다 | 「이미 지켜지니 안 적는다」 | 사람 승인 2026-09-08. INV-Z15 가 2026-09-07 에 같은 이유로 세워진 자리 |
| D2 | 계약 | **INV-N5 — 갱신으로 바뀔 수 있는 열은 `read_at` 하나뿐 (마이그레이션 `0019`)** | `0002` 가 참여자·채팅 참여·모집글·스터디를 열 단위로 좁힐 때 **알림만 그 목록에 없었다.** 접근 정책의 `with check` 는 받는 사람만 보므로 그 판정을 통과한 갱신이 `type`·`title` 을 요청이 보낸 값으로 바꾼다 — 「거절되었습니다」를 「수락되었습니다」로 고쳐 둘 수 있고, 남이 못 보는 자리라 티도 안 난다 | 정책 조건에 열 이름을 적기(정책은 열을 못 가른다) | 사람 승인. INV-Z8 이 같은 모양으로 세워진 자리 |
| D3 | 시각 | **드롭다운 하나로 간다 — 모바일도 같은 카드** | 이 서비스가 햄버거를 안 쓰기로 한 근거(「감추려고 여는 동작을 하나 더 만들면 손이 늘어난다」)가 그대로 적용된다. 데스크톱과 모바일이 같은 동작이면 새로 배울 것이 없다 | ① 모바일만 아래 시트 ② 오른쪽 서랍 ③ 가운데 모달 | 사람 결정 2026-09-08 (`docs/design/INTERVIEW.md`) |
| D4 | 시각 | **안 읽음 = 왼쪽 4px 코랄 세로 띠 + 문장이 잉크색·굵게** | 점은 승인된 규칙에서 「모집 상태」의 형태라 겹치고, 코랄 색면 위에서는 약한 잉크가 대비 미달이라 줄 전체를 진한 잉크로 바꿔야 한다 | ① 코랄 점 ② 배경 색면 ③ 굵기만 | 사람 결정. design-rules 「색이 겹치면 모양으로 가른다」 |
| D5 | 시각 | **알림 삭제는 한 번에 실행된다 — 「무게가 다른 삭제」로 예외 등재** | 승인된 「파괴적 행동은 두 단계」의 근거는 **잃는 것의 무게**인데, 알림은 이미 일어난 일의 복사본이라 지워도 참여 상태와 스터디가 그대로 남는다. 모집글은 지우면 그 회차 자체가 없어진다 | 규칙대로 두 단계(줄마다 확인 단계가 생겨 목록이 시끄러워진다) | 사람 결정. **예외가 다시 열리는 조건까지 규칙에 적었다** — 알림에만 있는 값이 생기면 근거가 성립하지 않는다 |
| D6 | 시각 | **코랄 띠 2.13:1 을 그대로 둔다** | 비텍스트 하한 3:1 에 미달이지만, 안 읽음을 **셋이 같이 말한다**(띠 · 문장 굵기·색 · 낭독기용 「안 읽음」 글자). 띠 하나가 정보를 혼자 지지 않으므로 그 하한이 적용되는 자리가 아니다 | ① 띠에 1px 잉크 윤곽 ② 폭 6px | 사람 결정. **미달이라는 사실은 규칙에 그대로 적었다** — 다음에 코랄을 「혼자 뜻을 지는 표시」로 쓰려는 사람은 거기서 멈춰야 한다 |
| D7 | 시각 | **읽은 줄에서도 스터디 이름은 잉크 700** | 이름까지 흐리면 어느 스터디 소식인지 훑을 수 없다. 읽음·안 읽음은 띠와 **나머지 문장**의 무게·색이 가른다 | 줄 전체를 약한 잉크로 | 사람 결정 |
| D8 | 시각 | **줄에는 삭제(×) 하나만. 줄 전체가 링크다** | 알림 한 줄은 좁고, 버튼이 둘이면 문장이 갈 자리가 없다. 마우스를 올렸을 때만 나타나는 방식은 터치 화면에 그 상태가 없어서 손가락으로 못 쓴다 | ① 읽음·삭제 아이콘 둘 ② hover 에서만 노출 | auto-decide. 「한 패널에 잉크 버튼은 하나다」와 같은 결 |
| D9 | 계약 | **알림 문장은 저장된 값(`type`·`title`)만으로 짓는다 (INV-N6)** | 지금의 스터디를 읽어 문장을 지으면 **이름이 바뀔 때 지난 알림의 문장까지 같이 바뀌고**, 원본이 사라진 알림은 문장 자체를 잃는다. 표 주석이 「그때 이런 일이 있었다는 사실 기록」이라고 적어 둔 그대로다 | 판독기에서 `studies` 를 조인해 지금 제목 쓰기 | 사람 승인. `0001_init.sql:190-204` 의 표 주석 |
| D10 | 계약 | **가리키는 대상이 지금 안 보이면 링크를 안 건다 (INV-N7)** | 알림 행에는 참조 대상 외래 키가 없어서 스터디가 지워져도 알림은 남는다. 링크를 그리면 **누르지 않아도 미리 가져오기가 404 를 만든다** — 2026-09-07 에 `/about` 이 없어서 났던 것과 같은 모양이다 | 링크는 그대로 두고 도착지에서 404 를 보여 주기 | 사람 승인. 표 주석이 「화면이 처리한다」고만 적고 무엇을 하는지는 아무 데도 없었다 |
| D11 | 구조 | **보이는지 묻는 질의는 `id` 만 가져온다** | 제목을 같이 읽으면 다음 사람이 그 값으로 문장을 짓게 되고 D9 가 깨진다 | `id, title` 을 한 번에 읽어 쓰기 | 검사가 이 조건을 붙든다(`studies:columns === "id"`) |
| D12 | 구조 | **종 옆 숫자를 클라이언트 컴포넌트가 그린다 — 목록을 받은 뒤에는 그 목록에서 센 값** | INV-N8 을 **구조로** 지킨다. 숫자와 화면이 다른 값에서 나오면 어긋날 자리가 생기는데, 같은 목록에서 세면 어긋날 수가 없다 | 서버가 센 값을 헤더가 그대로 그리기(읽음 처리 뒤 갱신이 늦으면 어긋난다) | INV-N8 |
| D13 | 구조 | **알림을 바꾸는 액션은 `revalidatePath("/", "layout")` 을 부른다** | 종 옆 숫자는 **모든 화면의 머리**에서 다시 세어지는 값이고, 지금 어느 화면에서 패널을 열었는지는 서버가 모른다. `"page"` 로 두면 「/」 한 장만 다시 그려지고 다른 화면은 옛 값으로 남는다 | 경로를 클라이언트가 실어 보내기(그 값은 요청이 들고 온 값이다) | INV-N8. 비용은 code-reviewer 에게 따로 물었다 |
| D14 | 검증 | **갱신·삭제 정책은 「조건 없는 문장」으로 따로 확인한다** | 조건이 붙은 갱신(`where id = …`)은 그 조건을 계산하느라 **조회 정책이 같이 걸린다.** 그래서 갱신 정책을 통째로 열어 놔도 조회 정책이 대신 막아 검사가 초록불이었다 — 변이 `n4-notifications-update-open` 이 빠져나갔다 | 조건 붙은 검사만 두기 | 2026-09-08 변이 검증이 실제로 잡았다. 이 사실을 스펙 INV-N4 아래에도 적었다 |
| D15 | 성능·계약 | **목록은 한 번에 100줄** | 여기서 자른 만큼 종 옆 숫자와 목록이 어긋난다(INV-N8). 숫자는 99 를 넘으면 「99+」로 적으므로 **100 이면 사용자가 볼 수 있는 모든 경우에 둘이 맞는다** | 20줄(안 읽음이 21건이면 숫자와 목록이 어긋난다) | INV-N8 |
| D16 | 화면 | **상대 시각에 분·시간을 더한다 (`relativeTime`)** | 알림은 오늘 온 것이 대부분이라 `relativeDay` 만 쓰면 방금 온 것과 아침에 온 것이 둘 다 「오늘」이 된다 | 새 어휘를 따로 만들기(한 서비스가 시각을 두 어휘로 말하게 된다) | 기존 `relativeDay` 를 감싸서 하루가 넘으면 그쪽 문장이 그대로 나온다 |
| D17 | 구조 | **클라이언트 컴포넌트는 슬라이스 대문이 아니라 `model` 파일을 직접 집는다** | 대문이 서버 전용 판독기(`next/headers` 를 지난다)를 같이 내보내므로, 대문을 집으면 브라우저 번들이 서버 코드를 끌고 들어가 빌드가 죽는다 | 대문에서 model 만 다시 내보내는 두 번째 대문 만들기 | `bundle/CLIENT_IMPORTS_SERVER` 게이트가 편집 즉시 잡았다 |
| D18 | 화면 | **640px 아래에서 `.anchor` 의 `position: relative` 를 뗀다** | 좁은 화면에서는 헤더가 두 줄로 접히면서 종 버튼이 오른쪽 끝에 안 붙는데, 패널은 그 버튼 기준으로 왼쪽으로 360px 를 뻗는다. **390px 화면에서 실제로 왼쪽 끝이 -203px 였다** — 화면 밖 왼쪽은 가로 스크롤도 안 만들어서 잘린 것을 아무도 못 본다 | 시트로 바꾸기(D3 을 뒤집는 일이다) | 2026-09-08 브라우저 실측. sticky 인 헤더가 대신 기준이 된다 |
| D19 | 검증 | **변이 열둘을 등록한다** — 정책 셋 · 삽입 정책 부재 · 트리거 둘 · 열 권한 · 앱 다섯 | 통과하는 검사는 그것만으로 증거가 아니다. 특히 **「없는 것」이 계약인 자리**(삽입 정책 부재)는 정책을 하나 **더해서** 무력화해야 한다 — 빠뜨린 것과 일부러 안 둔 것은 겉이 같아서, 다음 사람이 「삽입이 안 되네」 하고 만들어 넣는 것이 실제로 일어날 수 있는 변경이다 | 새 검사만 붙이고 변이는 안 만들기 | `.claude/rules/tdd.md` |
| D20 | 검증 | **`0019` 를 변이 복구 목록에 넣는다** | 목록에 없으면 `n5-notifications-columns` 가 심은 「표 전체 갱신」이 안 돌아오고, **그 뒤의 변이가 전부 알림을 마음대로 고칠 수 있는 데이터베이스에서 판정된다** | 그대로 두기 | 2026-09-06 에 `0015` 로 같은 일이 났다 |

## 리뷰 뒤에 정한 것 (2026-09-08, 리뷰어 넷)

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D21 | 검증 | **참가 사건을 그 사람의 공개 키 연결로 일으킨다** | 관리 연결은 접근 정책을 우회하므로, 그것만 쓰면 트리거가 한 번도 정책 아래에서 안 돈다. 그 상태에서는 `security definer` 를 지워도 전 스위트가 초록불인데 **제품은 참가 신청이 아예 안 된다**(트리거가 42501 로 실패하면서 참여자 삽입까지 롤백) | 준비물은 관리 연결로 두고 변이만 추가 | 2026-09-08 test-auditor · 변이 `n2-notify-invoker` |
| D22 | 계약 | **자르는 기준을 「최근」이 아니라 「안 읽은 것 먼저」로** | 「최근 100줄」로 자르면, 최근 것이 전부 읽음인 사람에게 **종 옆 숫자 50 / 목록의 안 읽음 0** 이 되고 화면이 숫자를 0 으로 바꾼다. 그러면 「전체 읽음」이 비활성이 되어 그 50건을 지울 손잡이가 사라진다 | ① 상한을 올리기(어긋나는 지점만 밀린다) ② 질의를 하나 더 두기(판독기가 복잡해진다) | 2026-09-08 code-reviewer · 변이 `n8-reader-oldest-first` |
| D23 | 구조 | **폼 칸 이름을 상수 한 자리로 뽑는다** | 보내는 쪽과 읽는 쪽이 각자 문자열을 적고 양쪽 검사도 각자 자기 문자열을 쓰면, 한쪽만 바뀌는 날 **읽음 처리와 삭제가 언제나 실패**하는데 전 스위트가 초록불이다 | 검사에서 문자열을 대조하기만 하기 | 2026-09-08 test-auditor · 변이 `n4-bell-form-key-drift` |
| D24 | 구조 | **「안 읽음」의 정의와 목록 옮기기를 엔티티로 올린다** | 「`readAt` 이 null 이면 안 읽음」은 표시 규칙이 아니라 도메인 어휘다. 화면 두 곳에서 각각 다시 쓰면 한쪽만 고쳐지는 날 띠는 붙는데 숫자는 안 세는 상태가 된다 | 화면에 두고 주석만 달기 | 2026-09-08 code-reviewer · ui-reviewer · `.claude/rules/ui-layers.md` |
| D25 | 화면 | **판독기가 「안 보임」과 「못 물어봤음」을 갈라서 준다 (`gone`)** | 가시성 조회가 한 번 실패하면 그 요청의 알림 전부가 「지워진 스터디예요」로 표시됐다. 링크를 안 거는 것은 맞고, 어긋난 것은 **거기에 이유까지 붙인 것**이다 — 알림 문장은 사실 기록인데 그 옆에 사실이 아닌 단정이 붙는다 | 문구를 「지금은 열 수 없습니다」로 낮추기 | 2026-09-08 security-reviewer · code-reviewer |
| D26 | 서버 | **읽음 처리를 멱등으로 만든다 (`.is("read_at", null)`)** | 읽은 알림을 다시 눌러도 저장된 「읽은 시각」이 지금으로 밀렸다. 지금은 null 여부로만 쓰니 티가 안 나지만, 그 시각을 보여 주거나 정렬에 쓰는 기능이 붙는 순간 값이 조용히 틀어진다 | 화면 쪽에서만 안 부르기 | 2026-09-08 code-reviewer |
| D27 | 접근성 | **「안 읽음」 낭독기 글자를 링크 안으로 옮긴다** | 코랄 띠(2.13:1)를 그대로 둔 근거가 「안 읽음을 셋이 같이 말한다」인데, 셋째 다리가 링크 밖에 있으면 **낭독기로 링크만 훑을 때 사라진다** — 상황에 따라 다리가 둘이 된다 | 그대로 두기 | 2026-09-08 ui-reviewer |
| D28 | 접근성 | **삭제 단추 이름에 그 줄의 문장을 담는다** | 전부 「이 알림 지우기」면 낭독기의 단추 목록에 같은 이름이 N개 뜨고 어느 것이 무엇인지 가릴 방법이 없다 | 그대로 두기 | 2026-09-08 ui-reviewer · 승인 시안이 이미 그렇게 그려 놨다 |
| D29 | 접근성 | **초점 갈 곳이 0개면 패널 자신을 붙든다** | `aria-modal="true"` 로 「이 안이 전부」라고 말해 놓고 Tab 이 뒤 페이지로 새면, 낭독기에 한 말과 실제가 다르다. 0개가 되는 경로가 넷 있다 | `aria-modal` 을 떼기(사람이 승인한 시안이 가두기를 전제로 한다) | 2026-09-08 ui-reviewer |
| D30 | 검증 | **트리거 변이를 갈래마다 쪼갠다** | 수락·거절·강퇴를 한 변이로 묶고 이름표가 하나였다. 그래서 **강퇴 갈래는 검사가 0개인데 「잡혔다」로 보고**됐다 | 그대로 두고 검사만 추가 | 2026-09-08 test-auditor · `.claude/rules/tdd.md` |
| D31 | 검증 | **세는 쪽에도 검사와 변이를 붙인다** | INV-N8 은 벽이 둘인데 그리는 쪽만 붙들려 있었고 `read-unread-count.ts` 는 검사 파일이 아예 없었다. 조건을 지우면 「숫자 3, 열면 안 읽음 0」이 된다 — 불변식의 위반 문장 그대로다 | 그리는 쪽 검사로 갈음하기 | 2026-09-08 test-auditor · 변이 `n8-count-ignores-read` |
| D32 | 범위 | **스터디 제목의 길이 상한을 이번에 안 고친다** | 알림이 그 값을 남의 헤더로 옮기는 첫 경로인 것은 맞지만, 고칠 자리는 알림이 아니라 **원본(스키마 제약)**이고 그건 다른 표의 계약이다. 알림 쪽에서 잘라 그리면 INV-N6 과 부딪힌다 | 알림 판독기에서 자르기 | 2026-09-08 security-reviewer · 백로그에 등재 |
| D33 | 범위 | **참가 상태 전이 제한을 이번에 안 정한다** | 호스트가 `rejected → kicked` 를 반복해 알림을 무한히 보낼 수 있다. **계약이 안 적은 자리라 코드보다 스펙이 먼저**이고, 그 판단은 참가·정원 스펙에 속한다 | 알림 쪽에 상한을 두기(증상만 가린다) | 2026-09-08 security-reviewer · 백로그에 등재 |
