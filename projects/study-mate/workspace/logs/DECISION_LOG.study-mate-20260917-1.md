# 결정 로그 — study-mate-20260917-1

사이클: **대시보드 재구성 v3 — 읽는 순서·라벨 필수·화면 어휘.** 제품 그래프 밖(킷 작업)이다.
새 기능이 아니라 있는 정보의 재배치와 어휘 통일이라, 결정의 대부분은 "무엇을 어디에 두나"와
"무엇을 무엇이라 부르나"다.

착수 전 질문 넷 중 셋은 사용자가 정했다(①b · ②a · ③기본안). 아래 표는 그 셋을 뺀,
내가 정하고 넘어간 것들이다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 구조 | **어휘표를 `assets/ui-vocab.mjs` 로 둔다**(JSON 아님) | 화면은 빌드 도구 없이 `<script type="module">` 로 자산을 그냥 읽는다. JSON 이면 `fetch` 가 하나 늘고, 404 로 떨어질 때 라벨이 통째로 비는 실패가 생긴다. `.mjs` 는 import 라 브라우저와 node 검사가 같은 것을 읽는다 | `report/ui-vocab.json` — 프롬프트가 열어 둔 다른 후보 | NEW (킷 관례: 자산은 전부 import 로 읽는다) |
| D2 | 구조 | **절 함수가 문자열이 아니라 `{ label, value, fromRecord }` 줄의 배열을 돌려준다** | "모든 값에 라벨이 붙는다"를 화면 코드의 성의가 아니라 데이터로 만들어야 검사가 전수로 볼 수 있다. 문자열을 돌려주면 라벨 누락이 눈으로만 보인다 | 화면이 라벨을 직접 붙이고 검사는 HTML 을 정규식으로 본다 | NEW |
| D3 | 검사 | **어휘 검사는 라벨 전수 + 렌더가 만든 값만**. 기록에서 실려 온 값은 대상 밖 | 값까지 막으면 "그래프 밖"이 들어간 요청 원문을 못 그린다. 그래서 줄마다 `fromRecord` 를 들고 다니게 하고 검사가 그것으로 가른다 | 화면 문자열 전체를 막는다(아무 요청도 못 그림) / 검사를 안 한다 | 사람 답변 ③ |
| D4 | 화면 | **「항목 진행」 행은 작업 종류가 아니라 「열린 요청이 있나」로 만든다** | B-1 은 킷 작업 행 다섯을 정의하고, 따로 "요청이 열려 있지 않으면 행을 만들지 않는다"고 적었다. 둘을 같이 만족하는 읽기는 하나뿐이다 — 요청이 열려 있으면 어느 작업이든 그 행이 있다. 킷을 고치는 중에도 열린 요청의 항목은 봐야 한다 | `current_node === null` 이면 무조건 뺀다(하네스 수리 중 열린 요청의 진행이 안 보임) | NEW |
| D5 | 화면 | **단계 띠를 그릴지도 「열린 요청이 있나」로 가른다.** 못 그리는 이유가 둘이라 문구도 둘 | "제품 파이프라인 밖이라 없다"와 "열린 요청이 없어 없다"는 사람이 다음에 할 일이 다르다. 한 문구로 합치면 둘 중 하나는 틀린 말이 된다 | 문구 하나로 합친다 | NEW |
| D6 | 화면 | **특이사항 줄을 손쓸 것(warn)과 알려 주는 것(info)으로 가른다.** 5·9번이 info | 드라이런에서 「검사 통과」가 경고색으로 떴다. 멀쩡한 상태가 문제로 읽힌다. 9번은 프롬프트가 "표시만"이라고 못 박은 줄이다 | 전부 경고색(프롬프트의 기존 모양) | NEW — 드라이런 실측 |
| D7 | 화면 | **「상태」 뒤 활동 공백은 임계가 아니라 실제 경과 분을 적는다** | 프롬프트의 `(활동 없음 <N>분)` 에서 N 이 임계인지 실측인지가 안 정해져 있다. 임계를 적으면 10분 끊긴 것과 세 시간 끊긴 것이 같은 글자가 된다 | 임계값을 적는다 | NEW (기존 7번 줄 "40분째 아무 기록 없음"과 같은 뜻으로 맞춤) |
| D8 | 화면 | **접힘 절은 사람이 한 번이라도 직접 여닫으면 그 뒤로 자동 규칙이 손대지 않는다** | 4초마다 다시 그리는 화면이라, 접어 둔 절이 저절로 펼쳐지면 손을 무시하는 것이 된다 | 매 갱신마다 자동 규칙을 다시 적용 | NEW |
| D9 | 구현 | **프로그램이 연 것과 사람이 연 것을 `toggle` 플래그로 안 가른다** — 코드가 정한 값을 적어 두고 지금 값과 비교한다 | `details` 의 `toggle` 은 그 자리에서가 아니라 나중에 따로 온다. 플래그를 걸었다 푸는 방식은 이벤트가 도착할 때 이미 풀려 있어서, 자동으로 펼친 것이 "사람이 폈다"로 기록된다 | 플래그(작동 안 함) | NEW |
| D10 | 화면 | **지도만 접힘 절의 좌우 여백을 안 쓴다** | 지도의 자연 폭 962px 이 이미 열 폭에 꽉 차 있어서, 접힘 절 여백 28px 을 더 먹으면 그만큼 일찍 가로 스크롤이 생긴다 | 여백을 그대로 둔다 | devtool-ui-standards 6절(폭 기준) |
| D11 | 검사 | **`ui-vocab.mjs` 를 설치본 사본 대조 목록에 등재한다** | 대조에서 빠진 파일은 「검사가 없다」가 아니라 「통과」로 보인다. 2026-09-15 에 서브에이전트 훅이 정확히 그렇게 낡았다 | 등재 안 함 | LESSONS(사본 대조 누락) |
| D12 | 검사 | **`title` 한도 40을 기록 쪽·화면 쪽 두 벌 두고, 계약 테스트가 두 값을 대조한다** | 브라우저가 `scripts/lib` 를 못 읽어서 두 벌이 불가피하다. 갈라지면 "기록은 받는데 화면은 자르는" 상태가 되고 그건 화면만 봐선 안 보인다 | 한 벌만 두고 화면은 안 자른다 | NEW |

## 뒤집힌 것

**「특이사항은 전부 경고색」** — 프롬프트가 물려준 기존 모양을 그대로 옮겼다가 드라이런
스크린샷에서 「최근 검사 결과 · 검사 통과」가 주황 테두리로 떴다. 통과한 검사를 문제처럼
그리고 있었다. 화면을 안 띄웠으면 218개 검사 전부 통과한 채로 지나갔을 자리다.
