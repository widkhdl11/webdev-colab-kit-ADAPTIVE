# 결정 로그 — study-mate-20260907-9

사이클: 화면 목록의 마지막 한 장인 스터디 수정(`/studies/[id]/edit`)을 세운다.
그 앞에 승인된 쓰기 인가 스펙의 빈칸 — 스터디를 **고치는** 것을 정한 불변식이 없다 — 을 메운다.
사람이 정한 것 둘(일정 편집을 넣는다 · 지워진 스터디는 못 고친다)이 이 사이클의 입력이다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 범위 | **모임 일정도 이 화면에서 고친다 — 바뀐 줄만 지우고 넣는다** | 개설 폼이 이미 「나중에 수정에서 늘릴 수 있습니다」라고 적어 두었고, 빼면 호스트가 잘못 넣은 일정을 영영 못 고친다. 지우기와 넣기를 한 요청으로 못 묶으므로(PostgREST 는 요청 하나에 한 표) **잃을 수 있는 것을 실제로 바뀐 줄로 좁히는 것**이 최선이다 | ① 일정 편집을 빼기 ② 데이터베이스 함수 하나로 한 트랜잭션에 바꾸기(노출되는 새 문이 생기고 그 안에서 호스트 판정을 직접 해야 한다) | 사람 결정 2026-09-07 |
| D2 | 계약 | **지워진 스터디는 호스트도 못 고치고, 되살리는 경로도 없다 (INV-Z16)** | INV-Z10 이 「지워진 스터디는 새 쓰기를 안 받는다」고 정했는데 스터디 행 자신만 예외였다. 되살리기가 열려 있으면 지워져 있던 동안 거부된 신청·모집글이 설명되지 않는다 | 그대로 두고 「스펙이 정한 적 없다」로 백로그에 올리기 | 사람 결정 2026-09-07 |
| D3 | 계약 | **INV-Z15 를 새로 세운다 — 스터디를 고치는 것은 호스트만, 호스트는 갱신으로 안 바뀐다** | 데이터베이스는 처음부터 막고 있었지만 **계약이 그 자리를 안 적었다.** 적히지 않은 것은 다음에 정책을 손대는 사람 앞에서 근거가 못 되고, 지워도 아무 검사가 안 난다 | 「이미 지켜지니 안 적는다」 | 백로그의 「스터디 수정을 만들기 전에 그 불변식을 스펙에 올려 승인받는다」 |
| D4 | 계약 | **`with check` 에는 「안 지워졌을 것」을 넣지 않는다** | 삭제는 행을 없애는 것이 아니라 `deleted_at` 을 채우는 **갱신**이고(INV-Z6), `with check` 는 바뀐 뒤의 행을 본다. 거기 넣으면 **삭제 자신이 거부된다** — 실패경로 검사 셋은 전부 초록불인 채로 아무도 스터디를 못 지우게 된다 | 양쪽에 다 넣기 | 변이 `z16-with-check-blocks-delete` 가 이 갈래를 붙든다(시나리오 S23d) |
| D5 | 데이터베이스 | **`private.study_is_editable` 을 새로 만든다 — `is_study_host` 에 삭제 검사를 더하지 않는다** | 그 함수를 부르는 나머지 자리(참여자 조회·상태 갱신·모집글 삽입·프로필 가시성)는 지워진 스터디에서도 참이어야 한다. 호스트는 자기가 지운 스터디의 명단과 모집글을 계속 본다 | `is_study_host` 본문에 `deleted_at is null` 더하기 | INV-Z11 · `study_is_visible` 이 이미 「볼 수 있다」를 따로 갖고 있다 |
| D6 | 구조 | **상한·어휘·폼 판독·일정 판독을 `entities/study/model/` 로 옮긴다** | 기능끼리는 import 할 수 없으므로(features → features 금지) 개설 기능 안에 두면 수정 기능이 열한 칸의 검사를 손으로 복사하게 되고, 한쪽만 고쳐지는 날 **개설은 되는데 수정은 거부되는 스터디**가 생긴다 | 수정 쪽에 같은 규칙을 다시 적기 | `entities/post/model/limits.ts` 가 2026-09-06 에 같은 이유로 옮겨진 자리 |
| D7 | 구조 | **폼 판독기가 데이터베이스 열 이름으로 돌려준다**(`max_participants` 등) | 부르는 쪽이 다시 매핑하면 그 매핑이 개설·수정 두 벌이 되고, 열이 하나 늘 때 한쪽만 고쳐진다 | 화면 어휘로 돌려주고 각자 매핑 | D6 과 같은 근거 |
| D8 | 범위 | **수정 화면의 칸은 개설 폼과 같은 열한 칸 + 모임 일정이다** — `closed_at`·`deleted_at` 은 안 싣는다 | 둘 다 갱신 권한 목록에는 있다. 실리면 **수정 폼 하나로 스터디를 지우거나 모집을 닫을 수 있게 된다** — 화면 목록이 이 화면에 배정한 일이 아니다 | 모집 닫기 토글을 같이 넣기 | `docs/IA.md` 화면표(`/studies/[id]/edit` = 스터디 수정) |
| D9 | 범위 | **스터디 삭제는 안 넣는다** | 화면 목록이 이 화면에 「수정」만 적었다(모집글 수정 화면과 다르다). INV-Z6 의 소프트 삭제는 아직 화면이 하나도 없는 별개의 작업이다 | 모집글 수정처럼 삭제 패널을 같이 붙이기 | `docs/IA.md` · PRODUCT 비범위 규칙 |
| D10 | 실패 설계 | **일정만 실패하면 수정 화면에 남는다**(개설은 반대로 떠나 보낸다) | 개설이 떠나 보내는 이유는 폼에 남으면 다시 눌러 **같은 스터디가 하나 더** 생기기 때문이다. 수정에는 그 사정이 없다 — 본문은 이미 저장됐으니 다시 눌러도 덧나지 않고, 여기 남아야 사용자가 일정만 다시 저장할 수 있다 | 개설처럼 `?slots=failed` 를 붙여 상세로 보내기 | 개설 액션의 근거 주석(2026-09-06 code-reviewer)이 말하는 조건이 여기서 성립하지 않는다 |
| D11 | 화면 | **정원 입력칸의 하한은 상수가 아니라 지금 인원이다** | 데이터베이스가 거부하는 값(INV-P3)을 입력칸이 받아 두면 사용자는 저장을 누른 뒤에야 막힌 이유를 찾는다 | `CAPACITY_MIN` 고정 | INV-P3 · 판독기가 `accepted_count` 를 이미 읽는다 |
| D12 | 화면 | **판독기가 일정을 요일·시각 순으로 세워서 준다** | 데이터베이스는 순서를 약속하지 않는다. 안 세우면 저장할 때마다 폼의 줄 순서가 뒤바뀌어 사용자가 「안 건드린 줄이 움직였다」로 읽는다 | 온 순서 그대로 | auto-decide(되돌리기 싼 표시 규칙) |
| D13 | 구조 | **시각을 `hhmm` 한 자리에서 맞춘다** | 데이터베이스는 `19:00:00`, 폼은 `19:00` 을 쓴다. 모양이 다르면 대조가 전부 「다름」으로 떨어져 **사용자가 아무것도 안 고쳤는데 일정 세 줄이 지워지고 다시 들어간다** | 대조할 때마다 잘라 쓰기 | 값이 두 벌인 자리를 만들지 않는다 |
| D14 | 라우팅 | **`PROTECTED_PATTERNS` 에 아무것도 안 더한다** | `PROTECTED_PATHS` 의 `/studies` 접두사가 이 주소를 이미 덮는다. 모집글 수정은 `/posts` 목록·상세가 공개라 패턴이 필요했지만 여기는 그 사정이 없다 | `/^\/studies\/[^/]+\/edit/` 를 더하기 | `route-access.ts` 의 `isUnder` |
| D15 | 카피 | **스터디 상세의 「지금은 일정을 다시 넣을 수 없습니다」를 고친다** | 이 사이클이 그 문장을 거짓으로 만든다. 넣을 자리가 생겼는데 화면이 없다고 말하면 사용자는 그 말을 믿고 안 찾는다 | 그대로 두기 | 이번 diff 가 만든 어긋남 |
| D16 | 시각 | **「스터디 수정」 버튼은 테두리** | 한 패널에 잉크 버튼은 하나이고, 이 패널의 잉크는 「채팅방 들어가기」 또는 「모집글 쓰기」다 | 잉크로 올리기 | design-rules 「잉크 행동 규칙」 |
| D17 | 문구 | **`dbErrorMessage` 인자를 「모임 일정을 저장」으로 고친다**(개설도 같이) | 이 함수는 인자 뒤에 「하지 못했습니다」를 붙인다. 「모임 일정 저장하지 못했습니다」는 한국어가 아니다 | 수정 쪽만 고치기 | 개설·수정이 같은 문구를 내야 한다 |
| D18 | 검증 | **변이 열넷을 등록한다** — 정책 다섯 · 소스 아홉 | 통과하는 검사는 그것만으로 증거가 아니다. 특히 「너무 막는」 갈래(D4)는 실패경로 검사만으로는 절대 안 드러난다 | 새 검사만 붙이고 변이는 안 만들기 | `.claude/rules/tdd.md` |
| D19 | 검증 | **옮긴 파일을 가리키던 변이 셋을 새 자리로 돌린다 + 복구 목록에 `0017` 을 넣는다** | 복구 목록에 없으면 그 파일이 건드리는 것을 무력화한 변이가 안 되돌아오고, **그 뒤의 변이가 전부 오염된 데이터베이스에서 판정된다** | 그대로 두기 | 2026-09-06 에 `0015` 로 같은 일이 났다 |
| D20 | 검증 | **0행 변이는 문구가 아니라 돌려주는 값을 바꾼다** | 문구만 바꾸면 동작이 그대로라 그것은 변이가 아니다 — 검사가 문구를 베껴 적었다는 것만 확인된다 | 실패 문구 바꾸기 | `.claude/rules/tdd.md` 「타입 단언으로 바꾸는 건 변이가 아니다」와 같은 갈래 |
| D21 | 검증 | **시각 칸에 `HH:MM` 모양 검사를 넣는다** | 안 보면 `"7시"` 가 Postgres 까지 가서 거부되는데, 수정 경로에서는 그때 이미 지우기가 끝나 있어 **있던 일정이 사라진 채로** 실패한다. 아래 시각 비교가 글자 순서 비교인 것도 자리수가 고정돼야 성립한다 — `"10:00" <= "9:00"` 이 참이라 09시~10시가 「끝이 앞선다」로 막히고 있었다 | 데이터베이스가 거부하게 두기 | 2026-09-07 code-reviewer |
| D22 | 보안 | **날짜 셋에도 모양 검사를 넣는다** | 22007 이 짓는 문장은 보낸 값을 그대로 되비치고 `dbErrorMessage` 가 그것을 로그에 찍는다. 값 안의 줄바꿈이 살아 있어서 로그인한 사람 누구나 **로그 한 줄을 통째로 지어낼 수 있다** | 로그 쪽에서 제어문자 지우기(그 자리는 표면이 하나가 아니다) | 2026-09-07 security-reviewer |
| D23 | 실패 설계 | **넣기 실패와 지우기 실패를 화면 문구에서 가른다 (`slotsWiped`)** | 지우기까지 끝난 뒤 넣기가 실패하면 바꾸려던 줄이 **지금 없는 상태**다. 그때 「일정은 안 바뀌었다」고 말하면 거짓이고, 사용자가 할 일도 다르다(기다리는 것이 아니라 다시 눌러 되돌려야 한다) | 문구 하나로 두 갈래를 덮기 | 2026-09-07 code-reviewer |
| D24 | 보안 | **일정 지우기에도 `study_id` 를 겹쳐 건다** | 조회 정책이 `sessions_read … using (true)` 라, 읽기 쪽 필터가 사라지는 날 **남의 일정까지 전부 읽히고 그것이 지울 목록이 된다.** 삭제 정책은 「내가 호스트인 스터디」를 통과시키므로 그 사람이 여는 다른 스터디의 일정이 같이 지워진다 | 읽기 필터 하나에 기대기 | 2026-09-07 test-auditor |
| D25 | 구조 | **정원 하한 판단(`capacityFloor`)을 화면에서 엔티티로 옮긴다** | 「지금 인원보다 적게 못 줄인다」는 표시 규칙이 아니라 도메인 규칙(INV-P3)이다. 화면 안에 두면 검사가 못 붙든다 — 같은 파일이 바로 위에서 줄 수 판단을 같은 이유로 빼 두고 있었다 | 그대로 두기 | 2026-09-07 ui-reviewer · `.claude/rules/ui-layers.md` |
| D26 | 카피 | **개설 폼의 「나중에 수정에서 늘릴 수 있습니다」를 사실에 맞춘다** | 수정 폼에 줄을 더하는 버튼이 없어서 세 줄을 다 채운 호스트에게는 **앱 안에서 실행 불가능한 약속**이다 | 「줄 추가」 버튼을 붙이기(새 상호작용이라 시각 판단이 따로 필요하다 — 백로그) | 2026-09-07 code-reviewer · ui-reviewer |
| D27 | 접근성 | **일정 칸 라벨에 줄 번호를 넣는다 (`1번째 시작`)** | 낭독기의 폼 목록에는 「시작」·「끝」이 줄 수만큼 나란히 뜨고 어느 줄인지 알 방법이 없었다. 560px 미만에서는 눈으로 보는 사용자도 같은 상태가 된다(3열이 세로로 풀린다) | `role="group"` 으로 줄을 묶기(공용 `FieldRow` 를 고쳐야 해서 범위가 넓다) | 2026-09-07 ui-reviewer |
| D28 | 화면 | **일정 실패 띠를 컨테이너 안에 넣고, 방향어 대신 링크를 준다** | 밖에 두면 그 띠만 화면 좌우 끝까지 뻗어 바로 아래 제목과 왼쪽 선이 안 맞는다. 「아래 「스터디 수정」에서」는 960px 이상에서 **틀린 말**이다 — 그 버튼은 아래가 아니라 오른쪽 패널에 있다 | 방향어만 빼기 | 2026-09-07 ui-reviewer · design-rules 컨테이너 규칙 |
| D29 | 캐시 | **`/chats` 를 다시 받는 목록에 넣는다** | 방 목록과 방 안쪽이 스터디 제목을 그린다(`CHAT_ROOM_SELECT` 가 `studies(title)` 을 읽는다). 「고친 값이 나오는 화면 전부」라고 적어 놓고 빠져 있었다 | 그대로 두기 | 2026-09-07 code-reviewer |
| D30 | 구조 | **스터디 상세가 스터디 쪽 진행 방식 이름표를 본다** | `meeting_mode` 는 `studies` 의 열이므로 이름표의 주인도 그 슬라이스다. 스터디 화면이 모집글 쪽 표를 읽고 있었다 | 두 표를 한 벌로 합치기 — **못 한다.** 슬라이스를 가로지르는 import 를 게이트가 막는다 | 2026-09-07 code-reviewer |
| D31 | 검증 | **넣기 갈래와 지우기 갈래를 검사도 변이도 따로 둔다 (S22d/S22e · S23c/S23f)** | 강제 장치가 정책 두 개로 따로 있는데 한 검사에 묶여 있었다. 그러면 지우기 정책이 통째로 열려도 넣기 쪽 단언이 대신 빨간불을 내서 **판정이 「잡혔다」가 된다** | 그대로 두기 | 2026-09-07 test-auditor · security-reviewer · `.claude/rules/tdd.md` |
| D32 | 검증 | **S22b 를 앱이 실제로 보내는 열한 칸으로 넓힌다** | 두 칸만 시험하면 `0002` 의 갱신 권한 목록에서 나머지를 빼도 전부 초록불인데, 그 상태의 제품은 **수정 저장이 100% 실패한다** — 열 권한은 정책보다 앞에서 요청을 통째로 떨어뜨린다 | 그대로 두기 | 2026-09-07 test-auditor |
| D33 | 검증 | **변이의 `replace` 는 깨끗한 소스에 없는 문장이어야 한다** | 복구 패스가 `replace→find` 로 되돌리므로, replace 가 원래 있던 문장이면 **손대지도 않은 줄을 복구가 바꿔 놓는다.** 실제로 `.in("id", …)` 를 replace 로 둔 변이 하나가 소스에 줄을 하나 복사해 넣었다 | 여러 줄 find 를 그대로 두기 | 2026-09-07 이 세션에서 실제로 발생 |
| D34 | 화면 | **스터디 상세도 id 를 정규화해 지나고, 판독기가 오류 원문을 문구에 안 담는다** | uuid 가 아닌 주소가 Postgres 까지 가서 22P02 가 나고 화면이 404 가 아니라 500 이 됐다. 그 오류 문장은 보낸 값을 되비친다 | 상세만 그대로 두기(수정 화면과 처리가 갈린다) | 2026-09-07 security-reviewer |

