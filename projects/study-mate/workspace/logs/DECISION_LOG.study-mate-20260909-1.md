# 결정 로그 — study-mate-20260909-1

사이클: 2026-09-08 ui-reviewer 가 시각 기준에 올린 「아직 안 정한 것」 둘을 닫는다.
둘 다 알림 패널이 처음 만든 자리라 승인된 기준이 다루지 않았고, 구현은 그동안
답을 고르지 않은 채(= `disabled` 만 걸리고 눈에 아무 변화가 없고, 지운 뒤 초점이
문서 맨 앞으로 떨어지는 채)로 돌아가고 있었다. 사람 결정 둘이 이 사이클의 입력이다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 시각 | **비활성은 「그 단추가 가진 선을 한 단계 내린다」** — 테두리가 있으면 테두리(잉크→괘선), 없으면 아이콘(약한 잉크→괘선) | 승인된 「비활성 톤」이 자리를 「전체 읽음」 하나로 못 박아 놔서, 테두리가 없는 단추에는 적용할 것이 없었다. 규칙을 갈래 둘로 넓히면 **새 색을 하나도 안 만들고** 두 자리가 한 문장으로 덮인다 | ① 없던 자리에 괘선 테두리를 켠다 ② `busy` 를 줄 단위로 바꿔 「전 줄이 꺼지는 상태」 자체를 없앤다 | 사람 결정 2026-09-09 |
| D2 | 시각 | **지운 뒤 초점은 다음 줄의 삭제 단추 → 없으면 이전 줄 → 그것도 없으면 패널** | 승인된 「초점도 따라 옮긴다」의 근거("두 번째 버튼에 갈 수 있어야 성립한다" = 하던 일 근처에 초점을 남긴다)와 같은 방향이고, 여럿을 연달아 지울 때 Tab 이 필요 없다 | ① 패널 머리의 「전체 읽음」으로 올리기 ② 패널 자신이 받기 — 둘 다 하나 지울 때마다 목록을 처음부터 다시 훑게 된다 | 사람 결정 2026-09-09 |
| D3 | 시각 | **삭제 단추 비활성에 테두리를 켜지 않는다** (D1 의 갈래 선택 근거) | 없던 자리에 선을 그으면 「못 누른다」가 「더 눈에 띈다」로 그려진다. 게다가 이 단추가 꺼지는 순간은 액션 하나가 서버에 갔다 오는 동안이고 그때 **줄 전부가 같이 꺼지므로**, 목록의 모든 줄에 선이 한꺼번에 떴다 사라진다 | 괘선 테두리를 켠다(「전체 읽음」과 글자 그대로 같은 표현이 된다는 장점은 있었다) | 사람 결정 2026-09-09 · design-rules 「비활성 톤」 |
| D4 | 접근성 | **괘선 아이콘이 거의 안 보이는 것을 미달로 치지 않는다** | 비활성 컨트롤에는 대비 하한(3:1)이 적용되지 않는다. 「못 누른다」가 뜻이므로 흐린 것이 곧 정보다 | 조금 덜 흐린 값을 새로 만들기 — **새 색 금지에 걸린다** | design-rules 「새 색을 만들지 않는다」 · 실측 `#6f6656` → `#ebe6dd` |
| D5 | 구현 | **`run()` 이 성공 여부를 돌려준다** | 삭제가 실패하면 줄이 그대로 남고 초점도 누른 단추에 그대로 있어야 한다. 성공을 모르면 실패한 뒤에도 초점이 엉뚱한 줄로 튄다 | 오류 state 를 보고 판단하기 — 갱신이 비동기라 그 시점의 값을 믿을 수 없다 | auto-decide(되돌리기 싼 내부 구조) |
| D6 | 구현 | **초점이 갈 곳을 「지우기 전에」 정한다** | 지운 뒤 목록에는 그 줄이 없어서 「다음」이 어디였는지 알 방법이 없다 | 지운 뒤 인덱스로 되짚기 — 같은 인덱스가 다음 줄일 때도 있고 아닐 때도 있다 | D2 를 성립시키는 유일한 순서 |
| D7 | 구현 | **초점 이동을 `useEffect` 로 미룬다** | `remove` 안에서 바로 부르면 지운 줄의 단추가 아직 화면에 있고, 옮길 대상도 아직 제자리가 아니다. 목록이 다시 그려진 뒤에 옮겨야 한다 | `flushSync` 로 즉시 그리고 옮기기 — 렌더를 동기로 강제할 이유가 없다 | auto-decide(되돌리기 싼 내부 구조) |
| D8 | 접근성 | **지운 사실은 `role="status"` sr-only 슬롯이 말한다. 보이는 문구는 안 띄운다** | 초점이 다음 줄로 가면 낭독기는 **그 줄 단추의 이름만** 읽고 「지워졌다」는 말이 없다. 눈으로는 줄이 사라지는 것이 이미 피드백이라 글자를 또 띄우면 시끄럽다 | ① 보이는 성공 문구 띄우기 ② 아무것도 안 하기 | design-rules 「폼의 성공 문구」(성공은 `status`, 실패는 `alert`) |
| D9 | 접근성 | **남은 알림 수를 문구에 같이 넣는다** | 문구가 매번 같으면 라이브 영역의 내용이 안 바뀌어서 **둘째 삭제부터 낭독기가 아무 말도 안 한다** | 「알림을 지웠습니다」 고정 | 검사 「지운 사실이 낭독기로 간다」가 이 갈래를 붙든다 |
| D10 | 접근성 | **라이브 영역을 패널이 열릴 때부터 그려 둔다** | 지운 뒤에 이 요소를 만들어 넣으면 낭독기가 새로 생긴 라이브 영역을 안 읽는 경우가 있다 | 문구가 있을 때만 그리기 | 검사가 「열자마자 빈 `role="status"` 가 있다」를 단언한다 |
| D11 | 검증 | **초점 검사에서 누르기 전에 실제로 `focus()` 를 준다** | jsdom 의 `click()` 은 초점을 안 옮긴다. 안 주면 패널이 초점을 쥔 채라 **「마지막 하나를 지우면 패널로 간다」가 저절로 참이 된다** — 실제로 처음 쓴 검사가 그 상태였고, 고친 것을 빼도 초록불이었다 | 그대로 두기 | `.claude/rules/tdd.md` 「green 이 무엇을 증명하는가」 |
| D12 | 검증 | **초점 이동을 실제로 빼서 셋이 다 빨간불이 되는 것을 봤다** | 통과하는 검사는 그것만으로 증거가 아니다 | 통과만 확인하고 넘어가기 | `.claude/rules/tdd.md` · CLAUDE.md 「위반을 일부러 심어 잡히는 것을 보는 항목」 |
| D13 | 검증 | **CSS 비활성 톤은 실제 스타일시트를 브라우저에 걸어 computed color 로 쟀다** | CSS 모듈 규칙은 유닛 검사가 못 잡는다. 규칙을 읽고 「적용될 것이다」로 넘기면 토큰 이름 오타 하나가 그대로 통과한다 | 규칙만 읽고 판단하기 | `.claude/rules/tdd.md` 「유닛이 다 green 인 것과 동작하는 것은 다르다」 |
| D14 | 리뷰 | **`security-reviewer` 를 뺐다가 다시 넣었다** | 이번 diff 는 서버 경계·인가에 안 닿아서 뺄 판단이었는데, **graph-stop 이 이 프로젝트에 감지된 위험 표면 때문에 `reviewers:` 에 그것을 요구한다.** 기계 하한선이라 판단으로 못 얇게 간다 | 안 돌리고 목록에만 적기 — 거짓 기록이다 | `gates/graph-stop.mjs` 가 낸 요구 |
| D15 | 문서 | **「아직 안 정한 것」 절을 없앴다** | 그 절 마지막 줄이 스스로 적어 둔 이유 그대로다 — 정해진 것을 남겨 두면 구현이 이미 답을 고른 뒤에도 문서는 미정이라고 말한다 | 「닫힘」 표시만 달고 남기기 | design-rules 자신의 문장 |
| D16 | 구현 | **삭제가 실패해도 초점을 누른 단추로 되돌린다** | 「실패하면 초점이 그대로 있다」는 내 전제가 틀렸다 — **브라우저는 `disabled` 가 된 요소의 초점을 뺏는다.** 액션이 도는 동안 그 단추가 꺼지므로, 다시 켜져도 초점은 문서 맨 앞이다 | 아무것도 안 하기(원래 코드) | 2026-09-09 code-reviewer |
| D17 | 검증 | **그 검사는 브라우저의 blur 를 손으로 흉내 내야 한다** | jsdom 은 비활성 시 blur 를 구현하지 않아서, 흉내 없이는 **고친 것을 빼도 초록불**이다. 실제로 처음 쓴 검사가 그 상태였다 | 그대로 두기 | 2026-09-09 code-reviewer · `.claude/rules/tdd.md` |
| D18 | 구현 | **「전체 읽음」도 성공하면 초점을 패널로 옮긴다** | 성공하면 안 읽은 것이 0이 되어 그 단추가 **영구 비활성**이 되고, 초점이 문서 맨 앞에 남아 다음 Tab 이 패널을 빠져나간다 — `aria-modal` 로 「이 안이 전부」라고 말해 둔 채로 | 삭제 쪽만 고치기 — **같은 결함이고, 한쪽만 고치면 더 눈에 띈다** | 2026-09-09 code-reviewer |
| D19 | 구현 | **`run` 을 `try/catch/finally` 로 감싼다** | 서버 액션은 `ok: false` 로만 실패하지 않는다. 오프라인·5xx·배포 불일치면 **약속이 깨지고** `setBusy(false)` 가 영영 안 돈다 — 패널의 단추가 전부 영구 비활성이 되고 오류 문구도 안 뜬다. 새로고침 전까지 죽은 화면이다 | 그대로 두기 | 2026-09-09 code-reviewer |
| D20 | 구현 | **목록 불러오기에도 `.catch` 를 붙인다** | 같은 모양이다. 깨지면 「불러오는 중입니다…」에서 멈추고 닫았다 열어도 같은 자리다 | 그대로 두기 | 2026-09-09 code-reviewer |
| D21 | 구현 | **다음 줄은 `await` 뒤에, 마지막으로 그려진 목록에서 정한다** | 부르기 전에 찍어 둔 스냅샷은 **틀릴 수 있다.** `busy` 는 액션별이 아니라 하나라 줄 링크의 읽음 처리가 먼저 끝나면 단추가 다시 켜지고, 종 단추는 아예 안 막혀서 닫았다 열면 목록이 통째로 갈린다. 남은 수를 세는 자리도 같은 문제였다 | ① `setItems` 갱신 함수 안에서 정하기(갱신 함수에 부수효과를 넣게 된다) ② `busy` 를 액션별로 쪼개기(범위가 넓다) | 2026-09-09 code-reviewer |
| D22 | 구현 | **초점 대상을 못 찾으면 패널이 받는다** | 목록이 갈려 그 줄이 없을 수 있다. 조용히 넘어가면 초점이 문서 맨 앞에 남는데 **그 상태가 정확히 이 기능이 없애려던 것**이다 | `?.focus()` 로 두고 없으면 아무 일도 안 하기 | 2026-09-09 code-reviewer |
| D23 | 구현 | **초점 이동을 `useLayoutEffect` 로 올린다**(D7 을 뒤집는다) | 보통 effect 는 화면을 그린 뒤에 돈다. 그 틈 동안 초점이 문서 맨 앞에 있고, 그때 Tab 이 들어오면 가둠이 통과시킨다 | `useEffect` 유지 | 2026-09-09 code-reviewer |
| D24 | 문서 | **「hover·focus 에서만 2px 잉크 테두리」를 「hover 에서만」으로 고친다** | 승인 시안도 구현도 hover 뿐이라 **문서 쪽이 시안과 안 맞았다.** 사람이 보고 승인한 것은 시안이므로 그쪽에 맞춘다. 초점에 테두리를 더 얹으면 포커스 링과 두 겹이 되고, D2 때문에 **지울 때마다** 그 두 겹이 보인다 | 코드에 테두리를 더해 문서에 맞추기 — 보이는 모양이 바뀌는 결정이라 사람 몫이다 | 2026-09-09 ui-reviewer · 승인 시안 `notification-panel.html` |
| D25 | 문서 | **「그 줄 글자가 전부 잉크로 올라간다」에 삭제 아이콘은 안 들어간다고 밝힌다** | 아이콘은 글자가 아니라 비텍스트 하한(3:1)을 받고, 괘선 위 4.55:1 은 통과다. 아이콘까지 올리면 줄에 마우스만 올려도 삭제가 강조된다 | 코드에 `.row:has(.link:hover) .del { color: var(--ink) }` 를 더하기 | 2026-09-09 ui-reviewer · 승인 시안 |
| D26 | 문서 | **「비활성 톤」의 적용 범위가 지금 알림 패널 둘뿐임을 밝힌다** | 공용 버튼은 `opacity: 0.7` 을 써서 어느 갈래에도 안 맞는다. 문서만 보면 앱 전체 규칙으로 읽히는데 실제로는 아니다 | ① 공용 버튼을 지금 고치기 — **잉크 채움의 「채움을 한 단계 내린다」가 무엇인지를 새로 정해야 해서 문장 하나로 안 끝난다**(백로그) ② 아무 말도 안 적기 | 2026-09-09 ui-reviewer |
| D27 | 구현 | **`초점갈곳` 과 이름이 겹치던 지역 변수를 `탭순서` 로 바꾼다** | 지역 쪽이 상태를 가려서, 이 핸들러에서 상태를 쓰려는 편집이 조용히 다른 값을 잡는다 | 그대로 두기 | 2026-09-09 ui-reviewer · code-reviewer |

## 빈칸 (승인된 방향 안인데 문서가 안 다룬 세부 — 임시로 골랐다)

없다. 이번에 고른 것은 전부 위 결정 둘에서 파생되거나 이미 승인된 항목을 인용한다.

## 보류 (사람이 정해야 한다)

없다. 다만 **D24 는 사람이 뒤집을 수 있는 자리**다 — 문서를 시안에 맞췄지만, 반대로
초점에도 테두리를 켜는 쪽으로 정하면 코드 한 줄이다. 보이는 모양이 바뀌므로 알린다.

## 이 사이클이 백로그로 뺀 것

- 공용 버튼의 비활성이 「비활성 톤」에 안 맞는다 (D26 의 나머지 절반)
- `revalidateHeader()` 가 삭제 성공 뒤에 돌아서, 거기서 깨지면 서버와 화면이 어긋난다
- 홀수 간격 하나(`padding: 13px`)와, 그 규칙이 자동으로 확인되지 않는다는 사실
