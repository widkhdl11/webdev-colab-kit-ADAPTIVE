# 결정 로그 — study-mate-20260906-6

사이클: 모집글 수정 화면 `/posts/[id]/edit` 한 장. 위험 표면(authz)에 닿지만 붙들 불변식은
이미 승인된 `write-authorization.md` 에 있다(INV-Z3·Z4·Z8) — 새로 쓸 스펙은 없다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 범위 | **삭제는 이 사이클에 안 넣는다** (보류 P14) | 제품 정의는 모집글 삭제를 필수로 적어 두었지만, 승인된 시각 기준에 **「되돌릴 수 없는 행동」에 배정된 색도 형태도 없다.** 버튼 톤 넷은 전부 다른 뜻에 배정돼 있고, 형광펜 8색은 카테고리 8종과 모집 상태가 다 쓴다 | 코랄을 삭제에 쓰기 — 「취미/라이프」·「마감」과 같은 색이 되고 형태로도 안 갈린다 | CLAUDE.md 「새 시각 방향은 언제나 보류로 올린다」 |
| D2 | 구조 | **길이 상한을 `features/create-post/model/` 에서 `entities/post/model/` 로 옮긴다** | 작성과 수정이 같은 값을 봐야 하는데 기능끼리는 import 할 수 없다(FSD). 작성 기능 안에 두면 수정이 숫자를 손으로 복사하게 되고, 한쪽만 고쳐지는 날 **「작성은 되는데 수정은 거부되는 본문」** 이 생긴다 | 수정 기능에 상수를 한 벌 더 두기 | `.claude/rules/domain-layers.md` · 2026-09-06 D12 가 옮긴 방향 그대로 |
| D3 | 구조 | 그 상한을 **`entities/post` 배럴로는 안 내보낸다** | 값을 쓰는 쪽에 폼(클라이언트 컴포넌트)이 있는데, 이 배럴은 서버 전용 판독기를 함께 내보낸다 — 배럴로 가져가면 브라우저 번들이 서버 클라이언트를 끌고 들어가 빌드가 죽는다 | 배럴에 얹고 폼만 조심하기 — 게이트가 잡아 주지만 매번 걸린다 | 게이트 `bundle/CLIENT_IMPORTS_SERVER`(2026-09-06 회고에서 붙었다) |
| D4 | 인가 | **갱신 대상을 「그 글」이자 「내가 쓴 글」로 좁힌다** (`.eq("author_id", user.id)`) | 정책(`posts_update_author`)이 이미 막지만, 정책에만 맡기면 거부가 데이터베이스 오류로 오고 사용자는 이유를 모르는 고정 문구를 읽는다. 이 줄은 **막히는 자리를 우리가 아는 것으로 만드는 것**이다 | 정책에만 맡기기 | INV-Z3 |
| D5 | 인가 | **0행을 성공으로 읽지 않는다** | 갱신은 삽입과 실패 모양이 다르다 — 조건에 안 맞으면 오류 없이 0행이 온다. 이 갈래를 성공으로 읽으면 「저장했습니다」가 나가면서 아무것도 안 바뀐다 | 오류만 보기 | 바로 옆 `change-status` 가 이미 내린 판단 |
| D6 | 인가 | **갱신에 싣는 것은 세 칸뿐이다** (제목·한 줄 소개·내용) | INV-Z8 이고, 데이터베이스의 열 단위 갱신 권한도 정확히 그 셋이다(`grant update (title, summary, content) on public.posts`). 넷째 칸을 실으면 요청이 통째로 거부된다 | 폼이 보낸 것을 그대로 넘기기 | INV-Z8 · `0002_review_fixes.sql:175` |
| D7 | 화면 | **없는 글과 남의 글을 같은 404 로 떨어뜨린다** | 「고칠 권한이 없습니다」를 따로 그리면 그 화면 하나로 「이 id 의 모집글이 있다」를 확인할 수 있다 | 권한 없음 화면을 따로 그리기 | 상세 화면이 지워진 스터디에 내린 같은 판단 |
| D8 | 화면 | **지워진 스터디의 모집글은 수정 화면도 안 연다** | `study_is_visible` 은 호스트에게 자기가 지운 스터디도 보여 주므로 `!inner` 만으로는 안 걸린다. 「내가 쓴 모집글」 목록이 그 글을 이미 빼고 있어서, 여기만 열면 **목록에 없는 글의 수정 화면**이 열린다 | 열어 두기 | `read-my-posts.ts` 가 같은 이유로 내린 판단 · INV-Z10 |
| D9 | 화면 | **어느 스터디의 글인지는 입력칸이 아니라 글로 적는다** | 못 옮기는 것이 계약인데(INV-Z8) 작성 화면에 있던 선택칸이 그냥 사라지면 사용자는 「옮길 수 있는데 여기 없나」로 읽는다. 테두리도 안 두른다 — 입력칸처럼 보이면 눌러 보고 나서야 못 고친다는 것을 안다 | 비활성 선택칸으로 그리기 | INV-Z8 |
| D10 | 모델 | **`PostDetail` 에 `authorId` 를 더한다** | 「내가 쓴 글인가」를 `author.id` 로 판단하면, INV-Z13 이 프로필을 감추는 사람에게 그 판단이 null 로 무너진다 — 작성자 본인에게는 항상 보이지만, 그 사실에 기대는 것과 값을 갖는 것은 다르다 | `author?.id` 로 비교 | INV-Z13 |
| D11 | 화면 | **수정 링크는 상세 화면이 판정하고 위젯은 자리만 낸다**(`authorAction`) | `applyAction` 과 같은 규약이다. 세션을 보는 판단은 화면이 하고 위젯은 조합만 한다 | 위젯에 `userId` 를 넘겨 위젯이 판정 | `.claude/rules/ui-layers.md` 「비즈니스 규칙 금지」 |
| D12 | 캐시 | **수정은 성공하면 캐시를 지운다** (`/posts/[id]` · `/posts` · `/profile`) | 작성은 안 지운다 — 방금 생긴 주소로 떠나므로 그 화면이 처음 그려진다. **수정은 이미 있던 주소로 돌아간다** — 안 지우면 라우터가 들고 있던 옛 화면이 나오고 사용자는 저장이 안 된 줄 안다 | 작성과 똑같이 안 지우기 | 2026-09-06 D2(캐시 지우기를 조립 안에) |
| D13 | 시각 | **폼 카드 껍데기를 `shared/ui/form-page/FormCard` 로 모은다** | `margin-top: 26` + `padding: 28px 24` 가 폼 다섯 장에 **바이트까지 같은 모양으로** 복사돼 있었다. `28px` 은 토큰에 없어서 「일회성은 px 허용」으로 넘어갔던 값인데 다섯 벌이면 일회성이 아니다 | 여섯 번째 복사본 만들기 | 백로그 「작업 화면 두 장의 껍데기 CSS 가 바이트까지 같다」의 되살릴 조건이 이번 diff 였다 |
| D14 | 시각 | **작성 화면 두 장을 `FormPage` 로 옮기고 각자의 `page.module.css` 를 지운다** | 두 파일이 15줄 전체 동일했다. 값은 이미 `shared/ui/form-page` 에 있었고 옮긴 것은 새로 만든 두 장뿐이었다 | 그대로 두기 | 같은 백로그 항목 |
| D15 | 시각 | 상세 화면의 좋아요·조회를 **한 덩어리로 묶는다**(`.statGroup`) | `.foot` 이 `space-between` 이라 숫자 둘이 양 끝으로 밀려 있었다. 작성자에게만 나오는 「수정」이 붙으면 그 배치가 **보는 사람에 따라 달라진다** — 남이 볼 때와 내가 볼 때 같은 숫자가 다른 자리에 앉는다 | 「수정」을 맨 앞에 두기 | 되돌리기 싼 결정 |
| D16 | 검사 | 변이의 `find`·`replace` 는 **한 줄로 적는다** | 작업 트리가 CRLF 인데(core.autocrlf=true) 등록소의 문자열은 LF 라, 줄을 걸치면 깨끗한 소스에서도 안 맞는다 — 도구는 그것을 「변이가 낡았다」로 보고하고 **고칠 곳을 소스라고 잘못 가리킨다** | 여러 줄 find 유지 | 이번에 실제로 났다(2026-09-06) |
| D17 | 검사 | `edit-post-zero-rows-ok` 는 조건이 아니라 **돌려주는 값**을 바꾼다 | 조건만 끄면 그다음 줄이 null 에서 `.id` 를 읽어 TypeError 를 던진다. 그때 빨간불은 **단언이 붙들어서가 아니라 터져서** 난 것이다 | `if (!data && false)` | 2026-09-06 D16 이 같은 함정에서 배운 것 |
| D18 | 검사 | 길이 상한 변이를 **한 자리(`entities/post/model/limits.ts`)에 심는다** | 작성·수정 양쪽 검사가 동시에 빨간불이 나야 「둘이 같은 값을 본다」가 증명된다. 한쪽만 빨간불이면 다른 쪽이 숫자를 손으로 복사하고 있다는 뜻이다 | 기능마다 따로 심기 | 실제로 2건이 잡았다 |
| D19 | 화면 | **수정 폼의 입력칸을 React 가 들고 있게 한다**(제어 컴포넌트) | `<form action={…}>` 은 액션이 끝나면 제어되지 않는 칸을 초기화한다. 액션이 실패를 **반환**하면(던지지 않는다) 정상 종료로 처리되므로, 오류 문구와 함께 **방금 고쳐 쓴 것이 저장된 옛 값으로 되돌아간다.** 브라우저에서 실측했고, 고친 뒤 다시 실측해 남는 것을 봤다 | `defaultValue` 만 두기 — 백로그에 「실측 안 함」으로 열려 있던 항목이 바로 이 자리였다 | 2026-09-06 실측(프로덕션 빌드) |
| D20 | 화면 | **지워진 스터디의 모집글에는 수정 자리를 안 낸다**(`canEditPost`) | `study_is_visible` 이 호스트에게 자기가 지운 스터디도 보여 주는데 수정 화면은 그 글을 안 연다 — 두 판단이 갈려서 **자기에게만 보이는 버튼을 눌렀는데 404** 가 났다. 브라우저에서 재현하고 고친 뒤 다시 확인했다 | 수정 화면의 필터를 빼기 — 그러면 「내가 쓴 모집글」 목록과 어긋난다 | code-reviewer 2026-09-06(높음) |
| D21 | 구조 | **글칸 셋의 규칙을 `validatePostText` 로 엔티티에 모은다** | 상한만 옮기고 규칙은 두 벌로 뒀더니, 문구까지 글자 그대로 같은 검사 여섯 줄이 작성·수정에 각각 있었다. 규칙이 하나 늘면(제어문자 금지 같은 것) 그 순간 작성과 수정이 갈린다 — D2 가 상수를 옮긴 이유가 규칙에는 안 따라왔다 | 두 벌로 두고 주석으로 「같이 고치라」 | code-reviewer 2026-09-06 |
| D22 | 구조 | **`PathDeps` 의 프로덕션 값을 `shared/lib/action-deps.ts` 로** | 바이트까지 같은 닫힘 함수가 폼 액션마다 한 벌씩 생기고 있었다. 그 파일이 타입을 모은 것과 같은 이유다 | 액션마다 두기 | code-reviewer 2026-09-06 |
| D23 | 캐시 | **홈(`/`)도 다시 받게 한다** | 홈이 최신 세 장의 제목·한 줄 소개를 그린다(`readLatestPosts(3)`). 주석이 「고친 값이 나오는 화면 전부」라고 말하고 있어서 둘 중 하나는 고쳐야 했다. 스터디 상세는 안 넣는다 — 그 화면은 모집글의 존재 여부만 읽는다 | 셋만 두고 주석을 고치기 | code-reviewer 2026-09-06 |
| D24 | 보안 | **`throwShapeError` 가 값 대신 키와 타입만 로그에 찍는다** | 진단에 필요한 것이 「어느 키가 빠졌나」이지 내용이 아닌데, 여기 오는 행에는 사람이 쓴 글이 통째로 들어 있다(본문 4000자) | 이 판독기만 다른 함수를 쓰기 — 같은 누출이 판독기 넷에 그대로 남는다 | security-reviewer 2026-09-06 |
| D25 | 보안 | **모집글 상세도 `throwDbError` 를 쓰고 id 를 정규화한다** | 이 한 줄만 `db-error.ts` 의 계약 밖에 있었다 — uuid 가 아닌 주소로 들어오면 Postgres 원문이 그대로 실려 나가고, 없는 글은 404 인데 형식 오류만 500 이라 응답이 갈렸다 | 수정 화면만 고치기 | security-reviewer 2026-09-06(중간) |
| D26 | 검사 | **가짜 데이터베이스가 폼과 다른 id 를 돌려주게 한다** | 같으면 성공 값·목적지·캐시 경로를 폼 값으로 바꿔도 단언이 통과한다 — 이 파일이 붙든다고 적어 둔 「열린 리다이렉트가 아니다」를 검사가 못 가르는 상태였다. 변이 둘을 같이 등록했다 | 그대로 두기 | security-reviewer · test-auditor 2026-09-06 |
| D27 | 검사 | **「거부는 오류가 아니라 0행」을 통합 검사로 실측한다** | 이 기능의 실패 설계 전체가 그 전제 위에 서 있는데, 주장하는 것이 유닛의 가짜 하나뿐이었다. 실제로 오류가 온다면 사용자는 다른 문구를 읽는데 그 상태에서도 유닛은 전부 초록불이다 | 가짜의 주장으로 두기 | test-auditor 2026-09-06 |
| D28 | 검사 | **`readPostForEdit` 에 검사 일곱을 붙인다** | 작성자 좁히기와 지워진 스터디 필터를 **둘 다 지워도 전 스위트가 초록불**이었다. 이 사이클의 결정 둘(D7·D8)을 아무도 안 붙들고 있었다 | 액션 검사만 두기 | code-reviewer · test-auditor 2026-09-06 |
| D29 | 화면 | **작성 폼도 제어 컴포넌트로 바꾼다** | 잃는 양이 수정보다 크다 — 수정은 저장된 옛 값으로 되돌아가지만 작성은 **빈칸**이 된다. 이번 diff 가 그 파일을 이미 만졌다 | 수정 폼만 고치기 | code-reviewer 2026-09-06 |
