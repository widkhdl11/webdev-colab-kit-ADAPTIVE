# 결정 로그 — `study-mate-20260904-1`

사이클 주제: 워킹 스켈레톤 — 들여온 문서를 근거로 Next+FSD 골격을 만들고 화면 한 장이
로컬에서 뜨는 상태까지 간다.

기록 하한: 실재하는 대안이 있었고 기각 이유를 한 줄로 쓸 수 있는 결정만 적는다.

`근거` 열: 적용한 규칙 id / `NEW`(커버 규칙 없음) / `HUMAN`(사람 개입).

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | harness | 기술 스택 문서를 `approved` 로 올린다 | 스택은 새로 정하는 것이 아니라 코드에서 읽은 사실이고, 승인은 "이 스택으로 다시 짓는다"는 확인이다 | draft 로 두고 인자로만 스캐폴딩 | HUMAN |
| D2 | harness | 데이터 계층을 이번 사이클에 붙이지 않는다 | 붙이는 순간 인증·인가 표면에 닿고 그 스펙들은 아직 승인 전이라 게이트가 막는다. 어느 데이터베이스에 붙일지도 되돌리기 비싼 결정이다 | 골격과 함께 붙여서 한 번에 끝내기 | HUMAN |
| D3 | harness | 스택 프로파일을 문서 링크가 아니라 `--stack` 인자로 지정한다 | 들여온 문서에는 프로파일 링크가 없고, 링크를 넣는 것은 문서 계약이 "아무도 안 읽는다"고 적어 둔 파일에 기계가 읽는 값을 넣는 일이다 | `tech-stack.md` 에 프로파일 링크를 추가 | NEW |
| D4 | harness | 라우트 화면을 한 장만 만든다 | 두 장째부터는 승인된 시각 기준을 요구받는데, 그 승인은 아직 사용자 결정 대기다 | 홈 화면까지 만들어 두기 | NEW |
| D5 | harness | 스캐폴딩이 만든 스펙 템플릿이 경계 검사에 걸린 것을 **킷 정본에서** 고친다 | 프로젝트 사본만 고치면 다음 프로젝트가 같은 위반을 다시 받는다 | 프로젝트 사본의 그 줄만 지우기 | NEW |
| D6 | harness | 판정 검사에 국면 인자(`--built`)를 둔다 | 들여오기 직후에는 산출물 폴더가 없어야 하고 짓기 시작하면 있어야 한다 — 한 검사가 두 국면을 다 맞출 수 없다 | 검사가 국면을 스스로 판정하게 하기(프로젝트 문서에서 신호를 읽어야 하는데 그 폴더는 하네스를 몰라야 한다) | NEW |
| D7 | harness | 후보 0건을 실패로 세지 않는다 | 새 프로젝트는 쌓인 판단이 없는 게 정상인데, 그걸 실패로 두면 프로젝트를 열 때마다 빨간불이 되고 상수가 된 빨간불은 아무도 안 본다 | 0건을 계속 실패로 두고 새 프로젝트마다 예외를 손으로 추가 | NEW |
| D8 | harness | 개발 서버가 만든 에이전트 규칙 파일을 설정으로 끈다 | 이 저장소에는 같은 이름의 협업 규칙 문서가 이미 있고 두 벌이 대조되는 구조라, 프로젝트 안에 또 생기면 규칙의 출처가 둘이 된다 | 생긴 파일을 그때그때 지우기 | NEW |
