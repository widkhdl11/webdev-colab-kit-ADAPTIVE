# 결정 로그 — study-mate-20260906-2

사이클: 보류 둘(P11 버튼 강조 · P12 모집글을 쓸 수 있는 스터디)에 사람 결정을 받아 반영한다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 계약 | **P12 를 새 불변식 INV-Z14 로 올린다** — "새 모집글은 「모집 중」인 스터디에만 붙는다" | 인가 표면이고 어기면 화면과 데이터베이스가 어긋난다. 스펙에 문장이 있어야 검사가 그것을 참조할 수 있고, spec-coverage 게이트도 그것을 요구한다 | INV-Z10 본문만 고치기 — 삭제 계약에 마감이 섞여 두 개념이 한 문장에 들어간다 | 사용자 결정 2026-09-06 |
| D2 | 인가 | 판정을 새로 만들지 않고 **화면이 「모집중」 배지를 그릴 때 쓰는 것과 같은 조건**을 부른다(`private.study_is_recruiting`) | 조건을 정책 안에 다시 적으면 두 벌이 되어, 한쪽만 고쳐지는 날 **화면에는 「마감」인데 글은 써지는** 상태가 조용히 만들어진다 | `deleted_at is null` 만 보는 함수를 따로 두기(전날의 `study_is_live`) — 그러면 마감이 안 막힌다 | 사용자가 고른 범위("화면의 모집중 그대로") |
| D3 | 마이그레이션 | **어제 만든 `0014` 를 새로 쓴다**(파일명도 바꿔서). 새 마이그레이션을 얹지 않는다 | `0014` 는 같은 세션에서 만들어 커밋도 안 됐고 로컬 개발 DB 말고는 어디에도 적용된 적이 없다. 얹으면 하루도 안 쓰인 `study_is_live` 함수가 영구히 남고, 다음 사람은 그것이 무언가를 지킨다고 믿는다 | 0015 를 새로 만들기 — 죽은 함수와 즉시 덮이는 정책이 이력에 남는다 | 적용 범위가 로컬 하나뿐임을 확인 |
| D4 | 검사 | **갈래 셋을 따로 본다**(지워짐 · 닫힘 · 정원 참) + 반대 절반 + 「정원이 찼다가 나가면 다시 쓸 수 있다」 | 하나로 묶으면 그중 하나만 붙들려 있어도 「잡혔다」가 나온다 — 2026-09-06 에 프로필 가시성에서 실제로 그랬다 | 실패경로 하나로 묶기 | `.claude/rules/tdd.md` |
| D5 | 검사 | 통합 검사가 **앱이 실제로 보내는 다섯 칸**(summary 포함)으로 넣는다 | 유닛은 「무엇을 보내는가」를, 통합은 「그 값이 정책을 지나는가」를 본다. 칸이 다르면 그 사이가 빈다(test-auditor P8 이 지적한 이음매) | 네 칸으로 넣기(기존 검사들이 그렇다) | test-auditor 지적 |
| D6 | 시각 | **P11 을 (나) 로** — 모집글이 없으면 「모집글 쓰기」가 잉크, 있으면 「채팅방 들어가기」가 잉크 | 「지금 이 화면에서 해야 할 일」이 강조된다. 모집글이 없는 스터디는 아무도 찾을 수 없고 그것을 푸는 행동은 하나뿐이다 | (가) 항상 모집글 쓰기를 잉크 — 멤버가 다 모여 대화만 하는 스터디에서도 계속 진하다 / (다) 그대로 두기 | 사용자 결정 2026-09-06 |
| D7 | 시각 | **「한 패널에 잉크 버튼은 하나」를 승인된 기준에 규칙으로 적는다** | 이번 판단의 근거가 그 문장인데 기준 문서에 없었다. 안 적으면 다음 화면이 같은 질문을 다시 하고, 그때 답이 달라질 수 있다 | 이번만 정하고 넘어가기 | 사용자 결정에 딸린 규칙 |
| D8 | 배치 | `StudyPage` 에 **개수가 아니라 유무**(`hasPosts`)를 둔다 | 화면이 쓰는 것은 「있나 없나」뿐이다. 개수를 두면 다음 사람이 그것으로 판단을 만들고, 그때 이 질의는 세는 질의가 되어야 한다 | `postCount` | 되돌리기 싼 결정 |
| D9 | 화면 | 목록에서 거르는 기준도 **계산 컬럼 `recruiting` 을 그대로 읽는다** | 정책과 같은 이유다 — 조건을 옮겨 적으면 화면에는 「마감」인데 목록에는 뜨는 상태가 생긴다. `recruiting` 은 계산 컬럼(함수)이라 PostgREST 필터로는 못 걸어서 읽은 뒤 거른다(내 스터디만이라 목록이 작다) | `closed_at`·`deleted_at` 을 직접 거르기 — 정원이 찬 경우가 빠진다 | INV-Z14 |
| D10 | 화면 | 빈 상태 문구를 **두 이유 다 맞는 문장**으로 | 목록이 비는 이유는 둘이다(만든 스터디가 없거나, 있는데 전부 모집 중이 아니거나). 화면은 그 둘을 구분할 근거가 없다 | 「스터디를 먼저 만드세요」만 두기 — 마감한 스터디가 있는 사람에게는 틀린 말이다 | 되돌리기 싼 결정 |
