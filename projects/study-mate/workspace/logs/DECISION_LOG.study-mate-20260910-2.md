# 결정 로그 — study-mate-20260910-2

사이클: 앞 사이클(`-1`)이 사용자에게 올린 건 하나 — 줄 구분자 U+2028·U+2029 가
`[[:cntrl:]]` 에 안 걸리는 자리. **막았다가 되돌렸다.** 근거가 측정으로 뒤집혔기 때문이다.

**이 사이클이 남긴 것은 코드가 아니라 측정값 하나와 기준 하나다.**

| id | 영역 | 결정 | 이유 | 뒤집힌 것 | 근거 |
|---|---|---|---|---|---|
| D1 | 스펙 | **먼저: 승인된 INV-M4 의 문면을 넓혔다** — 「제어문자」 → 「줄을 강제로 바꾸는 글자」 | 불변식이 이름 붙인 손해(세로 도배)가 문면 밖에서 재현된다고 봤다. 강제 장치만 넓히고 문장을 안 고치면 스펙이 코드보다 좁아진다 | — | 사용자 결정 2026-09-10 |
| D2 | 검증 | **그리고: 아무도 안 잰 것을 쟀다** | security-reviewer 가 「렌더를 확인하지 않았다」고 단서를 달았고, 레포는 `chr(8232) ~ '[[:cntrl:]]'` 는 실측해 적었는데 **화면 쪽은 아무도 안 쟀다.** 값이 결정의 전제였으므로 쟀다 | — | Chromium `<p>` 높이 · 대조군 포함 |
| D3 | 스펙 | **측정이 D1 의 근거를 뒤집었다 — 되돌린다** | **U+000A·pre-wrap 51px(두 줄) · U+2028·pre-wrap 26px · U+2029 26px · U+000B 26px · U+0085 26px.** CSS 에서 줄을 바꾸는 것은 U+000A 하나뿐이다. 유니코드 줄바꿈 규칙(UAX #14)의 「강제 줄바꿈」과 CSS 의 「segment break」가 같은 집합이 아니다 | D1 과 `0022` 전체 | 사용자 결정 2026-09-10(두 번째) |
| D4 | 스펙 | **「막는 값이 0이다」만으로는 안 막는다** | 되돌린 뒤 남은 이유는 둘이었다 — 다른 엔진을 안 쟀다 · 폼으로 못 만드는 글자라 막아도 손해가 없다. **그 둘은 폭 없는 공백·양방향 제어 문자에도 똑같이 해당한다.** 이것만 막으면 표 셋이 각각 다른 범위를 갖고, 다음에 그쪽을 정할 때 이미 박힌 규칙 하나를 끼고 맞춰야 한다 | 「예방으로 그냥 두기」 | 스펙 비범위 절 · `docs/BACKLOG.md` |
| D5 | 하네스 | **「마이그레이션만 고치면 리뷰 사인오프가 안 낡는다」를 킷 백로그에 올린다** | `review` 의 basis 는 `implement`(`src/**`)에 대한 해시인데, 이 레포에서 불변식을 실제로 강제하는 자리는 대부분 마이그레이션이고 그건 `design/schema-designer` 의 산출물이다. 제약만 고친 diff 는 사인오프를 안 낡게 한다 | — | 실측: `0022` 만 올렸을 때 `schema-designer` 해시는 바뀌었는데(`e421de8cf778`) `review` 는 clean 이었다 · `docs/references/harness-backlog.md` |

## 남긴 것

- **측정값**: 스펙 `docs/specs/chat-message-integrity.md` 의 비범위 절 · `docs/BACKLOG.md`.
  같은 것을 다음 사람이 다시 재지 않게, 그리고 **같은 근거로 다시 막지 않게** 적었다.
- **기준 한 줄**: 「눈에 안 보이는 글자」를 막을지는 **그 불변식이 이름 붙인 손해를 내는가**로
  가른다. 「막는 값이 0이다」는 기준이 아니다.
- **묶음 하나**: 양방향 제어 · 폭 없는 공백 · 줄 구분자 · 결합 부호를 한 항목으로 합쳤다.
  성질이 같아서 한 자리에서 같이 정해야 표 셋이 안 갈린다.

## 이번에 내가 한 실수

**리뷰어의 추론을 내가 잰 것처럼 사용자에게 옮겼다.** 「강제 줄바꿈이라 손해가 그대로
재현됩니다」로 적었는데, 리뷰어는 「렌더를 확인하지 않았다」고 분명히 단서를 달았고 나는 그
단서를 빼고 전달했다. 사용자는 그 문장을 근거로 「막는다」를 골랐다. 재는 데 든 시간은
브라우저 호출 두 번이었다.
