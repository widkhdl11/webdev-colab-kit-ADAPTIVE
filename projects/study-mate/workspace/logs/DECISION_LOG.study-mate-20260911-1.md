# 결정 로그 — study-mate-20260911-1

사이클: **눈에 안 보이는 글자를 어디까지 막나.** 앞 사이클(`-2`)이 「넷을 한 자리에서 같이
정한다」로 묶어 백로그에 남긴 것을 푼다. 백로그 위쪽의 세 항목이 사실 한 가지였다.

**이 사이클의 방식은 앞 사이클이 남긴 기준 그대로다** — 목록으로 정하지 않고, 각 부류가
「그 불변식이 이름 붙인 손해」를 실제로 내는지 재고 나서 정했다.

| id | 영역 | 결정 | 이유 | 뒤집힌 것 | 근거 |
|---|---|---|---|---|---|
| D1 | 검증 | **먼저 쟀다 — Postgres 문자 37개 + Chromium 렌더 3면** | 앞 사이클이 목록으로 막았다가 측정에 근거가 뒤집혀 되돌렸다. 같은 실수를 **반대 방향으로도** 하지 않으려면 「안 막는다」도 측정에 근거해야 한다 | — | 스펙 「근거」 절 ①~④ |
| D2 | 스펙 | **양방향 서식 문자는 부류 전체를 막는다**(U+200E·200F·202A–202E·2066–2069) | 한글 제목으로 재면 알림의 낫표 경계를 깨는 것은 U+202E 하나뿐이다. **제목을 히브리어로 바꾸니 RLE·RLI·FSI 도 똑같이 깼다** — 어느 문자가 깨느냐는 그 문자의 성질이 아니라 제목 내용이 정하고, 제목은 쓰는 사람이 쓴다 | **내가 한 시간쯤 믿었던 「U+202E 하나만 막으면 된다」** | Chromium 시각 순서 측정 44조합 · 사람 승인 2026-09-11 |
| D3 | 스펙 | **오른쪽-왼쪽 글자 자체는 안 막는다** | 제어 문자가 하나도 없는 아랍어·히브리어 제목은 경계를 안 깼다. 「RTL 이 위험하다」로 넓혔으면 정상 이름을 막으면서 실제 구멍은 그대로 뒀을 것이다 | — | 같은 측정 · 검사 S4c 가 이 판단을 고정한다 |
| D4 | 스펙 | **결합 부호는 비범위** | 손해는 실재한다(U+0303 서른 개면 잉크가 21px 줄에서 81px 까지 올라가 위 말풍선 둘을 덮는다). 그런데 **이 부류만 「막는 값이 0」이 아니다** — `cafe`+U+0301 은 macOS 입력기가 흔히 만든다. 고치는 자리도 표시 층이다 | 백로그가 적어 둔 「넷 다 성질이 같다」 | 측정 ④ · `docs/BACKLOG.md` |
| D5 | 스펙 | **U+200D(ZWJ)는 금지 목록에서 뺀다** | 가족 이모지가 ZWJ 로 이어 붙인 글자다. 대신 「지우고 나면 남는 게 있나」 쪽에만 넣어서, ZWJ 만으로 된 값은 거부되고 이모지 안의 ZWJ 는 통과한다 | — | Postgres 26개 값 대조 · 변이 `bidi-bans-zwj` 가 넓히는 쪽을 잡는다 |
| D6 | 구현 | **알림 줄의 제목을 `<bdi>` 로 감싼다**(INV-T3) | 제약은 **앞으로 저장될 값**을 맡는다. 알림의 제목은 사건 시점의 사본이라, 제약이 생기기 전에 복사된 행은 원본을 고쳐도 안 바뀐다. 저장을 막는 것과 이미 저장된 값이 그 문단에서 할 수 있는 일을 막는 것은 다른 일이다 | — | 실제 앱에서 확인 — 같은 행에서 태그만 되돌리면 시각 순서가 `「꼬리말」제목` 으로 뒤집힌다 |
| D7 | 구현 | **앱 쪽 판정 셋을 `shared/lib/text.ts` 하나로 모은다** | `hasControlChars` 가 두 벌 있었고 **범위가 달랐다** — 프로필 쪽만 C1 구역(U+0080–9F)을 안 봤다. 제목 경로에는 판정이 아예 없었다. 둘 다 「데이터베이스가 거부하는데 앱은 통과시킨다」라서, 사용자가 보는 것은 **다시 시도해도 절대 성공하지 않는 요청에 기다리라고 말하는 문구**다 | — | 실제 앱: U+0085 이름이 이제 「이름에 쓸 수 없는 글자가 있습니다」로 거부된다 |
| D8 | 구현 | **0020·0021 의 기존 제약을 안 뗀다** | 둘 다 새 판정이 보는 집합의 부분집합이라 남겨 두면 같은 값을 둘이 막을 뿐이고 새는 자리는 없다. 떼면 **승인된 INV-M3 의 강제 위치 이름**이 바뀌어 그 스펙과 검사를 같이 고쳐야 한다 | — | 0022 의 주석에 근거를 적었다 |
| D9 | 검증 | **변이 15건을 새로 등록했다 — 그중 셋은 「좁히는」 변이** | 제약을 통째로 떼는 변이는 아무 검사나 하나 있으면 잡힌다. **`bidi-narrowed-to-rlo` 가 이 사이클의 핵심**이다 — D2 에서 내가 틀릴 뻔한 그 되돌림을 그대로 심는다. 그것을 잡는 검사가 없으면 다음 사람이 같은 자리에서 같은 판단을 하고 아무도 못 막는다 | — | 18/18 빨간불 확인(새 15 + 내가 `find` 를 고친 기존 3) |

## 이번에 드러난 것 — 내가 안 찾고 있던 자리 둘

**`1` 로컬 데이터베이스가 변이된 채로 며칠 남아 있었다.** `notifications_title_length` 제약이
없었고, 통합 검사 하나가 코드와 무관하게 빨간불이 되고서야 드러났다. 변이 도구가 떼어 놓고
복구를 못 돌린 상태였다. **지문 대조는 변이 실행 안에서만 돌아서, 실행이 죽으면 비교할
사람이 없다.** 백로그에 올렸다.

**`2` 그리고 같은 도구가 이번엔 제 일을 했다.** 새 제약을 떼는 변이를 돌리자 복구 뒤 지문이
안 맞아 **바로 멈췄다** — `RESTORE_MIGRATIONS` 에 0022 가 없었기 때문이다. 목록에 넣었다.
①과 ②의 차이는 「실행이 끝까지 돌았나」 하나뿐이다.

## 이번에 내가 한 실수

**`1` 측정을 한 종류의 데이터로만 하고 결론을 낼 뻔했다.** 한글 제목으로 열 자를 다 재고
「U+202E 하나만 깬다」를 얻었다. 그 값이 맞긴 했는데 **내가 고른 제목의 성질**이었다.
히브리어 제목으로 다시 재니 넷이 깼다. 앞 사이클이 「재라」는 기준을 남겼는데, 재는 것만으로는
부족하고 **무엇을 바꿔 가며 재는가**가 같이 있어야 한다는 것이 이번에 추가된 부분이다.

**`2` 사용자에게 결정 셋을 나열해 올렸는데, 셋 다 내가 결정할 수 있는 것이었다.** 앞 사이클이
남긴 기준(「그 불변식이 이름 붙인 손해를 내는가」)에 이번 측정값을 넣으면 셋이 다 풀린다.
사용자가 「이게 꼭 내가 해야 하는 내용이야?」라고 물어서 알았다. 진짜로 사람 자리인 것은
**스펙을 `approved` 로 올리는 스위치 하나**였다.

**`3` 이 사이클이 다루는 바로 그 함정에 내가 빠졌다.** 검사 파일에 보이지 않는 글자를
리터럴로 박았고, 편집 도구를 한 번 지나가자 글자가 달라졌다. 레포는 이 함정을 2026-09-06 에
이미 겪고 주석으로 남겨 뒀다. 코드 포인트로 다시 썼다.
