# 결정 로그 — study-mate-20260906-4

사이클: 프로필 수정(`/profile/edit`)과 비밀번호 변경(`/profile/password`).
위험 표면 둘(파일 저장소 경로 규약 · 비밀번호 변경)에 닿아 스펙이 먼저다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 인증 | **비밀번호를 바꿀 때 현재 비밀번호를 다시 받는다** | 안 받으면 로그인된 창 하나만으로 계정을 통째로 가져갈 수 있다. 원래 주인은 그 자리에서 아무것도 못 느끼고 다음에 못 들어온다 | 안 받기(지금 설정) | 사용자 결정 2026-09-06 |
| D2 | 인증 | **인증 서버의 「안전한 비밀번호 변경」을 안 쓴다** | 그 기능이 요구하는 것은 현재 비밀번호가 아니라 **이메일로 보낸 일회용 번호**다(설치된 패키지의 타입 주석에서 확인). 정하기로 한 것과 다른 물건이고 메일 발송에 매인다 | `secure_password_change = true` | 실제 API 문서를 열어 확인 |
| D3 | 인증 | 현재 비밀번호 확인은 **쿠키를 안 건드리는 별도 클라이언트**로 한다 | 지금 세션 클라이언트로 확인하면 그 호출이 세션 쿠키를 갈아치운다 — 비밀번호를 틀렸을 때조차 로그인 상태가 흔들린다 | 세션 클라이언트로 확인 | INV-C2 |
| D4 | 인증 | **바꾸면 다른 기기를 끊고 지금 기기는 유지한다** | 비밀번호를 바꾸는 이유는 보통 「남이 들어온 것 같다」다. 안 끊으면 그 창이 살아남는다. 지금 기기까지 끊으면 바꾸자마자 로그아웃돼 무엇이 성공했는지 모른다 | 전부 끊기 / 아무것도 안 끊기 | 사용자 결정 2026-09-06 |
| D5 | 저장소 | **아바타는 자기 아이디 폴더 아래에만** (INV-E3) | 지금 정책은 버킷과 올린 사람만 보고 경로를 안 본다 — 아직 사진을 안 올린 사람의 폴더를 미리 차지할 수 있고, 화면이 경로 규약으로 주소를 만들면 그 파일이 그 사람 얼굴로 나간다 | 화면에서만 경로를 정하기 — 앱을 우회하면 그대로 뚫린다 | 백로그의 되살릴 조건이 「프로필 이미지 업로드를 만들 때」였다 |
| D6 | 저장소 | **프로필 사진은 지금처럼 공개** | 모집글 상세가 비로그인에게 열려 있고 거기 작성자 얼굴이 나온다. 사진만 막으면 그 화면이 깨진다 | 비공개 버킷 + 화면마다 서명된 주소 — 일이 꽤 늘고 이 데모의 위험에 비해 과하다 | 사용자 결정 2026-09-06 |
| D7 | 데이터 | **닉네임 중복을 허용한다** | 사람을 가르는 것은 내부 id 라 같은 이름이 둘 있어도 사고가 안 난다 | 유일성 제약 — 이미 중복인 데이터를 먼저 정리해야 하고 가입 실패 사유가 하나 는다 | 사용자 결정 2026-09-06 |
| D8 | 데이터 | **`profiles.region` 을 고정 목록의 외래 키로 만든다** (INV-E5) | 지금은 아무 문자열이나 들어간다. 목록에 없는 값이 들어가면 그 사람은 지역 필터의 어느 값으로도 안 걸리는데 화면에는 무언가 적혀 있다 — 오류가 아니라 "그런 사람이 없다"로 보인다 | 화면에서만 고르게 하기 — 앱을 우회하면 뚫린다 | 백로그의 되살릴 조건이 「`/profile/edit` 를 만들 때」였다 |
| D9 | 스펙 | **두 파일로 나눈다**(`profile-editing.md` · `password-change.md`) | 승인 단위가 곧 구현 단위다. 하나로 묶으면 둘 다 끝날 때까지 하류가 닫힌다 | 한 파일 | `/spec` 스킬의 승인 단위 규칙 |
| D10 | 검사 | **빨간불을 먼저 봤다.** 통합 검사 11건을 쓰고 돌려 6건이 실패하는 것을 확인한 뒤 `0015` 를 썼다 | 그 6건이 스펙이 지목한 구멍과 정확히 겹쳤다 — 남의 폴더에 올리는 것이 **성공**했고, 그다음 진짜 주인이 자기 폴더에 못 올렸다(덮어쓰기가 update 라 `owner_id` 가 안 맞는다). 구멍이 있다는 주장이 실측이 됐다 | 마이그레이션 먼저 쓰고 검사 붙이기 | `.claude/rules/tdd.md` |
| D11 | 저장소 | 경로 판정에 **`owner_id` 판정을 남겨 둔다** | 경로만 보면 폴더 이름이 자기 id 인 한 남이 만든 행도 고칠 수 있게 된다. 지금은 도달 불가지만 정책 하나가 바뀌면 열리고, 둘 다 요구하는 편이 싸다 | 경로 판정으로 갈아치우기 | INV-E3 |
| D12 | 저장소 | 아바타 크기 상한 **1MB**, 형식은 png·jpeg·webp | 얼굴 사진 한 장에 넉넉하다. 강제 위치는 화면이 아니라 **버킷**이다 — 앱을 우회한 업로드가 같은 자리를 지나야 한다 | 화면에서만 막기 | INV-E4 |
| D13 | 데이터 | `avatar_url` 에 **전체 주소가 아니라 경로**를 담는다 | 전체 주소를 담으면 프로젝트 주소가 데이터에 박혀 다른 환경에서 깨진 그림이 되고, 그 사실이 화면에 나기 전까지 안 보인다 | 전체 주소 담기(Supabase 예제의 흔한 모양) | 되돌리기 싼 결정 · 칸에 주석을 달아 이름이 거짓말 안 하게 |
| D14 | 인증 | 세션에 **이메일을 검증된 클레임에서** 싣는다(`VerifiedUser.email`) | 현재 비밀번호를 확인하려면 이메일이 필요한데, 폼에서 받으면 남의 계정으로 확인을 통과시키고 내 세션의 비밀번호를 바꾸는 조합이 열린다. 같은 토큰에서 꺼내므로 INV-A3 의 「검증된 클레임」 그대로다 | 폼에서 받기 / 매번 인증 서버에 물어보기 | INV-C1 의 신뢰 경계 |
| D15 | 화면 | 사진을 **`/profile` 에서 실제로 그린다**(`Avatar` 가 `src` 를 받는다) | 올릴 수만 있고 아무 데도 안 나오면 그 기능은 없는 것과 같다. 다른 화면(모집글 상세·스터디 상세)은 아직 이니셜이고 백로그로 넘긴다 | 업로드만 만들고 표시는 나중에 | 되돌리기 싼 결정 |
| D16 | 배치 | 이름 상한 상수를 **`model/limits.ts`** 로 옮긴다 | 클라이언트 폼이 서버 전용 모듈에서 그것을 import 하면서 서버 코드가 브라우저 번들로 딸려 들어가 **빌드가 죽었다**. **같은 자리가 네 번째다** — 유닛 152개가 그 상태에서 전부 초록불이었고, 잡은 것은 `npm run build` 한 번이었다 | 상수를 액션 파일에 두기 | 실행해서 확인 · 회고 대상 |
| D17 | 검사 | 기존 INV-Z7 검사의 **준비물만** 고쳤다(형식 없는 blob → 진짜 PNG) | 새 계약(INV-E4)이 그 검사의 준비물을 막았다. 검사의 주제는 「남이 올린 것을 지울 수 있나」라 주제는 그대로 두고 준비물만 바꾼다 | 검사를 지우거나 형식 제한을 느슨하게 | 새 INV 가 옛 검사의 전제를 좁힌 자리 |

## 리뷰어 넷의 지적 반영 (2026-09-06)

| id | 영역 | 결정 | 이유 | 근거 |
|---|---|---|---|---|
| D18 | 저장소 | **아바타 목록을 자기 것만 보이게 좁힌다**(INV-E7 신설) | 조회 정책에 조건이 없어서 비로그인이 버킷을 통째로 나열할 수 있었다 — 경로 규약이 `<사용자 id>/...` 라 **그 목록이 곧 회원 id 명부**이고, id 하나면 얼굴 사진이 따라 나온다. 2026-09-06 사람 결정이 승인한 것은 「주소를 아는 사람이 사진을 보는 것」이지 명부를 긁을 수 있는 것이 아니었다. 백로그의 되살릴 조건이 「아바타 업로드를 붙이기 전」이었고 그게 이번 diff 다 | security 높음 |
| D19 | 저장소 | 목록만 막고 **사진 주소는 그대로 연다** | 버킷이 공개라 내려받기는 이 정책을 안 지난다. 둘을 같이 붙드는 검사를 뒀다 — 앞만 보면 사진이 아무에게도 안 보이는 상태도 통과하고, 뒤만 보면 명부가 열린 상태도 통과한다 | security 제안 |
| D20 | 인증 | **비밀번호를 자르지 않는다** | `formText` 가 `trim()` 을 하는데 그것을 비밀번호에 쓰고 있었다. 끝에 공백이 있는 비밀번호를 쓰는 사람은 맞게 쳐도 영영 확인을 통과 못 하고, 공백이 든 새 비밀번호는 잘린 값이 저장돼 방금 정한 대로 쳐도 로그인이 안 된다 — 둘 다 빠져나올 길이 비밀번호 재설정뿐인데 화면에는 아무 오류도 안 뜬다 | code 높음 1 |
| D21 | 인증 | **다른 기기 끊기 결과를 값으로 알린다**(`othersSignedOut`) | 버리면 끊기가 실패했는데 화면이 「끊었습니다」를 사실로 말한다 — 비밀번호를 바꾼 이유가 무효인 채로 사용자만 안심한다. 전체를 실패로 뒤집으면 INV-C5 를 반대 방향으로 어긴다(비밀번호는 이미 바뀌었다) | code 높음 2 · security 중간 2 |
| D22 | 저장소 | **저장할 때마다 새 경로로 올리고, 성공한 뒤 옛 파일을 지운다** | 고정 경로를 덮어쓰면 셋이 난다 — 행 갱신이 실패해도 화면의 사진은 이미 바뀌어 있고, 주소가 같아 캐시가 최대 한 시간 옛 사진을 주고, 형식을 바꿔 올리면 옛 파일이 공개된 채 남는다. 실패하면 방금 올린 파일을 되돌린다 | code 높음 3 · 중간 6·7 · security 낮음 8 |
| D23 | 인증 | 확인용 세션을 **`scope: "local"` 로 즉시 끊는다** | `signInWithPassword` 는 인증 서버에서 진짜 로그인이라 세션 행이 하나 생기는데, 우리는 저장하지 않으므로 아무도 안 쥔 채 남는다. **범위를 반드시 적는다** — 인자 없는 `signOut()` 은 기본이 전부라 지금 기기까지 끊긴다 | security 낮음 5 |
| D24 | 인증 | 확인 실패를 **「틀렸다」와 「지금은 확인할 수 없다」로 가른다** | 로그인과 이 확인 호출이 같은 출발지에서 인증 서버를 부르므로 요청 제한 통을 나눠 쓴다. 그 통이 비면 맞게 적은 사람에게도 「맞지 않습니다」가 뜨고, 사용자는 자기 비밀번호를 의심하며 계속 시도한다 | security 낮음 6 |
| D25 | 인증 | 확인이 통과한 계정이 **이 세션의 계정인지** 본다 | 지금은 이메일이 계정마다 유일해서 자명하지만, 그 전제가 코드에 없으면 이메일 변경 기능이 생기는 날 「확인은 A 로 통과하고 변경은 B 세션에」가 열린다 | security 낮음 7 |
| D26 | 데이터 | **`avatar_url` 도 자기 폴더만 가리킬 수 있다**(INV-E8 신설) | 저장소 정책은 파일 올리는 것만 막는다. 스펙이 말한 피해는 파일을 안 올려도 이 칸으로 도달한다 — 모집글 상세에 작성자 사진이 붙는 순간 남의 얼굴을 자기 얼굴로 걸 수 있다 | security 낮음-중간 4 |
| D27 | 데이터 | **이름의 제어문자를 거부한다**(INV-E9 신설) | 0010 이 「규칙을 지키는 자리가 값이 들어오는 자리와 같아야 한다」고 적고 가입 경로에만 세척을 두었다. `/profile/edit` 가 두 번째 입력 경로를 열었으므로 규칙이 따라와야 한다. 지우지 않고 **거부한다** — 조용히 지우면 사용자가 적은 것과 저장된 것이 달라지는데 화면은 성공이라고 말한다 | security 중간-낮음 3 |
| D28 | 조회 | 프로필을 **한 질의로** 읽는다 | 같은 사이클의 `0015` 가 지역 외래 키를 걸면서 임베드가 가능해졌다. 목록을 통째로 받아 앱에서 찾던 코드는 근거를 잃었고, 그 주석의 「지역은 열일곱 줄」도 사실이 아니었다(여덟 줄) | code 중간 4 |
| D29 | 배치 | 설정 읽기를 **`shared/api/supabase/config.ts`** 한 곳으로 | 같은 여덟 줄이 네 클라이언트에 있었다. 그리고 확인용 클라이언트가 오류 클래스를 가져오느라 `next/headers` 를 끌고 왔다 — 「쿠키를 안 만진다」고 적은 모듈이 쿠키 모듈에 묶여 있었다 | code 낮음 9 |
| D30 | 화면 | **폼 화면 껍데기를 `shared/ui/form-page` 로** | 본문 폭 720px 과 제목 38px 이 폼 세 장에 각각 복사돼 있었고, 네 번째·다섯 번째가 그것을 안 물려받아 상세 값(전폭·42px)으로 들어갔다. 값이 한 곳에 있으면 다음 폼은 물려받는 것 말고 할 수 있는 일이 없다 | ui 높음 1 · 중간 3 · 낮음 3 |
| D31 | 화면 | **성공 문구를 `FormNotice`(`role="status"`)로** 공용화 | 이 앱의 첫 성공 문구인데 실패(`role="alert"`)의 관례 밖으로 나갔고, 값까지 두 파일에 복제됐다(주석은 「같은 값」이라 적었는데 굵기가 달랐다). 비밀번호 쪽은 보안 결과 통지라 안 들리면 무슨 일이 일어났는지 알 방법이 없다 | ui 높음 2 · 중간 1·2 |
| D32 | 화면 | **`Field` 가 힌트를 입력칸에 실제로 붙인다**(`aria-describedby`) | `id` 만 만들어 두고 아무도 안 가리켰다 — 눈으로는 보이지만 읽어 주는 기계에는 없는 글이었다. 부르는 자리 열두 곳이 각각 적게 하면 하나를 빠뜨리는 날 아무도 안 잡으므로 컴포넌트가 넘긴다. 백로그에 열려 있던 「힌트 일곱 개」도 같이 닫혔다 | ui 높음 3 |
| D33 | 화면 | **사진을 이니셜 위에 겹친다** | 사진만 그리면 로드에 실패했을 때 남는 것이 괘선으로 찬 빈 원이다 — 승인된 아바타(괘선 + 테두리 + **이니셜**)가 그 순간 사라진다. 서버 컴포넌트라 `onError` 를 못 달아서 겹치는 것이 유일한 방법이다 | ui 중간 6 |
| D34 | 검사 | **`with check` 하나만 거부의 이유가 되게 준비물을 바꾼다** | 그냥 남의 id 로 바꾸면 기본 키 중복이 대신 막아서, `with check` 를 통째로 지워도 초록불이었다. 프로필 행이 없는 계정을 만들어 이유를 하나로 좁혔고 오류 코드도 고정했다 | test-auditor P0-1 |
| D35 | 검사 | **주인과 경로를 가르는 준비물 둘**을 만든다 | 아바타 정책의 조건 슬롯 여섯 중 다섯이 서로를 가려 줘서, 하나만 지워 빨간불이 나는 것은 하나뿐이었다. 관리 키로 내 폴더에 넣기(주인만 다름) · 데이터베이스에 직접 넣기(경로만 다름)로 갈랐다 | test-auditor P0-3 |
| D36 | 검사 | **통합 검사가 프로덕션 확인용 팩토리를 그대로 쓴다** | 검사가 확인용 클라이언트를 손으로 만들어 끼워서 `createVerifierSupabase` 가 한 번도 안 돌았다 — 쿠키 클라이언트로 바꿔치기해도 아무것도 안 깨졌다. 끼우자마자 **환경변수 이름이 안 맞아 빨간불이 났다**(앱은 `NEXT_PUBLIC_*` 를 읽는데 통합 환경은 `SUPABASE_*` 만 넣었다) | test-auditor P0-2 |
| D37 | 검사 | **INV-C4 의 반대 절반**을 넣는다 — 서버가 받아 주는 최소 길이가 끝까지 통과한다 | 「서버 사유를 그대로 보여 준다」만 보면 우리 쪽에 `length < 8` 같은 하한을 넣어도 초록불이다. 규칙이 두 벌이 되는 것이 INV-C4 가 금지한 그 상태다 | test-auditor P1-5 |
| D38 | 검사 | **변이 다섯을 등록하고 `0015` 를 복구 목록에 넣는다** | 복구 목록에 없으면 그 파일이 건드리는 것을 무력화한 변이가 안 되돌아오고, 그 뒤 변이가 전부 오염된 데이터베이스에서 판정된다. 복구 확인에도 네 줄을 더했다(경로 판정·목록 제한·버킷 제한·지역 외래 키) | test-auditor 5번 |
