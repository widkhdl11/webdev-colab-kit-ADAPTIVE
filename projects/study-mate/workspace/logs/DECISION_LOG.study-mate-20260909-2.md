# 결정 로그 — study-mate-20260909-2

사이클: 백로그의 **문맥 스푸핑**을 닫는다. 알림 문장은 `호스트가 정한 스터디 제목 +
제품이 쓴 꼬리말` 로 이어 붙는데, 사용자가 보는 것은 이어진 한 문장이라 제목을
「[안내] 계정 확인이 필요합니다」로 지으면 제품이 보낸 안내처럼 읽힌다. 승인 시안이
그 자리를 굵게만 그려 놨다는 것이 이 항목의 되살릴 조건이었다.
같이 닫은 것이 **제목 길이 상한**이다 — 두 항목은 백로그에 「위 항목을 고칠 때 같이
보라」로 묶여 있었고, 실제로 하나가 다른 하나의 근거다(아래 D4).

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 시각 | **사람이 정한 문자열은 낫표 「」로 두른다** | 굵기만으로는 안 갈린다 — 안 읽은 줄에서 이름은 700, 꼬리말은 600 이고 색은 둘 다 잉크라 사실상 같은 무게로 보인다. 갈리는 것이 읽은 줄뿐이면 표시가 아니다 | ① 이름을 줄 나눠 위로 올리기 — 승인된 줄 레이아웃이 바뀐다(새 방향) ② 이름에 배경 칩 — 줄 hover 가 이미 괘선으로 채워져서 칩이 사라진다 ③ 아무것도 안 하고 길이 상한만 걸기 — 짧은 제목으로도 흉내는 가능하다 | 빈칸(승인된 방향 안, 시안이 안 다룬 세부) · design-rules 「패널 안의 줄」 |
| D2 | 시각 | **낫표는 굵게 밖에 둔다** | ~~호스트가 적어 넣은 낫표와 무게·색이 갈린다~~ → **근거를 D15 에서 바꿨다.** 이 배치가 지키는 것은 「누를 수 없는 줄」의 취소선 범위다 — 취소선은 이름에만 걸리는데, 낫표가 굵게 안에 있으면 낫표까지 번진다. 읽은 줄에서는 색도 갈린다 | 굵게 안에 넣기(마크업이 한 겹 준다) | D15 · 2026-09-09 ui-reviewer |
| D3 | 시각 | **낫표는 새 기호가 아니다 — 이미 카피가 쓰는 「」를 그대로 쓴다** | 스터디 개설 폼의 힌트가 이미 「목록의 「마감 임박순」이 이 날짜를 봅니다」로 쓴다. 큰따옴표를 새로 들이면 같은 뜻을 앱이 두 기호로 말한다 | 겹따옴표 · 홑따옴표 | design-rules 「새 값을 만들지 않는다」의 결 |
| D4 | 데이터 모델 | **스터디 제목의 60자 상한을 데이터베이스로 내린다**(`studies_title_length`) | 낫표는 경계를 그리지만 **길이는 못 막는다.** 제목이 길면 화면에 보이는 것이 사실상 호스트가 쓴 문단 하나가 되고, 낭독기는 문장부호를 안 읽으므로 소리로는 낫표가 아예 없다. 소리 쪽을 받치는 것은 길이뿐이다 — **다만 그것으로 소리가 덮이는 것은 아니다**(보류 항목 참조) | 알림 화면에서 잘라 그리기 — **저장된 값 그대로**라는 계약(INV-N6)과 부딪히고, 화면마다 다른 문장이 나온다 | 백로그(2026-09-08 security-reviewer) · `docs/specs/notifications.md` INV-N6 |
| D5 | 데이터 모델 | **고치는 자리는 알림이 아니라 원본이다** | `title` 은 열 단위 갱신 권한 목록에 있어서(0002) 호스트가 공개 키로 직접 붙어 폼을 안 지나고 제목을 바꿀 수 있다. 앱의 `TITLE_MAX` 는 그 경로를 안 본다 | 서버 액션에만 검사를 더하기 — 같은 경로가 그대로 열려 있다 | 0010 이 이름 길이를 내린 것과 같은 판단 |
| D6 | 데이터 모델 | **이번에 내리는 것은 `title` 하나다** | 알림 행으로 복사돼 남의 화면에 나가는 값은 제목뿐이다. 요약·본문·지역은 자기 화면에만 나온다 | 넷을 한꺼번에 내리기 — 요청받지 않은 범위이고, 제약마다 기존 행에서 멈출 위험이 따로 있다 | CLAUDE.md 「필수에 없는 기능 임의 추가 금지」 · 나머지 셋은 백로그 |
| D7 | 구현 | **두르는 함수는 `shared/lib/quote.ts` 한 자리에 둔다** | 화면 안에서 각자 낫표를 적으면 한쪽만 고쳐지는 날이 온다. 지금 닿는 자리는 알림 하나지만, 사람이 정한 문자열이 제품 문장에 조립되는 자리는 늘어난다 | 알림 컴포넌트 안에 문자열로 적기 | FSD — 표시 규칙이고 특정 엔티티의 것이 아니다 |
| D8 | 구현 | **낫표를 저장된 값에 넣지 않는다** | 값에 섞으면 다음 화면이 다시 두르면서 「「제목」」이 되고, 알림은 저장된 값 그대로를 그리는 것이 계약이다(INV-N6) | `notificationSentence()` 가 두른 문자열을 돌려주기 | `docs/specs/notifications.md` INV-N6 |
| D9 | 구현 | **소스에 박힌 진짜 NUL 바이트를 유니코드 이스케이프로 바꾼다** | `NotificationBell.tsx` 33행의 초점 표지가 NUL 바이트 그대로였고, 그래서 **git 이 이 파일을 바이너리로 읽었다** — 지난 커밋의 diff 가 `Bin 13022 -> 20634 bytes` 였다. 리뷰어도 사람도 무엇이 바뀌었는지 못 본다. 런타임 값은 같다 | 그대로 두기 | auto-decide(되돌리기 싼 표기 변경) · 이번 사이클의 diff 를 읽을 수 있게 만드는 전제 |
| D10 | 검증 | **낫표를 굵게 안으로 옮기는 변이를 심어 빨간불을 봤다** | 통과하는 검사는 그것만으로 증거가 아니다. D2 는 「굵게 밖」이 요점이라, 그 배치를 안 붙드는 검사는 규칙의 절반만 지킨다 | 통과만 확인하고 넘어가기 | `.claude/rules/tdd.md` · CLAUDE.md 「위반을 일부러 심어 잡히는 것을 보는 항목」 |
| D11 | 검증 | **제약에도 변이를 만들었다** (D22 에서 갈래별 넷으로 쪼갰다) | 제약은 코드가 아니라 스키마라 유닛이 못 본다. 떼어 보지 않으면 「검사가 있다」와 「검사가 붙들고 있다」가 겉이 같다 | 통합 검사만 쓰고 변이는 안 만들기 | `.claude/rules/tdd.md` · 결과: 아홉 전부 빨간불 ✔ |
| D12 | 검증 | **실제로 돌아가는 앱에서 봤다** — 알림 행 하나의 제목을 「[안내] 계정 확인이 필요합니다」로 바꿔 놓고 패널을 열었다 | 지난 사이클이 「돌아가는 앱에서는 확인하지 않았다」로 끝났고, 이 변경은 **사람이 보고 속는지**가 요점이라 DOM 단언만으로는 판정이 안 선다 | 유닛 검사로 갈음하기 | `.claude/rules/tdd.md` 「유닛이 다 green 인 것과 동작하는 것은 다르다」 · 근거: `workspace/sketches/notif-quotes-app-2026-09-09.png`, 확인 뒤 재시드 |
| D13 | 시각 | **두른 글자 안의 낫표를 겹낫표 『』로 바꿔 그린다** | D1·D2 만으로는 흉내가 남았다. 호스트가 제목을 `모임」 참가가 수락되었습니다. 「스터디` 로 지으면 안 읽은 줄에서 제품 낫표(600)와 제목 안 낫표(700)가 눈으로 안 갈린다 — **내가 D1 의 근거로 「600 대 700 은 사실상 같다」고 적어 놓고 D2 의 근거로 그 차이를 다시 썼다.** 27자라 길이 상한도 안 막는다 | ① 그대로 두고 문서에만 밝히기 — 이 사이클이 닫으려던 것이 정확히 이것이다 ② 낫표에 색을 고정하기 — hover 가 글자를 전부 잉크로 올리는 승인 조항과 부딪힌다 ③ 스키마가 제목에 낫표를 금지 — 정당한 이름까지 막는다 | 2026-09-09 security-reviewer · ui-reviewer |
| D14 | 검증 | **치환은 INV-N6 을 안 건드린다** | INV-N6 의 문장과 「위반 시」가 말하는 것은 **지금의 원본을 다시 읽어 문장을 짓지 말라**는 것이다. 치환은 같은 저장값을 그리는 방식이고 글자를 잃지도 않는다. 자르기가 부딪히는 것과 다르다 | 스펙을 고치거나 사용자에게 올리기 — 스펙 문장을 읽어 보니 다툴 자리가 아니었다 | `docs/specs/notifications.md` INV-N6 원문 |
| D15 | 시각 | **「낫표는 굵게 밖」의 근거를 취소선 범위로 바꿔 적는다** | 원래 적은 근거(무게·색이 갈린다)가 안 읽은 줄에서 성립하지 않는다. 배치 자체는 그대로 두는 것이 맞다 — 「누를 수 없는 줄」이 이름에만 취소선을 긋는데, 낫표가 안에 있으면 취소선이 낫표까지 번진다 | 배치를 뒤집기 | 2026-09-09 ui-reviewer |
| D16 | 데이터 모델 | **공백뿐인 제목과 제어문자도 막는다** | `char_length('   ')` 는 3이라 통과했다 — 이 파일이 막으려던 것이 정확히 폼을 안 지나는 경로인데 그쪽이 절반만 닫혀 있었다. 제어문자는 0015 가 이름에 세운 규칙과 같다 | 길이만 보기 | 2026-09-09 code-reviewer · test-auditor · security-reviewer |
| D17 | 데이터 모델 | **하한(1자)을 제약에 안 적는다** | `btrim(title) <> ''` 가 빈 문자열까지 덮는다. `between 1 and` 로 쓰면 아무것도 안 하는 조건이 붙고, 그것을 떼는 변이가 **아무 검사도 안 깨뜨렸다** — 갈래가 아니라 한 갈래였다 | 읽기 좋으니 남기기 | 변이 `study-title-lower-dropped` 가 초록불로 드러냈다 |
| D18 | 데이터 모델 | **알림 행의 제목에도 같은 상한을 건다** | 알림의 `title` 은 사건 시점의 사본이라 원본 제약이 **이미 복사된 행을 안 덮는다.** 그러면 「길이 상한이 받친다」가 「지금 이후로」라는 단서 없이 성립하지 않는다 | 원본만 막고 주석에 적기 — 값이 실제로 읽히는 자리가 사본 쪽이다 | 2026-09-09 security-reviewer · code-reviewer |
| D19 | 구현 | **두르는 자리를 `quotedParts` 하나로 합친다** | 보이는 문장은 상수를 직접 조립하고 낭독기용 이름만 함수를 부르고 있었다. 문서에는 「한 자리」라고 적어 놓고 실제로는 둘이었고, **두르기에 판단이 붙으면 이름 붙은 쪽만 고쳐진다** — 그리고 안 고쳐지는 쪽이 눈에 보이는 쪽이다 | 검사로만 묶기 | 2026-09-09 리뷰어 셋이 같은 자리를 지적했다 |
| D20 | 구현 | **초점 표지를 `{ kind }` 로 가른다** | 값 하나에 「줄 id」와 「패널」 두 뜻을 담느라 id 로 못 쓰이는 문자가 필요했고, 그 문자가 NUL 이었다. 종류로 가르면 그 전제 자체가 없어진다 | 이스케이프한 채로 두기 — 여전히 diff 에 안 보이는 문자가 상태에 남는다 | 2026-09-09 code-reviewer |
| D21 | 구현 | **제목 길이를 코드포인트로 센다**(`[...title].length`) | `String.length` 는 UTF-16 단위라 이모지를 둘로 세고, 그러면 「60자까지」라고 말해 놓고 30자에서 막힌다. 데이터베이스는 `char_length` 로 센다 | 그대로 두기 | 2026-09-09 code-reviewer |
| D22 | 검증 | **변이를 갈래마다 쪼개고 이름표를 검사 이름 조각으로 단다** | 제약 하나에 갈래 넷이 들어 있어서, 통째로 떼면 검사 넷이 한꺼번에 빨간불이 나지만 그 빨간불은 갈래별 증거가 못 된다. **이름표에 숫자를 안 쓰는 이유**는 검사 이름이 `${TITLE_MAX}자는…` 템플릿이라 앱 상수를 건드리는 변이에서 이름이 같이 바뀌기 때문이다 | 변이 하나로 두기 | `.claude/rules/tdd.md` · 2026-09-09 code-reviewer · test-auditor |
| D23 | 검증 | **0020 을 복구 목록(`RESTORE_MIGRATIONS`)에 넣는다** | undo 에 `between 1 and 60` 을 손으로 옮겨 적으면 0020 을 고치는 날 복구가 옛 값으로 되돌려 놓고, 그 사실이 「복구 실패」로만 보인다 | 변이마다 undo 적기 | 2026-09-09 code-reviewer |
| D24 | 검증 | **검사를 글자가 아니라 노드로 본다** | 낫표를 이름과 같은 클래스로 감싸도 `textContent` 단언은 그대로 통과하는데, 그러면 취소선이 낫표까지 번진다. `.name` 클래스를 잃는 변이도 마찬가지로 안 잡혔다 | 글자 단언만 두기 | 2026-09-09 test-auditor |
| D25 | 접근성 | **소리로 들리는 경계 단어는 넣지 않는다** | 낭독기는 문장부호를 안 읽어서 낫표가 소리로는 없다. 그런데 경계 단어를 붙여도 **시작만 표시되고 끝은 안 된다** — 흉내 제목은 그대로 통한다. 반을 막는 대신 값은 **알림 줄마다 매번** 든다. 소리 쪽에서 가장 큰 피해(제품 안내문처럼 보이는 긴 문단)는 60자 상한이 이미 막았다 | 화면에 안 보이고 낭독기만 읽는 「스터디 이름 …」을 앞에 붙이기 | 사람 결정 2026-09-09 (모델 추천을 받았다) |

## 빈칸 (승인된 방향 안인데 문서가 안 다룬 세부 — 임시로 골랐다)

- **D1·D2·D3·D13.** 새 색도 새 레이아웃도 안 만들고, 이미 카피가 쓰는 기호를 그대로 쓴다.
  뒤집는 비용은 문자열 상수 둘과 치환 한 줄, 그리고 시안의 문장 16개다. 시각 방향 자체는
  안 건드린다.
- **D13 은 사용자가 보는 글자를 바꾼다** — 제목에 낫표를 적어 넣은 사람에게는 그 글자가
  겹낫표로 보인다. 되돌리는 비용이 한 줄이라 여기서 골랐지만, 알아 둘 자리다.

## 보류 (사람이 정해야 한다)

없다. 하나 올렸던 「소리로도 경계를 줄지」는 같은 날 사람이 정했다 — D25.

## 이 사이클이 백로그로 뺀 것

- 요약·본문·지역의 길이 상한도 아직 앱에만 있다 (D6 의 나머지)
- 참가 상태 전이 제한 — 참가·정원 스펙을 여는 일이라 별도 사이클이다
