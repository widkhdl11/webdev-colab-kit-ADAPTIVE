# 결정 로그 — study-mate-20260906-5

사이클: 서버 액션 다섯에 검사를 붙인다. 위험 표면(인증·권한)에 닿지만
붙들 불변식은 이미 승인된 스펙에 있다 — 새로 쓸 스펙은 없고 검사만 채운다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 구조 | **네 액션 파일을 「본체·조립」과 「액션」으로 가른다** (`insert-application` · `change-status` · `send-message`/`mark-read` · `insert-study`) | 검사가 부를 수 있게 하는 것이 표면적 이유지만, 그보다 먼저 **보안 이유**가 있다 — `"use server"` 파일의 내보내기는 전부 클라이언트가 부를 수 있는 자리라, 판독기·클라이언트를 인자로 여는 조립 함수를 거기 두면 그 인자가 요청으로 들어올 수 있는 값이 된다 | 액션 파일에 그대로 두고 `vi.mock` 으로 감싸기 — 그러면 위 보안 문제가 남는다 | `create-post`·`change-password`·`edit-profile` 이 이미 그 모양이다 |
| D2 | 검사 | **캐시 지우기(`revalidateEntityPath`)를 액션 파일이 아니라 조립 안으로 옮기고 주입 가능하게 한다** | 액션 파일에 두면 그 호출을 통째로 지워도 전 스위트가 초록불이다 — 쓰기는 됐는데 화면만 낡은 상태가 아무 신호 없이 만들어진다. 조립 안에 있으면 가짜를 주입해 「불렀는가」를 단언할 수 있다 | 집 스타일대로 액션 파일에 남기기 — 2026-09-06 백로그가 지목한 빈칸이 그대로 남는다 | 백로그 「서버 액션 다섯에 검사가 0개다」의 뒷부분 |
| D3 | 검사 | **변이 등록소에 `suite: "unit"` 를 새로 만든다** | 지금 `mutation-run` 은 통합 스위트만 돌린다. 서버 액션의 가드·판정을 거기서 돌리면 붙들 검사가 한 건도 안 들어와서 「빠져나갔다」가 나온다 — **잡히지 않은 것과 잘못된 스위트를 돌린 것은 겉이 같다** | 손으로 한 번 무력화해 보고 로그에 적기 — 다음 사람에게 안 남는다(레포가 이미 적어 둔 교훈) | `.claude/rules/tdd.md` 「green 이 무엇을 증명하는가」 |
| D4 | 검사 | 기준선을 **이번에 돌릴 변이가 쓰는 스위트만** 잡는다 | 유닛 변이만 골라 돌릴 때 통합 스위트 한 바퀴(데이터베이스 왕복)를 헛돌지 않게 한다. 기준선이 초록이어야 한다는 요구는 그대로다 | 항상 둘 다 돌리기 | 되돌리기 싼 결정 |
| D5 | 검사 | 상한·범위 검사에는 **경계 안쪽 짝을 같이 단언한다** (메시지 길이 · 정원 · 진행 방식) | 바깥쪽만 보면 상한을 0 으로 바꾸거나 조건을 뒤집은 변이도 초록불이다 — 「거부한다」만 붙들고 「통과시킨다」는 아무도 안 붙든다 | 바깥쪽만 단언 | `.claude/rules/tdd.md` |
| D6 | 검사 | `mark-read` 의 가짜 데이터베이스에서 `.eq()` 를 **몇 번이든 이어 붙게** 만든다 | 실제 supabase-js 가 그렇다. 한 번만 받게 만들면 조건 하나를 지운 변이가 가짜에서 통과해 버려 이 검사가 아무것도 안 붙드는 것이 된다 | 두 번째 `.eq` 에서 바로 결과를 주기 | 변이 `markread-any-user` 가 이 선택을 판정한다 |
| D7 | 검사 | `touchLastRead` 에 **시계를 인자로 연다**(`now`) | 미는 값이 「지금 시각」이라는 것을 단언하려면 그 값을 고정할 수 있어야 한다. 안 그러면 `last_read_at` 에 무엇이 들어가는지 아무도 안 붙든다 | `vi.useFakeTimers` — 이 한 자리를 위해 전역 시계를 바꾼다 | 되돌리기 싼 결정 |
| D8 | 보안 | **`insert-study` 가 붙이던 데이터베이스 원문을 뗀다** | 폼 하나로 제약 이름·표 이름·정책 유무가 새 나갔다. `recruitUntil=infinity` 하나면 `studies_recruit_until_finite` 라는 이름이 화면에 뜬다 | 그대로 두기 — 같은 파일의 일정 갈래는 이미 `dbErrorMessage` 를 쓰고 있어서 한 파일 안에서 규칙이 갈렸다 | security-reviewer 2026-09-06 · `db-error.ts` 가 이미 적어 둔 계약 |
| D9 | 보안 | **`db-error.ts` 가 `23514` 를 통째로 통과시키던 것을 좁힌다** | D8 만으로는 절반만 닫힌다 — 검사 제약 위반은 트리거의 한국어 문장과 **같은 코드**로 오고, 그때 오는 것은 PostgreSQL 이 지은 영어 문장이다. 둘을 가르는 값이 코드에 없으므로 문장 모양(`violates check constraint`)으로 가른다 | 별도 사이클로 미루기 — 두 리뷰어가 같은 자리를 짚었고 고침이 세 줄이다 | security-reviewer · code-reviewer 2026-09-06 |
| D10 | 화면 | **일정 저장만 실패해도 만들어진 스터디로 보낸다** (`?slots=failed`) | 전에는 `ok:false` 로 id 를 버리고 개설 폼에 남겼다. 가장 자연스러운 다음 행동이 다시 제출이고, 스터디에는 유일 제약이 없어 **같은 스터디가 하나 더 생긴다.** 게다가 문구가 없는 화면(스터디 수정)을 가리키고 있었다 | 문구만 고치기 — 중복 개설 위험이 그대로 남는다 | code-reviewer 2026-09-06 |
| D11 | 검사 | **일정 줄을 데이터베이스에 가기 전에 거른다** (`model/slots.ts`) | 끝 시각이 앞서거나 같은 줄, 같은 요일·같은 시각이 두 번인 줄은 데이터베이스가 거부하는데 그때는 스터디가 이미 만들어진 뒤라 되돌릴 수 없다. 조용히 버리지 않고 몇 번째 줄인지 말한다 — 버리면 사용자가 넣은 줄이 이유 없이 사라진다 | 데이터베이스에 맡기기 | security-reviewer · code-reviewer 2026-09-06 |
| D12 | 구조 | **상수·어휘를 `model/` 로 옮기고 화면이 그걸 import 한다** (`chat/model/limits` · `create-study/model/limits` · `manage-participants/model/transitions`) | 화면이 같은 숫자와 목록을 다시 적고 있었다 — 서버 상한을 낮추면 입력창만 옛 값을 받는다. **화면이 다시 적을 수밖에 없던 이유는 배치였다**: 그 값들이 서버 전용 모듈 안에 있어서 클라이언트 컴포넌트가 값으로 import 할 수 없었다 | 화면에 그대로 두고 주석으로 「같이 고치라」 적기 | code-reviewer 2026-09-06 · `create-post/model/limits.ts` 가 이미 그 모양이다 |
| D13 | 구조 | **`deps` 타입을 `shared/lib/action-deps.ts` 한 자리로 모은다** | 구조가 똑같은 타입이 `ApplyDeps`·`ManageDeps`·`ChatDeps` 세 이름으로 세 번 선언돼 있었다. TypeScript 는 구조로 판정하니 이름 셋이 아무것도 가르지 못하면서 고칠 자리만 셋으로 늘었다 | 위치 인자로 통일 — 셋째부터 호출부에서 무엇인지 안 읽힌다 | code-reviewer 2026-09-06 |
| D14 | 검사 | **액션 파일 넷에 「배포되는 이름을 부르는」 검사를 붙인다** | 조립 함수만 검사하면 액션 파일이 `make*()` 에 가짜 판독기를 끼우도록 바뀌어도 전부 초록불이다 — **세션 가드가 프로덕션 경로에서만 빠지는 상태가 만점으로 나온다** | 조립 검사만 두기 | code-reviewer · test-auditor 2026-09-06 · `create-post.test.ts` 가 이미 그 모양이다 |
| D15 | 검사 | **상한 검사에 숫자를 박는다** (2000 · 2~100 · 60 · 80 · 4000) | 상수를 import 해서 상대적으로만 보면 상한을 옮겨도 양쪽이 같이 움직여 전부 초록불이다. 데이터베이스와 같아야 하는 숫자는 검사가 직접 들고 근거 마이그레이션을 주석으로 단다 | 상수 import | test-auditor 2026-09-06 · `insert-post.test.ts` 가 이미 적어 둔 교훈 |
| D16 | 검사 | **`chat-length-unbounded` 변이를 `chat-length-loosened` 로 바꾼다** | 상한을 `MAX_SAFE_INTEGER` 로 늘리면 검사의 `repeat` 이 `RangeError` 를 던져 빨간불이 난다 — 단언이 붙들어서가 아니라 문자열을 못 만들어서다. **판정이 거짓이었다.** 2000 → 20000 은 실제로 일어날 모양이고 단언이 실제로 잡는다 | 그대로 두기 | test-auditor 2026-09-06 |
| D17 | 검사 | **필터 단언을 순서가 아니라 「둘 다 걸렸는가」로 바꾼다**(`new Map` + 길이) | PostgREST 에서 필터는 순서가 없는 조건들이라, 두 줄을 바꿔 적으면 동작이 같은데 빨간불이 났다. 길이 단언이 같은 열을 두 번 거는 변이를 여전히 잡는다 | 순서 고정 유지 | test-auditor 2026-09-06 |
| D18 | 검사 | **INV 이름표 셋을 뗀다/옮긴다** (`INV-Z2` 탈퇴 검사 · `INV-P4` pending 검사 · `INV-Z8` 필터 검사) | 이름표가 안 붙드는 것을 주장하고 있었다. `spec-coverage` 는 INV 이름을 세므로, 유닛이 안 붙드는 불변식이 커버된 것처럼 집계된다 | 그대로 두기 | test-auditor 2026-09-06 · `insert-post.test.ts:76` 이 이미 내려 둔 판단 |
| D19 | 검사 | **어휘 대조에 「쓰기를 막는 목록」을 넣는다**(`MEETING_MODES`·`ALLOWED`) | `vocabulary.test.ts` 는 읽기용 union 두 벌만 데이터베이스와 대조하고 있었다. 실제로 쓰기를 막는 목록이 그 대조 밖에 있으면, 두 벌을 감시하려고 만든 장치가 새 복사본을 못 본다 | 읽기용만 대조 | code-reviewer 2026-09-06 |
| D20 | 구조 | **`mark-read` 는 0행을 성공으로 둔다** (주석으로 이유를 남긴다) | 바로 옆 `change-status` 는 0행을 실패로 보는데 그 액션은 결과가 호스트 화면의 문구가 된다. 읽음 표시는 부르는 자리가 결과를 안 쓰고, 방 멤버가 아니면 0행이 정상이다 — `.select()` 를 붙여도 아무도 그 값을 안 읽으므로 왕복만 는다. **두 액션이 다른 답을 갖는 이유를 파일에 적었다** | `.select("id")` 를 붙여 맞추기 | code-reviewer 2026-09-06 |
| D21 | 검사 | 검사 이름에서 **「조용히 성공으로 말하지 않는다」를 뺀다**(`mark-read`) | D20 대로 이 구현은 0행과 성공을 구분하지 못한다. 안 붙드는 것을 이름이 주장하면 다음 사람이 그 자리를 이미 덮인 것으로 읽는다 | 이름 유지 | test-auditor 2026-09-06 |
| D22 | 검사 | **`readSlots` 의 요일을 모양부터 본다**(`/^[0-9]+$/`) | `parseInt` 만 쓰면 `"1.5"` 도 `"1x"` 도 1 이 되어 통과한다. 폼의 select 가 보내는 값은 한 자리 숫자다 | `parseInt` 유지 | 검사를 쓰다 드러났다(2026-09-06) |
