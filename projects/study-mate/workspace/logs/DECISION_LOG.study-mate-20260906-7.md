# 결정 로그 — study-mate-20260906-7

사이클: 보류 둘(P13·P14)을 승인된 시각 기준으로 등재하고, P14 가 열어 준 모집글 삭제를 붙인다.
사람이 정한 것 셋이 이 사이클의 입력이다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 시각 | **파괴적 행동에 새 톤도 새 색도 만들지 않는다** — 테두리 톤 버튼 + 글자 | 형광펜 8색은 카테고리 8종이 나눠 갖고 그 위에 모집 상태가 겹친다. 코랄을 삭제에 주면 같은 색이 카드에서는 「마감」 버튼에서는 「삭제」가 되는데, **삭제는 버튼이라 카테고리 라벨(사각)·모집 상태(알약)와 형태로도 안 갈린다** | 코랄을 쓰기 · 9번째 색을 만들기(8색 체계가 깨지고 종이 톤 위의 강한 빨강은 「하지 말 것」 쪽이다) | 사람 결정 2026-09-06(보류 P14) |
| D2 | 시각 | **한 번에 실행되지 않는다** — 첫 버튼이 확인 문구 + 버튼 둘로 바뀐다 | **실수로 지워지는 것을 막는 것은 색이 아니라 두 번 누르게 하는 것이다.** 그리고 나중에 색을 주기로 하면 그때 붙이면 되지만, 8색 체계를 먼저 깨고 되돌리는 것은 비싸다 | 한 번에 실행 + 색으로 경고 | 사람 결정 2026-09-06 |
| D3 | 시각 | **브라우저 기본 확인 창(`confirm`)을 안 쓴다** | 회색 시스템 창이 이 세계관 밖이다 — 「파일 고르기 칸」에서 네이티브 버튼을 다시 그린 것과 같은 이유 | `confirm()` 한 줄 | design-rules 「하지 말 것」 |
| D4 | 시각 | **자리는 화면 맨 아래, 주 폼과 분리된 블록** (카드 밖 · 위쪽 점선 괘선) | 같은 카드에 넣으면 「저장」과 「삭제」가 한 패널에 나란해진다. 카드를 새로 두르면 폼 카드와 같은 무게를 갖는데, 삭제는 주 행동이 아니라 드물게 쓰는 뒷문이다 | 폼 카드 안 · 별도 카드 · 상세 화면의 작성자 줄 | 사람 결정 2026-09-06 |
| D5 | 시각 | **「내 참여 상태」는 지금 화면 값을 그대로 규칙으로 올린다** | 새 형태를 지어내지 않는 쪽이고, 알약(999px)이 아니라 버튼 둥글기라 모집 상태와 형태로도 갈린다. **화면 변경 0** — 문서가 구현을 따라잡았다 | 새 모양 배정 | 사람 결정 2026-09-06(보류 P13) |
| D6 | 시각 | **「거절됨」에는 색도 형태도 주지 않는다** | 거절에 색을 주면 화면이 그 줄을 강조하는데, **거절은 사용자가 다시 볼 일이 없는 정보라 눈에 띄면 안 된다.** 목록에서 빼지도 않는다 | 코랄 알약 · 목록에서 제외 | 사람 결정 2026-09-06 |
| D7 | 인가 | **삭제도 「그 글」이자 「내가 쓴 글」로 좁힌다** | INV-Z3 이 삭제를 명시적으로 덮는다(「고치거나 **지우는** 것은 작성자만」). 정책(`posts_delete_author`)이 인가의 주인이고 이 줄은 앱 경로의 한 겹이다 | 정책에만 맡기기 | INV-Z3 |
| D8 | 인가 | **목적지·캐시 경로는 데이터베이스가 돌려준 `study_id` 다** (`.select("study_id")`) | 글은 이제 없으므로 보낼 곳이 그 스터디인데, **그 값을 폼에서 받으면 그 자리가 열린 리다이렉트가 된다.** 바로 앞 사이클에서 리뷰가 잡은 것과 같은 자리다 | 폼에 `studyId` 숨은 칸으로 싣기 | security-reviewer 2026-09-06(앞 사이클) |
| D9 | 인가 | **0행을 성공으로 읽지 않는다** | 삭제도 갱신과 같다 — 조건에 안 맞으면 오류 없이 0행이다. 성공으로 읽으면 「지웠습니다」가 나가면서 글은 그대로 남는다 | 오류만 보기 | 수정 액션의 같은 판단 |
| D10 | 캐시 | **지운 글의 상세(`/posts/[id]`)는 캐시를 안 지운다** | 그 주소는 이제 404 다 — 지울 대상이 아니다. 대신 그 글이 사라져야 하는 화면 넷을 지운다: 목록 · 프로필 · 홈의 최신 세 장 · **그 스터디의 상세**(모집글이 0장이 되면 「모집글 쓰기」가 주 행동으로 바뀐다) | 상세도 같이 지우기 | 브라우저에서 스터디 상세가 실제로 그렇게 바뀌는 것을 확인했다 |
| D11 | 확인 문구 | **확인 문구에 형광펜을 안 쓴다**(`FormError` 의 코랄 슬롯이 아니다) | 실패가 아니라 되묻는 것이다. 폼의 성공 문구가 실패 문구와 같은 슬롯·같은 여백을 쓰되 색으로 갈리는 방식 그대로다 | `FormError` 재사용 | design-rules 2026-09-06 「폼의 성공 문구」 |
| D12 | 접근성 | 확인 문구에 `role="alert"` 를 준다 | 눌러서 나타난 것이라 초점이 안 옮겨 간다 — 그러면 화면을 안 보는 사용자는 무엇이 바뀌었는지 알 방법이 없다 | 아무것도 안 붙이기 | `Field.tsx` 의 같은 판단 |
| D13 | 범위 | **하드 삭제를 그대로 두고 보류로 올린다**(P15) | 좋아요가 `on delete cascade` 로 같이 사라지는데, 스터디 삭제는 반대로 정해져 있다(INV-Z6). **그 차이의 근거가 문서에 없다** — 지금 스키마 동작을 화면에 연 것뿐이라 이 사이클이 만든 문제가 아니고, 좋아요를 아직 누를 수 없어 지금이 바꾸기 싼 시점이다 | 이 사이클에서 소프트 삭제로 바꾸기(마이그레이션 + 새 불변식 + 스펙 재승인이 붙는다) | INV-Z6 · 백로그 2026-09-05 항목 |
| D14 | 캐시 | **지운 글의 상세(`/posts/[id]`)도 지운다** | 처음에는 「그 주소는 이제 404 니까 지울 대상이 아니다」로 뺐는데 틀렸다 — `revalidatePath` 가 지우는 것은 응답이 아니라 **라우터가 들고 있던 항목**이고, 삭제까지 오는 길이 상세 → 수정 → 삭제라 그 항목이 거의 항상 캐시에 있다. **뒤로 가기가 지워진 글을 신청 버튼째로 그린다** | 넷만 지우기 | code-reviewer 2026-09-06(중간) |
| D15 | 화면 | **오류 문구를 확인 갈래 안으로 옮긴다** | 밖에 두면 실패한 뒤 「취소」를 눌렀을 때 확인 블록만 접히고 오류 문구는 접힌 버튼 위에 남는다 — `useActionState` 의 결과는 스스로 비워지지 않는다 | 밖에 두기 | code-reviewer 2026-09-06 |
| D16 | 접근성 | **확인 단계로 갈 때 초점을 「지웁니다」로, 취소하면 원래 버튼으로 옮긴다** | 안 옮기면 초점이 `document.body` 로 떨어진다. 낭독기 사용자는 `role="alert"` 로 듣지만 **화면을 보면서 키보드만 쓰는 사용자**는 알림도 없이 Tab 을 문서 처음부터 다시 눌러야 한다 — **두 번 누르게 하는 설계는 두 번째 버튼에 갈 수 있어야 성립한다** | `role="alert"` 만 두기 | ui-reviewer · code-reviewer 2026-09-06 |
| D17 | 구조 | **되묻는 문구를 `shared/ui/field` 의 세 번째 슬롯(`FormConfirm`)으로 올린다** | 같은 슬롯 값이 세 벌이 됐고 셋째만 다른 파일에 있었다. `.error`·`.notice` 를 한 파일에 붙여 둔 이유가 그대로 적용된다 — 슬롯 여백을 고치는 날 이 파일은 안 따라온다 | 기능 CSS 에 두기 | ui-reviewer 2026-09-06 |
| D18 | 검사 | **두 단계 확인에 컴포넌트 검사를 붙인다**(이 레포의 첫 `.test.tsx`) | 승인된 규칙이 「실수로 지워지는 것을 막는 것은 두 번 누르게 하는 것」인데, `confirming` 삼항을 지워 첫 버튼을 `type="submit"` 으로 만들어도 전 스위트가 초록불이었다. **되돌릴 수 없는 동작의 유일한 방벽**이라 백로그로 미루지 않았다 | 백로그로 미루기 | test-auditor 2026-09-06(높음) |
| D19 | 검사 | **삭제의 「반대 절반」을 통합 검사로 붙이고 정책 변이 둘을 등록한다** | `posts_delete_author` 를 `using (false)` 로 바꾸거나 통째로 지워도 전부 초록불이었다 — 그 상태의 제품은 **작성자가 자기 글을 못 지운다.** 코드 주석이 「인가의 주인은 정책」이라고 적어 둔 그 주인 쪽에 변이가 0개였다 | 실패 절반만 두기 | test-auditor · security-reviewer 2026-09-06(높음) |
| D20 | 검사 | **필터가 아니라 정책이 막는 모양을 따로 잰다** | 앞서 붙인 0행 검사는 `.eq("author_id", stranger.id)` 가 이미 행을 0개로 만들어 정책이 평가될 기회조차 없었다. 「정책에 걸린 행도 0행으로 온다」는 여전히 아무도 안 재고 있었다 | 그대로 두기 | test-auditor 2026-09-06 |
| D21 | 화면 | **모집 중이 아닌 스터디에는 「모집글 쓰기」를 주 행동으로 안 올린다** | 삭제가 생기기 전에는 「모집글 0장」이 될 길이 없었다. 이제 마감한 스터디의 마지막 글을 지우면 그 화면이 못 쓰는 버튼을 잉크로 올린다(작성 목록이 모집 중인 것만 보여 준다 — INV-Z14) | 그대로 두기 | code-reviewer 2026-09-06 |
| D22 | 문서 | **화면 목록의 `/posts/[id]/edit` 를 「모집글 수정·삭제」로 고친다** | 시각 기준에는 「자리는 그 대상을 다루는 화면의 맨 아래」가 등재됐는데 화면 목록은 그대로라 두 문서가 이 화면이 무슨 일을 하는지 다르게 말했다 | 그대로 두기 | code-reviewer 2026-09-06 |
