# 결정 로그 — study-mate-20260910-1

사이클: 백로그 맨 위의 **「채팅 메시지에 직접 붙어 `created_at` 을 임의로 실을 수 있다」** 를 닫는다.
지난 두 사이클이 다룬 것은 **사람이 쓴 글자가 제품 문장과 같은 칸을 쓰는** 문제였고,
이번 것은 한 층 아래다 — **그 행에서 어느 값을 사람이 정하고 어느 값을 데이터베이스가 정하는가.**

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 스펙 | **열 단위 삽입 권한으로 막는다 — 정책을 고치지 않는다** | 정책의 `with check` 는 **조건에 적힌 열만** 본다. 나머지 열은 요청이 보낸 값 그대로 들어간다. 0002 의 4절이 갱신에 대해 이미 같은 판정을 내렸고, 열 단위 권한은 정책보다 **앞에서** 판정해 허용 안 된 열이 오면 정책을 보기도 전에 거부한다 | 정책에 `created_at` 조건을 더하기(`created_at between now() - interval '5s' and now()`) — 시계 오차만큼의 창이 남고, `id` 는 여전히 열려 있으며, 조건이 늘수록 판정이 느려진다 | `supabase/migrations/0002_review_fixes.sql:155-176` · `0010:238-242` |
| D2 | 스펙 | **`anon` 은 삽입 권한을 통째로 잃는다** | 정책이 어차피 거부하지만, 권한이 남아 있으면 「정책만 고치면 열리는」 자리가 하나 남는다. 두 층이 다 닫혀 있어야 한 층을 고치는 실수가 사고가 안 된다 | `authenticated` 만 좁히기 | `0021` 1절 |
| D3 | 스펙 | **본문 길이·공백·제어문자를 스키마로 내린다** | `src/features/chat/model/limits.ts` 가 **스스로** 「스키마에 제약이 없어 서버의 이 값이 유일한 강제 위치」라고 적어 놨는데, 직접 삽입은 그 액션을 안 지난다. 0010(이름 길이)·0015(이름 제어문자)·0020(제목)이 같은 판정을 이미 세 번 내렸다 | 서버 액션만 믿기 — 그 액션을 지나지 않는 경로가 이 사이클이 막으려는 바로 그것이다 | `0021` 2절 |
| D4 | 스펙 | **제어문자를 전면 금지한다 — 줄바꿈도 포함** | 입력창이 한 줄짜리 `<input>` 이라(`ChatRoomView.tsx:214`) 폼은 줄바꿈을 못 만든다. 즉 이 제약이 막는 것은 **폼이 만들 수 없는 값뿐**이다. 말풍선은 `pre-wrap` 으로 그려서 직접 넣은 줄바꿈이 그대로 살고, 2000자를 전부 줄바꿈으로 채우면 말풍선 하나가 화면 수십 개 높이가 된다 | 줄바꿈·탭 허용(`[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]`) — 지금 폼이 못 만드는 값을 미리 허용하는 것이고, 세로 도배가 통과한다. 여러 줄 입력창을 만드는 날 이 제약을 푼다 | 사용자 결정 2026-09-10 · `0021` 2절 |
| D5 | 스펙 | **읽음 시각을 이 사이클에 같이 넣는다** | 백로그는 「읽음 시각만 서버 시계다」라고 적었는데, 읽어 보니 그것은 **앱 서버 시계**였다(`mark-read.ts` 의 `new Date()`). 비교 대상인 `created_at` 은 데이터베이스 시계다. 두 시계가 어긋나면 안 읽은 수가 오류 없이 틀린다 — 앱이 빠르면 그 차이만큼 뒤에 온 메시지가 영원히 안 뜨고, 느리면 읽은 것이 다시 올라온다 | 백로그로 빼기 — 고치는 값이 트리거 네 줄이고, 미루면 아무도 모르는 채로 남는다 | 사용자 결정 2026-09-10 · `read-chats.ts:133` |
| D6 | 구현 | **읽음 시각은 `before update` 트리거가 조건 없이 찍는다** | 열 단위 권한은 **어느 열을 바꾸는가**만 정하지 **무슨 값이 들어가는가**는 안 본다. 이 표의 갱신은 「이 사람이 이 방을 읽었다」 하나뿐이고, 그 밖의 갱신(`chat_id`·`user_id` 옮기기)은 INV-Z8 이 금지한 바로 그것이다 | ① `is distinct from` 으로 좁히기 — 요청이 보낸 값과 저장된 값이 같은 순간에 아무 일도 안 하는 갈래가 생기고, 그 갈래를 지나는 요청은 자기가 보낸 값을 그대로 남긴다 ② `security definer` RPC 로 옮기기 — 더 좁지만 `mark-read.ts` 와 그 검사를 통째로 다시 쓰게 되고, 승인된 스펙의 INV-M2 문면(「갱신을 받는 자리에서 덮어쓴다」)을 벗어난다 | `0021` 3절 |
| D7 | 구현 | **앱은 `last_read_at: null` 을 보낸다 — 시각을 아예 안 만든다** | 갱신할 열이 하나는 있어야 요청이 성립한다. 앱 시각을 보내면 **트리거가 사라지는 날 옛 동작으로 조용히 돌아가고**, 그게 이 스펙이 막으려던 상태다. `null` 을 보내면 트리거가 사라졌을 때 모든 메시지가 안 읽음으로 보인다 — 시끄럽게 깨진다 | 앱 시각을 계속 보내고 트리거가 덮게 두기 | `src/features/chat/api/mark-read.ts` |
| D8 | 스펙 | **INV-M5(메시지 수정·삭제 금지)에는 새로 거는 것이 없다 — 검사만 붙인다** | 이미 거부되고 있다(정책이 하나도 없어서). **지키는 것이 「없음」이라 눈에 안 보이는 것**이 문제다. 나중에 메시지 수정 기능을 만들며 정책 한 줄을 더하면 규칙이 소리 없이 사라진다 | ① 권한까지 거두기 — 정책이 이미 막고 있어 얻는 것이 없고, 나중에 「자기 메시지 삭제」를 만들 때 고칠 자리가 정책 한 곳에 모여 있는 편이 낫다 ② 불변식으로 안 적기 — 그러면 다음 사람이 그 자리가 열려 있는지 닫혀 있는지 모른다 | `0021` 4절 |
| D9 | 문서 | **양방향 제어 문자는 이 스펙의 비범위로 적는다** | `[[:cntrl:]]` 에 안 잡히는 글자이고(U+202A–202E 등), 이름·알림 쪽에 같은 질문이 이미 열려 있다. 한 자리에서 같이 정해야 세 곳이 다른 규칙을 갖지 않는다 | 여기서 같이 막기 — 이름 쪽 결정 없이 메시지만 막으면 규칙이 갈린다 | `docs/BACKLOG.md` 「양방향 제어 문자가 알림의 낫표 경계를 깰 수 있다」 |
| D10 | 검증 | **불변식마다 「반대 절반」을 같이 잰다** | 「거부된다」만 보면 삽입 권한을 통째로 거두는 변이가 통과한다 — 채팅이 아예 안 되는데 검사는 전부 초록불이다. 그래서 S1c(허용된 셋은 들어간다) · S3b(상한값은 통과) · S4b(평범한 한 줄은 통과) · S1d(멤버가 아니면 셋도 거부)를 같이 둔다 | 거부 경로만 보기 | `.claude/rules/tdd.md` 「부재만 확인하는 단언은 절반이다」 |
| D11 | 검증 | **거부를 확인한 뒤 행 수도 센다** | 오류를 냈는데 행은 들어간 조합이 있을 수 있다. 「error 가 null 이 아니다」만으로는 그 조합이 안 걸린다 | error 만 보기 | `tests/integration/chat-message-integrity.test.ts` |
| D12 | 검증 | **본문이 긴 경우만 세는 방식을 바꾼다** | 조건이 주소줄에 실려서 2001자짜리 본문은 요청 자체가 「주소가 너무 길다」로 죽었다. 그러면 제약이 무슨 일을 했는지가 아니라 **검사의 사정**이 판정이 된다 | 본문으로 세기 | 첫 실행에서 실제로 났다 — `countInChat` 으로 갈랐다 |
| D13 | 검증 | **「지금」을 데이터베이스에 물어서 잰다** | 앱 시계로 재면 INV-M2 가 재려는 것(두 시계가 어긋나는 일)을 검사 자신이 저지른다. 스큐가 그대로 판정 오차가 되고 어느 쪽이 틀렸는지 못 가른다 | `Date.now()` 로 여유 창을 넓게 잡기 | `dbNow()` |
| D14 | 검증 | **옛 변이 `markread-frozen-clock` 을 지우고 방향을 뒤집은 것으로 바꾼다** | 그 변이는 「앱이 시계를 갖고 있다」를 전제로 기본 시계를 1970년으로 고정하는 것이었다. 앱이 시각을 안 만들게 됐으니 고정할 시계가 없다 — 이제는 **앱이 다시 시각을 만드는 것**이 변이다 | 그냥 지우기 — 그러면 그 자리를 붙드는 변이가 0개가 된다 | `scripts/mutate.mjs` 의 `markread-app-clock` · 실행 결과: 빨간불 1건 |

## 리뷰 뒤에 바뀐 결정

리뷰어 셋이 같은 자리를 여러 번 짚었다. 아래는 **위 표의 판단을 실제로 뒤집은 것들**이다.

| id | 영역 | 결정 | 이유 | 뒤집힌 것 | 근거 |
|---|---|---|---|---|---|
| D15 | 검증 | **앱이 실제로 보내는 값으로 누르는 검사를 쓴다** | 검사 둘이 전부 미래 시각을 실어 보냈는데 앱이 보내는 값은 `null` 이다. 트리거를 「값이 달라졌을 때만」으로 좁히면 미래 시각은 계속 덮이지만 null → null 은 안 덮이고, 그 갈래를 지나는 것이 **모든 방의 첫 읽음**이다 — 읽어도 값이 null 로 남아 그 방 전체가 영원히 안 읽음이 된다 | D10(「반대 절반을 잰다」)이 재는 절반을 잘못 골랐다 | 2026-09-10 test-auditor · code-reviewer(각자 같은 자리) · 내가 돌린 변이도 `chat-read-stamp-conditional` 을 「초록불 — 아무도 안 붙들고 있다」로 냈다 |
| D16 | 구현 | **「공백」 판정을 `btrim` 에서 `[[:space:]]` + BOM 으로 바꾼다** | 인자 하나짜리 `btrim` 은 U+0020 만 자른다. 전각 공백 세 칸이나 줄바꿈 없는 공백 하나로 된 본문이 「내용이 있다」를 통과해서 빈 말풍선이 그대로 들어간다. **0010 이 이름에서 실측해 적어 둔 함정인데 같은 자리에 다시 빠졌다** | D3 의 첫 구현 | 2026-09-10 security-reviewer · 실측: `chr(12288) ~ '[[:space:]]'` 참 · `chr(160)` 참 · `chr(65279)` 거짓 |
| D17 | 구현 | **제어문자 판정을 앱에도 둔다**(`hasControlChars`) | 없으면 본문 가운데의 제어문자가 데이터베이스까지 가고, 제약이 23514 로 거부한 것을 `dbErrorMessage` 가 「잠시 뒤 다시 시도해 주세요」로 덮는다 — **다시 시도해도 절대 성공하지 않는데 문구는 기다리라고 한다.** 0015 가 이름에 같은 짝을 이미 두었다 | D4 를 스키마에만 걸어 두려던 것 | 2026-09-10 code-reviewer · 닿는 경로: 스프레드시트 셀을 복사해 붙여 넣으면 탭이 남는다 |
| D18 | 구현 | **갱신·삭제 권한도 거둔다** | 「내 메시지 수정」 정책 한 줄을 더하는 날, 권한이 남아 있으면 같은 요청이 `content` 만이 아니라 `chat_id`·`created_at`·`sender_id` 까지 SET 에 실을 수 있다. **이 파일이 삽입에 대해 방금 닫은 것과 같은 실패다.** 오늘 동작은 하나도 안 바뀐다 | D8(「권한까지 거두지 않는다 — 고칠 자리가 정책 한 곳에 모여 있는 편이 낫다」) | 2026-09-10 security-reviewer · 실측: 다섯 열 전부에 UPDATE 권한이 있었다 |
| D19 | 구현 | **트리거를 `before update of last_read_at` 으로 좁힌다** | 조건 없이 찍으면, 이 표에 열이 붙어 백필을 돌리는 순간 **그 방 전원의 읽음 시각이 지금으로 밀린다.** 방향이 「다 읽었다」라서 배지가 조용히 꺼진다. `update of` 는 SET 절에 그 열이 있는지만 보고 값을 비교하지 않으므로 D6 이 거부한 갈래가 안 생긴다 | D6 의 「조건 없이 찍는다」 — 이유는 살리고 범위만 좁혔다 | 2026-09-10 security-reviewer |
| D20 | 검증 | **권한·정책 목록을 직접 묻는 검사를 쓴다**(S1e·S5c·S5d) | 행동으로 못 재는 것이 셋이다. ① `anon` 삽입 권한 — 정책이 대신 거부해서 권한이 열려 있어도 초록불이다 ② 갱신·삭제 권한 — 정책이 이미 막아 동작이 같다 ③ `security definer` 로 들어오는 수정 경로 — 정책을 우회한다 | 행동 검사만 두려던 것 | 2026-09-10 test-auditor · security-reviewer · code-reviewer |
| D21 | 검증 | **거부되는 「방식」이 아니라 결과를 단언한다** | `expect(error).toBeNull()` 로 적으면 **불변식을 더 강하게 만드는 변경이 검사를 깨뜨린다.** 실제로 D18 을 넣는 순간 그 단언이 빨간불이 됐을 것이고, 다음 사람은 방벽을 뺐을 것이다 | 첫 구현의 S5·S5b | 2026-09-10 test-auditor · security-reviewer |
| D22 | 검증 | **S3 과 S3b 를 검사 둘로 나눈다** | 하나로 묶으면 앞 단언이 실패할 때 vitest 가 거기서 멈춰 뒤 갈래를 아예 안 잰다. 그리고 넓히는 변이와 좁히는 변이가 같은 이름의 검사를 깨뜨려 어느 갈래가 붙들린 것인지 판정이 못 가른다 | 첫 구현 | 2026-09-10 test-auditor · `.claude/rules/tdd.md` |
| D23 | 구현 | **길이를 코드 포인트로 센다** | `content.length` 는 UTF-16 코드 단위라 이모지 하나가 2로 세지는데 스키마의 `char_length` 는 코드 포인트다. 「두 자리가 같은 숫자다」가 경계에서 참이 아니게 된다 | 첫 구현 | 2026-09-10 code-reviewer · security-reviewer · 레포 선례: `entities/study/model/study-form.ts:63` |
| D24 | 하네스 | **0021 을 복구 목록에 넣고 복구 방벽 넷을 더한다** | 넣지 않으면 첫 변이를 심는 순간 복구가 실패해 **변이가 하나도 안 돈다.** 그리고 목록이 낡는 날 조용히 열린 데이터베이스에서 그 뒤 판정이 전부 돈다 — 그래서 목록만이 아니라 「열려 있으면 멈춘다」는 검사를 같이 둔다 | 첫 구현 | 2026-09-10 test-auditor · **심어서 확인**: 0021 을 목록에서 빼고 돌리니 「복구 명령이 죽었다」로 멈췄다 |
| D25 | 하네스 | **변이 이름표에 숫자를 안 넣는다** | 「2000자는」으로 뒀더니 검사는 제대로 빨간불이 났는데 판정이 「엉뚱한 빨간불」로 나왔다 — 상한을 바꾸는 변이가 검사 **이름의 숫자까지** 바꾸기 때문이다 | 첫 구현 | 2026-09-10 실측 |
| D26 | 스펙 | **줄 구분자(U+2028·U+2029)는 고치지 않고 사용자에게 올린다** | 실측했다 — `[[:cntrl:]]` 에 안 걸린다. 둘 다 강제 줄바꿈이라 `pre-wrap` 말풍선에서 U+000A 와 같은 일을 하고, **INV-M4 가 이름 붙인 손해가 그대로 재현된다.** 그런데 INV-M4 의 문면은 「제어문자」이고 이 둘은 제어문자가 아니다 — 넓히려면 승인된 불변식의 문장을 고쳐야 한다 | 없음(새로 생긴 판단) | 2026-09-10 security-reviewer · CLAUDE.md 「spec-level 이거나 위험 표면이면 사용자에게 먼저 보고」 |
