# 결정 로그 — study-mate-20260906-3

사이클: `/profile` 내 프로필 화면 한 장. 네 구역(내 프로필 카드 · 내가 만든 스터디 ·
내 신청 현황 · 내가 쓴 모집글)을 읽기만으로 세운다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 범위 | `/profile` 세 장 중 **읽기 한 장만** 이번 사이클에 넣는다 | `/profile/edit` 는 아바타 경로 규약(파일 저장소 표면), `/profile/password` 는 비밀번호 변경(auth 표면)이라 둘 다 스펙이 먼저다. 읽기 한 장은 그 앞에 독립적으로 설 수 있고, 헤더의 「내 프로필」·종 아이콘이 없는 주소로 가는 것부터 닫는다 | 스펙부터 쓰고 세 장 한 번에 — 한 사이클이 길어지고, 지금 당장 고칠 수 있는 죽은 링크가 그만큼 오래 남는다 | 사용자 결정 2026-09-06 |
| D2 | 범위 | **「내가 쓴 모집글」을 구역으로 넣는다** — 화면 목록에 없던 것이다 | 사용자가 골랐다. IA.md 의 `/profile` 칸은 "내 스터디·신청 현황"까지만 적혀 있었고, 늘린 만큼 그 문서를 같이 고친다(대화와 문서가 다르면 문서가 우선) | 화면 목록 그대로 세 구역 | 사용자 결정 2026-09-06 |
| D3 | 화면 | 아바타 이미지는 안 그린다 — 승인된 아바타(괘선 채움 + 이니셜)를 쓰고 `avatar_url` 은 읽지도 않는다 | 그 칸을 채우는 길이 아직 없다(업로드는 `/profile/edit` 과 함께 온다). 읽어만 두면 다음 사람은 그것이 그려진다고 믿는다 | `avatar_url` 을 읽어 두고 나중에 쓰기 | 되돌리기 싼 결정 |
| D4 | 화면 | 「프로필 수정」·「비밀번호 변경」 링크를 안 건다 | 그 화면들이 이번 사이클에 없다. 걸면 이 사이클이 고치려던 결함(「없는 화면을 가리키는 링크 넷」)을 하나 더 만드는 것이다 | 링크를 미리 걸어 두기 | 백로그의 같은 항목 |
| D5 | 화면 | 신청 상태 다섯 중 **셋만** 보여 준다(`pending`·`accepted`·`rejected`). `withdrawn`·`kicked` 는 뺀다 | 승인된 시각 기준이 정한 상태는 셋이다(「신청 상태 3종」). 넷째·다섯째를 그리려면 승인된 적 없는 표현을 지금 지어내야 한다 | 다섯 다 보여 주기 — 「강퇴됨」의 시각 표현이 승인 밖이다 | design-rules 「신청 상태 3종」 |
| D6 | 화면 | 「참여 중」만 **민트 형광 밑줄**로 강조한다. 대기 중·거절됨은 약한 잉크 글자 | 형광펜 코딩 규칙이 「내 참여 상태」를 허용 뜻 넷에 넣었고(2026-09-06 P6), 밑줄은 일반 강조 장치라 카테고리(사각 라벨)·모집 상태(점 알약)의 형태 규칙과 겹치지 않는다. 알약을 하나 더 만들면 같은 자리에 세 번째 알약이 생긴다 | 민트 채움 알약 — 모집 상태 알약과 자리·모양이 겹친다 | design-rules 「형광펜 코딩 규칙」·「색이 겹치면 모양으로 가른다」 |
| D7 | 조회 | `readMyHostedStudies`(기존)에 옵션을 붙이지 않고 `readStudiesIHost` 를 따로 둔다 | 거르는 기준이 자리마다 다르다 — 모집글을 붙일 곳을 고르는 자리는 모집 중인 것만, 「내가 만든 스터디」는 마감한 것도 보여야 한다. 옵션으로 묶으면 그 기본값이 무엇이어야 하는지가 부르는 자리마다 갈린다 | 인자 하나로 합치기 | 되돌리기 싼 결정 |
| D8 | 조회 | 「내 신청 현황」에서 **내가 호스트인 스터디를 뺀다** | 스터디를 만들면 호스트 자신의 수락된 참여 행이 같이 생긴다(`setup_new_study`). 안 빼면 내가 만든 스터디가 위 구역과 아래 구역에 두 번 나온다 | 그대로 두기 | 0001 의 트리거를 읽고 확인 |
| D9 | 조회 | 스터디를 **바깥 조인**으로 읽고, 안 보이면 `available: false` 갈래로 떨어뜨린다 | `!inner` 로 묶으면 스터디가 지워졌을 때 신청 행까지 사라져 「내가 신청했던 그 스터디가 어떻게 됐지」에 답이 없어진다(INV-Z6). 필드를 null 로 두지 않고 갈래로 나누는 것은 `read-chats.ts` 와 같은 이유다 | `!inner`, 또는 필드마다 null 허용 | `entities/chat/api/read-chats.ts` |
| D10 | 조회 | `read-my-posts.ts` 는 반대로 **`!inner`** 를 쓴다 | 모집글이 보이는 조건이 곧 그 스터디가 보이는 조건이다(`posts_read` 가 `private.study_is_visible` 을 그대로 부른다). 스터디가 안 보이면 모집글 행 자체가 안 오므로 바깥 조인이 덮을 상태가 없다 | 여기도 바깥 조인 — 도달할 수 없는 갈래가 생긴다 | 0010 의 `posts_read` 원문 |
| D11 | 조회 | `readProfile` 은 지역 이름을 **두 번째 질의**로 붙인다 | `profiles.region` 은 `regions` 를 가리키는 외래 키가 아니라 그냥 텍스트라(0002 가 지역을 고정 목록으로 뽑을 때 프로필 쪽은 안 따라갔다) 한 질의로 임베드할 수 없다. 목록 전체를 읽어 찾는 대신 한 줄만 고른다 | 지역 목록 전체를 읽어 앱에서 찾기 / 코드를 그대로 화면에 그리기(`gyeonggi` 가 나온다) | 0001 의 `profiles` 정의 |
| D12 | 검사 | `readMyParticipations` 만 판독기를 기본 인자로 열고 유닛 검사 8개를 붙였다 | 판단 셋(내 것만·상태 셋만·호스트 제외)이 이 함수에 몰려 있다. 열지 않으면 셋을 통째로 지워도 초록불이다. 변이 다섯(user_id 필터·status 필터·호스트 제외·임베드 모양 검사·바깥 조인)을 각각 넣어 전부 빨간불을 확인했다 | 검사 없이 넘기기 — 백로그의 「서버 액션 다섯에 검사 0개」를 하나 더 늘린다 | `.claude/rules/tdd.md` · `features/create-post` 의 같은 모양 |
| D13 | 시드 | `scripts/seed.mjs` 의 프로필 `insert` 를 `upsert` 로 고친다 | 0010 부터 계정이 생기는 트랜잭션에서 트리거가 프로필을 같이 만든다(INV-A7). 시드가 첫 사람에서 기본 키 중복으로 죽고 있었다 — 화면을 실제로 볼 수 없는 상태였다 | `update` 로 바꾸기 — 트리거가 없는 데이터베이스에서 0행 갱신이 조용히 지나간다 | 실행해서 확인(시드 재실행 성공) |

## 리뷰어 셋의 지적 반영 (2026-09-06)

| id | 영역 | 결정 | 이유 | 근거 |
|---|---|---|---|---|
| D14 | 화면 | **참여 중·대기 중을 승인된 형태로 되돌린다** — 민트 채움 + 2px 잉크 테두리 / 점선 테두리. D6 의 형광 밑줄을 버린다 | 형광 밑줄은 「일반 강조」 장치로 이미 배정돼 있다(`.mark`). 그것을 뜻 하나에 다시 배정하면 밑줄이 두 가지를 뜻하게 되고 둘을 가르는 것은 **색뿐**이 된다 — 「색만으로 구분하지 않는다」가 금지한 상태다. D6 의 주석은 규칙을 거꾸로 인용하고 있었다 | ui-reviewer 1 (높음) |
| D15 | 화면 | 알약(999px)이 아니라 **버튼 둥글기**(`--r-btn`)를 쓴다 | 모집 상태가 이미 점 붙은 알약이고 안 읽음이 숫자 알약이라, 세 번째 알약을 만들면 같은 자리에서 형태가 안 갈린다. 승인된 「신청 상태 3종」이 버튼 값이므로 그 값을 그대로 옮기는 것이 새 형태를 지어내지 않는 길이다 | ui-reviewer 1 |
| D16 | 보류 | **P13 을 연다** — 「내 참여 상태」의 모양 등재 + 「거절됨」의 표현 | 기준 문서에 그 둘이 비어 있다. 지금 화면은 승인된 값 안에 있지만 규칙으로는 안 적혀 있어서, 다음 화면이 같은 질문을 다시 한다 | ui-reviewer A·B |
| D17 | 배치 | **「모집글을 쓸 수 있나」 판정을 `entities/study/model` 로 올린다**(`canOpenPost`) | 위젯이 `studies.length > 0` 으로 판단하고 있었다. 층 규칙 위반인 데다 **판정 자체가 틀렸다** — 조건은 「모집 중인 내 스터디가 있다」다(INV-Z14). 스터디가 전부 마감인 사람에게 링크가 뜨고, 눌러 가면 "고를 수 있는 스터디가 없습니다"로 받는다 | ui-reviewer 2 · code M3 |
| D18 | 조회 | **지워진 스터디의 모집글을 「내가 쓴 모집글」에서 뺀다**(`.is("study.deleted_at", null)`) | `study_is_visible` 이 「지워지지 않았거나 내가 호스트」라, 내가 지운 내 스터디의 모집글은 나에게 계속 보였다. 그러면 **「내가 만든 스터디」에는 없는 스터디 이름이 아래 목록에 그려지고** 그 링크가 살아 있는 상세로 간다 — INV-Z10 이 "그 스터디의 모집글은 목록에 나타나지 않는다"고 정한 그 상태다 | code H2 |
| D19 | 이름 | `readMyHostedStudies`/`readStudiesIHost` → **`readPostableStudies`/`readMyStudies`** | 두 이름이 같은 말("내가 호스트인 스터디")을 영어로 두 번 쓴 것이라, 배럴에 나란히 놓인 둘 중 무엇을 고를지 근거가 없었다. 이름이 **거르는 기준**을 말하게 바꿨다 | code H1 |
| D20 | 조회 | **임베드 모양 확인을 셋 다에 붙인다.** `?? ""` 같은 도달 불가 기본값은 지운다 | `!inner` 라 null 일 수 없는데 코드만 있을 수 있는 척하고 있었다. 반대로 계산 컬럼(`recruiting`·`accepted_count`)은 진짜로 빠질 수 있고, 빠지면 **모든 스터디에 「마감」 배지**가 붙는다 — 오류가 아니라 정상 화면으로 보인다 | code M4·M5 |
| D21 | 오류 | 조회 실패·모양 불일치를 던지는 문장을 **`shared/lib/db-error.ts` 의 `throwDbError`·`throwShapeError` 로 모은다** | 같은 세 줄이 여섯 벌 복사돼 있었다. 그리고 모양 불일치 쪽은 **행 원문과 스터디 id 를 예외 메시지에 담고 있었다** — 지금은 Next 가 digest 로 덮지만, `error.tsx` 를 붙여 `error.message` 를 그리는 순간 화면에 나간다 | code M7 · security 5 |
| D22 | 조회 | 프로필의 질의 둘을 **나란히 던지고 클라이언트를 하나만 만든다.** 지역 목록 조회 실패는 삼키지 않는다 | 직렬이면 이 갈래가 화면의 대기 시간을 정한다(나머지 넷은 병렬이다). 지역은 여덟 줄이라 통째로 받는 편이 왕복 한 번보다 싸다. 실패를 null 로 떨어뜨리면 「지역을 아직 안 정했다」와 구분되지 않는다 | code M6·L2 |
| D23 | 이름 | `MyProfile` → **`ProfileCard`**, 파일도 `read-profile.ts` 로 | 함수는 "내 것 전용이 아니다"라고 주석에 적혀 있는데 타입만 「내 것」이라고 말했다. 나중에 「내 것일 때만 맞는 필드」를 붙이는 사람이 잘못된 전제를 물려받는다 | code M8 |
| D24 | 경계 | **판독기 인자를 배럴 밖으로 안 내보낸다**(`api/public.ts`) | 「인가는 정책이 한다」가 성립하는 근거가 **세션에 묶인 클라이언트**다. 부르는 쪽이 그 클라이언트를 고를 수 있으면 근거가 한 칸 약해진다. 검사는 슬라이스 안에서 원본을 직접 부른다 | security 낮음 |
| D25 | 시드 | 4절이 **이번 실행이 만든 모집글만** 건드리게 좁히고, 목적지가 로컬이 아니면 시작 전에 멈춘다 | 전에는 `select id from public.posts` 로 표 전체를 훑어 `created_at` 을 uuid 순으로 다시 썼다 — 손으로 만든 확인용 모집글까지 목록 순서를 잃는다. D13 이 그 4절을 **처음으로 도달 가능하게 만들었다** | security 6 |
| D26 | 화면 | 아바타 크기를 **공용 컴포넌트의 `size` prop** 으로 옮긴다 | 밖에서 자손 선택자로 덮고 있었는데 `font-size` 는 아예 안 먹었고(컴포넌트가 직접 선언한다), 크기는 명시도가 같아 CSS 가 실린 순서에 따라 이겼다 졌다 했다 | ui-reviewer 4 |
| D27 | 화면 | 구역 제목을 **19px Pretendard 굵게**로(상세 언어의 값). 줄 제목 링크의 밑줄은 늘 자리를 차지하게. 소개에 `pre-wrap`·`overflow-wrap`. 프로필을 못 읽는 갈래에도 h1 | 각각: 22px 주아체는 상세 언어에도 랜딩에도 없던 제3의 값 / hover 에서 밑줄이 생기면 줄이 2px 밀린다 / 사람이 쓴 본문은 줄바꿈을 지켜야 한다(스터디 설명이 같다) / 그 갈래에는 화면에 h1 이 하나도 없었다 | ui-reviewer 7·8·9·10 |
| D28 | 문구 | 빈 목록 문구를 「아직 ~한 적이 없습니다」에서 **「보여 줄 ~이 없습니다」**로 | 만들었다가 전부 지운 사람과 신청이 전부 취소·종료된 사람에게 앞 문장은 거짓이다 | code L3 |
| D29 | 검사 | 나머지 조회 셋에도 검사를 붙인다(유닛 132개). **변이 아홉을 각각 넣어 전부 빨간불을 확인했다** | 리뷰어가 매긴 순서 그대로 — `readMyStudies` 가 1위(조용히 틀린 값을 셋이나 나른다), `readMyPosts` 가 2위(필터를 지우면 남의 글이 뜬다), `readProfile` 이 3위 | code 5번 답 · `.claude/rules/tdd.md` |
