# 결정 로그 — study-mate-20260909-3

사이클: 백로그의 **「사람이 정한 문자열이 제품 문장과 같은 칸을 쓰는 자리가 넷」의 ①②** 를
닫는다. 지난 사이클(`-2`)이 정한 것은 「제품 문장 안에 남의 글자가 섞일 때 경계를 그린다」
였고, 이번 것은 방향이 반대다 — **사람이 지은 이름만 오던 칸에 제품이 자기 말을 넣는** 자리다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 시각 | **이름 칸에 제품이 들어가면 이름의 모양을 벗는다** | 그 칸의 글자는 「누군가 스스로 붙인 이름」이라는 뜻인데 제품이 같은 칸에 쓰면 뜻이 둘이 된다. 사용자가 만들 수 있는 것은 글자뿐이므로 **모양**이 흉내 낼 수 없는 유일한 표시다 | ① 예약어로 막기(스키마가 `studies.title`·`profiles.username` 이 자리표시와 같아지는 것을 금지) — 정확히 같은 값만 막고 `지워진  스터디`(공백 둘)·뒤 공백 하나는 통과한다. 문구를 바꾸면 제약이 낡고, `profiles` 를 건드리면 `docs/specs/profile-editing.md` 가 열린다 ② 낫표를 이 칸까지 넓히기 — 목록의 모든 제목·태그가 낫표를 달게 되어 세부가 아니라 새 방향이 된다 | design-rules 「2026-09-09 (3)」 · 백로그(2026-09-09 security-reviewer · ui-reviewer) |
| D2 | 시각 | **목록 이름표에서 벗는 모양은 형광펜 칩이다** | 살아 있는 스터디는 반드시 칩을 달고 나오므로, 칩이 있다는 것 자체가 「이 글자는 사람이 지었다」가 된다 | 굵기·색만 내리기 — 지난 절이 「굵기만으로는 안 갈린다」고 이미 적었다 | `src/app/chats/StudyChip.tsx` |
| D3 | 시각 | **방 제목에서 벗는 모양은 글꼴이다** | `h-display` 는 이미 굵기 400 이라 「굵기 400 · 약한 잉크」로 내리면 남는 차이가 색 하나뿐이다. 주아체는 「보는 텍스트」인 페이지 제목의 글꼴이고(시각 기준 「글자」), 이름 없는 방에는 제목이 없다 | 굵기·색만 내리기 | `src/app/chats/StudyHeading.tsx` · 실측: `workspace/sketches/chat-room-spoof-*.png` 대 `chat-room-deleted-*.png` |
| D4 | 시각 | **채팅 이름 줄은 모양을 벗는 게 아니라 아예 안 그린다** | 말풍선 위 한 줄에는 이름밖에 없어서 벗길 모양이 없다. 제품이 그 줄에 글자를 안 쓰면 흉내 낼 대상 자체가 사라져 **이 자리는 완전히 닫힌다** | 「이름 없음」 같은 제품 표기를 약한 잉크로 넣기 — 그 글자도 20자 안이라 자기 이름으로 지을 수 있다 | design-rules 「이름을 모르면 이름 줄을 안 그린다」 |
| D5 | 구현 | **판독기가 `senderName: string \| null` 을 준다** | 자리표시를 화면에서만 빼면 판독기에는 제품 문자열이 남는다. 「모른다」를 값의 종류로 만들면 화면이 그것을 안 그릴 수밖에 없다 | 빈 문자열로 두기 — `""` 는 「이름이 빈칸인 사람」과 구분이 안 된다 | `src/entities/chat/api/read-chats.ts` |
| D6 | 구현 | **실시간 줄의 이름을 물어서 채우지 않는다 — 이번에는 자리표시만 없앤다** | 전에도 그 자리는 「멤버」여서 누구인지 안 보였다. 없어진 것은 글자이지 정보가 아니다. 이름을 채우는 것은 클라이언트 조회·실패 처리·캐시가 붙는 별도 작업이다 | 이번에 같이 하기 — 요청받지 않은 범위다 | CLAUDE.md 「필수에 없는 기능 임의 추가 금지」 · 백로그로 뺐다 |
| D7 | 구현 | **두 갈래를 컴포넌트로 뽑는다**(`StudyChip`·`StudyHeading`) | 갈리는 것이 글자가 아니라 모양이라 삼항 연산자로 두면 한쪽만 고쳐지는 날 다시 같은 모양이 된다. 그리고 **서버 컴포넌트 안에 두면 검사가 안 붙는다** — 뽑아야 실제로 불러 보는 검사를 쓸 수 있다 | 페이지 안에서 조건부 클래스 | `.claude/rules/tdd.md` 「소스 텍스트를 훑는 것은 테스트가 아니다」 |
| D8 | 구현 | **`readChatRoom` 이 판독기를 인자로 받는다**(기본값 있음) | 요청 맥락(쿠키)에 붙어 있어 노드 검사에서 그냥은 못 부른다. `readMyNotifications` 가 같은 이유로 같은 모양이다 | `vi.mock` 으로 서버 클라이언트 모듈 통째 가로채기 — 검증 대상 주변을 더 크게 가린다 | `src/entities/notification/api/read-my-notifications.ts:52` |
| D9 | 검증 | **글자가 같은 두 경우를 나란히 그려 놓고 비교하는 검사를 쓴다** | 글자만 보는 단언은 이 규칙을 못 붙든다 — 흉내 이름과 제품 자리표시는 `textContent` 가 같다. 검사도 그 사실을 먼저 단언한 뒤 모양을 본다 | 각 갈래를 따로만 보기 | `src/app/chats/study-name-slot.test.tsx` |
| D10 | 검증 | **변이 다섯을 갈래마다 따로 심어 전부 빨간불을 봤다** | 통과하는 검사는 그것만으로 증거가 아니다. 갈래를 묶어 빼면 하나만 붙들려 있어도 「잡혔다」가 나온다 | 통과만 확인하고 넘어가기 | `.claude/rules/tdd.md` · 결과: 판독기 자리표시 되살리기 2건 실패 · 실시간 「멤버」 1건 · null 검사 빼기 2건 · 칩 되살리기 2건 · 주아체 되살리기 2건 |
| D11 | 검증 | **돌아가는 앱에서 흉내를 실제로 심어 봤다** | 이 변경은 **사람이 보고 속는지**가 요점이라 DOM 단언만으로 판정이 안 선다 | 유닛으로 갈음하기 | 근거: `workspace/sketches/chats-name-slot-2026-09-09.png` 외 3장. 로컬 DB 에 살아 있는 스터디 하나를 「지워진 스터디」로 고치고 다른 하나를 실제로 지운 뒤 확인, 그 뒤 되돌렸다 |
| D12 | 시각 | **브라우저 탭 제목은 못 막았고, 못 막았다고 적는다** | 글자밖에 없는 자리라 모양·색·굵기를 실을 데가 없다. 「막았다」고 적으면 다음 사람이 그 자리를 안 본다 | 탭 제목에서 스터디 이름을 빼기 — 여러 방을 열어 둔 사람이 탭을 못 가른다 | design-rules 「안 풀린 자리 — 브라우저 탭 제목」 |
| D13 | 시각 | **빵부스러기의 링크 글자를 잉크로 올린다** | 그 줄은 통째로 약한 잉크이고 밑줄은 hover 에서만 들어온다. 정지 상태에서 「지워진 스터디」라는 이름과 진짜 지워진 스터디가 픽셀로 같았고, 터치 기기에는 hover 가 아예 없다. **한 화면 안에서 규칙이 적용됐다 안 됐다 하고 있었다** | ① 밑줄을 정지 상태에 켜기 — 새 값이 생긴다. 이 절이 이미 정한 대비(이름=잉크 / 제품=약한 잉크)를 쓰면 값을 안 만든다 ② 그대로 두고 「안 풀린 자리」로 등재 — 고치는 값이 이미 있는데 안 고칠 이유가 없다 | 2026-09-09 ui-reviewer · security-reviewer · code-reviewer(셋 다 같은 자리를 짚었다) · 실측(브라우저 computed style): 흉내는 잉크 / 진짜는 약한 잉크로 나왔다 |
| D14 | 시각 | **말풍선마다 방향을 낭독기에 말한다**(「내가 보낸 메시지.」/「받은 메시지.」) | 눈으로는 자리와 채움이 말하지만 둘 다 소리에 안 실린다. 전에는 남의 말풍선에 항상 이름 줄이 있어서 그 유무가 소리로 된 신호였고, **이번 변경이 그것을 없앴다** — 되돌리는 것이지 새로 넣는 것이 아니다 | 그대로 두기 — 내 말풍선과 이름 모르는 남의 말풍선이 소리로 똑같아진다. 실시간으로 오는 남의 메시지는 **전부** 그 상태라 드문 갈래가 아니다 | 2026-09-09 ui-reviewer · security-reviewer. 앞 절의 「소리로 들리는 경계 단어는 넣지 않는다」와 다른 판단인 이유는 design-rules 에 적었다 — 그 결정이 거부한 것은 시작만 표시되는 반쪽이고, 이 한 마디는 말풍선 하나에 대해 답이 완결된다 |
| D15 | 구현 | **`readChatRoom` 을 판독기 인자 없는 시그니처로 배럴에 내보낸다**(`api/public.ts`) | `entities/study` 가 같은 자리에 같은 규약과 이유를 이미 적어 뒀는데 이 슬라이스만 원본을 내보내고 있었다. 이 슬라이스는 특히 그렇다 — 방과 메시지가 보이는가를 **전부** 접근 정책이 판정하고 코드에는 그 판정이 한 줄도 없다 | 그대로 두기 | 2026-09-09 code-reviewer · `src/entities/study/index.ts:6-9` |
| D16 | 구현 | **메시지 질의의 오류를 본다** | 안 보면 `rows` 가 null → `messages: []` → 화면이 「아직 대화가 없습니다」를 그린다. **대화가 있는 방인데 없다고 말한다.** 같은 파일이 방 질의에는 이미 던지고 있었고 그 이유까지 적혀 있었다 | 그대로 두기 | 2026-09-09 code-reviewer · 변이 6 |
| D17 | 구현 | **보낸 사람 임베드의 모양을 본다** | `m.sender?.username ?? null` 이 세 경우를 뭉갠다 — 프로필 없음(정상) · PostgREST 가 배열로 풀어 줌 · select 키가 바뀜. 뒤 둘에서는 **모든 이름 줄이 조용히 사라지고** 화면이 그것을 「아직 이름을 모른다」로 그린다. 새로고침해도 안 채워지는데 곧 채워질 것처럼 보인다 | null 로 접기 — `toChatStudy` 가 스터디 임베드에 대해 막으려던 것과 같은 실패다 | 2026-09-09 code-reviewer · 변이 7 |
| D18 | 구현 | **오류 문구에 데이터베이스 메시지를 안 담는다**(`throwDbError`·`throwShapeError`) | `error.tsx` 가 `error.message` 를 그리면 화면에 나가고, 개발 오버레이에는 지금도 나온다. 그 짝이 이미 있는데 이 파일만 옛 방식이었다 | 그대로 두기 | 2026-09-09 security-reviewer · `shared/lib/db-error.ts` · 변이 8 |
| D19 | 검증 | **페이지가 컴포넌트를 지나는지 붙드는 검사를 쓴다** | 모양 검사는 컴포넌트를 직접 부르므로, 페이지가 컴포넌트를 지우고 문자열을 다시 박는 diff 는 전부 초록불이었다. **서버 컴포넌트는 그냥 async 함수라 렌더 없이 부를 수 있다** — 자식은 안 불리므로 이웃 셋만 가로채면 된다 | 소스를 정규식으로 훑기 — 함수를 한 번도 안 부르므로 검사가 아니다 | 2026-09-09 code-reviewer · `.claude/rules/tdd.md` · 변이 10·11 |
| D20 | 검증 | **탭 제목이 두 경우에 같다는 것을 검사로 고정한다** | 「못 갈랐다」가 문서에만 있으면, 나중에 누가 그 자리를 고쳐도 문서는 계속 못 갈랐다고 말한다. 단언이 깨지는 날이 문서를 고칠 날이다 | 문서에만 적기 | `src/app/chats/[id]/page.test.tsx` |
| D21 | 구현 | **메시지도 없고 스터디도 안 보이면 메타 줄을 안 그린다** | 빈 `p` 만 남아 뜻 없는 위 여백이 생겼다. 이름 줄에 한 판단과 같은 모양이다 | 그대로 두기 | 2026-09-09 ui-reviewer |
| D22 | 구현 | **`.cardTop` 에 최소 높이를 준다** | 칩(31px)이 있는 카드와 없는 카드의 줄 높이가 달라 **같은 행의 두 카드가 18px 어긋났다.** 알림 패널이 「읽은 줄도 투명한 띠 자리를 남긴다」로 이미 한 번 판정한 것과 같은 현상이다 | `.gone` 에 투명 테두리·세로 여백 주기 — 이 절이 "배경·테두리·안쪽 여백이 전부 없다"고 못 박았다 | 2026-09-09 ui-reviewer · 실측: `workspace/sketches/chats-final-2026-09-09.png` 에서 아래 두 카드의 문장 시작점이 같아졌다 |
| D23 | 시각 | **소리 경로에는 이 방어가 없다고 적는다** | 칩·글꼴·색은 낭독기에 안 들린다. 지금 소리로 갈라 주는 것은 「멤버 N명」의 유무인데 그건 부수 효과라 그 줄을 손대면 조용히 사라진다. 「칩으로 닫혔다」로 읽히면 다음 사람이 이 자리를 안 본다 | 안 적고 두기 | 2026-09-09 ui-reviewer · security-reviewer |
