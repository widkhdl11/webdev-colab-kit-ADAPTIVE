# 결정 로그 — study-mate-20260905-3

사이클: 리뷰가 사람에게 올린 보류 셋(P3·P4·P5)에 답을 받고 반영 → `review` 재사인오프.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 시각 기준 | 형광펜 허용 목록에 「안 읽음 개수」를 더한다 | 승인된 시안 3장이 이미 코랄 알약으로 그리고 있었고, 안 읽음은 카테고리·모집 상태와 같은 결의 상태 표시다. 규칙이 현실을 못 따라가면 같은 예외가 계속 쌓인다 — 채팅방 목록 배지에서 한 번 더 나왔다 | 시안 3장을 고쳐 규칙을 좁게 유지 — 승인된 시안과 구현 넷을 같이 고치고도, 다음에 같은 자리가 또 온다 | HUMAN(P3) |
| D2 | 시각 기준 | 본문 건너뛰기 링크의 옐로 배경은 뺀다(종이로) | 같은 결정의 반대쪽이다. 건너뛰기 링크는 상태 표시가 아니라 접근성 장치의 배경이라, 여기까지 넓히면 형광펜이 뜻을 잃는다. **대비는 판단 근거가 아니었다** — 잉크/옐로는 12.46:1 로 원래 미달이 아니었고, 종이로 바꿔 15.46:1 이 된 것은 덤이다 | 둘 다 예외로 인정 — 허용 목록이 두 종류가 되어 다음 화면에서 다시 묻게 된다 | HUMAN(P3) |
| D3 | 모집 상태 | 마감일은 모집 상태(INV-P6)에 넣지 않는다. 화면 문구를 고친다 | 지금 모델은 호스트가 직접 닫는 것이고(`closed_at`), 마감일은 호스트가 적어 둔 목표다. "마감일이 지났지만 아직 모집 중"이 제품으로 맞는 말이다 | INV-P6 개정 — 위험 표면(참여·정원) 스펙 개정 + 테스트 + 변이 재확인이 따라오고, 신청이 마감일로 거부되는 경로가 새로 생긴다 | HUMAN(P4) |
| D4 | 목록 정렬 | 「마감 임박순」을 **구간 → 날짜 → id** 세 키로. 구간은 0008 의 계산 컬럼(0 남았다 · 1 기한 없음 · 2 지났다) | D3 을 고르면 지난 마감일이 목록에 그대로 남는데, 날짜 오름차순 하나뿐이면 **가장 오래전에 지난 마감일이 1쪽 맨 위**에 온다. 화면 목록 문서는 "그 날짜가 가까운 순서"라고 정했고 지난 날짜는 오늘에서 먼 쪽이다 | 지난 것을 목록에서 빼기 — 모집 중인 스터디가 목록에서 사라진다(D3 과 어긋난다) | IA.md · HUMAN(P4) |
| D5 | 목록 정렬 | 「지났다」 구간 안의 순서는 날짜 오름차순 그대로 둔다 | PostgREST 는 구간마다 방향을 뒤집지 못하고, 그 구간은 목록의 꼬리다 | 방향을 뒤집으려 정렬 키를 하나 더 만들기 — 꼬리의 순서를 위해 계산 컬럼이 하나 더 는다 | NEW |
| D6 | 목록 정렬 | 이 규칙을 스펙이 아니라 `docs/IA.md` 에 두고 통합 검사로 붙든다 | 구간은 `current_date` 를 보는 시변 파생값이라 형식상 "구현 전 스펙" 표면에 걸린다. 그러나 D3 으로 이 값은 계약에서 빠졌고, 틀렸을 때 나는 사고는 "목록 순서가 이상하다"뿐이다 | `/spec` 을 먼저 돌리기 — 계약이 아닌 것에 불변식을 만들면 다음 사람이 그것을 계약으로 읽는다 | 사용자 동의(구현 전 선언) |
| D7 | 모집 상태 | INV-P6 의 공식에 「그리고 지워지지 않았다」를 더한다 | 계산 컬럼은 처음부터 지워진 스터디를 걸렀는데 계약에는 그 조건이 없었다. 그래서 그 가지를 붙드는 검사가 INV-P9(누가 묻든 같은 답)의 이름표를 달고 있었고, 성질이 다른 검사에 같은 이름표가 붙으면 빨간불이 무엇을 잡았는지 못 가른다 | INV-Z10 에 한 줄 더하기 — 모집 상태의 공식이 두 스펙에 나뉘어, INV-P6 만 읽는 사람이 이 조건을 못 본다 | HUMAN(P5) |
| D8 | 검사 | 테스트 이름과 변이 이름을 `p6-computed-drops-deleted` · INV-P6 으로 옮긴다 | 이름표가 판정의 근거다. D7 로 이 가지의 주인이 INV-P6 이 됐다 | 이름은 두고 스펙만 고치기 — 도구가 잡은 것과 계약이 어긋난 채로 남는다 | D7 |
| D9 | 변이 도구 | 파일 변이의 복구가 끝난 뒤 **원래 문장 1건 · 변이 문장 0건**을 세서 확인한다 | 복구는 `replace` 를 **전부** `find` 로 바꾼다. `replace` 가 깨끗한 소스에 원래 있던 문장이면 손대지 않은 줄까지 바뀌고, 도구는 「복구 완료」를 찍는다. 이번에 실제로 났다 — 정렬 키 둘 중 날짜 키가 구간 키로 덮여 기준선이 빨간불이 됐고, 그 화면에는 왜인지가 없었다 | 있음/없음만 보기(옛 검사) — 이 상태가 그대로 통과한다. 실제로 통과했다 | NEW |

## 리뷰어 넷의 지적을 반영하며 (같은 사이클, 2차)

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D10 | 목록 정렬 | **D4·D5 를 뒤집는다.** 구간 컬럼 + 날짜 컬럼 두 키를 **순위 정수 하나**로 합쳤다(0008 `study_deadline_rank`) | D5 에 적은 근거("구간마다 방향을 뒤집을 방법이 없다")는 **키를 둘로 나눴기 때문에** 생긴 제약이었다. 하나로 합치면 덩어리 순서도 덩어리 안의 방향도 이 함수가 정한다 — 지난 마감일이 **최근 것부터**가 되고, 행마다 도는 스터디 조회가 둘에서 하나로 준다 | 두 키 유지 — 6년 전에 끝난 모집이 지난주에 끝난 모집보다 위에 남는다. 그 근거가 실제 제약이 아니었으므로 유지할 이유가 없다 | HUMAN(code-reviewer 8번) |
| D11 | 목록 정렬 | 구간 경계를 `least` 로 잘라 겹치지 않게 만든다 | 세 띠의 값 범위가 상수끼리의 약속으로만 안 겹치면, 마감일이 아주 먼 스터디 하나가 「기한 없음」 띠로 넘어간다. 자르면 그 약속이 코드가 된다 | 상수만 크게 잡기 — 언제 깨지는지 아무도 안 본다 | NEW |
| D12 | 화면 | 마감일 문구가 `recruiting` 을 받는다 | 날짜만 보면 화면이 스스로와 부딪힌다. 옛 문구는 「모집중」과 부딪혔고, 그것을 "아직 신청받는다"로만 바꾸면 이번엔 호스트가 닫은 스터디에서 「마감」 배지와 부딪힌다 — 부딪히는 화면 수는 그대로다 | 문구만 바꾸기 — 모순의 방향만 뒤집는다 | HUMAN(ui-reviewer H2) · UI 레이어 규칙(판단은 위에서 받고 조합·표시만) |
| D13 | 화면 | 「호스트가 닫기 전까지 신청받습니다」라는 **설명**은 메타 행이 아니라 우측 패널의 안내 문장으로 | 메타 행은 값을 적는 자리고, 안내 문장은 이미 신청 조건을 풀어 주는 자리이며 `recruiting` 으로 갈라져 있다. 그리고 그 문구는 좁은 화면에서 메타 행만 세 줄로 만든다 | 메타 행에 전부 넣기 — 리듬이 깨지고, 마감된 스터디에서도 그 문장이 나온다 | HUMAN(ui-reviewer H2) |
| D14 | 시각 기준 | 안 읽음의 **형태**를 배정했다 — 점 없는 숫자 알약 | 허용 목록이 셋이 됐는데 「색이 겹치면 모양으로 가른다」는 아직 둘만 세고 있었다. 코랄은 이제 세 뜻을 지고, 카드 우상단에서 「마감」 배지와 자리가 겹친다 — 갈라 주는 것이 무엇인지 문서에 없으면 다음 화면에서 다시 묻는다 | 색만 허용하고 모양은 안 정하기 — 백로그의 「같은 자리 같은 색」 항목이 위반인지 아닌지 판정할 근거가 없다 | HUMAN(ui-reviewer M1) |
| D15 | 검사 | 준비물의 날짜를 **데이터베이스가 보는 오늘 기준 상대값**으로, id 를 **기대 순서의 역순**으로 손수 준다 | 구간을 가르는 것이 `current_date` 인데 준비물이 절대 날짜면 2030-01-01 에 코드와 무관하게 빨간불이 난다. 그리고 정렬 키가 죽으면 남는 것이 uuid 순이라, 기대 순서가 **우연히 맞을 확률이 4분의 1**이었다(실제로 그 확률로 「아무도 안 붙들고 있다」가 나온다) | 절대 날짜·자동 uuid — 앞은 시한폭탄이고 뒤는 판정을 운에 맡긴다 | HUMAN(code-reviewer 1·6 · test-auditor F1·F7) |
| D16 | 검사 | 오늘 마감인 준비물을 넣고 `deadline-rank-off-by-one` 변이를 등록한다 | 준비물에 경계가 없으면 `>=` 를 `>` 로 밀어도 전부 초록불이다 | 경계 없이 두기 — 「오늘까지」가 목록 꼬리로 가는 것을 아무도 못 본다 | HUMAN(code-reviewer 5) |
| D17 | 검사 | INV-P6 검사를 **자기 describe 로** 뺀다 | 판정기는 실패한 테스트의 **전체 이름**(describe + it)에서 이름표를 찾는다. INV-P9 describe 안에 두면 INV-P9 변이가 이 검사만 깨뜨려도 「INV-P9 가 잡혔다」로 보고된다 — 이름표를 옮긴 것이 사람 눈에만 보였다 | it 제목만 고치기 — 기계가 읽는 이름은 그대로다 | HUMAN(test-auditor F3) |
| D18 | 검사 | 같은 공식의 **두 벌**(계산 컬럼 · 함수 쌍)을 각각 누른다 (`p6-rpc-drops-deleted`) | 계산 컬럼 쪽만 붙들려 있어서 `study_is_recruiting` 의 `deleted_at is null` 을 지워도 전부 초록불이었다 — 방금 계약에 올린 조건이 한쪽에서 안 붙들려 있었다 | 한 벌만 누르기 — 위 이유 | HUMAN(test-auditor F2) |
| D19 | 검사 | 순서를 안 보는 검사에서 「마감 임박순」이라는 이름표를 뺀다 | 태그가 describe 제목에 있어 파일 안 검사 넷을 전부 물었다. 그중 하나는 순서를 아예 안 보는데, 그 검사의 빨간불이 순서 변이의 판정으로 읽힌다 | 태그 그대로 — 「무엇이 잡혔나」가 틀리게 보고된다 | HUMAN(test-auditor F5) |
| D20 | 검사 | `toSort` 를 `post-order.ts` 로 옮겨 검사가 부를 수 있게 한다 | 원래 자리(`read-posts.ts`)는 요청 맥락에 붙은 서버 클라이언트를 모듈 수준에서 끌어와 통합 검사가 부를 수 없었다. 그래서 화이트리스트가 **아무 검사에도 안 붙들려** 있었고 변이가 실제로 빠져나갔다 | 백로그로 미루기 — 이번 diff 가 그 파일을 이미 만지고 있고, 뚫리면 로그인 없이 누구나 낼 수 있는 500 이다 | HUMAN(security-reviewer Low-2) |
| D21 | 변이 도구 | 파일 복구를 **「전부 되돌리고 → 전부 검사」 두 바퀴**로 | 한 바퀴에서 자기 것을 되돌리고 바로 자기 것을 검사하면, 뒤에 있는 변이가 아직 심긴 상태로 앞 변이의 개수가 세어진다. 같은 파일의 줄이 겹치면 멀쩡한 복구가 「복구 실패」로 죽는다 | 한 바퀴 유지 — 지금은 우연히 안 겹칠 뿐이다 | HUMAN(code-reviewer 2) |
| D22 | 변이 도구 | 같은 개수 검사를 **심기 직전에도** 한다 | D9 의 검사는 복구 시점에야 걸리고, 그때 안내는 "git checkout" 이다 — 작업 트리가 이미 망가진 뒤다. 심기 전에 보면 한 글자도 안 쓰고 거절된다 | 복구에서만 잡기 — 사고가 난 뒤에 잡는다 | HUMAN(code-reviewer 3) |
| D23 | 변이 도구 | 「아무것도 안 바꿨다」 판정에서 파일 변이를 더 이상 빼지 않는다 | 그 제외의 근거(지문이 데이터베이스만 본다)가 지난 사이클의 D22 로 사실이 아니게 됐다. 빼 둔 채로는 사실상 no-op 인 파일 변이가 「초록불 — 아무도 안 붙들고 있다」로 보고된다 | 그대로 두기 — 위 이유 | HUMAN(code-reviewer 4) |
| D24 | 문서 | `0007` 의 인라인 근거를 지운다 | Postgres 는 `prosecdef` 뿐 아니라 `proconfig` 가 비어 있어도 아니면 인라인을 포기한다. 두 함수 다 `set search_path = ''` 를 달고 있어, definer 를 피해 얻으려던 인라인은 처음부터 없었다. 결론(invoker 로 둔다)은 안 바뀐다 | 근거를 남겨 두기 — 얻어지지 않는 이득이 다음 판단의 근거가 된다 | HUMAN(code-reviewer 7 · security-reviewer Low-1, 독립적으로 같은 지적) |
| D25 | 검사 | 통합 스위트에도 「로컬에만 붙는다」 검사를 건다 | 변이 도구는 `SUPABASE_DB_URL` 이 원격이면 거부하는데, 스위트가 실제로 쓰기를 보내는 곳은 `SUPABASE_URL` + secret 키였다. 둘이 따로 놀면 "변이는 로컬에, 사용자·모집글은 원격에" 만드는 조합이 열린다 | 도구 쪽 검사만 믿기 — 보호가 손으로 부르는 경로를 안 덮는다 | HUMAN(security-reviewer Low-3) |

## 델타 재리뷰의 지적을 반영하며 (같은 사이클, 3차)

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D26 | 마감일 | 마감일에 무한값을 못 넣게 하고(0009 제약), 순위 함수에도 안전 실패 가지를 둔다 | `date` 는 `'infinity'` 를 받는다. 비교는 통과하고 뺄셈은 죽어서(`cannot subtract infinite dates` — 실측), 호스트 하나가 자기 스터디에 무한값을 쓰면 `/posts?sort=deadline` 이 **비로그인 방문자까지 전원 500** 이 된다. 0007 에는 없던 자리이고 **0008 이 날짜 산술을 들여오며 생겼다** | 함수만 고치기 — 무한값이 데이터에 남는다 / 제약만 걸기 — 제약 이전 행이 그대로 500 을 낸다. 둘 다 붙이면 **어느 하나만 적용돼도 500 이 안 난다** | HUMAN(security-reviewer Medium) |
| D27 | 화면 | 마감일 문구를 **네 사분면**으로 짝지었다 (마감일 지났다/안 지났다 × 모집 연다/닫혔다) | H2 를 고칠 때 「지났다」 쪽만 모집 상태를 봤다. 그런데 `recruiting` 은 호스트가 닫았을 때만이 아니라 **정원이 찼을 때도** 거짓이고, 그때 마감일은 대개 미래다 — 「마감」 배지 옆에 「87일 남음」이 그대로 남아 있었다. H2 가 고친 모순의 정확한 거울상이다 | 두 가지만 두기 — 고친 모순이 반대편에 그대로 남는다 | HUMAN(ui-reviewer M-5) |
| D28 | 화면 | 안내 문장을 신청 상태로 가르고, 「마감일이 지났어도」는 **지났을 때만** 붙인다 | 버튼은 여덟 상태를 다 가르는데 안내는 모집 여부 하나로만 갈렸다 — 호스트가 자기 스터디에서 "신청하면 승인을 기다립니다"를 읽었다. 그리고 문장이 하나에서 넷으로 길어지면서 그 어긋남이 4배가 됐다 | 그대로 두기 — 위 이유 | HUMAN(ui-reviewer M-2·M-3) |
| D29 | 화면 | 그 설명을 **로그인하지 않은 방문자에게도** 준다 | 모집글 상세는 로그인 없이 읽는 화면이고, 「지났지만 아직 모집 중」의 이유가 가장 필요한 사람이 처음 보는 사람이다. 설명이 그 사람에게만 없었다 | 로그인 뒤에만 보여주기 — 위 이유 | HUMAN(ui-reviewer M-4) |
| D30 | 시각 기준 | 「참여 중 = 민트 채움」은 **결정하지 않고 보류로 올린다**(P6) | 허용 목록을 셋으로 못박자 원래 흐릿하던 자리가 모순으로 드러났다 — 참여 중도 그 셋 중 어느 것도 아닌데 시안이 이미 민트로 그렸다. **P3 과 구조가 같아 사람이 정할 것이다** | 스스로 넷째로 넣기 / 스스로 잉크로 바꾸기 — 둘 다 승인된 시안을 뒤집는 판단이다 | HUMAN(ui-reviewer M-1) · CLAUDE.md(새 시각 방향은 보류로) |
| D31 | 검사 | INV-P6 의 「마감일은 모집 상태가 아니다」에 검사 셋을 새로 쓴다 (계산 컬럼 · 함수 쌍 · 쓰기 경로) | 그 조항이 스펙·0008 주석·화면 목록 문서 **셋에 적혀 있는데 붙드는 것이 한 곳도 없었다.** 다른 검사의 스터디가 전부 마감일이 비어 있어, 파생값에 마감일을 더해도 70건이 전부 초록불이었다 | 파생값 둘만 붙들기 — 마감일 검사가 **쓰기 경로**에 들어가는 모양이 빠져나간다. 그때 사용자는 「모집중」을 보고 신청했다가 거부당한다 | HUMAN(test-auditor H1) |
| D32 | 검사 | 경계를 **양쪽에서** 민다 — 오늘과 어제를 둘 다 준비물에 넣고 변이도 둘 | 미는 방향이 하나뿐이면 `>= current_date - 1`(어제까지 봐 준다)이 통째로 빠져나간다. 「26/26」이라는 숫자는 **등록된 변이에 대한 값**이라 없는 변이를 못 본다 | 한 방향만 — 위 이유 | HUMAN(test-auditor H2) |
| D33 | 검사 | 「~ 검사를 지워도 되는가」에 걸린 단언을 **경계를 보는 것으로 바꾼다** | 앞 라운드에 넣은 둘째 단언이 첫째 단언에 완전히 포함돼 있었다. 그런데 주석은 그것을 「경계 하나짜리 단언」이라고 선언하고 있었다 — 실제 경계는 비어 있는데 「붙들려 있다」는 표지가 붙은 것이 더 나쁘다 | 지우기 — 경계가 여전히 빈다 | HUMAN(test-auditor M3) |
| D34 | 검사 | INV-P6 검사를 **전부** INV-P9 describe 밖으로 뺀다 | 앞 라운드에 하나만 옮겼는데 같은 이유가 걸리는 검사가 하나 더 있었다. 판정기는 전체 이름(describe + it)에서 이름표를 찾으므로, 그 검사는 이름표 둘을 동시에 달고 있었다 | 하나만 옮기기 — 절반만 고친 것이다 | HUMAN(test-auditor M4) |
| D35 | 검사 | 상한을 자르는 `least` 와 되돌림(`?? SORT_ORDER.latest`)에 각각 변이를 등록한다 | 주석이 「이 장치가 …를 보장한다」고 선언한 것이 검사 밖에 있으면, 그 문장이 초록불을 커버리지로 오해하게 만든다. 되돌림 쪽은 **어휘를 일부러 우회한 값**을 넣어야 그 줄이 처음 돈다 | 백로그로 미루기 — 이번 diff 가 만든 장치들이다 | HUMAN(test-auditor L7 · code-reviewer 1) |
| D36 | 검사 | 목록 검사를 검색어로 좁히고, 고정 id 를 심기 전에 지우고, 자정을 넘었으면 그렇게 말한다 | 셋 다 「빨간불은 나는데 원인이 화면에 안 보이는」 자리다 — 행 상한에 걸려 꼬리가 잘리는 것 · 앞선 실행의 정리 실패로 남은 행 · 준비와 단언 사이에 날짜가 넘어간 것 | 그대로 두기 — 실패 메시지가 순서 diff 뿐이라 원인을 다시 찾아야 한다 | HUMAN(test-auditor L8·L9 · code-reviewer 7) |
| D37 | 검사·도구 | 로컬 강제를 문자열 패턴에서 **URL 파싱**으로 바꾸고, API·데이터베이스 **둘 다**에 건다 | URL 문법에서 호스트를 정하는 것은 마지막 `@` 뒤다. `http://localhost:54321@evil.com` 은 「localhost 로 시작한다」를 통과하고 실제로는 evil.com 에 붙는다 — 그 요청에 secret 키가 실린다. 그리고 앞 라운드는 API 쪽만 막아, 슈퍼유저 직결(`rawClient`)이 그대로 열려 있었다 | 패턴 유지 / 한쪽만 막기 — 위 둘 | HUMAN(security-reviewer Low ×2 · code-reviewer 2) |
| D38 | 변이 도구 | 심기 전 검사에서 **모든** 파일 변이의 replace 충돌을 한 번에 훑고, `found === 0` 일 때 범인을 지목한다 | 그 순간이 트리가 깨끗하다고 방금 확인된 자리라 비용이 0 이고, 한 번도 심어 본 적 없는 변이의 잘못된 정의도 그때 드러난다. 그리고 「변이가 낡았다」는 안내는 **다른 변이가 심긴 채**일 때 틀린 처방이다 — 그대로 따르면 멀쩡한 변이를 망친다 | 심는 변이 하나만 보기 — 위 둘 | HUMAN(code-reviewer 3) |
| D39 | 변이 도구 | 심은 뒤 확인을 **디스크에서 다시 읽어** 한다 | 방금 만든 문자열을 보는 조건은 `found === 1` 을 확인한 이상 언제나 참이다 — 확인처럼 생겼지만 아무것도 확인하지 않고, 쓰기 실패도 못 본다 | 그대로 두기 — 검사처럼 보이는 빈 줄이 남는다 | HUMAN(code-reviewer 4) |
| D40 | 문서 | 「부르는 자리의 화이트리스트」와 「마지막 정렬 키」는 변이를 **등록하지 않고** 백로그로 | 붙들 검사가 없는 변이를 등록하면 「빠져나간 것 1」이 영구히 남아 그 숫자가 뜻을 잃는다. 앞의 것은 서버 컴포넌트라 통합 검사가 부를 수 없고, 뒤의 것은 동점 준비물과 쪽 나눔 단언이 있어야 한다 | 지금 등록하기 — 위 이유 | HUMAN(test-auditor M6·code-reviewer 9, 판단은 이쪽) |
