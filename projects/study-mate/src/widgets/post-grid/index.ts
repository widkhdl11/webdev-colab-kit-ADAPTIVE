export { PostGrid } from "./ui/PostGrid";

// **추천 구역은 여기서 안 내보낸다.** 그것은 모델 제공자를 끌고 오고, 그 모듈은
// 서버 전용 표시가 붙어 있어 클라이언트 문맥에서 불리는 순간 던진다(INV-G1 의 경계).
// 배럴로 내보내면 `PostGrid` 만 쓰려는 곳까지 그 모듈 그래프를 물려받는다 —
// 실제로 검사 하나가 그렇게 죽었다(2026-09-10 code-reviewer 가 미리 짚은 자리).
// 쓰는 쪽은 `./ui/RecommendedSection` 을 경로로 직접 가리킨다.
