/**
 * 초안 응답을 기다리는 상한 (INV-G5 의 손짓을 그대로 가져온 것).
 *
 * **2026-09-11 에 실제 키로 쟀다**: 5회 모두 1.66~1.91초. 12초는 그 최대의 약 6배로,
 * 꼬리 지연을 감안한 여유다.
 *
 * 추천(8초)보다 긴 이유는 **잃는 것이 다르기 때문**이다 — 추천은 늦으면 그 자리가 규칙
 * 순서로 채워지지만, 초안은 사용자가 단추를 누르고 기다리는 일이라 포기하면 아무것도
 * 안 남는다.
 *
 * **처음엔 20초였고 근거가 「초안이 만드는 글자 수가 훨씬 많다」였는데, 실측이 그 근거를
 * 뒤집었다** — 출력 토큰이 303 대 293 으로 추천과 거의 같다.
 */
export const DRAFT_TIMEOUT_MS = 12_000;
