/**
 * 폼에서 알림 id 를 담는 칸의 이름. **한 자리에만 둔다** — 보내는 쪽(패널)과 읽는 쪽(액션)이
 * 각자 문자열을 적으면 한쪽만 고쳐지는 날 읽음 처리와 삭제가 **언제나** 「어느 알림인지 알
 * 수 없습니다」로 떨어지는데, 양쪽 검사가 각자 자기 문자열을 쓰므로 전부 초록불이다
 * (2026-09-08 test-auditor).
 *
 * **서버 전용 import 가 없는 파일에 둔다.** 액션 파일에 두면 브라우저가 이 상수 하나
 * 때문에 `next/cache` 를 끌고 들어가 빌드가 죽는다(bundle 게이트가 잡는 자리).
 */
export const NOTIFICATION_ID_FIELD = "notificationId";
