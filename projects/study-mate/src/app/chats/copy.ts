/**
 * 채팅 화면의 자리 표기 문구.
 *
 * 두 화면(목록·방)이 같은 상태를 그리므로 한자리에 둔다 — 한쪽만 고치면 같은 상황에서
 * 두 화면이 다른 말을 한다. **엔티티에 두지 않는 이유**는 엔티티가 아는 것이
 * 「스터디 행이 안 보인다」까지이고, 그것을 사람에게 뭐라고 부를지는 화면의 판단이라서다.
 */
export const DELETED_STUDY = "지워진 스터디";
