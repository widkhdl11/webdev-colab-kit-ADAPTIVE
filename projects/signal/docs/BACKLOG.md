# BACKLOG — signal (미룬 것)
- [x] **건수 표시가 키워드 스펙 INV-N5 와 반대 방향인지 확인한다** (2026-09-18) — 열린 결정이라 코드를 먼저 손대면 안 된다 · 그 결정이 닫히면 (2026-09-23 닫힘) — 그 결정은 이미 닫혀 있었다: 2026-08-27 사용자 결정으로 INV-N5 를 폐기하지 않고 좁혔다(DECISIONS 「뱃지 숫자는 안 읽은 글 수다」). 전제가 없는 항목이었다
- [x] **상담에서 확정된 「뜨는 중 카드의 뱃지가 먼저」가 승인 규칙으로 안 옮겨졌다** (2026-09-19) — 구현은 design-rules 를 따르고 있어 위반이 아니다 · 뱃지 순서를 다시 정할 때 (2026-09-23 닫힘, 사용자 결정) — **승인된 규칙 문서가 기준이다.** 상담 기록의 「뜨는 중 카드 뱃지 먼저」는 옮기지 않는다. 구현이 이미 그쪽을 따르므로 코드 변경은 없다
- [x] **이슈성·중요도 기준을 진짜로 만든다** (2026-08-31, signal2 에서 옮김) — 지금 문턱 판정은 "몇 건 나왔나"만 보는 임시 기준이다(사용자 판단 2026-08-30: 나중에) · `docs/specs/keywords-and-kinds.md` 를 approved 로 올릴 때(INV-G2·N1·N3). 같은 사건 묶기 + 모델 판정 단계가 함께 필요하다 (2026-09-23 닫힘) — 2026-09-20 에 이미 만들었다(`docs/specs/hot-issue.md`: 질문 셋 → 중요도 0~3, 이슈성으로 순서). 남은 것 둘은 그 스펙이 자기 자리에 적어 두었다 — 같은 사건 묶기(교차 발행처 수, 「안 한다」 절)와 2번·3번 문(프로필에 매달려 있다)
- [x] **옛 글 백필은 안 하기로 했다** (2026-08-30) — 사용자 결정이다 · 뒤집기로 할 때 (2026-09-23 닫힘) — 미룬 일이 아니라 내려진 결정이라 백로그에 있을 이유가 없다. 2026-09-23 「밀린 옛 글은 안 한다 — 기준 시각을 박는다」(DECISIONS)가 같은 결정을 코드로 고정했다
- [x] **한국어 글이 번역 예산을 다 가져간다** (2026-09-21) — 요약과 번역이 한 호출로 묶여 있고 고르는 기준이 점수뿐이라, **원문이 이미 한국어인 글도 같은 칸을 놓고 경쟁한다.** 지금 상태로 선정을 돌리면 10칸 전부가 geeknews·aitimes·aitimes-kr·etnews-ai 로 간다 — 영어 소스는 한 칸도 못 받는다. 최근 3일 영어 제목 53건 중 번역된 것이 0건이고, techcrunch-ai·theverge·arstechnica·deepmind-blog 는 전체 기간 요약 0건이다 · 화면에 영어 제목이 계속 거슬릴 때. 고칠 자리 후보 셋: 번역 칸을 따로 두기 · 선정에서 이미 한국어인 글의 순위를 낮추기 · `ENRICH_BATCH` 올리기(요금이 따라 오른다) (2026-09-23 닫힘) — 원인이던 요약 10칸 상한(`ENRICH_BATCH`)을 2026-09-23 에 없앴다. 칸이 없으니 칸을 놓고 경쟁할 일도 없다. 영어 제목이 계속 번역 안 되면 새 항목으로 연다
- [x] **요약 대기열이 줄지 않고 자란다** (2026-09-21) — 하루 유입이 최근 7일 평균 44건(최대 81건)인데 한 바퀴 요약 예산은 `ENRICH_BATCH = 10` 이다. 대기 1,560건이고 밀린 글은 시간감쇠로 점수가 떨어져 **다시는 상위에 못 든다** — 영구 누락이다. `budgets.ts` 의 주석이 이미 이 실패를 경고하고 있었는데(소스당 상한을 만든 이유) 막은 것은 소스 쏠림이고 예산 총량은 그대로다 · 요금 상한을 정할 때 같이 (2026-09-23 닫힘) — 같은 날 상한을 없애고(그날 글은 그날 다) 기준 시각 이전 글은 안 하기로 해서(사용자 결정) 「대기열이 자란다」는 전제가 둘 다 사라졌다


지금 안 하기로 한 작업. wrap-up 이 쌓고, 세션 시작 브리핑은 미결 건수만 띄운다(0건이면 침묵).
- 미결은 `- [ ]`, 닫히면 `- [x]` 로 바꿔 "닫힘" 으로 옮긴다.
- 형식: `- [ ] **제목** (날짜) — 보류 이유 · 되살릴 조건
- 하네스 개선은 여기가 아니라 docs/references/harness-backlog.md.

## 미결

- [ ] **수집 결과의 실패 제목 목록에 길이 상한이 없다** (2026-09-23, 보안 리뷰 low) — 요약·번역 실패 제목(`failedTitles`, `run-ingest.ts` 의 push 네 곳)이 남의 피드가 준 제목을 자르지 않고 Cron 응답·콘솔에 싣는다. 걸러진 제목과 포기 목록은 `MAX_TITLE_LENGTH`(120자)로 자르는데 이쪽만 빠졌다. 응답은 CRON_SECRET 뒤에 있고 DB 에는 안 남아 급하지 않다. 이번 변경 전부터 있던 것이라 되돌릴 회귀가 아니라 기존 결함이다 · 다음에 수집 리포트를 만질 때. `.slice(0, MAX_TITLE_LENGTH)` 네 줄 + 테스트 하나


- [ ] **본문 추출이 148건 중 8건 실패한다** (2026-09-23) — 건수 상한을 없애 처음으로 많이 돌려 본 결과다. 이유 셋: `HTTP 403`(블룸버그·테크레이더 — 봇 차단으로 보인다) · `추출 결과가 비어 있음` · `응답이 너무 크다(2MB 초과)`. 148건 중 8건이면 5%라 급하지 않지만 **etnews 가 3건**이라 그 매체는 패턴일 수 있다. 실패해도 카드에는 출처 요약글이 남아 화면이 틀리지는 않는다 · 같은 매체가 매일 실패하는 것이 두어 번 더 보이면. 그때 매체별 실패율을 먼저 세고 차단인지 파싱인지 가른다

- [ ] **본문 추출이 요약을 굶긴다 — 단계 순서 문제** (2026-09-23) — 본문 긁기가 요약보다 앞 단계라, 본문이 시간을 다 먹으면 요약이 **한 건도 못 돈다**. 실측: 본문 148건에 104.5초를 쓰고 요약 0건. 이어달리기가 있어 다음 바퀴가 이어받지만, 본문이 밀린 동안은 계속 요약이 0일 수 있다. 밀린 글이 없는 평소에는 안 생긴다 — 기준 시각을 박은 뒤로는 그날 것만 보므로 본문도 20건 안팎이다 · 평소 실행에서도 요약이 0으로 나오면. 그때 단계마다 시간을 나눠 주는 쪽을 검토한다

- [ ] **이어달리기가 5바퀴째에 끊겼다** (2026-09-23) — 04:54 부터 4분 간격으로 다섯 바퀴가 이어지다가 05:09 실행 뒤로 멈췄다. 그 실행의 기록은 `요약 180` 이 남았다고 말하고 있었고 길이 상한(20)에도 안 닿았다 — **할 일이 남았는데 다음 주자가 안 뛴 것**이다. 다음 호출을 보내는 자리는 실패를 삼키도록 돼 있어(그래야 이미 끝낸 수집이 500 으로 안 보인다) 리포트에는 아무것도 안 남는다. 오늘은 밀린 글을 안 하기로 해서 오히려 다행이었지만, **글이 몰린 날 같은 일이 나면 그날 것이 안 끝난다** · 버셀 함수 로그에서 05:09 실행의 나가는 요청을 확인할 때. 서버리스 함수가 응답을 돌려준 뒤 얼어붙어 5초 타임아웃 안에 요청이 못 나갔을 가능성이 첫 번째 후보다 · 2026-09-24 보안 재리뷰 관찰: 보내는 자리는 응답 상태도 안 보므로 401·리다이렉트 거절(이번에 `redirect: "error"` 로 바꿈)로 끝나도 `dispatched: true` 와 구별이 안 된다 — 이 항목을 볼 때 상태 코드를 리포트에 남기는 것까지 같이 본다

- [ ] **결정층 v3.4 요청의 남은 항목 일곱** (2026-09-22) — 9월 19일에 연 「결정층 v3.4 와 남은 일」 요청을 3일 만에 닫으면서, 항목 일곱(결정층 문서 고치기 · 옮겨 적힌 문구 교체 · 등급표 패치 문서 · 줄바꿈 정규화 패치 문서 · 커밋 규약 · signal2 문서 대조 · 커밋과 push)이 닫히지 않은 채로 남았다. 커밋 기록(`cd1e398`·`66cddd3`·`cf359fb`)을 보면 대부분 실제로 끝난 것으로 보이지만 하나씩 대조하지는 않았다 — 끝난 것을 끝났다고 적으려면 확인이 먼저다 · 결정층 문서를 다시 손댈 일이 생길 때 하나씩 대조해 닫거나 다시 연다

- [ ] **요금 단가의 두 갈래가 근거를 잃었다** (2026-09-22) — `estimate-cost.ts` 는 2026-08-31 까지를 프로모션가($2/$10), 그 뒤를 정가($3/$15)로 갈라 계산한다. 그런데 공식 요금표의 `claude-sonnet-5` 단가가 $2/$10 이라, **정가 쪽에 적혀 있던 값은 이전 세대(Sonnet 4.6)의 단가였다.** 값은 정정했지만 이제 두 갈래가 같은 값이라 갈라 둘 이유가 없고, 화면의 「프로모션가 기준 / 정가 기준」 라벨도 뜻이 없다. 옛 실행의 금액이 실제로 얼마였는지는 청구서로만 확인된다 · 실제 청구서와 대조할 일이 생길 때. 그때 두 갈래를 하나로 접을지 정한다

- [ ] **핫이슈 판정이 10% 잘려 죽는다** (2026-09-22) — 배포본 실측에서 120건 중 12건이 「응답을 읽지 못함(잘렸거나 형식이 깨짐)」이었다. `HOT_ISSUE_MAX_TOKENS` 가 300 이라, 주제 판정이 200 에서 22% 죽던 것(같은 날 400 으로 올림)과 같은 모양으로 보인다. 다만 아직 `stop_reason` 을 직접 안 봤다 — 추정이다 · 다음 실행에서 실패 건수가 계속 10% 근처면 `stop_reason` 부터 확인하고 상한을 올린다 **(2026-09-23 관찰: 34건 중 2건 실패로 6%. 9-22 에는 46건 중 8건으로 17% 였다 — 건수가 적은 날에도 계속 나오므로 상한 문제가 맞는 쪽으로 보인다)**
- [ ] **주제 판정이 AI 기사를 걸러낸다** (2026-09-22) — 배포본 실측에서 걸러진 57건에 「카카오, 독립형 AI 서비스 카나나 종료」·「미 재무장관: 허깅페이스 해킹 책임은 오픈AI 경영진에」처럼 명백히 주제 안인 글이 섞여 있었다. 지시문 문제로 보인다. 지금은 오판이 빠지는 쪽(덜 보여 주는 쪽)이라 화면이 틀리지는 않는다 · 걸러진 제목을 며칠 더 모아 경향이 확인되면 지시문을 손본다

- [ ] **요금 상한 판정이 화면에 안 나온다** (2026-09-22) — 실행 기록에 `cost` 칸을 저장하기 시작했지만(0010 마이그레이션) 개발용 대시보드가 그 칸을 안 읽는다. 상한에 걸린 날의 화면은 「요약 0건·키워드 0건」으로만 보여서, 글이 없던 날과 구별되지 않는다(리포트 JSON 에는 남는다) · 상한에 실제로 한 번 걸리거나 대시보드를 다시 손댈 때
- [ ] **이어달리기 번호가 실행 기록에 안 남는다** (2026-09-22) — 한 예약 실행이 몇 번 이어졌는지를 저장된 기록만 보고는 못 센다. 지금은 체인이 짧아 로그로 따라갈 수 있다 · 이어달리기가 상한(20)에 자주 닿기 시작할 때

- [ ] **뱃지 넘침 가림에 그 상태를 지나는 검사가 없다** (2026-09-19) — jsdom 에서 사각형을 흉내내야 해서 한 줄로 안 끝난다 · 뱃지 줄을 다시 고칠 때(이번 측정 버그가 이 검사가 없어서 안 잡혔다)
- [ ] **scripts/keywords.ts 가 파이프라인의 또 다른 구현이다** (2026-08-31, 2026-09-19 등급 올림) — 상수를 리터럴로 다시 적고 본문 대체 경로가 없어 프로덕션 동작을 재현하지 못한다 · 뱃지 문턱·앵커 상한을 다시 잴 때
- [ ] **실패 이유 상한이 두 벌이다** (2026-09-19) — run-keywords 와 run-ingest 에 같은 함수가 따로 있고 값이 budgets 와 중복이다 · 리포트 상한을 바꿀 때
- [ ] **근거 고르기가 순수 함수와 질의 두 곳에 나뉘어 있다** (2026-09-19) — 지금 출력은 맞지만 질의 조건을 뒤집으면 전 스위트가 green 인 채로 근거가 바뀐다 · 근거 규칙을 손댈 때
- [ ] **본문에서 뽑은 근거에 「출처 요약글」 라벨이 붙는다** (2026-09-19) — 요약글이 없는 소스에서만 틀리고 모델 출력에 미치는 영향이 아직 관측되지 않았다 · 요약 품질을 다시 볼 때
- [ ] **창 길이 3일이 서로 모르는 두 자리의 기본값이다** (2026-09-19) — 지금은 우연히 같아서 동작한다 · 창 길이를 바꾸려 할 때
- [ ] **시크릿 비교가 상수시간이 아니다** (2026-09-19) — INV-IA3 은 「정확히 같은가」만 요구해 스펙 위반이 아니다 · 로그인이 생기거나 시크릿이 여럿이 될 때
- [ ] **시크릿 경계 검사 목록에 CRON_SECRET 이 없다** (2026-09-19) — 그 파일이 클라이언트 후보에서 빠져 오늘 새는 곳은 없다 · 수집 경로가 app/ 밖으로 나갈 때
- [ ] **INV-DA3 통합 단언이 테이블이 비면 공허하게 통과한다** (2026-09-19) — 통합 테스트는 기본 실행에 없어 매 턴 돌지 않는다 · 통합 테스트를 다시 돌릴 때
- [ ] **잘못된 형식의 id 가 404 대신 500** (2026-09-19) — 보안 문제가 아니라 오류 처리 건이다 · 상세 화면을 손댈 때
- [ ] **긴 키워드 하나가 가로 스크롤을 만들 수 있다** (2026-09-19) — 지금 관측된 42종이 전부 짧아 실제로는 안 터진다 · 긴 키워드가 한 번이라도 관측되면
- [ ] **알약 높이가 height 로 고정돼 글자만 키우면 둘째 줄이 잘린다** (2026-09-19) — 브라우저 줌에서는 안 깨지고 글자만 키우는 경로에서만 난다 · 접근성 실측을 돌릴 때
- [ ] **주제 판정이 크레딧 부족으로 실패한 39건** (2026-08-30) — 이미 판정한 것으로 표시돼 재판정되지 않는다 · 재판정 경로를 만들 때
- [ ] **프롬프트 캐싱 미적용** (2026-08-30) — 지금 비용이 문제가 되는 수준이 아니다 · 수집량이 늘 때
- [ ] **본문 추출 실패를 되풀이하지 않게 물러서는 자리가 없다** (2026-08-30) — 실패가 매 주기 반복되지만 다른 소스를 막지는 않는다 · 추출 실패율이 관측될 때
- [ ] **프로필 내용이 미정이다** (2026-08-31, signal2 에서 옮김) — PRODUCT 에 범위는 들어왔고 내용이 비어 있다. 물어본 네 가지가 미답 · 소식 배치(INV-N1)의 문 2·3번을 구현하려 할 때. 그 둘이 프로필에 매달려 있다 **(2026-09-23 사용자: 아직 미룬다)**
- [ ] **요약에 허용할 표시 범위를 안 정했다** (2026-08-31, signal2 에서 옮김) — 요약 안에 목록·표를 허용하기로 했는데 무엇까지인지 안 정했다 · 상세 화면을 다시 손대기 전. 요약은 신뢰 경계 밖이라 `docs/specs/content-safety.md` 를 다시 열어야 한다
- [ ] **스킬·툴 소스를 어디서 가져올지** (2026-08-31, signal2 에서 옮김) — 지금 소스는 전부 뉴스·블로그라 탭을 만들어도 채울 것이 없다. 분석·관점 글이 `뉴스` 인지, 종류를 셋으로 늘릴지도 미정 · 스킬·툴 탭 구현 착수 시 **(2026-09-23 사용자: 나중에 직접 추가한다)**
- [ ] **소스가 AI 쪽으로 편중돼 있다** (2026-08-31, signal2 에서 옮김) — 백엔드·프론트엔드 소스는 사용자가 구하기로 했다 · 사용자가 소스 목록을 주면 **(2026-09-23 사용자: 패스 — 사용자가 먼저 꺼낼 때까지 안 묻는다)**
- [ ] **뱃지 집계 창에 위쪽 상한이 없다** (2026-09-02 제기, 2026-09-20 이 구현에서 확인) — `entities/article/api/queries.ts` 가 `published_at >= 창시작` 만 건다. 발행 시각이 미래로 온 글(RSS 타임존 오류는 흔하다)이 미래 날짜 그룹을 만들고, 첫 화면이 하루 단위라 **그 잘못된 하루 하나로 채워진다.** signal2 에서는 더미가 시각을 눌러 이 경로를 못 밟았지만 여기는 실데이터다 · 창을 다시 손댈 때. 조회에 위쪽 상한을 더하고 적재 단계의 미래 시각 처리를 `ingestion-ranking.md` 에 적는다
- [ ] **Pretendard 를 CDN 으로 받는다** (2026-09-01, signal2 에서 옮김 — 이 구현에도 있다) — `src/app/globals.css` 첫 줄이 jsdelivr 링크다. CDN 이 죽거나 오프라인이면 시스템 폰트로 떨어져 타이포 실측값(자간·줄높이)이 어긋난다 · 폰트가 안 떠서 화면이 달라 보이는 일이 실제로 나면. 폰트 파일을 레포에 넣으면 용량이 늘고 `next/font` 배선이 따로 필요하다
- [ ] **favicon 이 없다** (2026-09-01, signal2 에서 옮김 — 이 구현에도 `public/` 이 없다) — `/favicon.ico` 가 404 라 콘솔에 에러 한 줄이 남는다. 기능에는 영향이 없지만 "콘솔 오류 0"을 확인할 때마다 이 한 줄을 예외로 세야 한다 · 배포 화면을 다시 손댈 때
- [ ] **날짜 묶기가 날짜 키를 글마다 다시 계산한다** (2026-09-02, signal2 에서 옮김 — 이 구현에도 있다) — `entities/article/lib/query.ts` 의 `orderForFeed` 가 `groupByDay` 를 두 번 돈다(펼칠 때 한 번, 자른 뒤 또 한 번) · 목록이 커져 정렬 토글·「더 보기」의 재계산이 눈에 띌 때
- [ ] **개발자용 파이프라인 대시보드를 배포하려면 관리자 로그인이 먼저다** (2026-08-31, signal2 에서 옮김) — 지금은 로컬 전용(`NODE_ENV=production` 이면 404)이고 사용자 결정으로 로그인 없이 배포하지 않는다 · 배포된 사이트에서 이 화면을 쓰고 싶어질 때. 그때는 auth 표면이라 /spec 이 먼저다

- [ ] **수집 목적지 판정이 Teredo 표기를 안 푼다** (2026-09-20) — `2001:0::/32` 안에 IPv4 가 섞여 들어가는데 지금 판정은 그 표기를 안 본다. 그 주소로 실제 트래픽이 흐르는 경로가 배포 환경에 있어야 위험해진다 · 배포 환경이 IPv6 터널을 쓰게 될 때
- [ ] **되돌려보내기 주소가 여러 개 오면 합친 값을 푼다** (2026-09-20) — `Location` 헤더가 중복이면 `", "` 로 이어진 문자열이 되고 실제 서버는 첫 값을 쓴다. 합친 값도 다시 판정을 받으므로 차단을 우회하지는 못한다 · 되돌려보내기 처리를 다시 손댈 때
- [ ] **브라우저가 막는 위험 포트를 우리도 막을지** (2026-09-20) — 지금은 포트를 제한하지 않는다. 멀쩡한 발행처를 막지 않으려는 판단이고, 값싼 보강으로 브라우저가 이미 막는 목록 정도는 거부할 수 있다 · 수집 대상이 늘어 공인 주소의 낯선 포트를 실제로 두드리게 될 때
- [ ] **한 소스가 핫이슈 열 칸을 거의 다 먹는다** (2026-09-21) — 첫 실행에서 뽑힌 10건 중 5건이 같은 매체의 같은 기획 시리즈였다. 중요도 1 에 23건이 몰려서 동률이 되고, 그 안의 순서를 이슈성(최근 × 소스 weight)이 정하는데 한 매체가 같은 시각에 여러 건을 올리면 그 줄을 통째로 차지한다. 요약 예산에 이미 같은 장치가 있다(소스당 상한) · 표본 검토에서 이 쏠림이 계속 보이면
- [ ] **뱃지 키워드가 3주째 한 건도 안 붙고 있다** (2026-09-21 제기 「두 바퀴 연속」 → 2026-09-22 실측으로 정정) — 키워드가 붙은 글은 1,818건 중 160건이고 그 `keywords_at` 은 전부 2026-08-30·08-31 의 두 바퀴다(각 80건 = `KEYWORD_BATCH`). **그 뒤 3주간 0건이고, 뱃지 창 3일 안의 136건은 전부 키워드가 없다** — 뱃지 줄이 화면에서 통째로 비어 있다는 뜻이다. 핫이슈 판정(2026-09-20 투입)이 원인이 아니다: 멈춘 날이 그보다 3주 앞선다. 키워드는 파이프라인의 **맨 마지막** 단계이고 최근 실행이 244초로 `INGEST_BUDGET_MS`(240초)에 닿아 `budget.exhausted()` 로 통째로 건너뛰어진다 — 예산이 떨어지면 항상 이 단계부터 없어진다 · 한 바퀴 예산 배분을 다시 정할 때(요약 대기열·번역 칸 문제와 같은 뿌리다). 계측 명령: `npm run badges`
- [ ] **명백한 기업·정책 보도에 `뉴스` 가 안 붙는다** (2026-09-21) — 판정 받은 117건 중 7건이 그랬다(`SKT, 선한 AI로…`·`LG CNS, 수학최적화 경진대회 성료`·`대기업 전문연구요원 'AI'로 부활` 등). 화면 배치는 2026-09-21 부터 종류를 안 보므로 **지금은 아무 증상이 없다** — 스킬·툴에 툴이 덜 잡히는 쪽만 남는다 · 뱃지나 필터가 종류를 읽기 시작할 때. 그때는 `KIND_RULE` 지시문부터 본다
- [ ] **판정 근거를 못 받은 옛 117건** (2026-09-21) — 0009 적용 전에 판정된 글이라 `hot_issue_answers` 가 비어 있다. 후보 조회가 `hot_issue_at is null` 이라 다시 안 묻는다. 표본 검토는 새로 쌓이는 것으로 충분하다고 봤다 · 옛 글까지 되짚어야 할 이유가 생기면(재판정하면 요금이 다시 나간다)
- [ ] **행별 update 120번** (2026-09-21 리뷰 1순위) — `saveHotIssue` 가 행마다 update 를 따로 쏜다. 중요도·근거 조합이 여덟 가지라 묶으면 여덟 번이면 된다 · 한 바퀴 시간이 예산에 닿을 때
- [ ] **스키마 검사에 「위반을 심어 잡히는지」 항목이 없다** (2026-09-21 리뷰) — `tests/hot-issue-schema.test.ts` 가 단어만 본다. 다른 테이블에 붙이거나 타입을 바꿔도 통과한다 · 마이그레이션을 다시 손댈 때
- [ ] **`isTrending` 이 읽는 화면 없이 계산된다** (2026-09-21 리뷰) — 뱃지가 판정으로 바뀌면서 그리는 쪽만 빠졌다. `markTrending`·`TRENDING_TOP_N` 이 매 조회마다 돈다 · 스펙 참조 절을 정정할 때 같이
- [ ] **실패 이유 상한 상수가 두 벌 · 저장 시각이 벽시계** (2026-09-21 리뷰 5순위) — 둘 다 작은 정리다 · 그 파일을 다른 이유로 열 때
- [ ] **뱃지 숫자가 자리를 안 본다** (2026-09-21 리뷰) — 핫이슈 자리에서 「코딩 12」를 누르면 0~2건이 나온다. 뱃지를 자리별로 셀지는 승인 문서에 조항이 없다 · 뱃지 줄을 다시 손댈 때
- [ ] **핫이슈 판정 결과를 사람이 표본 검토한다** (2026-09-21) — 첫 2주간. 질문 셋이 실제로 가려내는지, 한 매체 쏠림이 계속되는지를 본다. **검토 재료는 준비됐다**(2026-09-21: 근거 저장 + `npm run hot-issue`) · 배포 후 2주가 지났을 때

## 닫힘

- [x] **Claude Opus 5.5 를 요약 단계에 쓸지** (2026-09-23 → 같은 날 닫힘) — 사용자가 「바꾼다」. 단가표에 먼저 넣고 effort medium 명시, 실제 호출 1회 통과. 같은 자리에 있던 「생각 줄이기(effort low)」는 Opus 에서 low 로 더 내릴지로 남는다 — 내일 실행에서 잘림·포기가 보이면 그때.

- [x] **합친 시각 규칙 중 이 구현에 없는 넷을 만들지 정한다** (2026-09-20) — 상세 하단 이전/다음 글 · 모바일 하단 고정 조작 줄 · 「더 보기」의 하루 단위 · 주소가 화면 상태의 근거인 것. 기준 문서에는 들어왔지만 코드에는 없어서, 지금은 「만들 때의 기준」으로만 산다 · 화면 작업을 다시 시작할 때. 만들기로 한 항목은 그 절의 대조표에서 지운다 (2026-09-23 닫힘) — 사용자 결정으로 넷과 그에 딸린 둘(뱃지 켜면 창만큼 펴기 · 모바일 뱃지 44px)까지 만들었다. 이 구현에 맞춰 바꾼 것과 실측은 design-rules 의 그 절 대조표에 있다
- [x] **파이프라인 화면이 「무엇이 건너뛰어졌나」의 절반만 보여준다** (2026-09-22 제기 → 같은 날 닫힘) — 화면이 `skippedKeywords`·`skippedHotIssue` 를 읽게 했다. 이제 「키워드 아예 못 함」이 뜬다. 같은 김에 단계별 소요 시간·모델·요금도 화면에 올렸다

- [x] **판정 근거(어느 질문이 참이었나)가 저장되지 않는다** (2026-09-21 → 같은 날 닫힘) — 마이그레이션 0009 + 저장 경로 + 통합 테스트. 스펙 S31 이 요구하던 것이고 저장 단계만 빠져 있었다.
- [x] **핫이슈 열 칸을 한 소스가 거의 다 먹는다** (2026-09-21 → 같은 날 부분 닫힘) — 개수 상한 자체를 없애서 「열 칸」이 사라졌다. 쏠림 자체는 표본 검토 항목으로 남는다.

- [x] **첫 화면에 같은 소식이 두 번 나온다** (2026-09-20 → 2026-09-21 닫힘) — 수집이 3주 밀렸을 때만 나던 증상으로 보인다. 이후 실행 세 번에서 재현되지 않았고 콘솔의 중복 key 경고도 사라졌다.

- [x] **signal2 쪽 시각 문서와의 차이를 대조한다** (2026-09-18 → 2026-09-20 닫힘) — 사용자가 「기준에 합친다」를 골라 블록 넷과 시안 한 장을 합쳤다(1010 → 1447줄). 합친 것 중 이 구현에 없는 동작은 표로 박아 뒀다.
- [x] **signal2 백로그 24건을 훑어 여기로 옮긴다** (2026-09-18 → 2026-09-20 닫힘) — 스물네 건을 이 구현에 대조해 **열 건을 옮기고 열네 건은 안 옮겼다**(중복 5 · 이 구현에 해당 없음 6 · 이미 해결 2 · 버림 1). 판정과 근거는 병합 기록에 항목마다 적어 뒀다.

- [x] **대시보드를 이 프로젝트에도 설치한다** (2026-09-18) — 설치하고 계약 검사 267개 통과로 닫힘
- [x] **사이클 기록 자리를 만든다** (2026-09-18) — 사이클 기록 파일 신설로 닫힘
- [x] **수집 본문 추출에 목적지 제한이 없다(SSRF)** (2026-09-19) — 백로그에서 꺼내 다음 사이클 1호로 올려서 닫힘
