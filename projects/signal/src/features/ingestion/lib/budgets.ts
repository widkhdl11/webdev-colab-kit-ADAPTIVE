/**
 * 수집 한 바퀴가 **얼마나 하는지**를 정하는 숫자들.
 *
 * 왜 한 파일에 모으나 (2026-08-13 리뷰): 이 값들이 `api/ports.ts` 안에 있었는데,
 * 그 파일은 `server-only` 라 유닛 테스트가 한 번도 로드하지 않는다. 그래서
 * `ENRICH_POOL` 을 100 → 10 으로 바꿔도 450개가 전부 green 이었다 —
 * 그렇게 바꾸면 "점수로 고른다"가 항등 함수가 되어 이번에 고친 버그로 그대로 돌아간다.
 *
 * 여기 있는 값은 전부 `budgets.test.ts` 가 값 자체를 못 박는다. 바꾸려면 테스트도
 * 같이 바꿔야 하고, 그게 곧 "사람이 한 번 더 판단한다"는 뜻이다.
 */

/**
 * 비싼 단계가 볼 글의 범위 — **오늘을 포함해 며칠치인가** (2026-09-22 사용자 결정).
 *
 * 본문 긁기·요약·번역·핫이슈 판정·키워드가 전부 이 창 안의 글만 후보로 본다.
 * 창 밖 글은 지우지 않는다 — 화면에는 그대로 있고, 요약·키워드가 없을 뿐이다.
 *
 * 왜 3인가: 뱃지 줄의 창(`BADGE_WINDOW_DAYS`)과 같은 값이다. 여기가 더 좁으면
 * **뱃지에는 있는데 키워드가 없는 글**이 생기고, 더 넓으면 화면에 안 쓰일 글에 요금을 쓴다.
 * 두 값이 갈리면 그 차이는 조용하다 — 그래서 같이 움직여야 한다.
 *
 * 이 창이 없던 동안 요약 대기열이 1,560건까지 자랐고, 고르는 기준이 최신순이라
 * 그 글들은 다음 주기에도 순위 안에 못 들었다. 대기열이 아니라 영구 누락이었다.
 */
export const CANDIDATE_WINDOW_DAYS = 3;

/**
 * **이 시각 이전에 발행된 글에는 돈을 쓰지 않는다** (2026-09-23 사용자 결정).
 *
 * 본문 긁기·요약·번역·키워드가 전부 이 시각 이후 글만 후보로 본다. 주제 판정과 핫이슈
 * 판정은 안 걸린다 — 그 둘은 화면의 내용을 정하므로 멈추면 화면이 틀린다(INV-CB8 과 같은 기준).
 *
 * 왜 창(`CANDIDATE_WINDOW_DAYS`)을 좁히지 않았나: 3일 창이 하는 일은 **수집이 실패한 날
 * 글을 다음 날 주워 담기**다. 창을 하루로 좁히면 그 안전망이 사라진다. 기준 시각은 이미
 * 지나간 과거만 잘라 내므로 앞으로의 안전망은 그대로 산다.
 *
 * 왜 필요했나: 2026-09-23 에 건수 상한을 없애자 그때까지 밀려 있던 273건이 한꺼번에
 * 후보가 됐다. 다섯 바퀴에 $2.35 를 쓰고도 180건이 남았다. 그 글들은 이미 지나간 날짜
 * 자리에 있어서, 요약이 채워져도 **그날 아무도 안 본다** — 기록으로만 온전해지는 값에
 * 낼 돈이 아니라는 판단이다.
 *
 * 값은 2026-09-23 의 하루 경계(오전 7시 한국 시간)다. 예약 실행이 도는 시각과 같다.
 * **이 값은 지우지 않는다.** 지우면 그날 안 하기로 한 글들이 다시 후보가 된다.
 */
export const ENRICH_FLOOR_ISO = "2026-09-22T22:00:00.000Z";

/**
 * 한 바퀴에 요약·번역할 최대 건수는 **없다** (2026-09-23, INV-CB11).
 *
 * 여기 `ENRICH_BATCH = 10` 이 있었다. 2026-08-13 에 "첫 수집에서 수백 건을 한꺼번에 부르면
 * 비용·시간이 튄다"는 이유로 넣었고, 밀린 것은 다음 주기가 가져간다는 전제였다.
 *
 * 그 전제가 두 군데서 깨졌다. 하나는 예약 실행이 **하루 한 번**이라 "다음 주기"가 내일이라는 것.
 * 다른 하나는 이어달리기 판정이 「시간이 떨어졌나」만 봐서, 이 상한에 막혀 멈춘 경우를
 * **남은 일이 없는 것으로 읽었다**는 것이다. 둘이 겹쳐 하루 10건이 됐다.
 *
 * **값을 키우지 않고 없앤 이유**: 큰 수로 바꾸면 같은 사고가 더 드물게, 더 알아채기 어렵게
 * 날 뿐이다. 그날 것을 그날 다 하기로 한 이상 고를 이유가 없다 — 몰린 날을 끊는 것은
 * 하루 요금 상한(INV-CB8)이지 건수가 아니다.
 */

export const ENRICH_POOL = 500;

/**
 * 한 소스가 가져갈 수 있는 최대 칸수도 **없다** (2026-09-23, INV-CB11).
 *
 * 여기 `PER_SOURCE_ENRICH_LIMIT = 3` 이 있었다. 자주 올리고 weight 도 높은 소스 하나가
 * 그날 예산을 다 채워 다른 매체가 영영 번역·요약을 못 받는 것을 막으려던 값이다.
 *
 * 그 위험은 **예산이 모자랄 때만** 있다. 그날 것을 그날 다 하면 누가 먼저 뽑히느냐가
 * 아무것도 바꾸지 않는다 — 어차피 전부 처리한다. 상한이 남아 있으면 막는 것은 독차지가
 * 아니라 **좋은 글**이다: 그 매체 글이 아무리 나아도 네 번째부터는 버려지고, 버린 이유가
 * 품질이 아니라 순서다.
 *
 * 남는 위험은 한 매체가 비정상적으로 쏟아내는 경우인데, 그건 골라 담아 가릴 일이 아니라
 * 하루 요금 상한(INV-CB8)이 끊을 일이다.
 *
 * 고르는 함수(`pickEnrichTargets`)는 상한 인자를 아직 받는다 — 0 을 주면 상한이 없다.
 * 부르는 자리가 0 을 준다.
 */

/**
 * 단계마다 부르는 모델 (2026-09-22 사용자 결정).
 *
 * **단계마다 다른 모델을 쓴다.** 하는 일의 난이도가 다르기 때문이다 —
 * 제목 한 줄로 "소식인가"를 가르는 일과, 사람이 그대로 읽을 요약 문장을 쓰는 일은
 * 같은 모델을 쓸 이유가 없다. 단가는 `claude-haiku-4-5` 가 `claude-sonnet-5` 의 절반이다.
 *
 * 고른 근거 (실측 건당 요금 기준 월 71,000원 → 54,000원 — 요약도 sonnet 이던 2026-09-22 기준.
 *           요약을 opus-5-5 로 바꾼 뒤의 월 요금은 아직 안 쟀다):
 *   주제 판정  haiku  — 제목 한 줄, 답이 예/아니오. 틀려도 다음 주기에 다시 안 묻는 건
 *                       아니지만(판정은 한 번뿐이다) 위험은 "엉뚱한 글이 들어온다" 정도다
 *   키워드     haiku  — 1,200자에서 낱말 몇 개 뽑기. 표기 규칙은 지시문이 잡는다
 *   핫이슈     sonnet — "중요한 소식인가"의 판단이다. 이게 흔들리면 첫 화면이 흔들린다
 *   요약·번역  opus-5-5 — **사람이 그대로 읽는 문장이다.** 여기서 아끼면 바로 티가 난다.
 *                       2026-09-23 사용자 결정으로 sonnet-5 → opus-5-5($4/$20, sonnet 의 두 배).
 *                       생각은 끌 수 없고 깊이는 effort 로만 정한다 — ports.ts 의 호출에 적었다
 *
 * 값을 바꾸면 요금과 판정 품질이 같이 바뀐다. 보고서에 실행마다 **어느 모델을 썼는지**
 * 남기므로(`usage.models`), 바꾼 뒤 화면에서 전후를 비교할 수 있다.
 */
export const TOPIC_MODEL = "claude-haiku-4-5";
export const KEYWORD_MODEL = "claude-haiku-4-5";
export const HOT_ISSUE_MODEL = "claude-sonnet-5";
export const ENRICH_MODEL = "claude-opus-5-5";

/**
 * 주제 판정 응답에 줄 최대 토큰.
 *
 * **2026-09-22 에 `api/ports.ts` 에서 내려오면서 200 → 400 으로 올렸다.** 그 파일은
 * `server-only` 라 유닛이 로드조차 못 해서, 어떤 값으로 바꿔도 전 스위트가 green 이었다
 * (`ENRICH_POOL` 100→10 · `KEYWORD_TIMEOUT_MS` 와 같은 자리다).
 *
 * 올린 근거는 실측이다: 같은 날 한 바퀴에서 **물어본 218건 중 49건(22%)이
 * `stop=max_tokens` 로 죽었다.** 판정이 실패하면 INV-F3 에 따라 **거르지 않고 통과**시키므로,
 * 증상이 "필터가 5분의 1쯤 안 돈다"인데 리포트에는 실패 이유 한 줄로만 남는다.
 *
 * 왜 요금이 안 늘어나나: 출력 토큰은 실제로 생성된 만큼만 청구된다. 잘려 죽은 49건은
 * 이미 200 토큰을 다 쓰고 **버려졌다** — 상한을 올리면 그 지출이 쓸모 있는 판정이 된다.
 * 2026-08-12 에 잰 정상 응답은 32 토큰이었고, 지금은 생각이 그보다 길어진 제목이 있다.
 */
export const TOPIC_MAX_TOKENS = 400;

/**
 * 주제 판정 한 건에 줄 시간. 위와 같은 날 같은 이유로 내려왔다.
 */
export const TOPIC_TIMEOUT_MS = 15_000;

/**
 * 주제 판정을 한 번에 몇 건까지 동시에 물어볼지.
 *
 * 순차로 돌리면 건당 3.8초(2026-08-12 실측: 70건 264초)라 소스 14곳의 첫 수집이 29분이 되고,
 * Vercel 함수 상한 300초에서 잘린다 — **에러 없이** 배열 앞쪽 소스만 적재되고 뒤쪽은 매일 빠진다.
 *
 * 무제한으로 던지지 않는 이유: 429(요청 과다)를 맞으면 판정이 실패하고, INV-F3 이 실패를
 * **통과**로 처리하므로 필터가 조용히 열린다. 거르라고 만든 장치가 몰아쳐서 안 거르게 되는 셈이다.
 */
export const TOPIC_CONCURRENCY = 8;

/**
 * 한 주기에 뱃지 키워드를 붙일 최대 건수 (badge-keywords).
 *
 * 요약(`ENRICH_BATCH = 10`)보다 훨씬 큰 이유: 키워드는 근거를 1,200자로 자르고 응답이
 * 40토큰 남짓이라 건당 약 $0.005 다(2026-08-30 실측: 입력 1,378 + 앵커 80개 약 718).
 * 요약과 달리 **밀리면 뱃지 줄이 그만큼 비어 보인다** — 요약은 안 되면 카드가 제목만
 * 보여주지만, 키워드가 없는 글은 어느 뱃지에도 안 들어가 화면에서 아예 사라진 것처럼 된다.
 *
 * 하루 신규가 약 68건이라 80이면 한 주기에 다 소화한다. 밀린 것은 다음 주기가 가져간다.
 */
export const KEYWORD_BATCH = 80;

/**
 * 키워드를 한 번에 몇 건까지 동시에 물어볼지.
 *
 * `TOPIC_CONCURRENCY` 와 같은 값·같은 이유다(순차로 돌리면 Vercel 함수 상한에 걸린다).
 * 여기엔 이유가 하나 더 있다 — **한 청크가 끝나야 그 키워드가 다음 청크의 앵커 목록에
 * 실린다**(INV-B3). 동시 수를 키울수록 앵커가 늦게 자라 표기가 갈릴 여지가 는다.
 */
export const KEYWORD_CONCURRENCY = 8;

/**
 * 키워드 한 건에 줄 시간 (`api/ports.ts` 의 Anthropic 호출 타임아웃).
 *
 * **2026-08-31 에 `api/ports.ts` 에서 내려왔다.** 거기 있던 동안은 `server-only` 라
 * 유닛이 로드조차 못 해서 어떤 값으로 바꿔도 전 스위트가 green 이었다 —
 * `ENRICH_POOL` 100→10 사건(2026-08-13)과 같은 자리다.
 */
export const KEYWORD_TIMEOUT_MS = 15_000;

/**
 * 키워드 응답에 줄 최대 토큰.
 *
 * 40토큰이면 충분한 형식인데 300을 주는 이유: 모델이 thinking 블록에 먼저 쓰면 텍스트가
 * 0글자로 잘려 나온다. 2026-08-12 에 주제 판정이 `max_tokens: 5` 로 정확히 그렇게 죽었고,
 * **빈 문자열이 통과해서** 겉으로는 정상으로 보였다. 그 사고를 안 되풀이하려고 넉넉히 준다.
 * (같이 내려온 이유는 위 `KEYWORD_TIMEOUT_MS` 와 같다.)
 */
export const KEYWORD_MAX_TOKENS = 300;

/**
 * 키워드를 물을 때 근거로 실을 최대 글자 수.
 *
 * 본문 전체를 실으면 한 건이 2만 자라 비용이 통째로 달라진다(VentureBeat 16,000자 실측).
 * 키워드는 "무엇에 대한 글인가"만 정하면 되므로 앞부분으로 충분하다.
 * (같이 내려온 이유는 위 `KEYWORD_TIMEOUT_MS` 와 같다.)
 */
export const KEYWORD_EVIDENCE_LIMIT = 1_200;

/**
 * 한 바퀴에 쓸 수 있는 시간 (INV-C4 의 시간 판).
 *
 * Vercel 함수 상한이 300초인데 단계가 전부 순차라 최악치가 그것을 넘는다
 * (피드 14곳×15초 + 판정 실측 125초 + 추출 10×15초 + 요약 10×30초). 넘기면 함수가 죽고
 * **응답 본문이 없다** — 이번에 만든 failureReasons·notChecked·토큰 계측이 제일 필요한 날
 * 하나도 안 남고, 요금만 나간 상태가 된다.
 *
 * 그래서 300 이 아니라 240 이다. 남은 60초는 리포트를 만들어 돌려주는 몫이다.
 * 예산이 떨어지면 그 단계를 **건너뛰고 리포트를 정상 반환한다** — 건너뛴 사실은 리포트에 남는다.
 */
export const INGEST_BUDGET_MS = 240_000;

/**
 * 외부 응답에서 받을 최대 바이트.
 *
 * 타임아웃(15초)만으로는 부족하다 — 15초 동안 수백 MB 를 흘려보내면 함수가 메모리로 죽고,
 * 그 뒤 JSDOM 파싱은 동기라 더 나쁘다. 실패한 항목은 `content_html` 이 빈 채 남아
 * 다음 주기에도 같은 자리에서 다시 잡히므로(백오프 없음) 매일 같은 곳에서 죽는다.
 */
export const MAX_FETCH_BYTES = 2_000_000;

/** 리포트에 담을 서로 다른 실패 이유의 최대 개수. */
export const MAX_FAILURE_REASONS = 5;

/**
 * 리포트에 남기는 **제목** 한 줄의 최대 길이 (2026-08-16 리뷰).
 *
 * 실패 이유에는 길이 상한이 있는데 걸러진 제목에는 없었다. 제목은 남의 서버가 준 문자열이고,
 * 그게 Cron 응답 본문과 Vercel 로그에 그대로 들어간다. 개수는 막을 수 없다 —
 * INV-F2 가 "걸러진 것은 전부 제목을 남긴다"를 요구하기 때문에 한 줄 길이만 막는다.
 *
 * 실패 이유(200자)보다 **좁게** 잡는 이유: 제목은 크기를 아는 값이다(한국어 40자 안팎,
 * 영어 제목이 길어도 100자를 잘 안 넘는다). 실패 이유 쪽은 남의 서버 메시지라 무엇이 올지
 * 모르니 더 넉넉해야 한다. 120자면 정상 제목은 안 건드리면서 상한 노릇을 한다.
 */
export const MAX_TITLE_LENGTH = 120;

/**
 * 실패 이유 한 줄의 최대 길이.
 *
 * 개수만 막으면 절반이다 — 남의 서버가 준 `content-type` 헤더나 Supabase 오류 메시지에
 * 응답 본문이 실리면 한 줄이 수 KB 가 되고, 그게 Cron 응답과 Vercel 로그에 그대로 남는다.
 */
export const MAX_FAILURE_REASON_LENGTH = 200;

/**
 * 한 바퀴에 **핫이슈 판정을 물어볼** 최대 건수 (hot-issue.md).
 *
 * **요금을 정하는 값은 이것 하나다.** 몇 건이 핫이슈로 뽑히는지는 요금과 무관하다 —
 * 뽑는 것은 판정 결과가 정하고 개수 상한이 없다(INV-N4, 2026-09-21).
 *
 * 120 으로 두는 근거: 2026-09-20 실측으로 하루 유입이 88건이고 코드가 가정한 평상시는
 * 68건이다. 밀린 것이 섞이는 날을 감안해 그보다 넉넉히 두되, 이상한 날에 수백 건이
 * 한꺼번에 나가지 않도록 상한은 남긴다. 밀린 글은 `hot_issue_at` 이 비어 있어 다음 주기에
 * 그대로 다시 잡힌다.
 */
export const HOT_ISSUE_BATCH = 120;

/*
 * `HOT_ISSUE_GATE_CAP` 은 2026-09-21 에 없앴다 (INV-N4 개정).
 *
 * 10 이었고 근거가 둘이었는데 둘 다 무너졌다. ① "요약 예산이 하루 10건이라 그보다 많이 뽑아도
 * 한국어로 못 읽는다" — 요약 예산을 핫이슈 개수로 옮겨 쓴 것이고, 그 예산 자체가 대기열을
 * 못 따라가는 별개 문제다(BACKLOG). ② "크게 잡으면 판정이 헐거운지 화면에 안 드러난다" —
 * 반대였다. 상한이 그것을 가렸다.
 *
 * 실측이 결정적이었다: 문턱을 넘은 26건 중 10건만 남고 **중요도 2 세 건이 전부 잘렸다.**
 * 자르는 순서가 이슈성(최근 × 소스 weight)인데 거기에 중요도가 안 들어가기 때문이다.
 */

/**
 * 판정 지시문에 실을 「오늘 이미 뽑힌 제목」의 최대 개수 (INV-G4).
 *
 * **하루 상한(옛 `HOT_ISSUE_GATE_CAP`)이 이 값을 대신하고 있었다.** 하루에 10건까지만
 * 뽑히니 이 목록도 10건을 못 넘었다. 2026-09-21 에 그 상한을 없애면서 위쪽 한계가
 * 사라졌고, 목록은 **판정 한 건 한 건의 지시문에 통째로 실린다** — 그날 뽑힌 수가 n 이면
 * 입력이 n 에 비례해 커지고, 그날 판정 요금은 건수 × n 으로 는다.
 *
 * 40 으로 두는 근거: 2026-09-21 실측으로 문턱을 넘은 글이 하루 11~15건이다. 그 세 배쯤을
 * 두면 평상시에는 안 잘리고, 판정이 헐거워지는 날에만 걸린다. 걸리면 맨 아래(가장 오래된)
 * 제목이 중복 판정에서 빠지는데, 같은 사건이면 보통 몇 시간 안에 몰려 오므로 최신 40건
 * 안에 들어 있다.
 */
export const PICKED_TITLES_LIMIT = 40;

/**
 * 핫이슈 판정을 한 번에 몇 건까지 동시에 물어볼지.
 *
 * `TOPIC_CONCURRENCY`·`KEYWORD_CONCURRENCY` 와 같은 값·같은 이유다. 여기엔 이유가 하나 더
 * 있다 — **한 청크가 끝나야 그 글이 다음 청크의 「이미 뽑힌 목록」에 실린다**(INV-G4).
 * 동시 수를 키울수록 같은 사건을 다룬 두 글이 같은 청크에 들어갈 확률이 는다.
 */
export const HOT_ISSUE_CONCURRENCY = 8;

/**
 * 핫이슈 판정 한 건에 줄 시간 (`api/ports.ts` 의 Anthropic 호출 타임아웃).
 *
 * 키워드와 같은 값이다 — 입력 구성(제목 + 출처 요약글)과 응답 크기가 비슷하다.
 */
export const HOT_ISSUE_TIMEOUT_MS = 15_000;

/**
 * 핫이슈 판정 응답에 줄 최대 토큰.
 *
 * 필요한 형식은 40토큰 남짓인데 넉넉히 주는 이유는 `KEYWORD_MAX_TOKENS` 와 같다 —
 * 모델이 thinking 블록에 먼저 쓰면 텍스트가 0글자로 잘려 나오고, **빈 문자열이 통과해서**
 * 겉으로는 정상으로 보인다(2026-08-12 주제 판정이 정확히 그렇게 죽었다).
 */
export const HOT_ISSUE_MAX_TOKENS = 1200;
// 2026-09-23 300 → 1200. 두 가지 이유가 겹쳤다 — ① 300 에서 이미 6~17% 가 「응답을 읽지
// 못함」으로 죽고 있었다(백로그 「핫이슈 판정이 10% 잘려 죽는다」, 생각 블록이 먹는 것으로
// 보인다) ② 참인 질문마다 근거 한 문장을 같이 받는다(hot-issue INV-G2 · signal 포인트).
// 상한은 쓴 만큼만 청구된다. 1200 인 이유는 시간 상한(15초)이다 — 초당 약 95토큰(요약 호출
// 실측)이면 1200 은 13초 안팎이라 그 안에 든다.

/**
 * 하루에 쓸 수 있는 요금 상한 (USD) — ingest-chaining-budget INV-CB6~CB8.
 *
 * **할당량이 아니라 비정상을 끊는 장치다**(2026-09-22 사용자 결정). 글이 몰린 날에는
 * 걸리지 않아야 하고, 걸렸다면 그건 뭔가 잘못됐다는 뜻이다.
 *
 * 근거는 2026-09-22 실측 건당 요금이다: 평범한 날 약 $1.71(주제 판정 218건 $0.40 ·
 * 핫이슈 120건 $0.33 · 요약 64건 $0.57 · 키워드 90건 $0.41), 글이 두 배로 몰린 날 약 $2.19.
 * **평소의 대여섯 배**로 잡았다 — 너무 낮게 잡으면 글이 진짜 많은 날에 걸려 화면이 조용히
 * 얇아지고, 너무 높게 잡으면 나쁜 하루가 $10 로 끝난다. 둘 중 뒤쪽이 싸다.
 *
 * 위 숫자는 요약이 sonnet 이던 때 단가다. 요약을 opus-5-5($4/$20, 두 배)로 바꾸면 요약 몫이
 * $0.57 → 약 $1.14 가 되어 평범한 날이 약 $2.3 이다 — 상한은 그래도 네 배 남짓이라 그대로 둔다.
 */
export const DAILY_COST_CAP_USD = 10;

/**
 * 이어달리기를 몇 번까지 이어 붙일 수 있는가 (INV-CB5).
 *
 * 한 호출이 250초쯤 일하므로 20 이면 한 번의 예약 실행이 최대 한 시간 반쯤 이어진다.
 * 실제로는 할 일이 없으면 그전에 멈춘다 — 이 값은 평소에 닿는 수가 아니라
 * **안 멈추는 버그를 끊는 자리**다. 자기를 부르는 구조라 이 상한이 없으면
 * 일이 줄지 않는 버그 하나로 호출이 영원히 이어진다.
 */
export const MAX_CHAIN_LENGTH = 20;

/**
 * 본문 추출 한 건에 줄 시간 — **2026-09-22 에 `api/ports.ts` 에서 내려왔다.**
 *
 * 거기 있던 동안은 `server-only` 라 유닛이 로드조차 못 해서 어떤 값으로 바꿔도 전 스위트가
 * green 이었다(`ENRICH_POOL` 100→10 사건과 같은 자리다). 내려온 이유가 하나 더 있다:
 * INV-CB9 가 "남은 시간이 한 건의 최악 소요 시간보다 적으면 끊는다"를 요구하는데,
 * 그 최악 시간이 곧 이 타임아웃이다 — 두 값이 갈리면 판단이 틀린 수에 맞춰진다.
 */
export const EXTRACTION_TIMEOUT_MS = 15_000;

/**
 * 요약·번역 한 건에 줄 시간. 위와 같은 날 같은 이유로 내려왔다.
 *
 * 다른 단계(15초)보다 긴 이유: 응답이 한 문장 요약·핵심 셋·표에 생각까지 얹혀 생성할 토큰이
 * 몇 배 많다(요약 상한 `ENRICH_MAX_TOKENS` 대 다른 단계 수백).
 *
 * 출력 상한과 짝이다 — 상한만큼 쓰는 데 걸리는 시간이 이 안에 들어야 한다.
 * 2026-09-24 실측(`npm run probe:enrich`, opus-5-5 · medium · 4건): 요약 호출 초당 94~104토큰,
 * 평균 99. 2600토큰이면 26초 안팎이다. 실제 출력은 703~1124토큰(7.5~11.7초)이었다.
 * (sonnet-5 때의 근거 「초당 약 95토큰」과 거의 같다.)
 */
export const ENRICH_TIMEOUT_MS = 30_000;

/**
 * 요약·번역 호출의 출력 상한 — 2026-09-24 에 `api/ports.ts` 에서 내려왔다(리뷰: 거기 있으면
 * server-only 라 유닛이 못 본다). 실측 스크립트(`npm run probe:enrich`)도 이 값을 그대로 쓴다.
 *
 * 요약이 붙는 호출: 이 모델은 생각을 먼저 하고 그 양이 들쭉날쭉하다 — 같은 글이 1047·1640
 * 토큰으로 끝났다가 한 번은 1800 을 전부 생각에 쓰고 **요약 0글자**로 끝났다. 상한은 쓴 만큼만
 * 청구되므로 올려도 평소 요금은 그대로다. 위쪽을 막는 것은 시간 상한(`ENRICH_TIMEOUT_MS`)이다 —
 * 출력 속도 × 시간 상한 안에 들어야 한다. 근거 실측은 아래 `ENRICH_TIMEOUT_MS` 주석.
 */
export const ENRICH_MAX_TOKENS = 2600;

/**
 * 제목만 번역하는 호출의 출력 상한. 한국어는 글자당 토큰이 커서 긴 제목이면 JSON 껍데기까지
 * 넣다 잘린다. 잘리면 title_ko 가 계속 null 이라 **같은 글이 매 주기 다시 불린다** — 3회 포기는
 * 요약에만 걸리기 때문이다. opus-5-5 는 생각을 끌 수 없어 그 몫도 여기서 나간다.
 *
 * 2026-09-24 실측(`npm run probe:enrich`, opus-5-5 · medium · 4건): 94·141·179·**405** 토큰.
 * 405 인 건은 제목이 42자뿐인데 나머지가 전부 생각이었다 — 500 이면 여유가 20% 도 안 된다.
 * 1000 으로 올린다. 쓴 만큼만 청구되므로 평소 요금은 그대로고, 이 호출의 속도(초당 45~71토큰)로
 * 1000 은 최악 22초라 시간 상한 30초 안에 든다.
 */
export const ENRICH_TITLE_MAX_TOKENS = 1000;

/** 요약 근거로 보내는 본문 길이 상한(글자). */
export const ENRICH_EVIDENCE_LIMIT = 20_000;

/**
 * 모델이 답했는데 요약을 못 쓴 것(형식 불합격·응답 잘림·JSON 못 읽음)이 몇 번이면 그 글을 포기하는가 (ingestion-ranking INV-S3 S32, 2026-09-23).
 *
 * 한도가 없으면 **어떤 글을 돌려도 매번 떨어질 때** 3일 창이 끝날 때까지 매 주기 같은 글에
 * 요금이 나간다. 후보가 점수순이라 그런 글이 예산 앞자리를 먼저 먹는다. 3 인 이유: 모델 응답이
 * 들쭉날쭉해 한 번 떨어진 글이 다음에 붙는 일은 있지만(생각에 토큰을 다 쓴 경우), 세 번 연달아
 * 떨어진 글은 검사나 글 쪽이 원인이라 다시 불러도 같다. 포기한 글은 출처 요약글로 보인다(INV-S2).
 */
export const SUMMARY_MAX_FAILURES = 3;

/**
 * 모델 호출의 SDK 재시도 횟수 (INV-CB9, 2026-09-24 리뷰).
 *
 * SDK 기본값은 2 이고 **시간 초과도 재시도한다** — 그대로 두면 요약 한 건이 30초가 아니라
 * 최악 90초였고, 남은 31초에 시작한 호출이 Vercel 300초를 넘길 수 있었다. 게다가 시간 초과로
 * 끝난 호출은 요금이 응답에 안 실려 하루 상한에도 안 잡힌다.
 * 0 인 이유: 실패한 글은 다음 주기가 어차피 다시 부른다. 여기서 기다릴 이유가 없다.
 * 네 단계가 클라이언트 하나를 나눠 쓰므로 이 값은 클라이언트를 만드는 자리에서 한 번 건다.
 */
export const MODEL_MAX_RETRIES = 0;

/**
 * 단계마다 **한 건이 최악으로 얼마나 걸리는가** (INV-CB9).
 *
 * 왜 타임아웃 × (재시도 + 1) 인가: 한 건이 그보다 오래 걸릴 수 있는 길이 없다. 이 값으로
 * 남은 시간을 재서, 시작해 봐야 못 끝낼 건이면 아예 시작하지 않고 다음 호출에 넘긴다.
 *
 * 넉넉히 잡는 쪽이 안전하다 — 모자라게 잡으면 마지막 한 건이 Vercel 300초를 넘기고,
 * 넘기면 **응답 본문이 없다.** 그날의 실패 이유도 토큰 계측도 안 남고 요금만 나간다.
 */
export const WORST_CASE_MS = {
  topic: TOPIC_TIMEOUT_MS * (MODEL_MAX_RETRIES + 1),
  hotIssue: HOT_ISSUE_TIMEOUT_MS * (MODEL_MAX_RETRIES + 1),
  // 본문 추출은 모델이 아니라 fetch 라 재시도가 없다
  extraction: EXTRACTION_TIMEOUT_MS,
  enrich: ENRICH_TIMEOUT_MS * (MODEL_MAX_RETRIES + 1),
  keywords: KEYWORD_TIMEOUT_MS * (MODEL_MAX_RETRIES + 1),
} as const;
