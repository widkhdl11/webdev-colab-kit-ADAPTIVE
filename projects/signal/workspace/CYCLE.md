# 사이클 — 열린 것과 닫힌 것

한 줄이 사이클 하나다. 사이클은 **사용자 지시 하나**이고, 계획 승인에서 열려
종료 조건이 성립할 때 닫힌다. 세션과 다른 축이라 세션을 넘어 이어진다.
(상세: docs/references/decision-layer.md)

## 열린 사이클

- `signal-20260920-1` — 결정층을 v3.4 로 고치고(올린 뒤에도 실행이 계속 가게), 그 기준으로
  남은 일을 끝까지: 레포에 옮겨 적힌 옛 문구 교체 · 등급표와 줄바꿈 정규화 패치 문서 ·
  커밋 규약 · signal2 문서 대조 마무리 · 커밋과 push.
  2026-09-20 사용자 지시로 열림. 사용자가 정해 준 것 — escalate 는 보류 방식으로 갈 것 ·
  살림 판단(커밋·push·정리)은 자동 결정 등급으로 내릴 것 · 계획 승인은 escalate 등급에만 ·
  escalate 결정은 모아서 카드 하나로 올리고 나머지는 진행할 것.

- `signal-20260918-1` — 들여오기와 병합: 밖에서 만들어져 있던 이 프로젝트를 이 레포로 옮기고,
  게이트가 지적한 것을 고치고, `signal2` 가 갖고 있던 문서를 하나씩 대조해 합친다.
  2026-09-18 사용자 지시로 열림. 대상은 이 프로젝트의 문서와 설정이고, 제품 화면은 안 건드린다.
  사용자가 정해 준 것 — 예외 주석 세 개는 승인하되 불변식으로 옮기고 지울 것 · 원문 본문은
  새로 만들지 않고 여기 있는 구현을 쓸 것 · `signal2` 는 보관하고 사이클은 중단할 것 ·
  문서 병합은 원문 렌더 보안부터 시작할 것.

- `signal-20260920-2` — 수집이 아무 주소나 가져오는 것을 막는다(다음 사이클 1호).
  `ingest-auth.md` 에 목적지 판정(INV-IA5)과 리다이렉트마다 다시 판정(INV-IA6)을 더하고,
  구현·검사를 같은 사이클에 넣는다. 2026-09-20 사용자 지시로 열림.
  **두 조항의 승인은 아직 안 받았다** — 카드 `signal-20260920-2-d1`.

## 다음 사이클 대기

(비었다. 1호는 위에서 열렸다.)

### 열었을 때의 원안 (2026-09-19 기록)

- **1호 · 수집이 아무 주소나 가져오는 것을 막는다** — `ingest-auth.md` 에 불변식을 더한다.
  지금 `src/features/ingestion/api/ports.ts:318-335` 가 `item.original_url` 로 그대로 요청을
  보낸다. 스킴만 http/https 로 걸러지고 목적지 호스트에는 아무 제한이 없다. 소스 목록은 우리가
  정하지만 **피드 안의 링크는 발행처가 정한다** — 소스 한 곳이 내부 주소를 넣으면 서버가 그것을
  받아 와 본문으로 저장하고, 그 내용이 공개 화면에 렌더된다. 걸러내기를 거치므로 스크립트 실행은
  아니고, 문제는 **내부 응답이 밖으로 나가는 것**이다.
  2026-09-19 보안 리뷰에서 나왔고 사용자가 백로그에서 꺼내 이 자리로 올렸다.

## 닫힌 사이클

(아직 없다. 이 프로젝트의 2026-08~09 기록은 과정 기록 쪽에 날짜별로 남아 있다.)
