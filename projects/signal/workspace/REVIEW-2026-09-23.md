# 리뷰 지적 — 2026-09-23 상세 화면 재구성 (커밋 82714ae · 41b8961)

세션을 비우기 전에 받은 것을 적어 둔다.
**2026-09-23 저녁: 코드 high 1·2 고침**(아래 ✅). 나머지는 아직이다.
**코드 리뷰의 high 둘은 push 전에 반드시 고친다** — 둘이 합쳐지면 같은 글에 매 주기 돈이 나간다.

## UI 리뷰 (ui-reviewer)

순서·토큰·절 구성은 승인 블록(design-rules 「2026-09-23 (2)」)과 시안에 맞다. 명시도 충돌 없음,
폰 하단 고정 줄과 안 겹침(69px < --dock-h 78px). 대비 문제 없음.

**중간**
1. `summary` 에 키보드 포커스 링이 없다 — `src/app/globals.css:29` 선택자에 `summary:focus-visible` 추가.
2. 폰에서 옛 요약 펼치기(`.legacy > summary`)가 `display:flex` 라 ▸ 마커가 사라진다 —
   `article-view.module.css` 640px 블록. `display:list-item` + min-height + padding 으로 44px 을 맞추거나 글자 상태 표시.
3. 화면에 도메인 규칙이 있다 — `article-view.tsx` 의 "AI 요약일 때만 한 줄 요약"(INV-S2),
   `summary-markup.tsx` 의 "한 줄 요약 없으면 옛 형식". `displaySummary`(entities)가 oneLine·isLegacy 를 같이 돌려주게.

**낮음**
4. 요약 표에 이름이 없다 — `<caption className="sr-only">요약 표</caption>` 정도.
5. 폰에서 「피드로」 여백이 시안(16px 0 16px)과 다르다.
6. 원문 앞 안내 문구 아래 여백 24px → 시안은 `--space-20`.
7. 원문 본문에 `lang` 이 없다 — 소스 설정이 언어를 안다(`영어` 면 `lang="en"`).

**정리**
- 640px 블록의 `.prose p/li/h3~h6 → --fs-title` 규칙은 이제 효과 없음(둘 다 16px) — 지운다.
- 낡은 주석: css 367(17px 라고 적힘) · 377-378(요약 박스 4px 선) · 1-2(옛 시안 article.html).

**ASCII 규칙 위반 (CLAUDE.md)** — `SignalKey`(entities `summary-format.ts`)가 한글 키(변화·방향·기회)이고
`article-view.tsx` 의 `SIGNAL_LABELS` 가 그걸 객체 키로 쓴다. 화면·entities 쪽은 ASCII id(`change`·`direction`·`window` 등)로
두고, DB·모델 응답의 한글 키(0009 의 jsonb 키, `HOT_ISSUE_QUESTIONS`)와는 매핑 한 곳에서 잇는다.

## 코드 리뷰 (code-reviewer)

**high**
1. ✅ **형식 검사가 정상 문장을 떨어뜨린다** — `src/entities/article/lib/summary-format.ts` 의 `SENTENCE_END` 가
   "마침표 뒤가 공백이면 문장 끝"이라 `U.S. 정부` · `vs. ` · `Inc. ` · `No. 1` · `e.g. ` · `...` 에서 두 문장으로 센다.
   끝 검사도 `…했다"` · `…했다)` 처럼 닫는 따옴표·괄호로 끝나면 떨어뜨린다(핵심 셋 포함).
   고칠 것: 라틴 대문자 한 글자·알려진 약어 앞 마침표는 안 센다, 끝 검사는 닫는 따옴표·괄호를 떼고. 테스트에 이 표기들.
2. ✅ **형식 실패가 무한 재시도·반복 과금이 된다** — 불합격 3회에서 포기(`summary_failures`, 0011 에 칸 추가) — 실패면 아무것도 저장 안 하고 후보 조건이 `summary is null` 이라
   3일 창이 끝날 때까지 매 주기 같은 글에 2600토큰 호출. 1번 때문에 **결정적으로** 실패하는 글은 매번 실패하고,
   점수순이라 예산 앞자리를 먹는다. 시도 횟수 칸이 없다.
   고칠 것: 시도 횟수를 세어 N회에서 멈춘다(칸 추가면 0011 에 같이) 또는 실패한 원문을 리포트에 남긴다.

**medium**
3. 마이그레이션 전에 배포하면 **상세 페이지 전부 500**(`queries.ts` `DETAIL_COLUMNS` 가 새 칸을 select) +
   저장 실패 뒤 `hot_issue_at` 이 안 찍혀 **판정이 매 주기 다시 과금**. 0011 주석에 두 경우를 적는다.
4. 지시문이 약어 안의 마침표·따옴표로 끝나는 경우를 피하라고 안 한다(`prompt-text.ts` summaryRules) — 1번 보완.

**low**
5. 성공 경로에서 `summary` 와 `oneLine` 이 늘 같다 — `run-ingest.ts` 의 `if (out.oneLine !== null)` 은 늘 참.
6. 한글 객체 키 `SIGNAL_LABELS` — 위 UI 리뷰의 ASCII 항목과 같다.

**문제 없음 확인**: 거짓 질문의 근거는 저장 안 됨 · 카드/재시도 조건/옛 글 표시가 서로 맞음 · 저장된 one_line 이
새 검사에 떨어져도 문단으로 내려가 안 깨짐 · 목록 조회는 새 칸 안 읽음 · FSD 방향 이상 없음.
