# 결정 로그 — signal-20260924-1

판정 검토(핫이슈 판정을 사람이 매주 표본으로 채점). 사용자 지시: 붙여 넣은 설계 A~F +
「중요하거나 나의 개입이 필요한게 아니라면 추천으로 하고 착수」.

기록 하한: 실재하는 대안이 있었고 기각 이유를 한 줄로 쓸 수 있는 결정만 적는다.
`근거` 는 적용한 규칙 id, `NEW`(커버 규칙 없음), `HUMAN`(사람 개입) 중 하나다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 범위 | 백로그의 「배포 후 2주 뒤」를 기다리지 않고 지금 착수 | 근거 문장이 09-21부터 쌓여 첫 표본을 뽑을 수 있다 | 2주 대기 | HUMAN(추천 위임) |
| D2 | 실행 자리 | 주간 실행은 이 PC 의 Windows 작업 스케줄러(월 08:00, 놓치면 켜질 때 한 번) | 표본 파일이 이 PC 폴더에 있어 Vercel·클라우드 예약은 쓸 수 없다 | Vercel cron · 클라우드 예약 에이전트 | HUMAN(추천 위임) |
| D3 | 알림 | 검토 대기는 결정 카드가 아니라 특이사항 줄. 카드는 수정안(2주 연속 오류)에만 | 카드 자리가 하나라 매주 알림이 다른 결정과 부딪힌다 | 설계대로 매주 카드 | HUMAN(추천 위임) |
| D4 | 보안 | 쓰기 경로에 Origin·Host 검사를 더했다 | 127.0.0.1 바인딩만으로는 외부 사이트의 POST 와 DNS 재바인딩을 못 막는다 | 바인딩만 | HUMAN(추천 위임) |
| D5 | 위험 표면 | 스펙 `verdict-review.md` 를 `surfaces: [concurrency]` 로 쓰고 approved | 7일 경계·동시 답이 시변 상태다. 불변식은 사용자 설계를 옮긴 것이고 지시가 추천대로 착수였다 | draft 로 두고 카드로 승인 | HUMAN(추천 위임) |
| D6 | 구조 | 닫힘을 표본 파일에 적지 않고 집계 줄 유무로 판정. 표본 파일을 쓰는 프로세스는 서버 하나 | 주간 실행이 같은 파일을 쓰면 서버의 쓰기 줄 세우기가 프로세스 밖에서는 안 통한다 | 파일에 closed 칸 | NEW |
| D7 | 구조 | 지난주 표본은 7일이 몇 초 모자라도 다음 실행이 닫는다 | 매주 같은 시각에 돌아 7일 경계와 겹치면 열린 주가 둘이 된다 | 7일 경과만 | NEW |
| D8 | 설계 차이 | 틀리다의 방향은 판정에 맞는 것만 보인다(아님 글은 「핫이슈여야 함」 하나라 바로 저장) | 아님 글에는 근거가 없어 「근거가 엉뚱함」이 성립하지 않는다 | 설계대로 방향 셋 항상 | NEW |
| D9 | 설계 차이 | 2주 연속 오류는 특이사항에 표시까지만. 수정안 카드는 다음 세션에 에이전트가 만든다 | 수정안 문장과 전후 판정 재실행은 모델 호출이 필요하다 — 무인 실행에서 요금을 쓰지 않는다 | 무인 실행이 카드까지 | NEW |
| D10 | 킷 | 탭 줄(`tabs.json`)과 프로젝트 알림(`notices.json`)을 킷 대시보드의 일반 장치로 넣고, 판정 검토 화면은 프로젝트 파일로 둔다 | 판정 검토는 signal 전용인데 킷 설치본은 프로젝트 사본과 같아야 한다(검사 4번) | 킷 index.html 에 signal 탭을 직접 | NEW |
| D11 | 킷 | 정적 읽기 경로의 바깥 차단을 구분자까지 비교하도록 고치고 깨진 인코딩은 400 | 앞부분 비교는 옆 폴더를 통과시켰고 잘못된 %인코딩이 서버를 죽일 수 있었다 | 그대로 두고 백로그 | NEW |
| D12 | 커밋 | `review-samples/*.json`·`review-summary.jsonl` 은 커밋, `notices.json`·`index.json` 은 무시 | 앞의 둘은 다시 만들 수 없는 사람 판단이다 | 전부 무시(기존 `*.jsonl` 규칙) | NEW |
| D13 | 첫 표본 | 이번 주 표본에 근거 없는 핫이슈 8건·한 줄 요약 없는 글 18건이 섞였지만 다시 뽑지 않는다 | 09-23 이전 판정이라 칸이 없던 글이다. 다음 주부터는 창 밖으로 빠진다. 표본은 만든 뒤 고치지 않는다(INV-VR2) | 옛 칸으로 채워 다시 뽑기 | 스펙 INV-VR2 |
| D14 | 스펙 보강 | 수정안 필요 표시는 켜진 뒤 검토된 주에서 그 오류가 안 나올 때만 꺼진다(검토 안 한 주는 안 끔) | 코드 리뷰: 한 주를 못 보면 표시가 사라져 원인은 그대로인데 신호만 없어진다 | 마지막 두 주만 보기 | NEW |
| D15 | 스펙 보강 | 주간 실행이 닫기 전에 닫힘 표지를 남기고 300ms 기다린 뒤 다시 읽는다 · 실행 잠금 · 표본은 임시 파일+link 로 만든다 | 보안·코드 리뷰 둘 다 「읽은 뒤 줄을 쓰기 전 틈」을 짚었다. 잠금 파일 전체 방식은 과하다 | 백로그로 미루기 · 서버와 실행이 같은 잠금 | NEW |
| D16 | 킷 보안 | 읽기(GET)에도 Host 검사, 모든 응답에 틀 넣기 금지 헤더 | 보안 리뷰: 기존 읽기 경로로 활동 기록(명령 원문)이 DNS 재바인딩에 샌다 | 쓰기 경로만 막기 | NEW |
| D17 | 화면 말 | 「미검토·방향 미상·오류 분해·소스·W39」 → 「검토 안 함·어느 쪽인지 안 고름·어디서 틀렸나·출처·9/21 주」, 화면 말 표에 먼저 올림 | UI 리뷰: 표에 없는 말이 화면에 나갔다(8절). 파일 값(`unknown` 등)은 그대로 | 그대로 두기 | 킷 규칙(8절) |
| D18 | 범위 | 기존 대시보드 어휘표가 표를 안 따르는 것은 고치지 않고 킷 백로그로 | 이번 변경 밖이고 고치면 계약 검사 기대값이 같이 바뀐다 | 같이 고치기 | NEW |

## 이 사이클에서 안 적은 것

라벨 문구·CSS·파일 이름·검사 항목 이름처럼 대안을 한 줄로 쓸 수 없는 미시 선택.

## 자리 이전 (같은 날, 사용자 지적 「내가 말한 대시보드는 simoori의 대시보드였다」)

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D19 | 범위 | 판정 검토를 킷 진행 대시보드에서 simoori 개발자 대시보드(/dev/ingest/review)로 옮기고 킷 쪽은 걷어 냄. 킷 읽기 경로 보안 수리만 남김 | 사용자가 가리킨 대시보드가 달랐다 | 두 곳 다 두기 | HUMAN |
| D20 | 데이터 모델 | 저장은 Supabase 테이블 셋(0012), 매주 뽑기는 매일 수집 실행이 맡음 | 카드 d1 답 「(a) Supabase 테이블」 | 이 PC 의 파일 | HUMAN |
| D21 | 보안 | /dev/* 와 답 액션은 이 PC 주소(Host)·개발 서버에서만 — NODE_ENV·VERCEL·Host 셋을 봄 | 보안 리뷰: next dev 는 망 전체에 열리고 재바인딩을 Next 가 못 막는다 | dev 스크립트를 127.0.0.1 로 묶기(폰 테스트를 끊는다) | NEW |
| D22 | 구조 | 주와 표본은 DB 함수 하나로 한 트랜잭션에 만든다 | 코드 리뷰 high: 둘 사이에서 멈추면 표본 0건 주가 영구히 남는다 | 삭제로 되돌리기 | NEW |
| D23 | 구조 | 모양이 틀린 행은 버리지 않고 읽기를 실패시킨다 | 테스트 감사: 표본 행이 빠지면 「전부 답함」이 거짓으로 참이 된다 | 조용히 거르기 | NEW |
