# 결정 로그 — signal-20260920-1

기록 하한: 실재하는 대안이 있었고 기각 이유를 한 줄로 쓸 수 있는 결정만 적는다.
`근거` 는 적용한 규칙 id, `NEW`(커버 규칙 없음), `HUMAN`(사람 개입) 중 하나다.

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | 결정층 | `escalate` 를 보류 방식으로 못박는다 — 카드를 열어 두고 그 답에 의존하지 않는 일은 계속 한다 | 멈추면 실제 작업 시간은 적은데 경과 시간이 몇 주가 된다. 이 층이 애초에 그것을 풀려고 생겼다 | 예전 문구대로 멈추기 | HUMAN |
| D2 | 결정층 | 계획 승인(구현 전 합의문)은 `escalate` 등급 결정이 있는 작업에만 받는다 | 모든 작업에 합의문을 받으면 승인 한 번이 작업마다 붙어 등급을 나눈 의미가 사라진다 | 전부 받기 · 아예 안 받기 | HUMAN |
| D3 | 결정층 | 살림 판단(커밋·push·정리·파일 위치·검사 구성)을 자동 결정 등급에 넣는다 | 물어서 정하면 개입 횟수가 작업 내용과 무관하게 는다. 되돌리는 비용이 거의 0이다 | 지금처럼 매번 묻기 | HUMAN |
| D4 | 결정층 | 등급표의 정본을 `gates/lib/cycle-policy.mjs` 로 옮긴다 | 산문에만 있으면 경계가 읽는 사람마다 달라지고 어긋나도 아무 데도 안 걸린다 | 문서에만 두기 | NEW |
| D5 | 하네스 | 보호 파일 둘은 패치 문서 + 검사 스크립트로 넘긴다. 검사는 붙기 전 실패하고 붙은 뒤 통과한다 | 사용자가 코드를 안 읽고도 붙었는지 판정할 수 있어야 한다 | 직접 고치기(훅이 막는다) · 검사 없이 패치만 | 킷 규칙 |
| D6 | 하네스 | 패치 하나당 보호 파일 하나로 갈랐다 | 둘 이상을 한 패치에 묶으면 한쪽만 붙은 조합을 아무도 안 본다 | 한 패치에 둘 다 | 킷 규칙 |
| D7 | 문서 | 받아 둔 프롬프트 전문은 고치지 않고 그 위에 정정 주석을 단다 | 받은 것을 고치면 무엇을 받았는지가 사라진다. 그 기록이 그 문서의 목적이다 | 본문 문장 교체 | NEW |
| D8 | 스펙 | 보류 스펙을 `docs/specs/planned/` 에서 `status: parked` 로 옮긴다 | 위치로 보류를 표시하면 활성화할 때 파일이 움직이고 그 위치를 가리키던 문장이 낡는다. 실제로 낡아 있었다(참조 8곳) | 폴더 그대로 두기 | 킷 관례 |
| D9 | 제품 문서 | signal2 판본의 제품 정의·기술 문서는 안 가져온다 | 그쪽 판본의 상태 문장("코드가 0줄이다" 등)이 이 구현에서 전부 거짓이다 | 통째로 교체 · 절별로 섞기 | NEW |
| D10 | 백로그 | 스물네 건 중 열 건만 옮긴다 | 나머지는 중복 5 · 이 구현에 없는 코드 6 · 이미 해결 2 · 버림 1. 전부 옮기면 목록이 남의 코드 이야기로 반이 찬다 | 전부 옮기기 · 전부 버리기 | NEW |
| D11 | 백로그 | 「뱃지 집계 창에 위쪽 상한이 없다」는 문장을 이 구현 기준으로 고쳐 적었다 | 저쪽에서는 가짜 데이터가 가려 주던 경로가 여기서는 실제로 열려 있다 | 원문 그대로 옮기기 | NEW |
| D12 | 커밋 | 커밋은 부류별로 나누고 push 는 사이클이 닫힐 때 한 번 | 섞으면 제품을 되돌리려다 킷 수리까지 되돌아간다 | 한 번에 다 커밋 · 항목마다 push | HUMAN |
| D13 | 커밋 | 보호 파일은 커밋에 넣지 않고 미커밋으로 남긴 뒤 보고한다 | 붙이는 것이 사용자 몫이면 커밋도 사용자 몫이다. 아직 안 읽은 변경이 이력에 들어가면 안 된다 | 같이 커밋하기 | NEW |
| D14 | 디자인 | signal2 의 시각 기준 318줄은 자동으로 합치지 않고 카드로 올린다 | 승인된 시각 기준을 바꾸는 일이고, 그 규칙이 설명하는 화면이 이 구현에 없다 | 합치기 · 버리기 | 결정층 2절(escalate) |

## 이 사이클에서 안 적은 것

문구 다듬기·파일 이름·검사 항목 이름처럼 대안을 한 줄로 쓸 수 없는 미시 선택은 적지 않았다.
