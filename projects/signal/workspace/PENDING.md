# 보류 — 사람 결정 대기

되돌리기 비싼 결정(`escalate`)을 올려 두는 자리다. **올린다고 실행이 멈추지 않는다** —
그 항목이 막는 노드만 세워 두고 나머지 일은 계속 간다. 자세한 규약은
`docs/references/decision-layer.md` 2절.

- 항목은 `## 열린 항목` 아래의 것만 센다. 답이 오면 `## 닫힌 항목` 으로 옮긴다.
- `blocks:` 에는 **그 답 없이는 못 하는 노드만** 적는다. 없으면 줄을 비운다.

## 열린 항목

- **P1 · 예전 판본에만 있는 화면 규칙을 지금 기준에 넣을지**
  blocks: design
  보관해 둔 `signal2` 의 시각 기준 문서가 지금 것보다 318줄 길다. 늘어난 것은 2026-09-01 에
  확인받은 화면 결정들인데(이전 글/다음 글 · 작은 화면 아래 조작 줄 · 상세 키워드 줄 ·
  켜면 펴지는 범위 · 주소가 화면 상태의 근거), **이 구현에는 그 화면들이 없고** 적힌 실측값은
  그쪽 화면에서 잰 것이다. 셋 중 하나를 고른다 — 안 가져온다 / 참고로만 둔다 / 기준에 합친다.
  추천은 「참고로만 둔다」.
  근거 본문: `report/decision-detail.md` · 대조 결과: `workspace/MERGE.md` 5절 ·
  결정 카드: `signal-20260920-1-d1`

## 닫힌 항목

(아직 없다)
