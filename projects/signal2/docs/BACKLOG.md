# BACKLOG — signal2 (미룬 것)

지금 안 하기로 한 작업. wrap-up 이 쌓고, 세션 시작 브리핑은 미결 건수만 띄운다(0건이면 침묵).
- 미결은 `- [ ]`, 닫히면 `- [x]` 로 바꿔 "닫힘" 으로 옮긴다.
- 형식: `- [ ] **제목** (날짜) — 보류 이유 · 되살릴 조건`
- 하네스 개선은 여기가 아니라 킷의 docs/references/harness-backlog.md.

이 목록은 2026-08-31 에 이전 signal 구현의 PROGRESS "대기 중인 결정" 17건을 옮겨 온 것이다.
2026-09-02 에 workspace/ 에서 docs/ 로 옮겼다 — 미룬 제품 작업은 하네스를 바꿔도 그대로 남는다.

## 미결

- [ ] **이슈성·중요도 기준을 진짜로 만든다** (2026-08-31) — 지금 문턱 판정은 "몇 건 나왔나"만 보는 임시 기준이다(사용자 판단 2026-08-30: 나중에) · 되살릴 조건: `docs/specs/keywords-and-kinds.md` 를 approved 로 올릴 때(INV-G2·N1·N3). 같은 사건 묶기 + 모델 판정 단계가 함께 필요하다
- [ ] **프로필 내용이 미정이다** (2026-08-31) — PRODUCT 에 범위는 들어왔고 내용이 비어 있다. 물어본 4가지가 미답 · 되살릴 조건: 소식 배치(INV-N1)의 문 2·3번을 구현하려 할 때. 그 둘이 프로필에 매달려 있다
- [ ] **요약에 허용할 표시 범위** (2026-08-31) — 요약 안에 목록·표를 허용하기로 했는데 무엇까지인지 안 정했다 · 되살릴 조건: 상세 화면 구현 전. 요약은 신뢰 경계 밖이라 `docs/specs/content-safety.md` 를 다시 열어야 한다
- [ ] **스킬·툴 소스를 어디서 가져올지** (2026-08-31) — 지금 소스는 전부 뉴스·블로그라 탭을 만들어도 채울 것이 없다. 분석·관점 글이 `뉴스` 인지 종류를 셋으로 늘릴지도 미정 · 되살릴 조건: 스킬·툴 탭 구현 착수 시
- [ ] **소스가 AI 쪽으로 편중돼 있다** (2026-08-31) — 백엔드·프론트엔드 소스는 사용자가 구하기로 했다 · 되살릴 조건: 사용자가 소스 목록을 주면
- [ ] **프롬프트 캐싱 미적용** (2026-08-31) — 수집 프롬프트에 캐싱을 안 건다. 비용 항목이고 지금은 잴 데이터가 없다 · 되살릴 조건: 수집이 돌기 시작해 월 비용이 보일 때
- [ ] **본문 추출 실패에 백오프가 없다** (2026-08-31) — 재시도 간격을 담을 칸이 없어 계속 막히는 사이트를 매 주기 다시 두드린다 · 되살릴 조건: 스키마 설계(design/schema-designer) 때 같이 본다
- [ ] **Vercel 배포는 사용자가 직접 한다** (2026-08-31) — 배포 계정·환경변수가 사용자 것이다 · 되살릴 조건: deploy 노드에 도달했을 때
- [ ] **목록 조회 상한이 뱃지 창과 부딪힌다** (2026-08-31, 이전 구현) — 조회 200건 아래에서 창 3일이 176건이었고 하루 신규 ~68건이면 곧 넘는다. 넘으면 뱃지 건수가 조용히 작아진다 · 되살릴 조건: 조회 계층 구현 시
- [ ] **읽으면 뱃지 줄 숫자가 오름차순이 아니게 보인다** (2026-08-31, 이전 구현) — 자리를 전체 건수로 고정한 대가. 의도한 동작이지만 시안에 없던 모습이다 · 2026-09-01 구현에서 그대로 재현된다(더미 16건에서 `가이드 5 · AI 모델 2 · 검색 0 · 논쟁 1` 로 섞여 보인다) · 되살릴 조건: 뱃지 줄 시안을 다시 볼 때
- [ ] **Pretendard 를 CDN 으로 받는다** (2026-09-01) — `layout.tsx` 가 jsdelivr 링크를 쓴다. CDN 이 죽거나 오프라인이면 시스템 폰트로 떨어져 타이포 실측값(자간·줄높이)이 어긋난다. 지금 안 하는 이유: 폰트 파일을 레포에 넣으면 용량이 늘고 `next/font` 배선이 따로 필요한데 급하지 않다 · 되살릴 조건: 배포 준비 시, 또는 폰트가 안 떠서 화면이 달라 보이는 일이 실제로 나면
- [ ] **favicon 이 없다** (2026-09-01) — `/favicon.ico` 가 404 라 콘솔에 에러 한 줄이 남는다. 기능에는 영향이 없지만 "콘솔 에러 0"을 확인할 때마다 이 한 줄을 예외로 세야 한다 · 되살릴 조건: 배포 준비 시
- [ ] **앵커 목록을 `fenceData` 로 내리기** (2026-08-31, 이전 구현) — 렌더 경로 정리 건 · 되살릴 조건: 상세 화면 렌더를 다시 만들 때
- [ ] **"실패는 통과로"가 조용한 오염을 만든다** (2026-08-31, 이전 구현) — 주제 판정이 크레딧 부족으로 39건 실패했고 그 단계는 실패 시 통과시켜(INV-F3) 비주제 글이 섞여 남았다. `alreadyKnown` 때문에 재판정도 안 됐다. 새 Supabase 라 데이터는 리셋되지만 구조는 그대로다 · 되살릴 조건: 수집 파이프라인 구현 시 재판정 경로를 같이 설계
- [ ] **백필은 안 하기로 했다** (2026-08-31, 이전 구현) — 뱃지 도입 전 글에는 뱃지가 안 붙는다(2026-08-30 결정) · 되살릴 조건: 사용자가 옛 글에도 뱃지를 원하면
- [ ] **모델 프로모션 단가가 2026-08-31 에 끝난다** (2026-08-31, 이전 구현) — 비용 추정을 안 고치면 대시보드가 실제보다 싸게 보여준다 · 되살릴 조건: 비용 화면을 만들 때
- [ ] **개발자용 파이프라인 대시보드(`/dev/ingest`)를 배포하려면 관리자 로그인이 먼저다** (2026-08-31) — 지금 설계는 로컬 전용(`NODE_ENV=production` 이면 404). 사용자 결정으로 로그인 없이 배포하지 않는다 · 되살릴 조건: 배포된 사이트에서 이 화면을 쓰고 싶어질 때. 그때는 auth 표면이라 /spec 이 먼저다
- [ ] **이전 signal 코드를 옮길 때 zod 3 → 4 차이를 먼저 본다** (2026-08-31) — signal2 는 zod `^4.5.4` 인데 참고할 이전 구현은 zod 3 으로 쓰였다(`git show ccc8edc:projects/signal/...`). 메이저 변경이라 스키마 코드가 그대로 안 붙는다. 지금 안 하는 이유: 옮길 코드가 아직 0줄이다 · 되살릴 조건: 신뢰 경계 파싱(수집 응답·Supabase 조회)을 처음 구현할 때 — `ingestion-ranking.md`·`content-safety.md` 를 깨우는 시점이다

- [ ] **뱃지 집계 창에 위쪽 상한이 없다** (2026-09-02) — `keywords.ts` 의 `withinWindow` 가 `key >= fromKey` 만 본다. 발행 시각이 미래로 온 글(RSS 타임존 오류는 흔하다)이 미래 날짜 그룹을 만들고, 첫 화면이 하루 단위라 **그 잘못된 하루 하나로 채워진다**. 더미가 `Math.min(at, nowMs - 60_000)` 으로 클램프해서 지금은 이 경로를 못 밟는다 — 즉 더미가 결함을 가려 준다 · 되살릴 조건: Supabase 조회를 붙일 때. 창에 `key <= todayKey` 를 더하고 적재 단계의 미래 시각 처리를 `ingestion-ranking.md` 에 적는다
- [ ] **상세가 이웃 하나 찾으려고 전건을 정렬한다** (2026-09-02) — `app/articles/[id]/page.tsx` 가 `dummyArticles()` 전체를 거르고 정렬해 앞뒤 한 건을 꺼낸다. 16건이라 안 보인다 · 되살릴 조건: 목록이 수백 건이 될 때. 같은 정렬 키 기준 lead/lag 질의로 바꾸거나, 필터·정렬 조건을 그대로 SQL 로 내리는 설계를 함께 정한다
- [ ] **`Article.source` 가 수집 소스와 발행처를 겸한다** (2026-09-02) — `content-selection.md` INV-O1 은 "화면에 보이는 출처는 실제 발행처다, **수집 소스 이름이 아니라** 원문 주소에서 얻는다"인데 더미의 a03·a05·c01 이 `source: "Hacker News"` + `sourceUrl: news.ycombinator.com` 으로 그 반대 모양을 굳히고 있다. INV-L1 의 소스 층(`daily`/`deep`)도 들어갈 칸이 없다 · 되살릴 조건: 스키마를 정할 때. 칸을 둘로 가르는 건 마이그레이션 사안이라 타입에서 먼저 정하는 편이 싸다
- [ ] **「핫이슈」가 코드에서는 정렬이고 스펙에서는 탭이다** (2026-09-02) — `FeedSort = "hot" | "latest"` 는 날짜 그룹 **안의 정렬**이고 거르지 않는데, `keywords-and-kinds.md` INV-G2/G3 의 「핫이슈」는 **탭**이고 문턱을 넘어야 들어간다. 여기에 `isTrending`(「🔥 뜨는 중」)까지 겹쳐 "뜨는 것"을 뜻하는 개념이 셋이다 · 되살릴 조건: 탭을 실제로 만들 때. 그때 `sort: "hot"` 과 `tab: "hot"` 이 같은 이름으로 부딪히므로, 값을 `"score" | "recent"` 로 바꾸는 건 지금이 제일 싸다
- [ ] **조작이 서버 왕복을 타는데 화면에 표시가 없다** (2026-09-02) — 정렬·「더 보기」·뱃지가 `router.replace` 라 `force-dynamic` 서버 렌더를 한 번씩 돈다. `useOptimistic` 덕에 화면은 즉시 바뀌지만 실제 데이터가 오기까지는 옛 목록이다. design-rules 「아직 안 정한 것」의 "오류·로딩 화면"이 이 자리다 · 되살릴 조건: Supabase 를 붙여 왕복이 실제로 느려질 때. 그때 서버에서 거를지(지금 구조가 유리) 클라이언트로 되돌릴지도 같이 정한다
- [ ] **`feed-url.ts` 가 entities 에 있는 것이 맞는지** (2026-09-02) — 파일이 스스로 "피드 **화면**의 상태"라 적고 있고 `days`("펼쳐 둔 날 수")는 `Article` 의 불변식이 아니다. `domain-layers.md` 기준으로는 `features/` 쪽이다. 다만 `sort`·`keyword` 는 `FeedSort`·`badgeKey` 라는 entities 개념에 붙어 있어 반반이고, **지금 버그를 내는 배치는 아니다** · 되살릴 조건: 화면이 하나 더 늘어 같은 상태를 공유하게 될 때
- [ ] **`groupByDay` 가 날짜 키를 글마다 다시 계산한다** (2026-09-02) — `orderForFeed` 가 이미 `{article, dayKey}` 쌍을 만들었다가 버리고, `groupByDay` 가 `Date.parse` + `new Date` 를 또 돈다. 게다가 그 안의 `if (dayKey === null) continue` 는 두 호출처 어디에서도 도달하지 않는다(테스트로 확인) · 되살릴 조건: 목록이 커져 정렬 토글·「더 보기」의 재계산이 눈에 띌 때

## 닫힘

- [x] **briefing 계약 테스트** (2026-08-31 닫힘) — 킷 항목이라 이 파일이 아니라 `docs/references/harness-backlog.md` 가 관리한다. 중복으로 세지 않게 여기서 닫는다.
- [x] **넓은 화면에서 `더 보기` 가 죽은 버튼이 된다** (2026-09-01 닫힘) — 뱃지 줄을 구현하며 `ResizeObserver` 로 넘침을 재게 됐다. 넘친 것이 0이면 버튼을 없애지 않고 `aria-disabled` + 「모두 보임」으로 바꾼다(포커스가 안 튄다). 같은 측정값이 `inert` 도 정한다 — 1280px 접힘 0 · 390px 접힘 4, 둘 다 실제로 잘린 수와 일치.
- [x] **읽은 카드에서 뱃지 두 축이 하나로 합쳐진다** (2026-09-01 닫힘) — 예외를 **안 열기로** 했다. design-rules 2026-08-27 「카드와 상세」가 이미 정한 대로 두 축 모두 `--tag-read-bg`/`--tag-read-ink` 로 내려앉고 호버·포커스에서 복구된다. 축 구분을 읽은 카드에서도 남기려면 「뱃지 중립화」에 예외를 열어야 하는데 그 규칙이 막으려던 것("이미 본 글이 색으로 튀지 않게")과 정면으로 부딪힌다. 실측 4.74:1 로 하한도 통과.
