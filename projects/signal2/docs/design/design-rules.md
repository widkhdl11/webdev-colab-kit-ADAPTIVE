---
status: approved
# 2026-08-06 review 실패로 draft 로 내렸다가, 시안 mockups/article-prose.html 을
# checkpoint 승인받아 복귀. 그때 덧붙인 것이 맨 아래 "2026-08-06 (2)" 블록이다.
#
# 2026-08-31 review 실패로 다시 draft — 접힘의 키보드 처리를 이 문서가 정하지 않아
# 가려진 뱃지 21개가 전부 탭에 잡히고 있었다(390px 실측). 새 블록 "2026-08-31 · 뱃지 줄"
# 로 그 결정을 닫고 같은 날 승인받아 approved 로 되돌렸다.
#
# 2026-08-31 (2) 사용자 요구로 다시 draft — 이 문서에 없는 화면 요소 둘을 더한다:
#   ① 상세 화면 하단의 이전 글 / 다음 글
#   ② 모바일(640px 아래) 하단 고정 조작 줄
# **뒤집는 결정은 없다.** "2026-08-06 · 목록을 늘리는 버튼"과 "2026-08-02 · 날짜 그룹"은
# 그대로다 — 「더 보기」가 한 번에 불러오는 양이 카드 9장에서 하루치로 바뀔 뿐이고,
# 그 양은 이 문서가 정한 적이 없다(구현이 고른 값이다).
# 2026-09-01 시안 mockups/article-nav-and-mobile-dock.html 을 checkpoint 승인받아
# 맨 아래 "2026-09-01" 블록을 덧붙이고 approved 로 되돌렸다.
---

# design-rules — signal

승인된 시각 기준만 모아둔다. 화면을 만들 때는 이 문서가 근거다.
새 화면이 여기 없는 요소를 필요로 하면, 그 부분만 시안으로 확인받고 블록을 덧붙인다.

근거 문서: [INTERVIEW.md](INTERVIEW.md)(피드+상세 상담 확정본) ·
[INTERVIEW-badge-row.md](INTERVIEW-badge-row.md)(뱃지 줄 상담 확정본) ·
[refs/biko-style.md](refs/biko-style.md)(레퍼런스 정찰)
승인된 시안: [mockups/feed-a.html](mockups/feed-a.html) · [mockups/article.html](mockups/article.html) ·
[mockups/feed-card-density.html](mockups/feed-card-density.html)(카드 밀도 A+B) ·
[mockups/article-prose.html](mockups/article-prose.html)(원문 본문 타이포 — 앞의 두 장에 있던
포커스 라운드 결함을 고친 판이다. 충돌하면 이쪽이 최신) ·
[mockups/no-summary-and-original-title.html](mockups/no-summary-and-original-title.html)
(요약 없는 카드 · 원문 제목 병기 자리 — A/B/C 비교안) ·
[mockups/official-badge.html](mockups/official-badge.html)(공식 표시 두 종류 — B 채택) ·
[mockups/badge-row.html](mockups/badge-row.html)(뱃지 키워드 줄 — 색 A/B 중 **B 채택**.
고정 5개 칩을 대체한다. 충돌하면 이쪽이 최신)

---

## 2026-08-02 · 피드 + 소식 상세 · checkpoint 승인

첫 시각 방향이다. 이후 화면은 특별한 사정이 없는 한 이 언어를 물려받는다.

### 한 줄

**깔끔한 뼈대에 귀엽고 가벼운 디테일.** 가벼운 SNS 피드처럼 훑되 피로감은 낮게.
발랄함은 캐릭터나 일러스트가 아니라 **포인트색·라운드·작은 움직임**에서 나온다.

### 색

라이트 전용. 다크 모드는 지금 범위가 아니다.

| 토큰            | 값        | 쓰임                                |
| --------------- | --------- | ----------------------------------- |
| `--bg`          | `#EFEFF5` | 페이지. 라벤더 기가 도는 옅은 회색  |
| `--surface`     | `#F8F8FC` | 카드·요약 박스. 페이지보다 **밝다** |
| `--surface-h`   | `#FDFDFF` | 호버. 순백 직전까지만               |
| `--sunken`      | `#E5E5EE` | 코드 블록. 글 흐름에서 눌러 앉는 면 |
| `--hairline`    | `#E2E2EC` | 헤더 밑선                           |
| `--dot`         | `#C9C9D6` | 점선 구분선                         |
| `--text`        | `#1F2024` | 본문. 순검정은 쓰지 않는다          |
| `--text2`       | `#4A4E55` | 요약문                              |
| `--meta`        | `#656A72` | 출처·시각 등 메타                   |
| `--accent`      | `#4F46E5` | 포인트색(인디고)                    |
| `--accent-soft` | `#E9E8FB` | 뱃지 배경                           |

**순백 배경은 쓰지 않는다.** 매일 훑는 리더라 눈부심이 그대로 비용이 된다.
레퍼런스(biko)는 흰 페이지에 회색 카드였지만 우리는 명암을 뒤집었다 — 회색 페이지에 밝은 카드.
부수효과로 카드 분리가 더 뚜렷해졌다.

**포인트색은 면적을 최소로.** 활성 상태, '뜨는 중' 뱃지, 로고 점, 링크 정도에만 닿는다.
전면 배경으로 까는 것은 금지. 2차 강조색(초록 등)도 두지 않는다.

### 형태와 입체감

- 카드 라운드 **12px**, 칩·버튼·세그먼트 토글은 **완전 알약(999px)**, 이미지 8px
- **입체감은 "아직 안 봄"의 신호다.** 안 읽은 카드에만 아주 옅은 그림자를 두 겹으로 준다:
  `0 1px 2px rgba(31,32,36,.05)`(경계를 또렷하게) + `0 4px 10px rgba(31,32,36,.05)`(면을 살짝 띄우기).
  읽은 카드는 그림자가 없다. 호버는 `0 2px 4px rgba(31,32,36,.06)` + `0 10px 22px rgba(31,32,36,.08)`
  에 `translateY(-1px)`
  > 처음에는 레퍼런스를 따라 "그림자 없이 면으로만 분리"로 갔다가, 읽음/안읽음 구분이 약해
  > 이 규칙으로 대체했다. 입체감이 상태를 나르는 쪽이 흐릿함과 같은 방향으로 겹쳐 작동한다.
- 테두리 선은 거의 쓰지 않는다. 면과 그림자로 나눈다

### 타이포

- **Pretendard** 하나. 코드 블록만 모노
- **크기 단계를 늘리지 말고 굵기로 위계를 만든다.** 굵기 800·700·600·500을 먼저 쓴다
- 실제 단계(2026-08-06 구현하며 실측해 정정): 28 상세 제목 / 22 피드 제목 / 20 본문 소제목 /
  19 로고 / 17 상세 본문 / 16 카드 제목·모바일 본문 / 15 기본 글자·요약 박스 / 14 버튼 /
  13.5 카드 요약·코드 블록·날짜 제목 / 13 태그라인·컨트롤 칩 / 12.5 건수·주석 / 12 메타 /
  11.5 뱃지·태그 칩
  > 승인 시점에 문서는 다섯 단계라고 적었지만 승인된 시안이 이미 이만큼 쓰고 있었다.
  > 다음에 단계를 **늘리지 않기 위해** 실제 값을 적어둔다. 13과 12.5는 육안 구분이 어려워
  > 합칠 후보지만, 승인된 시안을 바꾸는 일이라 지금은 그대로 둔다.
- **본문 16px 하한의 예외: 카드 요약 13.5px.** 목록 미리보기는 본문이 아니다 —
  진짜 본문(상세 17px)은 하한을 넘는다. 반대로 화면에 문장 하나만 남는 자리
  (빈 상태 등)는 미리보기가 아니므로 15px 이상을 쓴다
- 초대형 헤드라인 금지. 피드 페이지 제목도 22px/700까지
- 상세 본문은 **17px, 줄높이 1.78, 읽기 컬럼 704px** — 이 사이트의 핵심 가치가 "편하게 읽기"다

### 레이아웃

- 최대 폭 **1168px** 중앙 정렬. 카드 그리드 3열, gap 16px, 카드 안쪽 padding 16px
- 데스크톱 우선. 980px에서 2열, 640px에서 1열
- 상단 헤더는 아주 얇게. 네비 메뉴는 두지 않는다(2페이지짜리 개인용)

### 시그니처 (이 사이트를 알아보게 하는 것)

1. **카드 안 점선 구분선** — 제목·요약 블록과 아래 메타 줄을 나눈다. 상세에서도 이어 쓴다
2. **알약 필터 칩 줄** — 정렬 세그먼트 토글 + 주제 태그 칩이 목록 위에 나란히
3. **뱃지로 압축한 정보** — '뜨는 중', 태그 칩

### 상태 규칙

- **읽음**: 카드 채움을 페이지 쪽으로 눕히고(`#F3F3F8`), 제목 `#666B73`/굵기 600, 요약·메타 `#6A6F77`,
  태그 칩 글자 `#63676F`, 그림자 제거.
  **흐릿하게 = 눌러두는 것이지 못 읽게 만드는 게 아니다** — 읽은 항목도 대비 4.5:1을 지킨다
- **호버·포커스에서 완전 복구**: 읽은 카드도 색·굵기·입체감이 안 읽은 카드와 같아진다.
  다시 읽으려는 순간에는 또렷해야 한다. 키보드 이동(`:focus-visible`)도 같다
- **뱃지 중립화**: 읽은 카드의 '뜨는 중' 뱃지는 포인트색을 잃는다.
  색은 "아직 안 본 것"에만 쓴다
- 상태를 색으로만 알리지 않는다. 읽음에는 스크린리더용 텍스트를 함께 둔다

### 날짜 그룹

수집이 매일 아침 한 번 도는 배치라, **날짜가 1차 묶음이고 정렬(뜨는순/최신순)은 그 안에서 2차**다.
점수가 날짜를 가로지르면 "오늘 뭐 왔지"를 볼 수 없다.

- 섹션 헤더는 절제한다: 13.5px, 메타색 + 굵기. 옆에 그날 건수를 작게(`오늘 · 5건`),
  남는 폭은 시그니처인 점선으로 채운다
- 제목 계층은 h1(페이지) → h2(날짜) → h3(카드)
- '뜨는 중' 뱃지는 전역 상위가 아니라 **그룹 안에서의 상위**에 붙는다

### 접근성

- 텍스트 대비 **4.5:1 이상**. 포커스 스타일을 제거하지 않는다
- 이건 장식 규칙이 아니라 실제로 설계를 몇 번 바꿨다:
  레퍼런스의 메타색 `#8A8F95`는 우리 카드 위에서 2.95:1이라 탈락했고,
  읽음 상태를 더 흐리게 하려다 색으로는 바닥에 막혀 **굵기까지 낮추는 쪽**으로 풀었다.
  태그 칩은 배경이 카드보다 어두워서 글자색을 따로 뒀다
- 현재 최저값 4.57:1(읽은 카드의 요약·메타). **더 흐리게 하려면 색이 아닌 수단을 써야 한다** —
  카드를 접어 부피로 줄이는 방식이 다음 후보다
- 움직임은 `prefers-reduced-motion`을 존중한다

### 하지 않는 것

- 포인트색 전면 배경(히어로·네비 풀블리드)
- 초대형 헤드라인(60px 굵은 제목류)
- 마스코트 캐릭터·3D 이모지 일러스트 — 귀엽되 유치하지 않게. 작은 이모지 포인트까지만 허용
- 복잡하고 산만한 포털 룩: 사이드바·배너·위젯 나열. 한 줄기 피드 + 최소 컨트롤만
- 카드 제목을 한 줄에서 `…`로 자르는 것 — 2줄까지 허용한다
- 순백 배경, 2차 강조색

### 아직 안 정한 것

- 다크 모드 (범위 밖)
- 오류·로딩 화면 — 필요해질 때 시안으로 확인받고 여기 덧붙인다
  (빈 상태는 2026-08-06 블록에서 확정)
- 모바일 전용 조정 — 지금은 1열로 접히는 수준까지만 봤다

---

## 2026-08-06 · 피드 구현 중 확정 · 사용자 승인

시안에는 없었지만 실제로 만들다 보니 정해야 했던 것들. ui-reviewer 지적을 사용자가
승인해 덧붙인다. 새 시각 방향이 아니라 위 언어 안에서의 마감이다.

### 빈 상태

주제 필터 결과가 0건일 때. **문장 한 줄, 15px, 메타색, 중앙 정렬, 위아래 여백 56px.**
일러스트·아이콘·재시도 버튼을 두지 않는다 — 필터를 바꾸면 끝나는 상황이라
화면이 사건처럼 보이면 과하다. (오류·로딩은 여전히 미정)

> 2026-08-06 (2) 덧붙임 — **문구는 두 가지다. 형태는 위와 같다.**
> 필터가 켜져 있으면 "이 주제로 모인 소식이 아직 없습니다",
> 전체가 0건이면 "아직 모인 소식이 없습니다".
> 켜지도 않은 필터를 탓하면 화면이 엉뚱한 것을 가리킨다.
> 404 화면도 같은 언어를 쓴다(문장 한 줄 + 피드로 돌아가는 링크).

### 메타 줄 태그 칩

카드 아래 메타 줄의 칩은 **2개까지.** 초과분은 `+N` 칩 하나로 접고,
접힌 태그 이름은 스크린리더용 텍스트로 함께 둔다.

> 칩을 그냥 자르면 화면이 거짓말을 한다 — 그 태그로 필터하면 카드는 나오는데
> 칩은 안 보인다. 상세 화면은 폭이 넉넉하므로 태그를 전부 보여준다.

### 입체감이 나르는 상태를 가리지 않기

상단 헤더가 sticky 라, 키보드로 이동해 온 카드·날짜 섹션은 `scroll-margin-top: 72px`
를 둬서 헤더 밑에 깔리지 않게 한다. "읽은 카드도 포커스에서 완전 복구된다"는 규칙은
포커스 링이 화면에 보여야 성립한다.

### 목록을 늘리는 버튼

"더 보기"는 **다 불러온 뒤에도 사라지지 않는다.** 문구를 "모두 불러왔습니다"로 바꾸고
`aria-disabled` 로 표시하되 포커스는 그대로 둔다 — 버튼이 사라지면 키보드 사용자는
방금 불러온 카드가 아니라 페이지 맨 위로 되던져진다. 늘어난 건수는 화면 밖으로도 알린다.

### 카드 밀도 — A+B 채택 (비교 시안 승인)

사용자 피드백: "너무 글자글자글자~ 한 느낌". 비교 시안
[mockups/feed-card-density.html](mockups/feed-card-density.html) 에서 A(숨 쉴 곳)+B(출처 표식)를 골랐다.
C(위계 벌리기)는 타이포 단계를 하나 더 늘리는 대가가 있어 보류 — A+B로 부족하면 그때 얹는다.

- **어절 단위 줄바꿈**: 카드 제목·요약에 `word-break: keep-all`. 상세 본문에는 원래 있던 규칙인데
  카드에만 빠져 있었다. 한국어가 어절 중간에서 끊기는 것이 밀도 체감을 키우던 주범이다
- **요약은 2줄까지** (3줄에서 줄임). 3줄이면 카드 절반이 회색 덩어리가 된다
- **카드 안쪽 여백 20px** (16px에서 키움), 제목 아래 10px, 요약 아래 16px, 발 위 14px
- **출처 표식**: 메타 줄 맨 앞에 지름 20px 원. 한글 출처는 첫 한 글자, 로마자는 두 글자
  (한 글자면 GitHub 과 Google 이 구별되지 않는다). 10.5px/700, 배경은 태그 칩과 같은 계열
  - **색은 쓰지 않는다.** 포인트색은 "아직 안 본 것" 전용이라는 규칙을 그대로 지킨다
  - 읽은 카드에서는 태그 칩과 같이 중립색으로 내려가고, 호버·포커스에서 함께 복구된다
  - 출처 이름을 줄인 것뿐이라 스크린리더에는 `aria-hidden` — 옆에 전체 이름이 이미 있다
- 이때 `--fs-mark: 10.5px` 가 추가됐다. 위 타이포 단계 목록에서 **가장 작은 단계**이고
  출처 표식 원 안에서만 쓴다

### 실측 대비 (2026-08-06 재계산)

피드 18개 조합 최저 **4.57:1**(읽은 카드 요약·메타), 상세 13개 조합 최저 **4.75:1**.
새 토큰 `--on-accent`(포인트색 면 위 흰 글자) 6.29:1, `--code-ink`(코드 면 위 글자) 9.78:1.

---

## 2026-08-06 (2) · 원문 본문 타이포 확장 + 요약 크기 · checkpoint 승인

승인된 시안: [mockups/article-prose.html](mockups/article-prose.html)

review 사인오프가 실패해 되돌아온 자리다. 새 시각 방향이 아니라 **이미 승인된 언어의 빈칸 메우기**다.
빈칸은 두 종류였다 — 문서가 스스로 모순인 곳, 그리고 스펙이 허용하는데 시각 기준이 없는 곳.

### AI 요약 박스는 16px (15px 에서 올림)

`ui-layers` 의 "본문 16px 이상" 하한을 요약 박스도 지킨다. 이 문서 65행이 "15 기본 글자·요약 박스"라고
적고 71~73행은 하한 예외를 "카드 요약 13.5px" 하나만 인정해 **서로 다른 말을 하고 있었다.** 카드 요약이
예외인 근거는 "목록 미리보기는 본문이 아니다"인데, 상세의 요약 박스는 미리보기가 아니라 실제로 읽는
여러 문장이다. 그러니 예외에 해당하지 않는다 — 올리는 쪽이 맞다.

대가: 원문 본문이 17px 이라 이제 **1px 차이밖에 안 난다.** 크기로는 더 이상 구분되지 않으므로
위계를 네 축으로 옮긴다. 넷이 같이 작동해야 갈라진다.

| 축   | 요약 박스                                   | 원문                                      |
| ---- | ------------------------------------------- | ----------------------------------------- |
| 면   | `--surface` (페이지보다 밝은 카드면)        | 페이지 배경 위 맨몸                       |
| 색   | `--text2`                                   | `--text` (원문이 더 진하다 = 원문이 본체) |
| 형태 | 라운드 12px + 왼쪽 4px 포인트선 + 라벨 알약 | 테두리 없음                               |
| 질감 | 줄높이 1.70 (조여서 "블록")                 | 1.78 (벌려서 "흐름")                      |

### 하한이 걸리는 자리와 안 걸리는 자리

혼란의 원인이 예외 목록이 짧았던 것이므로 여기서 경계를 분명히 한다.

- **16px 하한 대상(본문)**: 상세 원문 17px, AI 요약 박스 16px, 모바일 본문 16px
- **하한 대상이 아닌 것(부속)**: 문장에 딸린 덧말 `small` 15px, 그림 설명 `figcaption` 12px,
  위·아래 첨자 12px, 메타 줄·뱃지·칩. **문장 단위로 읽는 글이 아니라 문장에 붙는 부속**이다
- **기존 예외 유지**: 카드 요약 13.5px (목록 미리보기)

### 원문 본문의 제목 h1~h6

sanitize 는 `h1`~`h6` 를 전부 통과시키는데(content-safety INV-D2) `.prose` 에는 `h2` 값만 있었다.
그래서 원문에 `h3` 가 오면 Preflight 리셋 그대로 **본문보다 작고 굵기도 없는 글**로 나왔다.

크기 단계를 늘리지 않는다(63행). 크기는 **20 / 17 두 값만** 쓰고 굵기·색·여백으로 6단계를 만든다.

| 태그 | 크기 | 굵기 | 색                   | 위 여백 |
| ---- | ---- | ---- | -------------------- | ------- |
| `h1` | 20px | 800  | `--text` + 점선 밑선 | 2.4em   |
| `h2` | 20px | 700  | `--text`             | 2.1em   |
| `h3` | 17px | 700  | `--text`             | 2em     |
| `h4` | 17px | 600  | `--text`             | 1.8em   |
| `h5` | 17px | 600  | `--text2`            | 1.6em   |
| `h6` | 17px | 600  | `--meta`             | 1.5em   |

- **원문 `h1` 을 20px 로 누른 이유**: 페이지 제목(28px)은 우리가 붙인 것이고 원문 안의 `h1` 은
  출처가 쓴 것이다. 28px 로 나오면 "이 글의 제목이 둘"로 보인다. 크기는 `h2` 와 같이 두고
  굵기 800 + 시그니처 점선 밑선으로 "구획의 시작"만 알린다
- **`h3`~`h6` 이 본문과 같은 17px 인 이유**: 17 아래는 16→15 인데 제목이 본문보다 작아지면 위계가
  뒤집힌다. 17 위로 열면 63행 위반. 남는 길은 같은 크기에서 굵기·색으로 나누는 것뿐이다
- **`h4`·`h5`·`h6` 이 굵기 600 으로 같고 색만 다른 이유**: 500 은 본문 400 과 붙어 제목으로 안 읽힌다.
  세 번째 축을 색으로 열었고 `--text → --text2 → --meta` 는 이미 쓰는 사다리(제목→요약→메타)다
- 17px/600 제목은 굵은 문장과 헷갈릴 수 있다. **위 여백을 문단 간격보다 확실히 넓게** 잡아
  "새로 시작한다"를 여백이 나르게 한다

### 목록 불릿을 되살린다

Tailwind Preflight 가 `ol, ul { list-style: none }` 으로 마커를 지우는데 `.prose` 가 복구하지 않아
**점도 번호도 안 보이는 상태였다.** 마커 색만 `--meta` 로 정해두고 정작 칠할 마커가 없었다.

- `ul` = `disc`, `ol` = `decimal`. 중첩 2단계까지 모양을 달리한다(`circle` / `lower-alpha`)
- 마커 색은 `--meta` 로 절제. 마커가 글자보다 튀면 목록이 장식처럼 보인다
- AI 요약 박스 안의 목록도 같다

### 표

- 셀 **16px**. 본문 17px 그대로면 열이 넓어져 가로 스크롤이 상시로 생긴다
- 헤더 면은 `--surface`, 글자는 `--text`/700. `--sunken`(코드 면)을 쓰면 `--meta` 가 **4.35:1 로 미달**이고
  면이 어두워 표가 코드 블록처럼 보인다
- 선은 거의 쓰지 않는 규칙대로 **세로줄 없음**, 가로 구분만 점선(`--dot`)
- 머리글은 `white-space: nowrap`, 표 자체는 `min-width: 480px` — 더 좁아지면 한글이 글자 단위로 부서진다

### 가로로 넘치는 것은 키보드로도 닿아야 한다

표와 코드 블록은 읽기 컬럼 704px 을 넘긴다. **페이지가 통째로 밀리면 안 되고**, 스크롤 영역이
마우스로만 닿아도 안 된다(키보드 사용자는 오른쪽을 영영 못 본다).

- 표는 `overflow-x: auto` 껍데기로 감싸고 `tabindex="0"` + `role="region"` + `aria-label`
- 코드 블록 `pre` 도 `tabindex="0"`
- **이 껍데기는 sanitize 결과에 없다** — 원문에는 class 를 붙일 수 없으므로(속성이 전부 제거된다)
  렌더 계층(html-react-parser)이 끼워 넣어야 한다

### 문장 안의 표식들

- **`mark`**: `--accent-soft` 면 + `--text` 글자. 2차 강조색 금지라 노랑을 못 쓴다.
  글자를 `--accent` 로 하면 링크로 오인되므로 면만 빌린다
- **`u`**: 이 화면에서 밑줄+포인트색은 링크의 표식이다. `u` 는 글자색을 두고 밑줄만 `--dot` 으로 눕혀 분리
- **`s`**: 지워진 내용이므로 `--meta` 로 한 칸 내린다
- **`small`**: 15px + `--text2`
- **`span`**: **아무 스타일도 주지 않는다.** sanitize 가 class·style 을 버려 빈 껍데기로 남으므로,
  여기 효과를 주면 원문이 의도하지 않은 강조가 생긴다
- **`hr`**: 점선(`--dot`). 이 사이트의 구획선은 전부 점선이라 실선만 이물감이 생긴다

### 포커스에서 형태가 변하지 않게 (기존 규칙 정정)

전역 포커스 스타일에 있던 `border-radius: var(--r-focus)` 를 **뺀다.** `a:focus-visible` 의 명시도가
`.card` 를 이겨서, 키보드 포커스가 닿는 순간 **카드가 12px→6px, 알약 버튼이 999px→6px 로 각지게
변하고 있었다.** 94~95행("호버·포커스에서 완전 복구… 키보드 이동도 같다")과 정면으로 어긋난다.

빼면 아웃라인이 각 요소 자신의 라운드를 따라간다. 라운드가 없는 인라인 링크는 사각 아웃라인이
나오고 그게 정상이다. 승인된 시안 두 장(feed-a·article)도 같은 결함을 갖고 있었으므로
[mockups/article-prose.html](mockups/article-prose.html) 에서 고쳤다 — 안 고치면 다음 화면이 물려받는다.

### 새 토큰

`--r-inline: 6px` — 글 흐름에 박히는 인라인 칩(`code`·`mark`)의 라운드. 지금 `code` 라운드가 6px 로
하드코딩돼 있는데 `mark` 가 들어오며 같은 값이 두 곳이 된다. 값은 기존과 같아 보이는 것은 안 바뀐다.

### 앞선 블록의 문서 누락 정정 (값 변경 아님)

구현이 승인된 시안을 따랐는데 이 문서 목록이 그걸 못 따라간 것들. **새 결정이 아니라 기록 보정이다.**

- 타이포 단계 목록(64~67행)에 **모바일 ≤640px: 상세 제목 23px, 본문 16px** 추가 (시안 `article.html` 승인분)
- 66~67행의 "11.5 뱃지·태그 칩" 은 두 자리를 뭉쳤다 →
  **11.5 뱃지·카드 태그 칩 / 12 메타·상세 태그 칩** 으로 나눈다. 12 는 이미 있는 단계라 단계가 늘지 않는다

### 실측 대비 (2026-08-06 (2) 계산 — 시안 CSS 값 기준)

상세 화면 12개 조합 최저 **4.75:1**(`--meta` on `--bg`: `h6`·`s`·`figcaption`·마커)로 기존 하한 유지.
`--text` on `--bg` 14.21 · `--text2` on `--bg` 7.30 · `--accent` on `--bg` 5.49 ·
`--text` on `--accent-soft` 13.49(mark) · `--text` on `--surface` 15.36(표 헤더) ·
`--text2` on `--surface` 7.89(요약 본문) · `--meta` on `--surface` 5.14(요약 주의문구).

**쓰면 안 되는 조합: `--meta` on `--sunken` = 4.35:1 미달.** 이 계산이 표 헤더 면을 바꿨다.

> 이 수치들은 CSS 값 계산이지 브라우저 실측이 아니다(해당 세션에 Playwright 미연결).
> 다음에 Playwright 가 붙으면 렌더 상태에서 재확인할 것.

---

## 2026-08-08 · 우리 제목과 원문 제목 가르기 + 이미지 자리 예약 · checkpoint 승인

승인된 시안: [mockups/article-prose.html](mockups/article-prose.html) (같은 파일에 덧그렸다)

review 사인오프 직전에 남아 있던 결정들을 처리한 자리다. 새 시각 방향이 아니라
**이미 승인된 언어 안의 마감**이라 시안 루프를 새로 돌지 않고 기존 시안에 반영해 확인받았다.

### 상세 화면에는 `h1` 이 둘이고, 그대로 둔다

페이지 제목은 우리가 붙인 것이고 원문 안의 `h1` 은 출처가 쓴 것이다. 파서에서 강등(h1→h2)하는
안을 검토했지만 **채택하지 않았다** — HTML5 에서 `h1` 이 여럿인 것은 위반이 아니고, 강등은
문서 개요를 위해 원문 마크업을 우리가 고쳐 쓰는 일이다. 대신 **눈으로 갈라지게** 만든다.

대가는 정직하게 적어둔다: 스크린리더의 제목 목록에서 원문 제목이 페이지 제목과 같은 층으로
보인다. 색과 세로선은 시각 신호라 그 목록에는 안 나타난다.

### 포인트색 면적 규칙을 넓힌다 (2026-08-02 "색" 절 정정)

그 절은 포인트색이 "활성 상태, '뜨는 중' 뱃지, 로고 점, 링크 정도에만 닿는다"고 적었다.
여기에 **상세 화면의 페이지 제목**을 더한다.

> 이 문서의 기존 참조들이 줄 번호를 쓰는데(63행·71~73행 등) 문서가 자라면서 이미 몇 줄씩
> 어긋나 있다. 이 블록부터는 **절 이름으로 가리킨다** — 줄은 움직여도 절 이름은 안 움직인다.

- **`--accent` 는 "우리가 붙인 것"의 표식이다.** 상세 화면에서 제목이 포인트색이면 우리 것,
  먹색이면 출처가 쓴 것. 원문 안의 `h1`~`h6` 에는 포인트색을 쓰지 않는다
- 피드의 "색은 '아직 안 본 것'에만 쓴다"("상태 규칙" 절)와 부딪히지 않는다 — 상세는
  **여는 순간 읽음이 되는** 화면이라 안 읽음/읽음 구분 자체가 없다. 두 규칙이 서로 다른 화면에서 산다
- 대비 `--accent` on `--bg` = **5.49:1**. 링크가 이미 쓰던 조합이라 새 하한이 생기지 않았다
- 넓히는 것은 여기까지다. "하지 않는 것" 절의 **포인트색 전면 배경 금지**는 그대로다

### 원문 시작을 구획선으로 선언한다

"아래는 출처에서 가져온 원문입니다" 줄 **위에** 점선 한 줄. 안내문은 굵기 600 으로 한 칸 올리고
아래 여백을 24px 로 키운다. 크기·색은 그대로(12px, `--meta`) — 읽는 문장이 아니라 안내다.

- **아래쪽에는 선을 두지 않는다.** 위아래를 다 막으면 안내문이 상자에 갇히고, 이 화면은 이미
  점선을 여러 곳(메타 줄·요약 주의문구·원문 h1·hr)에서 쓰고 있어 한 줄만 더해도 충분하다
- **요약이 없으면 이 선을 뺀다.** AI 요약 박스는 조건부라(생성 실패 시 안 그린다) 그때 원문은
  메타 줄 바로 뒤에 오는데, 메타 줄이 이미 아래에 점선을 갖고 있다. 선이 둘 나란히 서면
  실수처럼 보인다 (`.metaRow + .prose { border-top: 0 }`)

### 원문 안 `h1`·`h2` 에 왼쪽 세로선

`--dot` 2px, 안쪽 여백 14px, **본문 칼럼 바깥으로 16px 내민다**(`margin-left: -16px`).

- **내미는 이유**: 안쪽으로 들여쓰면 제목이 본문보다 오른쪽에서 시작해 위계가 뒤집혀 보인다.
  내밀면 선만 밖에 서고 글자는 본문과 정확히 같은 자리에서 시작한다(2px + 14px = 16px).
  `.read` 의 좌우 여백이 20px 이라 모바일에서도 화면 밖으로 나가지 않는다
- **`h3`~`h6` 에는 두지 않는다.** 깊어질수록 장식이 줄어드는 쪽이 자연스럽고,
  17px 제목에까지 선이 붙으면 원문이 온통 세로줄이 된다
- **선은 본문 칼럼 최상위 제목에만.** 인용문·목록·표 **안**의 제목에서는 선을 접는다.
  내밀기의 근거("`.read` 의 여백 20px 안에 들어간다")가 최상위에서만 성립하고, 그 안에서는
  인용문의 4px 선에 딱 붙어 6px 실선처럼 보이거나 표의 가로 스크롤에 잘린다.
  거기서는 이미 부모가 구획을 나르고 있다

**이 화면의 왼쪽 세로선은 셋이다.** 넷째를 발명하지 않도록 적어둔다:

| 자리 | 색 | 두께 | 위치 |
| --- | --- | --- | --- |
| AI 요약 박스 | `--accent` | 4px | 상자를 감싼다(우리가 만든 것) |
| 원문 인용문 | `--dot` | 4px | 안으로 들여쓴다 |
| 원문 `h1`·`h2` | `--dot` | 2px | 칼럼 **바깥**으로 내민다(출처가 쓴 것) |

인용문이 제목보다 두꺼운 같은 색 선을 쓰는 건 구조상 거꾸로지만, 내밀기/들여쓰기로 갈리고
승인된 시안에 둘이 같이 그려져 있어 그대로 둔다.

### 원문 이미지는 도착 전에 자리를 잡는다

지금까지는 `height: auto` 뿐이라 **로드 전 높이가 0** 이었고 이미지가 뜨는 순간 아래 글이 통째로 밀렸다.
예약을 두 갈래로 나눠 맡긴다:

- **원문이 치수를 들고 온 이미지 → 브라우저 기본 동작에 맡긴다.** `.prose img` 에 `aspect-ratio` 를
  두지 않는다. 그 속성이 로드 전 상자(비율 + 폭)를 정한다
- **안 들고 온 이미지 → `.prose img:not([width]):not([height])` 에 `aspect-ratio: 16 / 9`**
- 원문 치수는 sanitize 가 조건을 통과시킨 값만 남긴다(content-safety INV-D2 · S10~S13).
  치수가 거부되면 이 화면에서는 위 대체 비율이 그대로 받는다 — 선택자가 `[width]` 유무만 보므로
  자동으로 맞물린다
- 남는 대가: 치수 없는 이미지가 16:9 에서 멀면 로드 후 한 번 조정된다. 높이 0 에서 튀어오르는 것보다 작다
- `display: block` 은 장식이 아니라 **예약의 전제**다. 인라인이 되면 폭이 칼럼을 안 채워 예약 높이가 같이 줄어든다

> **한 줄(`aspect-ratio: auto 16/9`)로 두 경우를 덮으려다 되돌렸다.** 그러면 작성자 선언이
> 치수에서 온 값을 덮는지에 결과가 걸리는데(캐스케이드에서 표현 힌트와 작성자 스타일의 우열),
> 이번 세션에 브라우저가 없어 실측하지 못했다. 선택자를 가르면 **어느 해석이 맞든 결과가 같다** —
> 답을 몰라도 되게 만드는 쪽을 골랐다. code-reviewer 가 이 자리를 HIGH 로 지적했다.

### 실측 대비

**새로 생긴 텍스트 색 조합이 없다.** 페이지 제목이 쓰는 `--accent` on `--bg`(5.49:1)는 링크가
이미 쓰던 조합이고, 나머지 변경은 선·여백·굵기·비율이라 색을 건드리지 않는다. 하한은 **4.75:1** 유지.

> 이 세션도 Playwright 가 붙지 않아(MCP·로컬 모두 부재) **브라우저 실측을 못 했다.**
> 위 수치는 CSS 값 계산이다. 2026-08-06 (2) 에 이어 두 세션 연속이라, 실측이 필요한
> 변경을 하기 전에 Playwright 연결을 먼저 해결하는 편이 낫다.

---

## 2026-08-10 · 요약 없는 카드 · 원문 제목 병기 자리 · checkpoint 승인

승인된 시안: [mockups/no-summary-and-original-title.html](mockups/no-summary-and-original-title.html)

**둘 다 이미 화면에 나가 있던 것을 뒤늦게 확인받은 자리다.** 실제 데이터를 넣고서야 생긴
상태(요약이 없는 소식)와, 제목 번역(INV-S6)을 붙이며 생긴 줄인데 시각 기준이 없었다.
새 시각 방향이 아니라 승인된 언어 안의 마감이라 A/B/C 비교안 한 장으로 확인했다.

### 요약이 없는 카드는 그대로 둔다 (빈칸을 메우지 않는다)

`displaySummary` 가 AI 요약도 출처 요약글도 못 찾으면 카드에서 요약 문단이 통째로 빠진다
(2026-08-10 실측: 101건 중 9건). `.cardBody` 가 `flex: 1 1 auto` 라 점선 발은 카드 바닥에
붙고 제목 아래가 빈칸으로 남는다. **그 빈칸을 그대로 둔다.**

- 카드 높이는 그리드가 같은 행끼리 맞춘다 — 어느 안을 골라도 **점선 발 위치는 안 흔들린다.**
  시그니처(카드 안 점선)가 걸린 문제가 아니었다
- 검토한 두 대안과 시안에서 잰 결과:
  - **요약 없을 때만 제목 3줄 허용**: 잘렸던 제목이 펴져 빈칸이 81px→58px 로 줄지만,
    제목이 짧은 카드에서는 104px 그대로다. 되는 카드와 안 되는 카드가 갈린다
  - **"요약을 준비하지 못했습니다" 한 줄**: 빈칸이 49px 까지 줄지만 같은 문장이 9개 카드에
    반복되고, 지금 요약이 없는 항목은 **근거가 없어 호출 자체를 건너뛴 것**이라
    (`skippedNoEvidence`) 재시도해도 안 생긴다 — 곧 채워질 것처럼 읽히는 문구가 사실과 다르다
- 빈칸은 "요약이 없다"를 그대로 보여준다. **빈 상태 규칙**("일러스트·재시도 버튼을 두지 않는다")과
  같은 방향이다 — 없는 것을 말로 채우지 않는다
- 다시 볼 조건: 요약 없는 항목이 지금처럼 열에 하나 수준이 아니라 절반을 넘어가면,
  그때는 빈칸이 화면의 기본 상태가 되므로 이 결정을 다시 확인받는다

### 원문 제목 병기 줄은 메타 줄 안으로

번역 제목을 보여주므로(INV-S6) 대조할 원문이 필요하다. 그 줄을 **제목 바로 밑이 아니라
출처·시각과 같은 묶음(메타 줄)에 둔다.** 메타 줄 안에서 한 줄을 통째로 차지한다.

- **왜 제목 밑이 아닌가**: 제목 바로 밑 6px 에 붙으면 우리가 붙인 제목과 한 덩어리로 보인다.
  이 화면은 "우리가 붙인 것 = 포인트색 / 출처가 쓴 것 = 먹색"으로 가르는 중인데
  (2026-08-08 "포인트색 면적 규칙"), 원문 제목은 출처가 쓴 것이라 제목 묶음에 들어가면 안 된다
- **라벨 "원문 제목:" 을 유지한다.** 제목에서 떨어져 나온 만큼 이 줄이 무엇인지는 라벨만 나른다
  (라벨을 빼는 안도 같이 봤고, 원문인지 부제인지가 문맥에만 기대게 되어 채택하지 않았다)
- 크기·색은 그대로 12px `--meta`. 읽는 문장이 아니라 대조용 참고다
- 여백은 메타 줄의 `gap`(10px)이 준다 — 여기에 `margin` 을 또 주면 둘이 더해진다.
  이전의 `margin-top: -6px` 은 제목과의 간격을 맞추려던 값이라 함께 없앴다
- 메타 줄 높이가 46px → 74px 로 한 줄 두꺼워진다. 대신 제목 아래가 비어 h1 이 홀로 선다
- **번역이 없으면 이 줄은 아예 안 나온다** — 원문이 이미 제목 자리에 있다

> 이 줄은 이제 메타 줄의 **자식**이라, "요약이 없으면 원문이 메타 줄의 바로 다음 형제"
> (2026-08-08 `.metaRow + .prose`)가 번역 유무와 무관하게 성립한다. 제목 밑에 두면
> 번역이 있는 소식에서만 그 전제가 깨질 수 있었다. 테스트로 붙들어 뒀다.

### 실측 대비 (2026-08-10 — **브라우저 실측**)

**106개 조합 전부 4.5:1 이상, 최저 4.75:1**(원문 제목 12px `--meta` on `--bg`).
기존 하한과 같은 값이라 **새 하한이 생기지 않았다.** 콘솔 에러 0 · 실패 요청 0 ·
900px 에서 가로 스크롤 없음.

> 2026-08-06 (2)·2026-08-08 두 블록은 CSS 값 계산이었는데, 이번에는 렌더 상태에서 쟀다
> (Playwright 1.61.1 + chromium-1228 로컬 캐시, `scripts/preview.mjs` 로 서빙).
> 앞 두 블록의 수치는 아직 계산값 그대로다.

---

## 2026-08-11 · 공식 표시 두 종류 · checkpoint 승인

승인된 시안: [mockups/official-badge.html](mockups/official-badge.html) — A/B/C 비교안 중 **B 채택**

스펙 INV-O2 가 요구하는 것은 하나다: **주소 근거와 내용 근거가 같은 확실성으로 보이면 안 된다.**
주소 근거(`byUrl`)는 원문 주소의 도메인을 기계가 대조한 것이고, 내용 근거(`byContent`)는 모델 판단이라
틀릴 수 있다. 둘이 같아 보이면 모델이 틀렸을 때 사용자가 그대로 믿는다.

### 자리 — 제목 위 뱃지 줄

'뜨는 중' 과 **같은 줄에 나란히** 선다(순서: 뜨는 중 → 공식 표시).

- **메타 줄에 두지 않는다.** 메타 줄은 출처 이름·시각과 태그 칩이 한 줄을 나눠 쓰는데,
  실측상 이미 포화다(아래 "메타 줄은 이미 꽉 찼다"). 거기에 표시를 더하면 출처 이름이 잘린다
- 대가: **뱃지 줄이 흔해져 제목이 그만큼 아래로 내려간다.** 표시가 없는 카드와 한 행에 섞이면
  제목 시작선이 31px 어긋난다(실측). 표시가 붙는 카드가 많아질수록 행 안에서는 오히려 정렬된다

### 두 종류를 가르는 수단 — 문구와 형태 (색이 아니다)

| | 문구 | 면 | 글자 | 테두리 |
| --- | --- | --- | --- | --- |
| 주소 근거 `byUrl` | **공식 발표** | `--tag-bg` 채움 | `--text` / 700 | 없음 |
| 내용 근거 `byContent` | **공식 발표라고 함** | 채우지 않음 | `--meta` / 500 | 1px 점선 `--dot` |

- **포인트색을 쓰지 않는다.** 피드에서 `--accent` 는 "아직 안 본 것"의 신호다("상태 규칙" 절).
  공식 여부는 읽음 상태와 무관하므로 같은 색을 쓰면 두 뜻이 섞인다
- **두 번째 강조색(초록 체크 등)을 만들지 않는다** ("하지 않는 것" 절 유지).
  그래서 확실성 차이를 **말이 나른다** — 형태(채움/점선)는 거들 뿐이고, 문구만 읽어도 갈린다
- 크기는 기존 뱃지 단계 그대로 11.5px. 새 타이포 단계를 만들지 않았다
- **점선 테두리**는 이 화면에서 예외적이다("테두리 선은 거의 쓰지 않는다"). 채움을 안 쓰면서
  알약 형태를 유지할 다른 수단이 없고, 점선은 이미 시그니처(카드 안 구분선·날짜 줄)라 이물감이 적다
- **스크린리더 텍스트를 함께 둔다** — 무엇을 근거로 한 판단인지는 색·형태로는 안 전해진다
  (`공식 발표` 옆에 "원문 주소가 〜 도메인입니다", `공식 발표라고 함` 옆에 "글 내용을 보고 AI 가 판단했습니다")

### 읽은 카드

'뜨는 중' 뱃지와 같은 규칙("뱃지 중립화")을 그대로 따른다.

- 주소 근거: `--tag-read-bg` 면 + `--tag-read-ink` 글자
- 내용 근거: 테두리 `--dot-read`, 글자 `--meta-read`
- 호버·포커스에서 완전 복구

### 메타 줄은 이미 꽉 찼다 (태그 칩 개수를 여기서 못 늘린다)

이 시안을 고르는 근거로 "메타 줄에 태그 자리를 비운다"가 나왔는데, **재보니 비워도 부족하다.**
태그 칩 하나가 60px 이고 메타 줄은 출처 이름·시각과 한 줄을 나눠 쓴다.

| 카드 폭 | 출처 `Anthropic · 3시간 전` | 출처 `Simon Willison's Weblog · 어제` |
| --- | --- | --- |
| 365px (1280px·3열 최대) | 칩 3개까지 온전, 4개부터 출처 잘림 | 2개부터 잘림 |
| 309px (1000px) | 2개부터 잘림 | 2개부터 잘림 |
| 303px (981px·3열 최소) | 2개부터 잘림 | 1개부터 잘림 |

- **"메타 줄 태그 칩은 2개까지"(2026-08-06)는 취향이 아니라 폭이 정한 값**이었다. 이 블록은 그 규칙을 그대로 둔다
- 칩을 3~5개 보이려면 메타 줄에 자리를 만드는 게 아니라 **칩을 다른 줄로 내려야 한다** —
  카드 구조가 바뀌므로 시안 한 바퀴가 따로 필요하다. 이 블록에서 정하지 않는다
- 공식 표시를 제목 위로 올린 것은 그 방향에 **불리하지 않다**: A안이었다면 메타 줄에서
  36px + 간격 8px 을 더 먹었을 것이다

### 실측 대비 (2026-08-11 — 브라우저 실측)

**27개 조합 전부 4.5:1 이상, 최저 4.57:1**(읽은 카드의 요약·메타 — 기존 하한과 같은 값이라
**새 하한이 생기지 않았다**). 새 요소 두 개는 `공식 발표` 4.74:1(읽은 카드 기준, 안 읽은 카드는 더 높다),
`공식 발표라고 함` 은 `--meta` on `--surface` 조합이라 기존 메타색 그대로다.
콘솔 에러 0 · 실패 요청 0 · 900px 가로 스크롤 없음 · Pretendard 로드됨.

**구현 실측 (2026-08-12 — 시안이 아니라 실제 화면에서)**

시안에는 없던 조합(읽은 카드 + 내용 근거)까지 브라우저에서 쟀다. 시안 B의 읽은 카드는
`공식 발표`만 그려서 이 조합은 승인 당시 측정 대상이 아니었다.

| 상태 | 대비 | 비고 |
| --- | ---: | --- |
| 안 읽음 · 공식 발표 | 14.21:1 | `--text` on `--tag-bg` |
| 안 읽음 · 공식 발표라고 함 | 5.14:1 | |
| **읽음 · 공식 발표라고 함** | **4.57:1** | 기존 하한과 같은 값 — 새 하한 없음 |
| 읽음+호버 · 공식 발표라고 함 | 5.36:1 | 안 읽은 값으로 완전 복구 확인 |

- 두 알약의 바깥 높이가 **26.4px 로 같다.** 구현은 시안의 `border: 1px dashed` + 홀수 패딩(3/9px)
  대신 `outline: 1px dashed` + `outline-offset: -1px` + 짝수 패딩(4/10px)을 쓴다 —
  ui-layers 의 "간격은 짝수"를 지키면서 시안과 같은 바깥 상자를 얻는 방법이다.
  `outline` 은 레이아웃을 밀지 않으므로 border 가 차지하던 1px 자리에 그대로 그려진다.
- 점선 색(`--dot` / 읽음 `--dot-read`)은 장식이라 대비 기준 대상이 아니다. 확실성 차이는 문구가 나른다.

> 시안 주석에 "A안은 메타 줄이 밀려 잘린다"고 써뒀다가 **실측에서 틀린 것으로 드러나 고쳤다**
> (1280~380px 전 구간 A안 새 잘림 0건). 대신 C안이 981~1000px 에서 "공식 발표로 보…" 로
> 잘리는 것이 나왔다. 안 쟀으면 틀린 근거로 안을 골랐을 자리다.

---

## 2026-08-27 · 뱃지 키워드 줄 · checkpoint 승인

승인된 시안: [mockups/badge-row.html](mockups/badge-row.html) — 색 A/B 비교안 중 **B 채택**
근거 상담: [INTERVIEW-badge-row.md](INTERVIEW-badge-row.md) (4회차 확정)

고정 5개 태그(모델·에이전트·MCP·엔지니어링·툴)를 **모델이 글마다 만드는 키워드**로 바꾸면서
필터 칩 줄이 통째로 다시 설계됐다. 칩이 5개에서 수십 개가 되고 **축이 둘**로 늘어서
(`분야` / `사건종류`) 기존 언어로는 커버되지 않는 요소다.

### 줄 구성

세그먼트 줄과 **뱃지 줄을 나눈다.** 한 줄에 못 둔다 — 뱃지가 수십 개다.

- `① 세그먼트(핫이슈 / 최신) + 전체 칩` → `② 뱃지 줄`
- 세그먼트 이름을 `뜨는순 / 최신순` → **`핫이슈` / `최신`** 으로 바꾼다. **글자만 바뀐다** —
  두 쪽 다 같은 글 전부를 보여주고 날짜 묶음 안에서 순서만 다르다. `핫이슈` 는 거르지 않는다
  - **지금 `핫이슈` 는 내용을 본 판단이 아니다.** 점수가 `시간감쇠 × 소스 weight` 뿐이라
    구조적으로 "최근 + 신뢰도 높은 소스"다. 스펙의 진짜 핫이슈 판정(INV-G2, 질문 셋)이
    붙으면 그 자리를 물려받는다. 사용자가 이름을 먼저 바꾸기로 했고, 화면에서 이걸
    말해 주는 자리는 페이지 부제("하루 안에서는 점수 순으로 정리합니다")뿐이라 **그 줄을 지운다면
    이 결정도 다시 봐야 한다**
- 기본은 **`핫이슈`** (지금 기본값 그대로)
- `전체` 칩은 남기고 **숫자를 안 붙인다** — 붙이면 "오늘 안 읽은 소식 37건"이 되어
  개수 표시 금지 규칙(스펙 INV-N5)이 막으려던 그 숫자가 된다

### 두 축을 색으로 가른다

`보안`(분야)과 `사건사고`(사건종류)가 한 줄에 섞이면 같은 것으로 읽힌다.

| 축 | 면 | 글자 | 새 토큰 |
| --- | --- | --- | --- |
| 분야 | 청회색 `#d8e4ee` | `#26323b` | `--kw-field-*` |
| 사건종류 | 모래빛 `#eee0d4` | `#3b322a` | `--kw-kind-*` |

- **인디고(`--accent`)에서 가져오지 않는다.** 포인트색은 "아직 안 본 것" 전용이라
  거기서 빌리면 두 뜻이 섞인다("상태 규칙" 절)
- **2차 강조색을 만든 게 아니다.** `--tag-bg` 의 이웃에서 채도를 낮게 잡은 면이고,
  두 면의 **명도비가 1.00** 이라 밝기가 같고 색상으로만 갈린다
- **비채택 A안(명도 차)을 남긴 이유**: 두 면의 명도비가 1.11 이라 나란히 붙어 있을 때만
  갈리고, 카드에 뱃지가 두세 개만 붙으면 어느 쪽이 분야인지 알 수 없었다.
  같은 함정을 다시 파지 않도록 시안에 근거로 남겨 뒀다
- 대가: **팔레트에 따뜻한 색상이 처음 들어온다.** 라벤더 기 도는 회색 화면에서
  모래빛이 한 자리 온도를 가른다
- 축은 색으로만 전해지므로 **모든 뱃지에 스크린리더용 접두사**(`분야 ` / `사건종류 `)를 둔다

### 숫자는 안 읽은 글 수다

- 뱃지 옆 숫자 = **그 뱃지가 붙은 글 중 안 읽은 것의 수.** 글 하나를 읽으면 거기 달린
  뱃지가 전부 1씩 준다(뱃지와 글은 다대다)
- **자리·순서·노출 여부는 전체 건수로 고정하고, 숫자만 안 읽은 수로 바뀐다.**
  읽음 상태가 `localStorage` 에 있어 첫 렌더가 항상 "아무것도 안 읽음"으로 시작하기 때문이다
  (서버 화면과 맞추려고 일부러 그렇게 해 뒀다). 안 읽은 수로 자리까지 정하면
  **화면이 뜬 직후 줄 전체가 다시 배열되고, 누르려던 뱃지가 눈앞에서 움직인다**
- 0이 되면 `0` 을 그대로 쓰고 **취소선 + 글자 굵기 600→500.** 누르는 건 여전히 된다
- **0이 된 뱃지도 자리를 지킨다.** 뒤로 밀지 않는다 — 창이 3일이라 다음 날 새 글이 붙으면
  숫자가 되살아난다. 0 상태는 하루 안에서만 유지된다

#### 취소선은 뱃지 전체를 가로지른다 (가상요소 1px 선)

**`text-decoration: line-through` 를 쓰지 않는다.** 두 번 실패한 자리라 적어 둔다:

- 숫자에만 걸면 `코딩 0` 이 **`코딩 θ`** 로 보인다 — 빗금 친 0 이라는 다른 글자로 읽히지
  "다 봤다"로 안 읽힌다
- 알약 전체에 걸면 알약이 `inline-flex` 라 **선이 flex 항목마다 따로 그려져 두 토막**이 난다
- 그래서 `::after` 로 **절대 위치 1px 선 하나**를 깐다. 좌우를 그 칩의 padding 에 맞추면
  이름 시작부터 숫자 끝까지 끊기지 않는다(실측: `left:10px / right:10px`, 이름 시작 10px · 숫자 끝 10px)
- 선 색은 `currentColor` 를 55% 로 눕힌다 — **색 토큰을 만들지 않는다.**
  축·호버·읽음 중립화를 자동으로 따라가서 색을 여덟 개 더 정의할 필요가 없다
- **선은 장식이라 4.5:1 대상이 아니다.** 2026-08-11 이 공식 표시 점선에 대해 내린 판단과 같다.
  대신 **글자색은 안 내린다** — 취소선을 넣으면서 흐리게까지 하면 하한에 부딪힌다

### 넘칠 때 — 접고 펼친다

- **가로 스크롤을 쓰지 않는다.** 마우스로만 닿는 자리가 생겨 "가로로 넘치는 것은 키보드로도
  닿아야 한다"(2026-08-06 (2))를 또 우회하게 된다
- 접힘 **2줄**(`--kw-badge-h × 2 + --space-6`) + `더 보기` / `접기`
- 기본 노출 상한 **24개**, 노출 기준 **2건 이상**, 창 **3일** — 전부 잠정값이다
- **`더 보기` 라벨에 개수를 적지 않는다.** 몇 개가 가려질지는 화면 폭이 정한다 —
  숫자를 박아 두면 폭이 바뀔 때 화면이 거짓말을 한다

#### 접힘이 실제로 자르는 폭 (2026-08-27 브라우저 실측, 뱃지 24개)

| 폭 | 보임 | 가림 |
| --- | ---: | ---: |
| 1168px (최대) | 24 | **0** |
| 980px | 21 | 3 |
| 380px | 7 | 17 |

**넓은 화면에서는 접힘이 아무것도 안 가린다.** 24개가 2줄에 그대로 들어가고 둘째 줄에
329px(뱃지 네 개쯤)이 남는다. 1168px 에서도 자르려면 상한이 29개쯤 돼야 한다.

**그대로 두기로 했다**(2026-08-27 사용자 결정) — 3일 창에서 실제로 몇 개가 기준을 넘는지는
파이프라인이 키워드를 저장하고 며칠 돌려야 나온다. 지금 29로 맞추는 것도 추측이다.
**다시 볼 조건**: 실제 데이터로 뱃지 개수가 나왔을 때.

### 누르기

- **하나만 켜진다** (지금 필터 칩과 같다)
- 켠 뱃지는 **줄 맨 앞으로 끌어올린다** — 수십 개 중 하나를 켜면 접힌 줄 아래에 숨을 수 있다
- 켜진 표시는 지금 필터 칩과 같은 처리(`--accent-soft` 면 + `--accent` 글자 + `--accent-ring`).
  **켜진 동안에는 축 색이 포인트색에 덮여 사라진다** — 그 순간 뱃지가 나르는 것은
  축이 아니라 "지금 켜져 있음"이다

### 카드와 상세

- 카드: 뱃지를 **메타 줄에서 빼서 별도 줄로 내린다** (제목·요약 아래, 메타 줄 위).
  카드가 한 줄 두꺼워지는 대가를 진다
  - 근거는 사용자 문장이다: "제목만 보고서 해석하는 것만으로도 피로감이 생길 수 있다.
    뱃지만 보고서 대충 이것들에 대한 내용이구나 파악할 수 있어야 한다."
    2026-08-06 의 "너무 글자글자글자~"와 **반대 방향의 피로**를 가리킨다
  - **부수 소득**: 메타 줄에서 태그 칩이 빠지면서 2026-08-11 의 "메타 줄은 이미 꽉 찼다"가 풀린다
- 카드에 **4개까지**, 넘으면 `+N` (붙을 수 있는 최대는 5 = 분야 3 + 사건종류 2).
  `+N` 은 두 축에 걸쳐 있어 **축 색을 안 쓴다**(`--tag-bg` / `--meta`)
- **카드 뱃지에는 숫자를 안 붙인다.** 건수는 줄 전체의 집계값이라 카드에서는 뜻이 없다
- 상세: **전부** 보여준다 (폭이 넉넉하다 — 2026-08-06 규칙 그대로)
- **읽은 카드에서는 두 축이 하나로 내려앉는다.** "뱃지 중립화"를 그대로 따라
  둘 다 `--tag-read-bg` / `--tag-read-ink` 가 되고, 호버·포커스에서 복구된다.
  축 구분을 읽은 카드에서도 남기려면 그 규칙에 예외를 열어야 해서 **열지 않았다**

### 새 토큰

`src/shared/ui/tokens.css` 에 색 8개 + 높이 3개. 간격은 새로 만들지 않고 `--space-6` 을 쓴다.

| 토큰 | 값 | 쓰임 |
| --- | --- | --- |
| `--kw-field-bg` / `-bg-h` | `#d8e4ee` / `#c8dbea` | 분야 뱃지 면 · 호버 면 |
| `--kw-field-ink` / `-zero-ink` | `#26323b` / `#4c5860` | 분야 글자 · 0건 글자 |
| `--kw-kind-bg` / `-bg-h` | `#eee0d4` / `#e6d2c2` | 사건종류 뱃지 면 · 호버 면 |
| `--kw-kind-ink` / `-zero-ink` | `#3b322a` / `#605649` | 사건종류 글자 · 0건 글자 |
| `--kw-badge-h` | 26px | 뱃지 줄 알약 높이 — **접힘 2줄 계산의 근거** |
| `--kw-card-chip-h` / `--kw-detail-chip-h` | 22px / 24px | 카드 · 상세 알약 높이 |

**새 타이포 단계를 만들지 않았다** — 뱃지 줄 13px(`--fs-ui`) · 카드 11.5px(`--fs-chip`) ·
상세 12px(`--fs-meta`), 전부 이미 있는 단계다.

### 실측 대비 (2026-08-27 — **브라우저 실측**)

**전 조합 4.5:1 이상, 최저 4.57:1**(읽은 카드 요약·메타 — 기존 하한과 같은 값이라
**새 하한이 생기지 않았다**). 콘솔 에러 0 · 1168 · 980 · 380px 전부 가로 스크롤 0.

새 토큰 자리 중 최저는 **사건종류 0건 + 호버 4.91:1**, 그다음이 분야 0건 + 호버 5.15:1.
0건을 색만으로 흐리게 하면 하한에 부딪히므로 **굵기 600→500 을 같이 내려서** 풀었다 —
읽음 상태에서 이미 쓴 방법이다("접근성" 절).

> 비채택 A안의 값도 시안에 남아 있고 그쪽 최저는 4.83:1(분야 0건 + 호버)이었다.
> 두 안 다 하한을 안 내렸으므로 **색 선택은 대비가 아니라 "축이 갈리는가"로 갈렸다.**

### 아직 안 정한 것

- 노출 기준(2건) · 상한(24) · 창(3일)의 **진짜 값** — 키워드가 며칠 쌓인 뒤 정한다
- 읽은 카드에서 두 축을 갈라 보일지 (지금은 안 가른다)
- 뱃지 줄 아래에 축 범례를 둘지 — 시안에는 있지만 실제 화면 요소로 정하지 않았다

## 2026-08-30 · 뱃지 줄 — 시안이 아니라 **실제 앱**에서 잰 값

시안(`mockups/badge-row.html`)이 아니라 `npm run dev` 로 띄운 진짜 화면을 Playwright 로 열어
computed style 로 쟀다. 데이터도 더미가 아니라 실제 수집분이다(창 3일 안 176건 · 그중
키워드가 붙은 것 78건 · 서로 다른 키워드 42종).

이 절은 **잰 사실만** 담는다. 노출 기준·상한·창의 값은 여전히 미정이다(위 절 그대로).

### 대비 — 시안값이 실제 화면에서 그대로 나왔다

| 조합 | 실측 | 시안값 |
| --- | ---: | ---: |
| 분야 · 안읽음 (이름/숫자) | 10.15 | — |
| 사건종류 · 안읽음 | 9.70 | — |
| 분야 · 호버 | 9.22 | — |
| 사건종류 · 호버 | 8.57 | — |
| 분야 · 0건 | 5.66 | — |
| 사건종류 · 0건 | 5.56 | — |
| ~~켜짐(양 축 공통, `--accent` on `--accent-soft`)~~ | ~~5.21~~ | — |
| 분야 · 0건 + 호버 | **5.14** | 5.15 |
| **사건종류 · 0건 + 호버** | **4.91** | 4.91 |
| 카드 뱃지 · 읽은 카드(두 축 공통) | 4.74 | — |

페이지 전체를 훑어(글자를 직접 들고 있는 요소 33종) **미달 0건**. 전체 최저는 여전히
**4.57:1**(읽은 카드 요약·메타) — 2026-08-27 에 적은 하한 그대로이고 **새 하한이 생기지 않았다.**
0건 상태는 실제 `localStorage` 읽음 기록으로 만들었다(글 13건을 읽음 처리 → 0건 뱃지 4개).

> **2026-08-31 정정 — `켜짐 5.21` 한 칸은 실측이 아니었다.** 표의 나머지는 브라우저에서 잰
> 값이 맞는데, 이 한 줄만 **토큰 쌍을 계산한 값**이다. 같은 날 리뷰에서 캐스케이드 결함이
> 나와 실제 앱을 다시 재 보니, 켠 뱃지의 계산값은 `rgb(38,50,59)` on `rgb(216,228,238)`
> = **10.15:1** 로 꺼짐과 완전히 같았다. `--accent` 를 `--accent-soft` 위에 놓으면 계산값이
> 정확히 5.21 이라 출처가 갈렸다 — **앱은 그 조합을 한 번도 그린 적이 없다.**
>
> 대비 미달은 아니다(10.15:1). 문제는 **켜짐이라는 상태가 안 보이는 것**이다. 남는 신호인
> `--accent-ring` 1px 을 면 위에 합성하면 `rgb(186,193,236)`, **면 대비 1.36:1** 로
> UI 상태 표시 하한(3:1)에 한참 못 미친다. 원인과 고침은 아래 2026-08-31 블록.
>
> **교훈**: "전 조합 실측"이라고 적을 때 한 칸이라도 계산이 섞이면 그 표는 전부 실측처럼 읽힌다.
> 계산값을 섞을 거면 그 줄에 표시한다.

### 접힘이 실제로 자르는 폭

상한을 풀기 **전**(뱃지 24개):

| 폭 | 보임 | 가림 | 가로 스크롤 |
| --- | ---: | ---: | --- |
| 1280px | 24 | **0** | 없음 |
| 980px | 23 | 1 | 없음 |
| 390px | 8 | 16 | 없음 |

390px 에서 `더 보기` 를 누르면 줄 높이가 66px → 194px 로 펴지고 가려진 16개가 다 나온다
(`접기` 로 되돌아간다). **넓은 화면에서는 접힘이 아무것도 안 가린다** — 2026-08-27 의 시안
실측과 같은 결과다.

상한을 60으로 푼 **뒤**(뱃지 29개)도 넓은 화면에서는 **여전히 아무것도 안 가린다**:

| 폭 | 줄 수 | 가림 | 둘째 줄 남는 폭 |
| --- | ---: | ---: | ---: |
| 1280px | 2 (14 + 15) | **0** | — |
| 1168px | 2 | **0** | 40px |

**상한을 풀면 넓은 화면에서도 접힘이 일할 거라고 예상했는데 틀렸다.** 늘어난 다섯이
`시연`·`인사`·`행사`·`계약 해지`·`국방` 처럼 짧은 말이라 두 줄에 그대로 들어갔다.
1168px 에서 40px 밖에 안 남으니 **한 개만 더 늘어도 넘친다** — 경계에 있는 상태다.
넓은 화면의 `더 보기` 가 눌러도 아무 일 없는 문제는 **그대로 남아 있다**(아래 절).

### 창 3일 안의 실제 분포 (2026-08-30, 키워드 42종)

`npm run badges` 로 센 값이다. 상한 24 를 걸기 **전** 개수:

| 노출 기준 | 기준을 넘는 키워드 | 상한 24 적용 후 |
| --- | ---: | ---: |
| 2건 이상 | 29종 (분야 20 · 사건종류 9) | 24 |
| 3건 이상 | 27종 (분야 19 · 사건종류 8) | 24 |
| 4건 이상 | 21종 (분야 15 · 사건종류 6) | 21 |

**지금 화면을 정하는 것은 노출 기준이 아니라 상한이다.** 2건과 3건 사이에는 2종
(`계약 해지`·`국방`) 차이뿐이고 둘 다 꼬리에 있어서 상한 24 에 잘린다 — **그려지는 줄이 완전히
같다.** 2026-08-27 에 "1168px 에서도 자르려면 상한이 29개쯤 돼야 한다"고 적었는데, 실제로
2건 기준을 넘는 것이 정확히 29종이다.

꼬리 두께: 1건짜리 13종 · 2건짜리 2종 · 3건짜리 6종. 1건을 빼기로 한 결정(2026-08-30)이
**42종 중 13종을 걷어낸다.**

> 창 안 176건 중 키워드가 붙은 것은 아직 **78건(44%)** 이다 — 한 주기에 80건씩 물어보므로
> 밀린 것이 남아 있다. 창이 다 차면 건수가 전반적으로 올라간다.

### 결정 — 상한을 화면에서 뺀다 (2026-08-30 사용자 결정)

**`BADGE_LIMIT` 24 → 60.** 24 였을 때는 이 값이 노출을 정하고 있었다 — 2건 기준을 넘는
29종이 24에서 잘려서, **뉴스가 몰린 날과 한산한 날이 화면에서 똑같이 24개로 보였다.**
사용자 문장: "상한을 없애고 이슈성과 중요도에 따라 나오게 하는거 어때? 그때그때 어떨때는
5개, 어떤날은 20개가 나올 수 있는거".

- 완전히 없애지 않는다. 뱃지 줄은 클라이언트 컴포넌트라 여기 담긴 것이 그대로 브라우저로
  직렬화된다 — 상한이 없으면 이상한 날에 수백 개가 통째로 실린다. 60은 **평소에 닿지 않는
  사고 방지선**이다(실측 최대 29종)
- **문턱은 2 로 확정.** 2와 3의 비교는 위 표대로 그려지는 줄이 같았다. 1건 제외는 결정 그대로
- 결과: 화면 뱃지 **24 → 29개**. 개수가 그날 뉴스 양을 따라 달라진다

**이 기준은 임시다.** 문턱 2건은 "몇 건이나 나왔나"만 본다 — 한 매체가 시시한 것을 여러 번
써도 뜨고, 중요한 발표가 한 번만 실리면 안 뜬다. 사용자가 말한 **이슈성·중요도**는
`docs/specs/keywords-and-kinds.md` 의 INV-N3(교차 발행처 수 × 소스 weight × 시간감쇠)와
INV-G2(핫이슈 문턱 질문 셋)인데 **둘 다 코드에도 스키마에도 없다** — 교차 발행처 수는 "같은
사건 묶기"가 필요하고(지금 중복 제거는 정규화 원문 주소 기준이라 매체가 다르면 다른 글이다),
중요도는 모델 판정 단계가 새로 필요하다. 그게 붙으면 이 상수를 조정하는 게 아니라
**고르는 기준 자체가 바뀐다.**

### 넓은 화면의 `더 보기` 는 눌러도 아무 일이 없다 — 안 고쳤다

1280px 에서 뱃지가 전부 두 줄에 들어가 `scrollHeight 66 = clientHeight 66`, 가려진 것이
0개인데 `더 보기` 는 그대로 떠 있다. 누르면 `접기` 로 글자만 바뀌고 화면은 안 변한다.

`src/widgets/feed/ui/keyword-badges.tsx` 가 **일부러** 조건부로 없애지 않는다 —
사라지는 순간 포커스가 문서 맨 위로 떨어지기 때문이다. 이 저장소는 같은 문제를 피드
`더 보기` 에서 **`aria-disabled` 로 남기기**로 풀었었는데(2026-08-06 ui-reviewer 지적)
여기에는 그 처리가 없다. 어려운 점은 **넘쳤는지를 렌더 시점에 알 수 없다**는 것이다 —
몇 개가 가려지는지는 화면 폭이 정하므로 레이아웃 뒤에 재야 한다(`ResizeObserver`).

**아직 안 고쳤다.** 상한을 60으로 풀면 저절로 풀릴 줄 알았는데 29개도 두 줄에 들어갔다.

### 읽으면 뱃지 줄 숫자가 오름차순이 아니게 된다 — 시안에는 없던 상태

자리는 전체 건수로 정하고 숫자만 안 읽은 수로 바뀌므로, 몇 개를 읽고 나면 줄이
`논쟁 10 · 정책 11` 처럼 **숫자가 들쭉날쭉하게 보인다.** 의도한 동작이지만
(자리가 눈앞에서 움직이지 않게 한 대가), 시안에는 읽음 상태가 없어서 이 모습이 안 나왔다.
그대로 둘지는 **아직 안 정했다.**

---

## 2026-08-31 · 뱃지 줄 — 접힘의 키보드 처리와 켜짐 표시 (리뷰 지적 반영) · 승인

> 2026-08-31 승인. 시안을 새로 그리지 않고 **규칙 문장으로** 승인받았다 — 리뷰가 잡은 것이
> 시각 취향이 아니라 접힘의 동작이고, 판단 근거는 아래 390px 실측이라 시안이 더할 것이 없었다.

2026-08-31 리뷰어 4종이 뱃지 줄에서 셋을 잡았다. 그중 **접힘의 키보드 처리**는 승인된 접힘
방식(개수를 안 정하는 높이 자르기)이 원인이라 여기서 정한다(qa-classifier 판정: design).
나머지 둘(켜짐 캐스케이드·켜진 뱃지 끌어올림)은 이 문서가 이미 정한 것을 코드가 안 지킨
것이라 조항을 새로 만들지 않고 **아래에 실측만 기록한다.**

### 정한 것 — 넘침을 재서 닫는다

- **접힘 밖으로 밀려난 뱃지는 키보드로도 닿지 않는다.** `ResizeObserver` 로 레이아웃 뒤에
  실제 넘침을 재고, 넘친 뱃지에 `inert`(+ `tabindex="-1"`)를 건다. **펼치면 푼다.**
- **같은 측정값이 `더 보기` 도 정한다.** 넘친 것이 0이면 버튼을 없애지 않고
  `aria-disabled="true"` + 문구를 "모두 보임"으로 바꾼다(피드 `더 보기` 가 2026-08-06 에
  쓴 처리와 같은 말투). **없애지 않는 이유는 그대로다** — 사라지면 포커스가 문서 맨 위로 떨어진다.
- 측정은 **한 자리에서 한 번**만 한다. 두 곳에서 따로 재면 "가렸다고 표시하는데 탭은 통과"
  같은 어긋남이 생긴다.

**왜 이 방식인가.** 이 문서가 가로 스크롤을 뺀 이유가 "마우스로만 닿는 자리가 생긴다"인데,
세로 접힘이 **반대 방향으로 같은 문제**를 만들고 있었다. 후보 둘을 재고 골랐다:

| 안 | 접힘 2줄 유지 | `더 보기` 도 같이 풀림 | 대가 |
| --- | --- | --- | --- |
| **넘침을 재서 `inert`** (채택) | 유지 | **풀림** | 구현이 제일 무겁다(`ResizeObserver`) |
| `:focus-within { max-height: none }` | **안 지켜짐**(키보드에서만 펴짐) | 안 풀림 | 탭 누를 때 목록이 아래로 밀린다 |
| 접힘 때 DOM 에서 뺀다 | 유지 | 안 풀림 | 펼칠 때 마운트라 포커스가 튀고, 스크린리더가 뱃지 수를 사실과 다르게 읽는다 |

**첫째 안만 두 문제를 한 장치로 닫는다** — `더 보기` 죽은 버튼은 2026-08-30 에 "넘쳤는지를
렌더 시점에 알 수 없다"는 이유로 미룬 항목이고, 그 측정이 정확히 여기 필요한 것과 같다.

### 실측 — 무엇이 얼마나 잘못돼 있었나 (2026-08-31, dev 3210 · 실제 데이터 뱃지 29개)

| 폭 | 보임 | 가림 | 가려진 것 중 탭에 잡히는 수 |
| --- | ---: | ---: | ---: |
| 1280px | 29 | **0** | 0 |
| 390px | 8 | 21 | **21 (전부)** |

390px 에서 20번째 뱃지에 포커스를 주니 줄이 **144px 스크롤**됐다(`scrollTop` 0 → 144).
첫 뱃지 `AI 모델` 이 화면 밖으로 밀려나고 줄 내용이 `신소재·제조업·검색…` 으로 바뀐다.
**스크롤바가 없어 무슨 일이 일어났는지 화면에 설명이 없고**, `접기`/`더 보기` 를 눌러도
`scrollTop` 은 안 돌아온다. `overflow: hidden` 은 그리기만 자르고 포커스는 못 자른다.

### 조항을 새로 만들지 않은 둘 — 이미 정해져 있다

- **켠 뱃지에 포인트색이 안 입혀진다.** 「누르기」 절이 `--accent-soft` 면 + `--accent` 글자를
  이미 정했고 승인 시안도 그렇게 그렸다. 구현이 시안의 복합 선택자(`.kwBadge.is-on`, 명시도 0,2,0)를
  한 클래스(`.kwOn`, 0,1,0)로 줄이면서 뒤에 오는 `.kwField`/`.kwKind` 에 진 것이다.
  실측은 위 대비표의 2026-08-31 정정 참고. **고치면 그 조합을 처음으로 실제로 재게 되므로,
  재고 대비표의 그 줄을 실측값으로 되살린다.**
- **켠 뱃지를 줄 맨 앞으로.** 「누르기」 절이 이미 요구한다("수십 개 중 하나를 켜면 접힌 줄
  아래에 숨을 수 있다"). 390px 실측에서 21개가 접힘 밖이므로 이 조항이 막으려던 상태가
  실제로 일어난다. 집계 순서는 건드리지 않고 **그리기 직전에만** 자리를 옮긴다 —
  "자리를 전체 건수로 고정한다"는 규칙은 읽음 숫자로 자리를 정하지 말라는 것이라 안 부딪힌다.

---

## 2026-09-01 · 상세 하단 글 이동 + 모바일 하단 고정 줄 · checkpoint 승인

승인된 시안: [mockups/article-nav-and-mobile-dock.html](mockups/article-nav-and-mobile-dock.html)

사용자 요구 셋에서 나왔다 — "매일 부담없이 가볍게 파악" · "폰으로 볼 때 한손으로 간편하게" ·
"이전/다음은 상세에서만, 메인은 더 보기로 하루 전날 것을 본다".

**뒤집은 결정이 없다.** 「2026-08-02 · 날짜 그룹」도 「2026-08-06 · 목록을 늘리는 버튼」도
그대로다. 이 블록은 이 문서에 없던 요소 둘을 더하고, 이 문서가 정한 적 없던 값 하나를 정한다.

### 「더 보기」가 한 번에 불러오는 단위는 하루다

이 문서는 버튼의 동작만 정했고 **양은 정한 적이 없다** — 구현이 카드 9장으로 골랐던 값이다.
하루치로 바꾼다.

- 근거는 수집 주기다. 매일 아침 한 번 도는 배치라 사용자가 세는 단위가 하루이고,
  「날짜 그룹」이 1차 묶음인 것과 같은 이유다
- **부수 효과: 날짜 옆 건수가 화면의 카드 수와 항상 맞는다.** 9장에서 끊기면
  「어제 · 5건」인데 3장만 그려지는 그룹이 생겼다 — 그룹이 잘린 채로 그려지는 일이 없어진다
- 버튼 문구에 **넘어올 날의 건수를 적는다**: `더 보기 · 어제 5건`.
  눌러 보기 전에 그날이 한산한지 알 수 있다
- 다 불러오면 「모두 불러왔습니다」 + `aria-disabled`. 2026-08-06 규칙 그대로다

### 상세 화면 하단 — 이전 글 / 다음 글

- **자리**: 원문 링크 아래, 점선 구분선 뒤. 좌우 반반으로 나눈다
- **순서는 피드에 보이던 것 그대로다.** 정렬(핫이슈/최신)을 바꾸면 이 순서도 같이 바뀐다.
  **날짜 경계를 넘어간다** — 오늘 마지막 글의 「다음」은 어제 첫 글이다
- **제목을 같이 보여준다** (2줄까지, `word-break: keep-all` — 카드 제목과 같은 규칙).
  「다음 글 →」만 있으면 누르기 전에 무엇인지 모른다
- **끝에 닿아도 없애지 않는다.** 점선 테두리 + `aria-disabled` + 「마지막 글입니다」.
  없애면 키보드 포커스가 문서 맨 위로 떨어진다 — 「더 보기」와 같은 이유다.
  점선은 이미 「공식 발표라고 함」이 쓰는 표식이라 새 언어가 아니다
- **형태는 알약이 아니라 카드 라운드(12px)** + `--border-soft` 테두리.
  두 줄짜리 제목이 들어가는데 알약이면 세로가 늘어져 버튼으로 안 읽힌다

### 모바일(640px 아래) 하단 고정 조작 줄

**화면마다 담기는 것이 다르다** — 그 화면에서 반복해서 쓰는 것만 둔다.

| 화면 | 고정 줄에 담기는 것 | 높이 |
| --- | --- | ---: |
| 메인 | 정렬 세그먼트(핫이슈/최신) | 76px |
| 상세 | 이전 글 / 다음 글 | 68px |

- **「더 보기」는 안 넣는다.** 다 읽고 나서 한 번 누르는 것이라 본문 끝이 제자리다
- **키워드 필터도 안 넣는다.** 한 번 고르면 한동안 안 바꾼다 — 두 화면 모두 위에 남긴다
- 면은 `--surface`, 위쪽에 1px `--hairline`. **새 색을 만들지 않았다**
- 아래 여백에 `env(safe-area-inset-bottom)` 을 더한다(홈 인디케이터)
- **누르는 것은 전부 44px 이상.** 데스크톱 세그먼트는 33px 인데 그건 마우스가 겨냥하는
  값이라 엄지에는 안 통한다. 시안을 처음 그렸을 때 이 값을 그대로 가져왔다가 재서 잡았다
- **데스크톱에는 두지 않는다.** 마우스는 어디든 같은 비용으로 닿고, 가로로 넓은 화면에서
  아래에 띠를 깔면 카드 세로 공간만 줄어든다

### 카드 키워드 칩 줄 — 2026-08-27 결정의 자리 확인

새 결정이 아니라 그 블록이 정한 것을 시안에서 처음 그려 확인한 것이다. 요약 아래·메타 줄 위의
별도 줄, 카드에 4개까지 + `+N`, 카드 뱃지에는 숫자를 안 붙인다.

**키워드 줄은 점선 발 바로 위에 고정한다** (2026-09-01 사용자 지적으로 추가). 요약이 없는 카드에서
키워드가 제목 밑으로 딸려 올라가면 같은 행의 카드끼리 줄이 안 맞는다 — 실측으로 한 행 안에서
83px 어긋나 있었다. **빈칸은 제목과 키워드 사이에 남긴다**: 그래야 "요약이 없다"가 그대로 보이고
(2026-08-10 「요약이 없는 카드는 그대로 둔다」) 키워드는 어느 카드에서나 같은 자리에 선다.
칩이 두 줄로 접히는 카드가 있으므로 **맞추는 것은 줄의 위가 아니라 아래**다.
고친 뒤 실측: 6개 행 전부 키워드 줄 아래끝 어긋남 0px · 점선 발 어긋남 0px.

### 아직 안 정한 것 — 키워드 뱃지 줄의 집계 창

상단 뱃지 줄은 **최근 3일**을 세는데 메인을 처음 열면 오늘치만 보인다. 이 어긋남을 화면에
밝힐지(줄 끝에 「최근 3일」 표기) 정하지 않았고, **시안에는 표기를 넣지 않았다** — 승인은
그 상태 그대로다.

> **뒤의 절반은 2026-09-01 에 정해졌다** — "뱃지를 눌렀을 때 날짜 범위가 어떻게 되는가"는
> 아래 「뱃지를 켜면 집계 창만큼 편다」 블록으로 닫혔다. 표기 여부는 여전히 안 정한 채다.

### 실측 대비 (2026-09-01 — 브라우저 실측)

시안을 `scripts/preview.mjs` 로 띄워 Playwright 로 쟀다.

**글자를 직접 든 요소 38종 전부 4.5:1 이상, 최저 4.75:1.** 새 하한이 생기지 않았다 —
이 블록이 **새로 만든 색이 하나도 없기 때문**이다(면·테두리·글자 전부 기존 토큰).
콘솔 에러 0.

터치 영역 실측: 「더 보기」 44 · 「핫이슈」 44 · 「최신」 44 · 「이전 글」 44 · 「다음 글」 44.
고정 줄 바깥 높이 77px / 69px(테두리 1px 포함).

**구현 실측 (2026-09-01 — 시안이 아니라 실제 앱에서)**

`npm run dev` 로 띄운 화면을 Playwright 로 열어 쟀다. 읽음 상태는 실제 `localStorage` 로 만들었다.

- 글자를 든 요소 **28종 · 미달 0건 · 최저 4.57:1**(읽은 카드 요약·메타). 하한 그대로다.
  새 요소인 카드 키워드 칩은 읽은 카드에서 4.74:1 — 2026-08-30 에 잰 값과 같다
- 콘솔 에러 0 · 1280/980/390px 전부 가로 스크롤 0
- 하루 단위 「더 보기」: `오늘 6건/카드 6` → `어제 5건/카드 5` → `8월 30일 5건/카드 5`.
  **모든 그룹에서 건수와 카드 수가 같다** — 이 블록이 노린 것이 실제로 그렇게 나왔다
- 상세 이전/다음이 정렬을 따라간다: `a02` 의 다음이 핫이슈에서는 `a06`, 최신에서는 `a03`

> **고정 줄이 비워야 할 자리를 1px 잘못 잡았다가 실측에서 잡았다.** 안쪽 높이 76px 으로
> 맞췄더니 푸터가 딱 1px 가려졌다 — 위쪽 헤어라인 1px 이 바깥 높이를 77 로 만든다.
> `--dock-h` 를 78px 로 올려 고쳤다. 눈으로는 안 보이는 크기라 재지 않았으면 그대로 남았다.

**뱃지 줄 구현 실측 (2026-09-01)** — 「2026-08-27 · 뱃지 키워드 줄」이 정한 것을 처음 구현했다.

| 조합 | 실측 | 비고 |
| --- | ---: | --- |
| 분야 · 안읽음 | 10.15 | 2026-08-30 값과 같다 |
| 사건종류 · 안읽음 | 9.70 | 같다 |
| 분야 · 0건 | 5.66 | 같다 |
| 사건종류 · 0건 | 5.56 | 같다 |
| **켜짐(`--accent` on `--accent-soft`)** | **5.21** | **처음으로 실제 화면에서 잰 값** |
| 「더 보기 / 모두 보임」 | 4.75 | |

페이지 전체 38종 · 미달 0건 · 최저 4.57:1(읽은 카드 요약·메타) — 하한 그대로다.
호버 값은 토큰을 안 바꿨으므로 2026-08-30 실측(9.22 · 8.57 · 5.14 · 4.91)이 그대로 유효하고,
이번에는 다시 재지 않았다.

> **「켜짐 5.21」이 실측으로 바뀌었다.** 2026-08-30 표의 그 줄은 토큰 쌍을 계산한 값이었고
> "앱은 그 조합을 한 번도 그린 적이 없다"고 2026-08-31 에 정정해 뒀던 자리다. 이번 구현이
> 그 조합을 처음 그렸고, 재 보니 계산값과 같은 5.21 이 나왔다.

**접힘 실측 (390px · 뱃지 11개 + 전체 칩)**

| 상태 | 줄 높이 | inert | 실제로 잘린 것 |
| --- | ---: | ---: | ---: |
| 접힘 | 82px | 4 | 4 |
| 펼침 | 110px | 0 | 0 |

가려진 뱃지 넷은 탭에서 빠진다 — 마지막으로 보이는 뱃지에서 Tab 을 누르면 「더 보기」로
바로 간다(`scrollTop` 0 유지). 2026-08-31 리뷰가 잡은 "가려진 21개가 전부 탭에 잡히고
줄이 144px 스크롤된다"가 재현되지 않는다.

> **구현하면서 같은 계열의 결함 둘을 만들었다가 실측에서 잡았다.**
> ① 「더 보기」를 눌러도 안 펴졌다 — `.expanded`(0,1,0)가 미디어쿼리 안의 `.list`(0,1,0)에
> **뒤에 온다는 이유로** 졌다. 2026-08-31 에 켠 뱃지 색이 죽은 것과 정확히 같은 모양이라
> `.list.expanded`(0,2,0) 복합 선택자로 고쳤다.
> ② 그때 `inert` 는 풀려 있었다 — "펼쳤으니 가려진 것 없다"고 **재지 않고 단정**했기 때문이다.
> 화면은 가리는데 탭은 통과하는 상태였다. 펼친 상태에서도 재도록 고쳐서, **재는 것 하나만**
> 판정하게 뒀다(「정한 것」 절의 "측정은 한 자리에서 한 번만"이 요구하는 바다).

## 2026-09-01 (2) · 리뷰 반영 — 모바일 뱃지 터치 · 상세 키워드 · 뱃지를 켜면 펴는 범위

`ui-reviewer`·`code-reviewer` 지적을 반영하며 정한 것. **새 시각 방향이 아니다** — 셋 다 이미
이 문서에 쓰여 있던 것을 코드가 못 지키고 있던 자리라, 시안을 새로 뽑지 않고 규칙과 실측만 고쳤다.

### 모바일 뱃지 줄 — 44px

「모바일 하단 고정 조작 줄」의 "누르는 것은 전부 44px 이상"을 뱃지 줄에도 적용한다.
그 문장은 고정 줄 절 안에 있었고 뱃지 줄에 44px 하한을 건 문장은 없었지만, **코드가 이미
그 목표를 선언해 놓고 못 닿아 있었다**(주석: "한손 조작 — 뱃지도 누르는 것이다").

- 640px 아래에서 `.badge` · `.toggle` 의 `min-height` 는 `--touch-min`(44px)
- 접힘 2줄 공식도 **같은 근거에서 뽑는다**: `--touch-min × 2 + --space-6` = 94px
  (데스크톱은 `--kw-badge-h × 2 + --space-6` 그대로다)

> **왜 고쳤나.** 이전 값은 `min-height: --kw-badge-h`(26px) + 세로 여백 6px 씩이었는데,
> `font: inherit` 로 body 의 `line-height: 1.6` 을 물려받아 글자 줄이 20.8px 이라 실제 높이가
> **32.8px** 이었다. 26px 은 물지도 않았다. 결과는 이 문서가 "엄지에는 안 통한다"고 판정한 그
> 33px 이고, 여백 비용만 내고 터치 이득이 없는 중간 상태였다. 되돌리든 올리든 중간은 아니다.
>
> 접힘 값도 같이 틀려 있었다. `(--kw-badge-h + --space-12) × 2 + --space-6` = 82px 은 줄 높이를
> 38px 로 가정한 값이라, 실제 32.8px 짜리 2줄이 71.6px 만 쓰고 **아래 10.4px 이 빈 채**였다.

### 상세 화면 키워드 칩 줄

2026-08-27 「카드와 상세」가 정한 "상세: 전부 보여준다"가 **구현에 통째로 빠져 있었다.**
토큰 `--kw-detail-chip-h` 는 정의만 되고 아무 데서도 안 쓰이는 상태였다.

- 자리: 메타 줄 아래 · AI 요약 박스 위
- 높이 `--kw-detail-chip-h`(24px) · 글자 `--fs-meta`(12px) · 축 색은 카드와 같은 토큰
- 접지 않는다(카드의 `4개 + N` 은 카드 폭 때문이고 상세는 704px 이라 그 압박이 없다)
- 스크린리더용 축 접두사("분야 " / "사건종류 ")는 카드와 같은 문구

### 뱃지를 켜면 집계 창만큼 편다

뱃지 숫자는 창 3일치를 센 값인데 목록은 하루씩 나와서, 「6」이라고 적힌 뱃지를 누르면
카드가 1장만 보이고 나머지 5장이 「더 보기」 뒤에 숨었다.

- 뱃지를 **켤 때** 보이는 날 수를 `max(지금, 3일)` 로 올린다
- **줄이지 않는다.** 이미 「더 보기」로 5일치를 펼친 사람에게는 보던 것이 사라지는 일이 된다
- **끌 때도 안 돌린다.** 「더 보기」를 두 번 누른 것과 같은 상태다

> 목록에 창을 거는 것이 아니다 — 「더 보기」의 **시작점**만 창에 맞춘다. 창 밖 글은 「더 보기」로
> 계속 갈 수 있어서 `keywords-and-kinds.md` INV-B4("뱃지 집계에만 창을 걸고 글 목록에는 안 건다")는
> 그대로 지켜진다.
>
> **숫자와 카드 수가 같아지는 것은 아니다.** 뱃지에 찍히는 값은 `unread` 이고 목록에는 읽은 것도
> 나오므로, 한 건이라도 읽으면 다시 갈린다. 숫자의 뜻이 원래 "카드 수"가 아니라 "안 읽은 수"다.

### 실측 (2026-09-01 · 앱, 390px / 1280px)

`npm run dev` 를 Playwright 로 열어 쟀다.

| 재는 것 | 값 |
| --- | ---: |
| 뱃지 높이(390px) | 44px |
| 「더 보기 / 접기」 높이(390px) | 44px |
| 접힘 줄 높이 | 94px (2줄: 0–44, 50–94) |
| 펼침 줄 높이 | 144px (3줄 × 44 + 간격 12) |
| 상세 키워드 칩 높이 | 24px |
| 상세 고정 줄 바깥 높이 | 68.7px (`--dock-h` 78px 안) |

- 접힘: inert 4 · 실제로 잘린 것 4 (일치) / 펼침: inert 0 · 잘린 것 0 · `scrollTop` 0.
  **빈 10.4px 이 사라졌다** — 2줄이 94px 를 정확히 채운다
- 대비: 피드 21종 · 미달 0 · 최저 4.57:1 / 상세 18종 · 미달 0 · 최저 4.75:1.
  **새 색을 하나도 안 만들었다** — 상세 칩은 카드 칩과 같은 축 토큰이다
- 1280px 가로 스크롤 0 (문서 1265 / 창 1280) · 콘솔 에러 0
- 날짜 그룹 사이 간격 6px — `.day + .day` 규칙이 **선택자만 있고 클래스가 안 붙어 죽어 있었다.**
  붙이니 의도대로 6px 이 생겼다

> **접힘 82px / 펼침 110px 은 낡은 값이다.** 위 「접힘 실측(390px · 뱃지 11개)」 표는 뱃지가
> 32.8px 이던 때의 값이고, 44px 로 올리면서 94 / 144 로 바뀌었다. 그 표를 지우지 않고 남기는
> 이유는 110px 이라는 값이 "줄 높이가 38px 이 아니라 32.8px 이었다"의 증거이기 때문이다.

## 2026-09-01 (3) · 재리뷰 반영 — 켜짐이 0건에서 죽던 것 · 주소가 근거

두 번째 리뷰가 잡은 것. 앞 블록과 같이 **새 시각 방향이 아니다** — 이미 정한 규칙이 코드에서
안 지켜지던 자리 셋과, 앞 블록을 만들면서 **내가 새로 낸 결함 둘**이다.

### 0건 뱃지를 켜면 포인트색이 죽었다 (같은 사고의 세 번째)

`.field.zero`·`.kind.zero` 가 `.badge.on` 과 명시도가 같고(0,2,0) 파일에서 뒤에 있어 이겼다.
실측: 켠 0건 뱃지의 글자가 `#605649`(축 0건 색)로 남았다. 「2026-08-27 · 누르기」가
"켜진 동안에는 축 색이 포인트색에 덮여 사라진다"고 정한 것과 반대다.

켜짐이 나르는 신호 셋 중 글자가 죽으면 남는 것은 이것뿐이다 — **둘 다 밝기로는 안 보인다.**

| 남는 신호 | 대비 |
| --- | ---: |
| `--accent-soft` 면 vs 꺼짐 면 | 1.07 : 1 |
| `--accent-ring`(22% 투명) 링 | 1.36 : 1 |

즉 그 상태에서 켜짐은 **색상으로만** 전해진다. 「상태를 색으로만 알리지 않는다」에 걸린다.

**고친 방법: 명시도를 올리지 않고 `:not(.on)` 으로 서로 배타로 만들었다.** 명시도 경쟁을
한 칸 더 올리면 다음 규칙이 또 같은 자리에서 부딪힌다 — 배타로 두면 순서에서 아예 떨어진다.
굵기(`.zero { font-weight: 500 }`)도 같이 `:not(.on)` 으로 묶었다: 굵기를 내린 이유가
"색만으로 흐리게 하면 하한에 부딪힌다"인데 켜진 순간에는 흐리게 하는 것이 아니다.
**이 판단(켜지면 600으로 돌아온다)은 문서에 없던 조합이라 내가 정했다.**

### 모바일 dock 의 좌/우 정렬이 미디어쿼리에 순서로 졌다

`.navPrev`/`.navNext`(0,1,0)가 640px 블록의 `.navButton { align-items: center }`(0,1,0)에
뒤에 온다는 이유로 져서, 화살표는 좌·우를 가리키는데 글자는 둘 다 가운데로 몰렸다.
승인 시안(`article-nav-and-mobile-dock.html:676`)은 명시도를 올려 좌/우를 지킨다.
`.readNav .navPrev`(0,2,0)로 맞췄다. 실측(390px): 「← 이전 글」 31–86px(버튼 16–191px 왼쪽),
「다음 글 →」 304–359px(버튼 199–374px 오른쪽).

### 비활성 호버를 레포 전체에서 한 방법으로 모았다

`:hover` 뒤에 원래 값을 **다시 적는** 방법은 기본값을 두 번 적는 것이라, 기본값이 다른 곳
(미디어쿼리)에서 바뀌면 조용히 틀린 값이 된다. 상세의 `.navButton` 이 그렇게 깨졌고,
뱃지 토글은 이번에 640px 블록을 이미 건드려서 다음 차례였다.
셋(`.navButton` · `.moreButton` · `.toggle`)을 전부 `:not([aria-disabled="true"]):hover` 로 두었다.
**레포에 "원래 값을 다시 적는 자리"가 이제 없다.**

### 주소가 화면 상태의 유일한 근거다

앞 블록의 「뱃지를 켜면 집계 창만큼 편다」를 만들면서 정렬·필터를 `useState` 에 두고 주소에는
베껴 쓰기만 했다. 그러면 주소가 쓰기 전용이 되어 뒤로가기가 안 맞는다 — **실제로 안 맞았다.**
「최신」으로 바꾸고 글을 연 뒤 뒤로 오면 주소가 `/` 로 돌아가고 화면도 「핫이슈」였다
(`history.replaceState` 는 Next 라우터가 자기 히스토리를 따로 들고 있어서 남지 않는다).

- 피드는 `useSearchParams()` 로 주소를 읽고 로컬 상태를 안 든다. 바꿀 때는
  `router.replace(href, { scroll: false })` — 히스토리를 안 쌓고 스크롤도 안 튄다
- **펼친 날 수(`days`)도 주소에 싣는다.** 정렬·필터만 싣던 동안 「더 보기」로 5일치를 본 뒤
  글을 열고 돌아오면 3일치로 접혔다. 셋 중 하나만 왕복에서 죽는 것이 제일 나쁘다
- 기본값은 안 싣는다. 뱃지를 켠 상태의 `days` 기본값은 집계 창(3)이라 그때는 그 값이 생략된다
- 주소를 조립하는 자리는 `entities/article/model/feed-url.ts` **하나다.** 전에는 피드 · 카드 링크 ·
  「돌아가기」 셋이 각자 조립했고 이미 갈려 있었다(카드만 `sort=hot` 을 실었다)

### 필터 밖 글의 주소로 들어오면 필터를 놓는다

상세가 **거른 목록으로 존재 판정까지** 하고 있었다. `/articles/a06?kw=field:MCP` 처럼 켠 뱃지가
안 붙은 글의 주소로 들어오면 **있는 글이 404** 였다(손으로 고친 주소·오래된 북마크·공유 링크).
존재 판정은 거르기 전 목록으로 하고, 그 글이 필터 밖이면 필터를 놓는다 — 그 목록에 없는 글이라
지킬 이웃 약속이 없고, 필터를 든 채로 두면 이전/다음이 둘 다 비어 주소만 거짓말이 된다.

### 켠 뱃지가 줄에 없어도 끌 수 있다

필터가 주소로도 들어오면서 "켠 뱃지는 반드시 줄에 있다"는 전제가 깨졌다. 창(3일) 밖에만
있거나 문턱(2건) 미만인 키워드로 링크를 받으면 집계에 안 잡히고, 문턱을 넘는 키워드가 하루도
없는 날에는 줄 전체가 사라져 **「전체」 칩까지 같이 없어졌다** — 필터를 끌 컨트롤이 화면에
존재하지 않게 된다. 켠 키워드가 줄에 없으면 숫자 0짜리 칩을 맨 앞에 세운다.

### 실측 (2026-09-01 · 앱)

| 재는 것 | 값 |
| --- | ---: |
| **켜짐 + 0건**(`--accent` on `--accent-soft`) | **5.21 : 1** |
| 피드(뱃지 켠 상태) 글자 요소 | 26종 · 미달 0 · 최저 4.57:1 |
| 모바일 dock 「이전 글」 글자 자리 | 31–86px (버튼 16–191px) |
| 모바일 dock 「다음 글」 글자 자리 | 304–359px (버튼 199–374px) |

- 뒤로가기 왕복 실측: 「최신」 → 글 열기 → 뒤로 → **주소 `?sort=latest` · 「최신」 눌림.**
  「더 보기」 두 번(`?days=3`) → 글 열기 → 뒤로 → **`?days=3` · 3일치 그대로**
- `/articles/a06?kw=field:MCP` 가 404 가 아니라 글로 뜬다
- 콘솔 에러 0

## 2026-09-01 (4) · 3차 리뷰 반영 — 왕복에서 죽던 마지막 하나 · 연타 · 합성 칩의 뜻

앞 블록 「펼친 날 수(`days`)도 주소에 싣는다」를 **한 경로에서 안 지키고 있었다.** 이번 회차는
그 나머지와, 조작이 서버 왕복을 타게 되면서 새로 생긴 자리들이다.

### 「피드로 돌아가기」에서 `days` 가 죽었다

`articleHref` 가 `sort`·`kw` 만 싣고 `days` 를 뺐다. 그래서 3일치를 펼쳐 놓고 그저께 글을 연 뒤
화면 위 링크로 돌아오면 **오늘치만 남아 방금 읽고 나온 글이 피드에 없었다.** 브라우저 뒤로가기는
제대로 복원하는데 링크만 다른 곳으로 갔다 — 두 경로가 같은 곳으로 가야 한다.
`articleHref` 도 `toFeedQuery` 와 같은 생략 규칙으로 `days` 를 싣는다.

**필터를 놓을 때는 `days` 도 같이 놓는다.** 그 값이 주소에 없이 "뱃지를 켰으니 3일"로 합성된
것이면 필터를 놓는 순간 근거가 사라진다 — 안 놓으면 필터도 안 걸고 「더 보기」도 안 눌렀는데
돌아간 피드가 3일치로 펼쳐진다. 주소에 `days` 가 적혀 있으면 그건 사용자가 실제로 펼친 값이라 둔다.

### 조작이 서버 왕복을 타면서 요청이 사라졌다

`router.replace` 는 서버 렌더가 끝나야 커밋된다. 그동안 주소는 예전 값이라, 그 값을 근거로
다음 요청을 만들면 두 가지가 깨진다 — **「더 보기」를 연달아 누르면 둘 다 `days: 1+1` 을 요청해
하루만 늘고**(실측: 두 번 눌러 `days=2`), **뱃지를 켠 직후 정렬을 바꾸면 켠 필터가 소리 없이 풀린다.**

`useOptimistic` 으로 **마지막으로 요청한 상태**를 화면이 보게 했다. 트랜지션이 끝나면 저절로
주소 쪽 값으로 돌아와서 뒤로가기와도 안 싸운다. 덤으로 누른 즉시 화면이 바뀌어서
"눌렀는데 아무 일도 안 일어난다"가 사라진다 — 그것이 연타를 부르던 원인이었다.

> **한계를 적어 둔다.** 두 클릭이 **같은 이벤트 루프 틱**에 들어오면 여전히 하나가 사라진다
> (React 상태는 동기적으로 안 바뀐다). 사람 손으로는 재현되지 않고 60ms·100ms 간격 실측에서는
> 둘 다 살아남는다. 자동 클릭·접근성 도구가 그렇게 부를 수 있으므로 아는 채로 둔다.

**상태 전이 규칙을 화면에서 뺐다.** 「켤 때 집계 창만큼 펴고 끌 때 안 줄인다」·「하루씩 늘린다」가
위젯에 있어서 테스트가 하나도 안 붙들고 있었다. `withKeyword` · `withMoreDays` · `withSort` 로
`entities` 에 옮기고 상한(`MAX_FEED_DAYS`)도 거기서 자른다 — 상한이 읽는 쪽에만 있으면 반쪽이라,
쓰는 쪽이 범위 밖 값을 실으면 읽는 쪽이 조용히 기본값으로 되돌려 **펼친 것이 통째로 접힌다.**

### 합성 칩의 0 은 「다 읽었다」가 아니다

켠 키워드가 집계에 없어 세운 칩(`total === 0`)과 다 읽어서 0 이 된 칩(`total > 0, unread === 0`)은
다른 것이다. 앞엣것을 뒤엣것과 같은 모양으로 그리면 두 상태가 한 모양이 된다.

- **취소선을 안 그린다.** 취소선은 「다 읽었다」의 표식이고 이 칩은 읽을 것이 아예 없는 것이다
- **낭독을 가른다.** 「건 — 다 읽었습니다」가 아니라 **「건 — 최근 3일 집계에는 없습니다」**
- 흐린 색·굵기는 0건과 같고, 켜지면 둘 다 포인트색으로 돌아온다

**켠 뱃지의 취소선은 유지한다** — 켜짐은 필터 상태고 취소선은 읽음 상태라 서로 다른 것을 나른다.
선 색은 `currentColor` 를 따라 포인트색이 된다.

### 모바일 dock 의 제목은 눈에서만 감춘다

640px 아래에서 제목을 감추는 것은 승인 시안 그대로다 — 고정 줄에 두 줄짜리 제목 둘이 들어가면
줄이 화면의 3분의 1을 먹고, 시안의 dock(68px = 버튼 44 + 위아래 24)은 처음부터 역할 글자만 그렸다.
데스크톱의 "제목을 같이 보여준다"는 그대로고 이 문장이 그 예외다.

**다만 감추는 것은 눈으로만이다.** `display: none` 이면 링크의 접근 이름이 「다음 글 →」뿐이 되는데,
제목을 같이 보여주는 근거("누르기 전에 무엇인지 모른다")는 화면을 못 보는 사람에게 더 세게 걸린다.
sr-only 와 같은 방식으로 뺀다. **끝에 닿았을 때는 역할 대신 이유를 보여준다**(「← 이전 글」 자리에
「첫 글입니다」) — 좁은 화면에서 역할 글자만 남으면 왜 안 눌리는지 알 방법이 점선 테두리뿐이다.

### 「원래 값을 다시 적는 자리」가 한 군데 더 있었다

앞 블록이 "레포에 이제 없다"고 적었는데 `.badge.on:hover` 가 남아 있었다(켜짐 면을 한 번 더 적어
축 호버를 누르는 방식). 축 호버 쪽을 `.field:not(.on):hover` · `.kind:not(.on):hover` 로 배타로 만들고
그 블록을 지웠다. **이제 정말로 없다.**

### 실측 (2026-09-01 · 앱)

| 재는 것 | 값 |
| --- | --- |
| 「더 보기」 3회(100ms 간격) | `days` 1→2→3, 세 번째는 `allLoaded` 라 막힘 |
| 뱃지 켜고 60ms 뒤 정렬 변경 | `?sort=latest&kw=…` — **필터 살아남음** |
| `?days=3` → 카드 → 「돌아가기」 | `/?days=3` — 펼친 3일치 그대로 |
| 「더 보기」 누른 뒤 포커스 | 버튼에 남음(body 로 안 떨어짐) · `scrollY` 0 |
| `aria-live` 영역 | 같은 노드로 문구만 갱신(「16건 중 6건」→「6건 중 6건」) |
| 합성 칩(`?kw=field:없는키워드`) | `.absent.on` · 취소선 없음 · 낭독 「최근 3일 집계에는 없습니다」 · 5.21:1 |
| 페이지 전체 | 17종 · 미달 0 · 최저 4.75:1 |
| 메인 고정 줄 바깥 높이 | **76.67px** (`--dock-h` 78px 안) |
| 맨 아래까지 내렸을 때 푸터 아래끝 ↔ 고정 줄 위끝 | **1.02px** (안 가림) |

> **여유가 1px 이다.** 상세(68.7px)만 적혀 있어서 지금까지 여유가 9px 처럼 읽혔다. 메인 고정 줄에
> 무엇 하나만 더 붙으면 푸터를 다시 가리고, 재기 전에는 안 보인다 — 2026-09-01 에 정확히 그렇게
> 한 번 물렸다.
>
> 시안 실측은 77px 이었는데 앱은 76.67px 이다. 세그먼트 버튼의 줄 높이가 소수점이라 그렇고,
> **시안 값을 그대로 옮겨 적지 않고 앱에서 다시 쟀다.**
