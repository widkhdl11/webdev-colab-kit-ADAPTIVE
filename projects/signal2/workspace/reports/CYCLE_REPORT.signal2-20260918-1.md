# 사이클 리포트 — `signal2-20260918-1` (종료)

주제: 결정 카드. 에이전트가 승인을 요청할 때 정해진 칸을 **사용자 말로** 채운 기록을 남기고,
대시보드가 그것을 화면 맨 위에 그린다. 채팅의 긴 합의문은 근거 자료로 남고 결정은 카드에서 한다.
대상은 하네스이고 제품 파이프라인 밖이다. 제품 코드는 한 줄도 안 건드렸다.

상태: **2026-09-18 닫힘 — 종료 조건 3(사용자 중단 선언).** 같은 턴에 `ACTIVE` 가 `signal` 로
바뀌면서 signal2 의 사이클을 전부 중단하기로 했다.

1~4 가 봐야 하는 전부다. 5~7 은 참고용이다.

---

## 1. 보류 목록 — 사람 결정 대기 (3건, 전부 이월)

이 사이클이 연 결정 하나는 닫혔다 — 아래 5절.

- **P1 · `git push origin main`** — 아무 노드도 막지 않는다. 사용자 행동이다.
- **P2 · Codex 미러(`.agents/`)를 계속 둘 것인가** — 규약상 두 벌을 맞춰야 해서 편집마다 비용이 든다.
- **P3 · `feed.tsx` 의 건수 표시 셋이 `keywords-and-kinds.md` INV-N5 와 반대 방향** — 코드를
  뺄지 스펙에 예외를 적을지. signal2 코드에 대한 것이라 `ACTIVE` 가 바뀌면 signal 쪽에서 다시
  판단해야 한다(같은 화면이 저쪽에도 있다).

셋 다 `projects/signal2/docs/BACKLOG.md` 가 아니라 이 리포트에 남는다 — 사람 결정 대기이고,
signal2 가 비활성이 되면 물어볼 자리가 없어지기 때문이다.

## 2. provisional 규칙 — 이번에 승격된 것 (1건)

- **승인 요청은 카드와 근거 본문을 같은 턴에 쌍으로 기록한다.** `CLAUDE.md` 「항상 지키는 규칙」에
  올라갔고 `docs/references/report-contract.md` 12절이 정본이다. 근거가 된 2건: ① 원문 본문
  합의문이 채팅에서 길어져 읽히지 않은 것 ② 대시보드에 승인 대기가 안 뜨던 것.
  뒤집는 방법: 그 규칙 문단과 12절을 지우고 `scripts/report-decision.mjs` 를 뺀다.

## 3. 디자인 방향 빈칸 (0건)

화면은 만들었지만(대시보드 카드 절) 승인된 시각 기준 안이다.

## 4. 구조 변경 요약

- 기록 규약이 하나 늘었다 — `docs/references/report-contract.md` 12절.
- 판정기와 기록 스크립트 — `scripts/lib/decision-model.mjs` · `scripts/report-decision.mjs`.
  카드를 쓰는 자리는 이 스크립트 하나뿐이다.
- 금지 표현 표 — `.claude/skills/report-dashboard/assets/decision-vocab.json` 과 설치본
  `projects/*/report/decision-vocab.json`.
- 대시보드가 signal2 에도 설치됐다(그전엔 study-mate 하나뿐이었다).
- 계약 테스트 43개 추가 — 218 → 261.

## 5. NEW 결정

- **카드 문장을 모델이 요약하지 않는다.** 요약이 틀려도 틀렸다는 신호가 안 뜨기 때문이다.
  기각한 대안: 합의문을 LLM 이 줄여 카드로 만든다.
- **카드 id 는 사이클 id 접두 + 별개 연번**(`signal2-20260918-2-d1`).
- **금지 표현 검사는 카드 칸만 본다** — 근거 본문에는 경로·규칙 이름이 있어야 한다.
- **첫 실전 카드 `signal2-20260918-2-d1`(원문 본문 착수 여부) = `아니`.**
  이유는 카드 밖에서 왔다 — 같은 날 `webdev-colab-kit-GRAPH` 의 `signal` 을 들여왔고,
  그 구현(`src/features/content-render/`)이 테스트까지 갖춰 이미 있었다.

## 6. 규칙 적용 결정

세지 않았다.

## 7. 지표

- HUMAN 개입: 4건(카드 규약 네 가지를 사용자가 정했다)
- NEW 결정: 4건
- 보류: 3건(상한 미달)
- 뒤집기: 0건

**실측 판정은 절반만 됐다.** "첫 세 칸만 읽고 답할 수 있나"를 보려고 실제 승인 요청을 카드로
다시 썼는데, 사용자의 답은 카드를 읽어서가 아니라 **바깥에서 온 새 사실**(signal 을 들여옴)로
정해졌다. 카드가 결정을 싸게 만들었는지는 다음 카드에서 다시 봐야 한다.
