# 결정 로그 — `signal2-20260903-1`

사이클 주제: v3.3 결정층 구축(0단계 · A단계). 대상은 하네스다.

기록 하한: 실재하는 대안이 있었고 기각 이유를 한 줄로 쓸 수 있는 결정만 적는다.
그 미만의 미시 선택(변수명·파일 배치 순서 등)은 적지 않는다.

`근거` 열: 적용한 규칙 id / `NEW`(커버 규칙 없음) / `HUMAN`(사람 개입).

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | api | 두 벌 대조를 줄 번호 나란히가 아니라 최장 공통 부분수열로 한다 | 삽입 하나에 뒤가 전부 밀려 한 블록 누락이 수십 건으로 불어났다. 안 보이는 보고는 사람이 읽지 않는다 | 줄 번호를 나란히 놓고 비교(처음 구현) | NEW |
| D2 | api | 어긋난 미러 파일을 손으로 깁지 않고 치환을 적용해 생성한다 | 미러 쪽에 고유 내용이 하나도 없고 다른 줄은 전부 정본의 옛 판본임을 실측으로 확인했다 | 빠진 블록만 손으로 삽입 | NEW |
| D3 | api | 치환 사전을 기존 관례(`.claude`↔`.Codex`) 그대로 둔다 | `.Codex/` 폴더가 실재하지 않는다는 것이 드러났지만 Codex 를 계속 쓸지가 먼저 정해져야 한다 | `.claude`↔`.agents` 로 즉시 교정 | HUMAN |
| D4 | data-model | 정책 규칙의 `scope` 를 단일 값이 아니라 목록으로 둔다 | 시드 규칙 셋 중 둘이 두 영역에 걸친다(`open-for-extension`=data-model·api, `symmetric-features`=api·ui). 단일 값이면 규칙을 쪼개야 하고, 목록으로 두는 비용은 읽는 쪽에서 `fmList` 를 쓰는 것뿐이다 | `scope: <어휘 하나>` (계획 문서의 초안) | open-for-extension |
| D5 | data-model | 규칙 파일명이 곧 `id` 이고, 다르면 규칙을 버린다 | 로그가 가리키는 `id` 로 파일을 못 찾으면 "그 규칙을 근거로 썼다"는 기록이 대조 불가능해진다 | `id` 를 파일명과 무관하게 자유롭게 두기 | NEW |
| D6 | data-model | `README.md` 는 규칙 목록에서 뺀다 | 폴더에 설명이 필요한데, 설명을 다른 곳에 두면 규칙 형식과 설명이 갈라진다 | 설명을 상위 폴더에 두고 정책 폴더는 규칙만 담기 | NEW |
| D7 | api | 계약 문서의 새 절을 1절 옆이 아니라 뒤(7절)에 붙이고 기존 번호를 그대로 둔다 | 다른 문서 둘이 이미 5절·6절을 번호로 인용하고 있다. 재배열하면 그 인용이 조용히 엉뚱한 절을 가리킨다 | 스펙 frontmatter 바로 뒤에 삽입하고 이후 절 번호를 밀기 | NEW |
| D8 | api | 못 읽는 규칙은 버리되 그 `scope` 를 사람에게 올린다 | 버리기만 하면 근거 없이 결정하고, 살리기만 하면 틀린 근거로 결정한다. 둘 다 나쁘므로 묶는다 | 스펙과 같은 방향(모르면 무효 취급하고 끝) | HUMAN |
| D9 | api | 규칙 읽기 경로를 소비자가 없는 지금 미리 낸다 | 자리가 정해지기 전에 소비자가 생기면 각자 정규식을 만든다 — v3.2 에서 네 곳 복붙이 사고를 냈다 | 첫 소비자가 생기는 B단계까지 미루기 | HUMAN |
| D10 | copy | 계약 문서 5절의 "위가 전부다"를 "이 문서에 적힌 것이 전부다"로 고친다 | 등재가 늘 때마다 그 문장이 조용히 거짓이 되는 자리였다 | 5절에 "7절 포함"을 덧붙이기 | NEW |

## 이 사이클에서 규칙이 실제로 근거가 된 자리

`open-for-extension` 이 D4 에서 쓰였다 — "트레이드오프가 크지 않으면 나중에 넓힐 수 있는
선택을 한다". `scope` 를 목록으로 두는 비용은 읽는 쪽에서 목록 읽기 함수를 쓰는 것뿐이었고,
단일 값으로 뒀다면 규칙이 두 영역에 걸칠 때마다 파일을 쪼개야 했다.

나머지 아홉 중 여섯이 `NEW` 다. 이 층을 막 만든 사이클이라 당연한 값이고, **이 숫자가
사이클을 거듭해도 안 줄면 그때가 이 층이 작동하지 않는다는 신호다.**
