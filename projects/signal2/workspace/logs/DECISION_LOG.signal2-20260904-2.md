# 결정 로그 — `signal2-20260904-2`

사이클 주제: intake 스킬 — 하네스 밖에서 만들어진 프로젝트를 읽어 문서 계약 형식의 docs 로
추출해 들여온다. 드라이런 대상은 `study-mate`. 대상은 하네스이고, 산출물에 프로젝트 문서
한 벌이 딸린다.

기록 하한: 실재하는 대안이 있었고 기각 이유를 한 줄로 쓸 수 있는 결정만 적는다.

`근거` 열: 적용한 규칙 id / `NEW`(커버 규칙 없음) / `HUMAN`(사람 개입).

| id | 영역 | 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|---|---|
| D1 | harness | intake v1 은 `projects/<slug>/src/` 를 만들지 않는다 | 게이트 셋(fsd·risk-surface·BEFORE_UI)이 편집한 파일이 아니라 프로젝트 트리 전체를 매번 본다 — 규약을 안 지키는 코드가 들어오면 레포 전체의 턴이 막힌다 | 사이클 기준선을 지금 구현해 들여온 코드의 기존 실패를 면제하기 | HUMAN |
| D2 | harness | 기준선 대신 **프로젝트 단위 스캔 범위 선언**을 백로그에 트리거와 함께 등재한다 | 기준선은 사이클마다 스냅샷을 다시 떠서 들여온 위반이 영구 면제되고, 줄어드는 경로가 생기지 않는다 | 기준선을 이번 사이클에 구현 | NEW |
| D3 | harness | 소스 파일 경로·대응표는 `docs/` 가 아니라 `workspace/` 리포트에 둔다 | 소스에 `scripts/` 폴더가 실제로 있어 그 경로를 docs 에 적으면 이전 리허설이 하네스 참조로 잡고, 한 스크립트가 전 프로젝트를 함께 판정하므로 다른 프로젝트 검사까지 같이 실패한다 | docs 에 출처 경로를 적고 리허설 패턴을 완화하기 | NEW |
| D4 | harness | kickoff↔intake 갈림을 스킬이 아니라 협업 규칙 문서의 세션 시작 절에 적는다 | 세션 시작에 읽히는 것이 그 문서이고, 들여올 프로젝트는 현재 kickoff 신호("합의문이 비어 있다")에 그대로 걸린다 | intake 스킬 description 에만 적기 | NEW |
| D5 | harness | 갈림의 신호를 "코드가 있나"가 아니라 **"동작하는 코드가 있나"**로 한다 | 골격만 만든 빈 프로젝트도 코드가 있어서, 그대로 두면 스캐폴딩 직후 상태가 intake 로 간다 | "projects/ 아래에 없으면 intake" | NEW |
| D6 | harness | `check-intake` 를 상시 게이트에 배선하지 않는다 | "스펙 전부 draft" 는 사람이 스펙 하나를 승인하는 순간 실패해야 마땅한 검사가 아니라 산출물에 대한 1회 판정이다 | run-gates 에 카테고리로 편입 | HUMAN |
| D7 | harness | `[추정]` 건수를 리포트에 손으로 적지 않고 검사 출력을 그대로 붙인다 | 손으로 적은 숫자는 산출물과 어긋나고, 어긋나면 어느 쪽이 맞는지 알 방법이 없다 | 리포트에 숫자를 적고 검사가 그 줄을 파싱해 대조하기 | NEW |
| D8 | harness | 시각 방향은 **발명 금지·보존 허용** — 코드 역산은 금지하되 사람이 써 둔 명시적 선언은 `[문서에서 확인]` 으로 옮긴다. status 는 draft, 유효성은 질문 목록으로 | 코드에서 역산할 수 없는 것과 사람이 이미 써 둔 것은 다르다. 후자를 버리는 것은 신중함이 아니라 있는 지식을 버리는 것이다 | 지시 원안대로 방향 절을 비워 두기 | HUMAN |
| D9 | harness | 사이클 파일은 활성 프로젝트를 바꾸는 동안에도 `signal2` 의 과정 기록에 둔다 | 한 사이클의 기록이 두 폴더로 갈리면 리포트가 자기 로그를 못 찾는다 | 활성 프로젝트를 따라 사이클 파일도 옮기기 | NEW |
| D10 | harness | 앞 사이클(`-1`)을 닫지 않고 병행한다 | 남은 일이 전부 사용자 행동(패치 적용·푸시)이라, 지금 닫으면 안 끝난 상태로 마감 리포트를 쓰게 된다 | `-1` 을 먼저 닫고 시작 | HUMAN |
| D11 | harness | 하네스 참조 패턴 목록의 사본을 하나 더 만들고, 합치는 대신 백로그에 등재한다 | 합치려면 지금 통과 중인 검사 둘을 고쳐야 하는데 그건 이번 사이클의 대상이 아니고, 갈라져도 조용하지 않다 — 절차가 이 판정 바로 뒤에 전 프로젝트 리허설을 돌린다 | 지금 `scripts/lib/` 로 모으고 리허설을 고치기 | NEW |
