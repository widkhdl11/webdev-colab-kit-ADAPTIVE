---
feature: (기능명)
status: draft        # draft → (spec-auditor 감사) → 사람 승인 후 approved. approved 전 구현 금지
---
# (기능명) 스펙

<!--
  이건 킷 정본 스펙 템플릿이다. 새 프로젝트를 스캐폴딩하면
  projects/<이름>/docs/specs/_TEMPLATE.md 로 복사된다 (원본: docs/references/spec-template.md).
  실제 스펙은 이 파일을 복사해 projects/<이름>/docs/specs/<기능명>.md 로 쓴다.
  ─ 이 파일과 사본(_TEMPLATE)이 게이트에 안 걸리는 이유는 **이름이 아니라 frontmatter 의 `status: draft`** 다.
    spec-coverage 에 밑줄 접두 예외는 없다(2026-08-10 확인) — `_` 를 붙였다고 면제되지 않으니
    approved 로 바꾸지 않는 것이 유일한 방벽이다.

  ▶ 고도화 규칙: 스펙을 쓰다 "여기 물어봤어야 했다"는 빈칸을 발견하면 retro에서 이 정본
    (docs/references/spec-template.md)을 보강한다. 특정 프로젝트 사본만 고치지 말 것 —
    다음 프로젝트가 같은 빈칸에 다시 빠진다. 프로젝트 진행할수록 이 템플릿이 두꺼워지는 게 정상.
-->

<!-- [작성 전 1분 체크 — 다 쓰면 이 주석 블록은 지운다]

  1) 이 기능은 왜 스펙이 필요한가? (아래 중 하나라도 닿으면 정식 스펙 대상)
     결제·인증·권한·격리 · 동시성 · 시간에 따라 변하는 상태·파생 속성 · 신뢰 경계(외부 입력).
     닿지 않으면 스펙 없이 진행해도 된다 (단순 UI/콘텐츠).

  2) 데이터 모델(엔티티·필드)을 건드리나? → docs/references/modeling-checklist.md 를 필드 단위로 훑고,
     아래 9개 함정 중 걸리는 것만 INV로 옮긴다. (추상적으로 묻지 말고 도출된 필드를 앞에 놓고 물을 것)
       ① 시간에 따라 저절로 변하는 값인가        ② 다른 값에서 파생되나 (예외 처리?)
       ③ 과거 시점 값을 되돌아봐야 하나          ④ 삭제되면 어떻게 되나 (소프트?)
       ⑤ 상태 전이가 있나 (되돌릴 수 있나)        ⑥ 값이 바뀌면 기존 데이터가 위반되나
       ⑦ 유일성 기준이 무엇인가 (대리키?)        ⑧ 기준 시점·단위·시간대가 명시됐나
       ⑨ 하나인가 여러 개인가 (카디널리티)
     ▶ 권장 패턴: 시변·파생값은 저장하지 말고 앵커(원천+규칙)를 저장하고 조회 시 계산한다.

  3) 항목마다 외부 유료 호출(모델 API 등)이나 반복 작업이 붙나? → 설계를 정하기 전에 숫자를 잰다.
       ① 실제로 적용되는 항목의 비율   ② 한 건 단가   ③ 제일 비싼 한 건
     평균만 보면 이 셋 중 아무것도 안 보인다. 재고 나서 INV 를 쓴다 — 실측이 설계를 세 번 바꿨다:
     본문 번역은 본문이 있는 항목이 11% 뿐이라 제목 번역으로 교체 · 제일 비싼 한 건(주제 밖 글)이
     평균의 5배라 걸러내는 판정을 요약 앞으로 당김 · 지시문을 조건별로 안 가르면 번역만 하는 항목이 7배.

  4) 각 INV는 아래 4가지가 다 있어야 한다 (없으면 아직 산문이지 스펙이 아니다):
     · 참/거짓으로 판정 가능한 문장   · 위반 시 무슨 일이 일어나는지
     · 신뢰 경계 (믿는 값 / 안 믿는 값)   · 어디서 강제되나 (서버 RLS / 클라 / 게이트 / DB 제약)
-->

<!-- 한 문단: 이 기능이 무엇이고, "어기면 사고"인 핵심 위험이 무엇인지. (독자가 왜 이 스펙이 있는지 알게) -->

## 불변식 — 각각 참/거짓 판정 가능한 문장. 위반 시 무슨 일이 일어나는지, 어디서 강제되는지 명시

<!-- ID 규칙: INV-<기능접두><번호> (예: 인증=INV-A1, 랭킹=INV-R1). 산문 금지 — ID 없는 규칙은 테스트가 참조 못 한다. -->

- INV-X1: ... (강제 위치: 서버 / 클라이언트 / 게이트 / DB 제약)
  위반 시: ...

## 시나리오 — 각각 어느 불변식을 검증하는지 ID 참조 (불변식마다 실패 경로 1개 이상)

<!-- Given/When/Then. 정상 경로 + 실패(거부) 경로. 테스트 설명문에 INV ID를 넣어야 spec-coverage가 추적한다. -->

- S1 (INV-X1): Given ... / When ... / Then ...
- S1b (INV-X1, 실패경로): Given ... / When ... / Then 거부 / 무시 / 변화 없음.

## 신뢰 경계

<!-- 믿는 값 = 서버가 검증했거나 우리가 통제하는 값. 안 믿는 값 = 클라이언트·URL·외부 응답 등 표시/UX용일 뿐 판단 근거가 아닌 값. -->

- 믿는 값: ...
- 믿지 않는 값: ...

## 비범위

<!-- 이 스펙이 일부러 다루지 않는 것. 나중에 "왜 이건 안 막았지?"를 예방. -->

-
