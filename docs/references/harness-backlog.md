# 하네스 백로그 — 보류된 업그레이드 (트리거로 재검토)

지금은 값싸게(프로즈로) 두고, **무엇이 관찰되면 승격할지**를 트리거로 적어둔다.
잊지 않기 위한 장치: retro가 이 목록의 트리거 충족 여부를 훑고(②), briefing이 미결 건수를 띄운다(③).

- 미결 항목은 `- [ ]`, 승격·반영되면 `- [x]` 로 바꾸고 아래 "졸업"으로 옮긴다.
- 형식: 결정(지금은 무엇을) · 보류 이유 · **승격 트리거**(무엇이 관찰되면) · 근거.

## 미결 (보류 중)

- [ ] **브리핑의 순서 가드가 "데이터 계층을 만든다"와 "만든 것을 리뷰한다"를 구별 못 한다** (2026-09-05)
  - 지금: 가드가 `다음 할 일` 문장에서 낱말로 판정한다. 2026-09-05 랩업에서 "스키마에
    security-reviewer 를 파견해 review 를 닫고, 이어서 홈 화면을 만든다"라고 적었더니
    **"데이터 계층이 화면보다 먼저 적혀 있다"로 경고**했다. 화면은 같은 문장 뒤에 있고,
    앞부분은 만드는 일이 아니라 **이미 만든 것을 리뷰하는 일**이다.
  - 왜 문제인가: 프론티어가 `review` 를 가리키는 상태에서 그대로 적으면 매번 경고가 뜬다.
    **상수가 된 경고는 아무도 안 본다** — 이 레포가 이미 두 번 겪은 실패 모양이다.
    반대로 경고를 피하려고 문장을 비틀면 가드가 문장 표현만 검사하는 장치가 된다.
  - 보류 이유: 1회 관찰이다. 그리고 고치는 방향이 둘인데 어느 쪽인지 아직 모른다 —
    ① 낱말 대신 **프론티어 노드**를 보고 판정한다(프론티어가 review 면 순서 원칙을 적용하지
    않는다) ② 리뷰·검증을 뜻하는 낱말을 예외로 둔다. ①이 더 옳아 보이지만 가드가 지키려는
    것이 "화면 국면이 남았는데 데이터로 새는 것"이라 프론티어만으로 충분한지 확인이 필요하다.
  - **승격 트리거**: 같은 오탐이 한 번 더 관찰되면. 그때 고칠 검사에는 **진짜 위반(화면이
    남았는데 데이터 계층을 먼저 적은 문장)을 일부러 심어 잡히는 것을 보는 항목**을 넣는다 —
    안 그러면 "경고 0건"과 "가드가 아예 안 돈 것"이 겉이 같다.
  - 근거: 2026-09-05 study-mate 랩업. `scripts/briefing.mjs` 의 순서 점검.

- [ ] **시안과 DB 설계의 순서를 그래프가 표현하지 못한다** (2026-09-05)
  - 지금: `design/page-designer` 와 `design/schema-designer` 는 **형제(parallel)** 라 서로
    순서가 없다. 그런데 사용자가 정한 순서는 "시안을 보고 결정 → 승인 → 그다음 DB 설계"다.
    시안을 보고 나서야 필요한 필드가 보이고, 순서를 뒤집으면 설계를 두 번 한다.
  - 그리고 schema-designer 에는 page-designer 와 **같은 모양의 구멍**이 있다. HANDOFF 에
    `"hash": null`(파일이 하나도 없다는 뜻)인데 status 는 `clean` 이다 — 조건이
    `gate: ["fsd","security"]` 뿐이라 검사할 파일이 0개면 에러도 0건이라서 통과한다.
  - 보류 이유: 구멍만 막으려고 `exists_nonempty` 를 걸면 **시안을 그리기도 전에 마이그레이션을
    요구하게 되어 순서가 거꾸로 걸린다.** 순서를 표현하려면 토폴로지를 바꿔야 하고
    (schema-designer 가 page-designer 에 `depends_on`), 그건 데이터 한 줄이 아니라 구조 변경이다.
    형제를 부모-자식으로 바꾸면 `parallel` 의 집계 규칙·`--mark` 의 자식 지정·전파가 다 걸린다.
  - **승격 트리거**: study-mate 의 시안이 checkpoint 승인되어 실제로 DB 설계로 넘어가는 순간.
    그때 이 순서가 기계에 없으면 또 사람이 기억해야 한다. **검사에는 "시안 승인 전에
    마이그레이션을 만들면 잡히는가"를 일부러 심어 확인하는 항목을 넣는다.**
  - 근거: 2026-09-05 사용자 지적("전 하네스부터 잡아놨던 워크플로우인데 왜 빠진거야").
    page-designer 쪽 절반은 `docs/references/pending-patches/2026-09-05-mockup-required.md` 로
    먼저 냈다 — 그쪽은 형제 관계를 안 건드리고 데이터 한 줄로 끝난다.

- [ ] **가드를 안 거친 서버 액션을 잡는 장치가 없다** (2026-09-04) — **리뷰어 셋이 독립적으로 지적**
  - 지금: 세션 가드(`requireSession`)는 만들어졌지만 **액션이 그 가드를 거치는지는 아무것도 안 본다.**
    누군가 `deleteStudyAction` 을 가드 없이 export 하면 INV-A4 가 깨지는데 빨간불이 하나도 안 뜬다.
    risk-surface 게이트는 "auth 표면에 approved 스펙이 있나"만 보고, 유닛 테스트는 가드 자체만 본다.
  - 보류 이유: 지금 이 프로젝트에 서버 액션이 **하나도 없다.** 검사를 지금 붙이면 "위반 0건"과
    "검사가 안 돈 것"이 겉이 같은 상태로 시작한다 — 그건 방벽이 아니라 방벽의 모양이다.
  - **승격 트리거**: study-mate 에 `"use server"` 파일이 처음 생기는 순간. 그때 붙일 검사는
    "액션 파일에서 export 되는 async 함수가 가드를 통과한 것인지" 보는 게이트다(소스 스캔이어도 된다 —
    테스트가 아니라 게이트다). **검사에는 가드 없는 액션을 일부러 심어 잡히는 것을 보는 항목을 넣는다.**
  - 근거: 2026-09-04 세션 가드 리뷰. security-reviewer(M3) · code-reviewer(M3) · test-auditor(6) 가
    같은 자리를 따로 지적했다. 구현 주석도 "막을 방법이 없다"고 스스로 적고 있다.

- [ ] **깨진 에이전트 파일이 조용히 사라진다** (2026-09-04) — **파일은 고쳤다. 재발 방지는 미결**
  - 무엇이 있었나: `.claude/agents/test-auditor.md` 의 `description` 이 `"` 로 시작하고 그 뒤에
    본문이 이어졌다. 겹따옴표로 시작한 값은 닫는 따옴표에서 끝나야 하므로 그 뒤 글자가 문법 오류다.
    그래서 이 에이전트만 **세션 시작 시 등록되지 않았다** — 오류 메시지도, 빠졌다는 표시도 없다.
    review 사인오프 문서는 `test-auditor` 를 표면별 리뷰어 넷 중 하나로 적고 있는데, 파견하려는
    순간에야 목록에 없다는 것을 알았다.
  - 확인 방법(2026-09-04): `.claude/agents/*.md` 일곱 중 description 이 따옴표로 시작하는 것은
    이 파일 하나였고, 세션에 등록되지 않은 것도 이 파일 하나였다. 이 레포에 YAML 파서가 없어
    파싱 실패 자체를 실행으로 보이지는 못했다. 파일은 홑따옴표로 감싸 고쳤고, 등록은 다음 세션에
    확인된다(등록은 세션 시작 때만 읽힌다).
  - 보류 이유: 검사를 붙이려면 frontmatter 를 진짜로 파싱해야 하는데 파서가 없다. 정규식으로
    흉내 내면 검사 자체가 또 하나의 거짓 통과를 만든다.
  - **승격 트리거**: 같은 일이 한 번 더 나거나, 에이전트·스킬 파일이 지금보다 늘면 →
    `.claude/agents/*.md` 와 `.claude/skills/*/SKILL.md` 의 frontmatter 를 파싱해 보고,
    **이름 목록이 파일 목록과 일치하는지**까지 대조하는 검사를 붙인다. 검사에는 일부러 깨뜨린
    파일이 잡히는 항목을 넣는다.
  - 같이 볼 것: `check-mirror-sync.mjs` 가 이미 `.claude`↔`.agents` 의 **존재**를 대조한다.
    거기에 "읽히는가"를 얹는 편이 새 검사를 만드는 것보다 싸다.

- [ ] **위험 표면 감지가 Supabase 의 지금 API 를 못 본다** (2026-09-04) — **실측으로 확인됨. 방벽이 열려 있다**
  - 지금: `run-gates.mjs` 의 `RISK_SURFACES.auth` 는 `auth.getUser`·`auth.getSession`·`cookies()`·
    `access_token`·`Bearer` 같은 이름을 찾는다. 그런데 지금 Supabase 가 **서명을 검증하는 방법으로
    권하는 것은 `getClaims()`** 이고, `@supabase/ssr` 의 쿠키 처리는 `request.cookies.getAll()` ·
    `response.cookies.set()` 이다. 둘 다 목록에 없다.
  - 관찰(2026-09-04): study-mate 에 세션 가드를 넣은 뒤 `auth-session.md` 의 `status` 를 잠깐
    `draft` 로 내리고 게이트를 돌렸더니 **통과했다**(exit 0). 스펙이 없어도 이 코드는 들어올 수 있었다.
    감지 신고줄에도 study-mate 의 auth 표면은 뜨지 않는다 — wama 것만 뜬다.
  - 보류 이유: 패턴을 늘리는 일은 오탐이 늘어나는 일이기도 하다. `cookies` 를 낱말로 잡으면
    쿠키를 만지는 모든 UI 코드가 걸리고, 그건 `for update`·`ON CONFLICT` 가 이미 겪은 오탐이다.
    어떤 표현까지 잡을지는 실제 코드를 몇 개 더 보고 정하는 편이 낫다.
  - **승격 트리거**: 인증 코드가 study-mate 에 한 번 더 늘어나면(로그인·회원가입 액션) 그때
    실제 파일들을 놓고 패턴을 정한다. 최소한 `getClaims` 는 지금 목록의 `getUser`·`getSession`
    옆에 들어가야 한다 — 이건 오탐 위험이 없다.
  - 같이 볼 것: 이 구멍은 "게이트가 통과했다"가 "검사가 돌았다"를 뜻하지 않는 또 하나의 사례다.
    위반을 심어 보지 않았으면 통과를 그대로 믿었다.

- [ ] **`protect-secrets` 훅이 `process.env.X` 를 환경파일 읽기로 본다** (2026-09-04) — **오탐. 우회는 있다**
  - 지금: 훅은 명령에 환경파일 이름 패턴이 있고 동시에 `cat`·`grep` 류가 있으면 막는다.
    그런데 `process.env.NEXT_PUBLIC_...` 도 그 이름 패턴에 걸린다. 그래서 **환경변수를 읽는
    소스 코드를 heredoc(`cat > 파일`)으로 쓰는 것**이 "환경파일 내용 출력"으로 차단된다.
  - 관찰(2026-09-04): study-mate 미들웨어 클라이언트를 Bash 로 쓰려다 두 번 막혔다.
    Write 도구로 우회했으므로 작업은 멈추지 않았다.
  - 보류 이유: 훅은 보호 파일이라 사용자가 적용한다. 그리고 과차단은 과통과보다 안전한 쪽이다 —
    급하지 않다.
  - **승격 트리거**: 같은 차단이 한 번 더 나오면 고친다. 고칠 방향은 "읽기 명령"과 "쓰기 리다이렉션"을
    가르는 것(`cat > 파일` 은 읽기가 아니다)이고, 패치에는 **진짜 환경파일을 `cat` 하는 시도가
    여전히 막히는지** 확인하는 항목이 반드시 들어가야 한다.

- [ ] **`last_applied` 로 오래 안 쓰인 규칙을 걷어내는 위생 판정** (2026-09-03) — **값은 쌓이기 시작했다. 판정만 미결**
  - 지금: 로그가 근거로 인용한 규칙의 `last_applied` 에 그 사이클이 없으면 게이트가 실패한다
    (`[cycle/LAST_APPLIED]`). 그래서 "언제 마지막으로 쓰였나"가 규칙 파일에 실제로 쌓인다.
    쌓인 값을 **읽어서 판정하는 쪽은 아직 없다.**
  - 보류 이유(사용자 결정, 2026-09-03): 값이 세 건뿐이라 "오래 안 쓰였다"의 기준을 정할 근거가
    없다. 몇 사이클이 지나야 오래된 것인지는 데이터를 보고 정해야 한다.
  - **승격 트리거**: 정책 규칙이 열 개를 넘거나, 사이클이 다섯 번 이상 쌓여 `last_applied` 분포가
    보이면 → 오래 안 쓰인 규칙을 리포트에 띄우는 판정을 붙인다(지우는 것은 사람이 한다).
  - 근거: 2026-09-03 v3.3 검증. 계약 문서 7절은 이 필드를 "위생 판정에 쓴다"고 등재했는데
    읽는 쪽도 쓰는 쪽도 없었다. 쓰는 쪽만 먼저 붙였다.
  - **2026-09-04 관찰 — 이름이 값과 다르다.** 검사는 사이클마다 로그를 따로 대조해서
    `last_applied` 에 **그 사이클이 들어 있는지**를 본다(쉼표 목록). 규칙 하나가 두 사이클에서
    근거가 되면 값이 둘이 되므로 "마지막"이 아니라 "전부"다. 계약 문서는 코드에 맞춰 고쳤지만
    **필드 이름은 그대로 뒀다** — 이름을 바꾸면 검사·게이트·규칙 파일을 한꺼번에 고쳐야 하고,
    지금 값이 다섯 건뿐이라 헷갈릴 일이 없다. 목록이 길어져 실제로 오해가 생기면 그때
    이 위생 판정과 **같이** 고친다(따로 고치면 두 번 만진다).

- [ ] **훅 검사(`check-hooks`)를 언제 돌릴지가 사람 손에 있다** (2026-08-16) — **도구는 있고 트리거만 없다**
  - 지금: `node scripts/check-hooks.mjs` 를 손으로 돌린다. 훅이 실제로 막는지 + 닿아야 할 도구에
    등록됐는지를 검사한다(현재 56건). 2026-08-16 에 이 검사가 없어서 보호 5개 중 4개가 죽어 있던 것을
    한참 뒤에야 발견했다 — 훅은 등록돼 있었고, 실행도 됐고, 그냥 통과시켰다.
  - 보류 이유(사용자 결정, 2026-08-16): **아직 하네스를 엔지니어링하는 중이라** `.claude/` 를 자주 만지는데,
    편집마다 2초짜리 검사가 붙으면 작업이 느려진다. 지금은 수동으로 충분하다.
  - **승격 트리거**: 하네스 변경이 잦아드는 시점이 오거나, 보호가 다시 조용히 깨진 게 관찰되면
    → `settings.json` 의 PostToolUse 에 훅 하나(편집 대상이 `.claude/` 아래일 때만 check-hooks 실행).
    훅 파일이라 제안만 하고 사용자가 반영한다.
  - **2026-09-04 트리거 충족 — 패치를 냈다.** 사용자가 v3.3 을 닫고 제품 작업으로 넘어간다고
    선언했다. 보류 이유가 "아직 하네스를 엔지니어링하는 중이라 `.claude/` 를 자주 만진다"였으니
    그 전제가 깨진 자리다. `scripts/check-hooks-on-edit.mjs` 신규 + `check-hooks` 에 배선 검사
    4건(58 → 62). 설정 한 곳은 사용자 적용 대기:
    `docs/references/pending-patches/2026-09-04-check-hooks-auto.md`.
    실측: `.claude/` 편집 2.4초 · 그 밖 86ms. 붙기 전 61/62(exit 2), 붙은 뒤 62/62(exit 0).
  - 근거: 2026-08-16 세션. 검사기 신설은 같은 날 반영됐고, 자동 실행만 보류됐다.

- [ ] **문서에 박힌 줄 번호 참조가 조용히 썩는다** (2026-08-16) — **참조는 전부 고쳤다. 재발 방지는 미결**
  - **3번째 관찰(2026-09-02, v3.2)**: 새 사실 하나 — **재매핑 도구는 기준 판본이 틀리면 오차를
    줄이지 않고 늘린다.** `--base` 를 이번 브랜치의 중간 커밋으로 주고 돌렸더니 의심이
    38 → **43** 이 됐다. 문서의 줄 번호가 그 판본과 애초에 안 맞아서, 틀어진 번호를 기준으로
    옮겨 오차를 실어 날랐다(도구 머리말이 경고하는 바로 그 경우다). 되돌렸다.
    → **운용 규칙 하나**: 재매핑 뒤 `check-doc-refs` 총계가 **줄지 않으면 즉시 되돌린다.**
      기준 판본이 틀렸다는 신호이고, 그 상태로 커밋하면 오차가 다음 회차의 기준이 된다.
    → 현재 낡은 참조 38건은 전부 `docs/v2/**` 안이고 v3.2 가 민 것이 아니다(대상 파일을
      안 건드렸다). 별도 항목으로 아래에 있다.
  - 관찰: `gates/run-gates.mjs` 참조 58개 중 절반 가까이가 어긋나 있었다. risk-surface 블록이 커지면서
    그 뒤 참조가 200줄 넘게 밀렸는데 아무도 몰랐다 — 예: `design/BEFORE_UI` 를 L213-225 라고 적어 뒀는데
    실제로는 L455-468. `graph.mjs` 참조 80개는 **더 심하다** — `implement.clean_when` 을 L64 라고 하는데
    실제로는 L82 고, 오늘 변경과 무관하게 이전부터 그랬다.
  - 왜 위험한가: 링크를 눌러 엉뚱한 코드를 보면 문서가 틀린 게 아니라 **코드가 그런 줄 안다.**
    게이트가 안 보는 층이라 실패가 소리를 안 낸다(브리핑 파서 어긋남과 같은 계열).
  - 지금: **2026-08-16 에 전부 고쳤다** — run-gates 참조 58개 + graph.mjs 참조 80개 + 링크 밖 본문 참조 10개.
    일괄 이동이 아니라 문서 문장과 코드를 하나씩 대조했다(같은 옛 줄 번호가 문서마다 다른 것을 가리키고 있었다).
    같이 드러난 것: 오늘 변경으로 **사실이 틀려진 문장** 6곳(인용된 `clean_when` 목록, "테스트가 없으면
    qa 가 아무것도 안 보고 clean 이 된다" 등)도 함께 고쳤다.
  - **재발은 같은 세션 안에서 확인됐다**: 참조 58개를 고친 뒤 게이트 파일에 7줄을 넣자 그중 24개가
    다시 밀렸다. 게이트·그래프를 고칠 때마다 기계적으로 재발한다.
  - **2026-08-16 반영**: `scripts/check-doc-refs.mjs` 신설(retro 승인). 기계가 확신하는 것만 실패로
    잡는다 — 범위 밖 · 빈 줄 · 닫는 괄호만 가리키는 참조. 나머지는 `--all` 로 사람이 대조한다.
    게이트로는 안 만들었다: 문서 링크 하나 때문에 작업이 막히는 건 비용이 더 크다.
  - **남은 것(새 발견)**: 검사기를 문서 전체에 돌리니 **참조 532개 중 43건이 의심**이다 —
    `CLAUDE.md`(11) · `gates/graph-stop.mjs`(10) · `.claude/rules/tdd.md`(4) · `settings.json`(4) ·
    `scripts/briefing.mjs`(4) 등, 오늘 고친 두 파일 밖이다(그 둘은 0건).
    한 건씩 문장과 코드를 대조해야 해서 오늘 범위에 넣지 않았다.
  - **2026-08-17 재측정: 45건으로 늘었다.** 그날 `run-gates.mjs`(+17줄)·`graph-stop.mjs` 를 패치하면서
    그 파일을 가리키던 참조가 또 밀렸다 — **게이트를 고칠 때마다 기계적으로 재발한다**는 위 관찰이
    같은 세션 안에서 다시 확인된 셈이다.
  - **2026-08-30 3번째 재발 + 도구 승격**: 게이트 패치로 참조 84개가 밀렸다(`graph-stop` 460→519줄).
    이번엔 손으로 옮기지 않고 **`scripts/remap-doc-refs.mjs`** 를 만들어 돌렸다(retro 승인) —
    줄 번호 산술이 아니라 기준 판본에서 그 줄의 *내용*을 꺼내 새 파일에서 찾는다. 76개 자동, 못 찾은
    8개는 손으로 봤는데 **전부 이번에 밀린 게 아니라 원래부터 엉뚱한 코드를 가리키던 것**이었다
    (예: "분류기 필요를 출력한다"가 리뷰어 부족 메시지를 가리킴). 그건 문장이 말하는 코드로 다시 겨눴다.
  - **같은 세션에서 도구 없이 한 번 더 틀렸다**: `CLAUDE.md` 에 3줄을 넣고 재매핑을 건너뛴 채 커밋해
    참조 3개가 빈 줄로 밀렸고, 나중에 `HEAD` 기준으로 돌리자 그 오차를 그대로 실어 날랐다.
    도구에 `--base <커밋>` 을 붙여 "참조가 마지막으로 맞았던 판본"을 고를 수 있게 하고 되돌려 고쳤다.
    **교훈: 게이트만이 아니라 참조되는 파일을 고쳤으면 전부 돌려야 하고, 커밋 전에 돌려야 한다.**
  - **2026-08-31 4번째 재발 + 새로운 종류의 비용**: `spec-coverage.mjs` 패치를 붙이다 `}` 가 앞줄로
    딸려 올라가 66→65줄이 되면서 문서가 가리키던 `L38`·`L56`·`L60` 이 엉뚱한 곳을 가리켰다
    (`}` 를 제자리로 돌리자 저절로 맞았다). 여기까지는 늘 나던 것인데, **그 뒤가 새롭다** —
    같이 넣으려던 설명 주석 6줄을 "넣으면 6줄이 밀린다"는 이유로 **안 넣었다.**
    `remap-doc-refs.mjs` 를 돌리면 되는 일이었는데 안 돌렸다.
    **줄 참조 방식이 게이트 코드에 주석을 못 넣게 만든 것이다** — 참조가 낡는 것과 달리 이건
    산출물 자체가 나빠지는 방향이고, 아래 "심볼 참조" 후보의 근거가 하나 늘었다.
  - **승격 트리거**: 그 43건 중 하나 때문에 실제로 잘못된 코드를 읽는 일이 생기거나, 문서 정리 세션을
    따로 잡게 되면 → `--all` 로 훑어 한 번에 정리한다. (도구가 생겨 기계적 밀림은 값이 싸졌지만,
    **문장의 뜻이 낡는 것**은 여전히 사람이 봐야 한다 — 그래서 이 항목은 안 닫는다)
  - 더 나은 방향은 아래(심볼 참조)인데, 그건 참조 방식 자체를 바꾸는 일이라 따로 판단한다.
  - 더 나은 방향(후보): 줄 번호 대신 **심볼 이름**으로 가리킨다(`graph.mjs 의 implement.clean_when`).
    줄은 움직여도 이름은 안 움직인다. 다만 기존 참조 140개를 다 바꾸는 비용이 있다.
  - 근거: 2026-08-16 CROSS_SLICE·NOT_RUN 작업 중 발견. 검증기 초안은 그 세션의 스크래치패드에 있었다.

- [ ] **스킬을 고친 세션에서 그 스킬을 부르면 옛 판본이 돈다** (2026-08-15)
  - 관찰: wrap-up SKILL.md 를 고친 뒤 같은 세션에서 `/wrap-up` 을 불렀더니 **고치기 전 15줄짜리**가 왔다
    (디스크는 49줄, extract-harness 가 그렇게 읽는다). 새로 추가한 BACKLOG 절이 통째로 없는 판본이다.
    훅·settings 와 같은 계열 — 세션 시작 시점에 잡힌다. `/reload-skills` 는 "no changes" 라고 했는데
    그건 스킬 **목록**(이름·설명) 기준으로 보이고, description 을 안 바꿨으니 목록상 변화가 없는 게 맞다.
    본문 갱신 여부는 그 메시지로 판별할 수 없다.
  - 왜 이 킷에 걸리나: `retro`·`setup` 이 **스킬을 고치는 절차**다. 고친 직후 그 스킬을 쓰면 옛 판본이 돌고,
    "고쳤는데 왜 그대로지"가 된다. 아무 신호도 없다는 점이 위험하다(오늘은 본문 길이 차이로 알아챘다).
  - 지금: 없음. 고친 세션에서는 그 스킬을 바로 쓰지 않는 것으로 회피.
  - **승격 트리거**: 2번째로 헷갈리면 → retro·setup 의 "스킬을 고쳤다" 단계 끝에 "이 세션에서 그 스킬을
    쓸 거면 `/reload-skills` 먼저, 그래도 옛 판본이면 세션을 다시 연다" 한 줄. 프로즈로 충분한 종류다.
  - 근거: 2026-08-15 wrap-up 1회.

- [ ] **모델링 체크리스트 → 게이트 강제 승격** (2026-07-23)
  - 지금: 프로즈 라우팅 — kickoff(얕게 플래그)·/spec(깊게 INV)·CLAUDE.md 위험목록. 게이트 없음.
  - 보류 이유: 절차 무게 최소화. 프로즈로 먼저 굴려보고 실효를 본다.
  - **승격 트리거**: 플래그된(시변·파생) 엔티티가 스펙 없이 구현되는 게 눈에 띄면
    → `model/BEFORE_ENTITIES` 게이트 신설(승인된 스펙 없으면 entities 구현 차단, 기존 design/BEFORE_UI 패턴).
  - 근거: retro 사다리상 게이트(②, 100% 강제) > rules/CLAUDE 프로즈(③④). 프로즈가 무시되는 게 관찰되면 사다리를 올린다.

- [x] **게이트 CROSS_SLICE에 app·shared 레이어 예외** (2026-08-02) — **2026-08-16 반영**
  - 반영: `run-gates.mjs` 에 `SLICELESS_LAYERS = new Set(["app","shared"])` 를 두고 같은-레이어 검사에서
    건너뛴다. 실측으로 대조했다 — 같은 임시 파일에 옛 판본은 3건(app·entities·shared), 새 판본은
    1건(entities)만 낸다. `shared → entities` 상향 import 는 새 판본에서도 `UPWARD_IMPORT` 로 잡힌다.
  - 지금: 게이트가 app·shared의 루트 파일도 각각 슬라이스로 봐 같은-레이어 import를 막음
    (canonical FSD는 app·shared 무슬라이스). signal에서 provider를 shared로 우회.
  - 보류 이유(당시): 우회(shared로 내림)가 값싸고 1회뿐. 전역 게이트 변경은 전 프로젝트 영향.
  - **승격 트리거**: app/shared 슬라이스 예외가 2번째로 필요해지거나, app 레이어가 여러 파일
    협업이 정당히 필요할 때 → run-gates CROSS_SLICE에서 app·shared 레이어 예외(제안만, gates 보호).
  - **2번째 관찰(2026-08-09)**: `shared/api` ↔ `shared/config` 가 서로 다른 슬라이스로 판정돼 막혔다.
    또 우회했다(환경변수 모듈을 `shared/api/` 로 합침). 정식 FSD 에서 shared 는 슬라이스가 없고 세그먼트만 있다.
  - 제안 내용: `run-gates.mjs` 의 같은-레이어 검사에서 `fromLayer` 가 `app`·`shared` 면 CROSS_SLICE 를 건너뛴다.
  - 근거: docs/references/architectures/nextjs-fsd.md에 기록. signal에서 2회 관찰.

- [ ] **출력 형식을 정하는 쪽(스킬)과 읽는 쪽(스크립트)이 조용히 어긋난다** (2026-08-10)
  - 관찰: `scripts/briefing.mjs` 는 `멈춘 지점:` 을 찾는데 wrap-up 스킬은 `- **멈춘 지점**: …` 로 쓴다.
    굵기 표시 때문에 정규식이 안 맞아, PROGRESS 에 다섯 필드가 다 적혀 있어도 브리핑은 "기록 없음"이라고 말했다.
    여러 세션 동안 아무도 못 알아챘다 — 게이트가 안 보는 층이라 실패가 소리를 내지 않는다. (파서는 고쳤다)
  - 보류 이유: 1회 관찰이고 고쳤다. 계약 테스트를 지금 만들면 검사 대상이 하나뿐이다.
  - **승격 트리거**: 스킬 출력 ↔ 스크립트 파서 어긋남이 2번째로 관찰되면 → wrap-up 이 쓰는 형식 견본을
    briefing 파서에 통과시켜 5개 필드가 다 나오는지 보는 계약 테스트. 같은 관계가 더 있으면 함께 묶는다.

- [ ] **스택 판정을 `projectProfile()` 하나로 모으기 + 게이트 요약에 프로파일 표시** (2026-08-02)
  - 지금: 스택 판정이 `isNextProject()` 하나로 게이트 안에 흩어져 있다. 스택이 둘(vite-fsd·nextjs-fsd)
    뿐이라 이걸로 충분히 굴러감.
  - 보류 이유: 스택 2개에 판정 지점 1개. 지금 모으면 추상화가 사례보다 앞선다.
  - **승격 트리거**: 세 번째 스택이 들어오거나 스택별 분기 지점이 2개째가 되면
    → `projectProfile(projDir) → "nextjs-fsd" | "vite-fsd" | ...` 로 판정을 한 곳에 모으고,
    게이트 요약 줄에 판정 결과를 찍는다(`2개 프로젝트: signal=nextjs-fsd, wama=vite-fsd`).
    가정이 매 실행마다 눈에 보여야 오판정이 조용히 지나가지 않는다. (제안만, gates 보호)
  - 근거: docs/references/architectures/README.md "아직 안 한 것"에 기록.
  - **2026-08-31 관찰 — 승격 트리거가 발동했다(스택이 셋이 된 게 아니라 분기 지점이 2개째).**
    `scripts/scaffold.mjs` 에 `resolveProfile()` 이 생겼다. 게이트의 `isNextProject()`(설정 파일 존재)와
    **판정 근거가 다르다** — scaffold 는 새 프로젝트라 `next.config.*` 가 아직 없어서
    `docs/tech-stack.md` 의 `architectures/<이름>.md` 링크를 1순위로 본다. 지금은 둘이 어긋나면
    scaffold 가 멈추게 해서 막아 뒀지만, **판정 로직이 두 벌이라 한쪽만 고치면 조용히 갈린다.**
    승격할 때 `projectProfile()` 은 두 근거(문서 링크 · 설정 파일)를 다 받아야 한다.
    검사는 `scripts/check-scaffold-profile.mjs` 에 이미 있다(29/29).
  - **2026-08-31 회고 — 싼 절반만 반영한다(패치 대기).** 게이트 통과 요약 줄에 판정 결과를 찍는다:
    `게이트 통과 (38개 파일, 2개 프로젝트: signal2=nextjs-fsd, wama=vite-fsd …)`. 가정이 매 실행마다
    보이면 오판정이 조용히 지나가지 않는다. 패치·검사기:
    `docs/references/pending-patches/2026-08-31-frontmatter-guards-and-profile.md` ·
    `scripts/check-profile-summary.mjs`(붙기 전 1/2, 후 2/2).
    **판정 통합(`projectProfile()`)은 이 항목에 그대로 남는다** — 지금 어긋남은 scaffold 가 멈추는
    것으로 막혀 있고, 통합은 `gates/` 와 `scripts/` 를 같이 고치는 패치라 부분 적용 조합이 생긴다.
    요약 줄은 게이트의 판정 하나만 찍으므로 **두 판정의 일치까지 보여주지는 않는다.**

- [x] **보류 스펙 상태 도입** (2026-08-02 제안 → **2026-08-30 승격, 이름은 `parked`**)
  - **트리거 충족 근거**: 트리거가 "planned/ 파킹이 혼란스럽거나(복귀 누락·중복)" 였는데, 복귀 누락이
    실제로 났다. `ingestion-ranking.md` 가 `planned/` 에서 나와 `docs/specs/` 로 옮겨졌는데 그 파일을
    가리키던 문장 셋이 안 따라왔다 — `PRODUCT.md:95`(없는 경로를 가리킴) · 그 스펙 자신의 9줄("나는
    파킹돼 있다") · `review.md:34`("활성화 시 별도 리뷰" — 활성화됐고 그 리뷰는 없었다).
    **위치로 상태를 표시하면 활성화할 때 파일이 움직이고, 그 위치를 가리키던 문장은 조용히 낡는다.**
  - **반영**: `graph.mjs` 의 spec 노드에 `skip_when: "status: parked"`, `gates/graph-stop.mjs` 의
    `frontmatterOK` 가 그 선언을 읽어 해당 파일만 판정에서 뺀다. 패치는
    `docs/references/pending-patches/2026-08-30-parked-status.md`, 검사는 `scripts/check-parked-status.mjs`.
    브리핑에 `◇ 보류 스펙: N건` 한 줄(`scripts/briefing.mjs`), spec 스킬·스펙 템플릿 갱신.
  - **이름을 `approved-deferred` 로 안 지은 이유(중요)**: 검사식이 `/^\s*status:\s*approved\b/m` 인데
    `\b` 가 하이픈을 단어 경계로 인정해 `approved-deferred` 가 **승인으로 통과한다**(실측).
    그 이름을 썼다면 보류 스펙으로 위험 표면을 덮을 수 있었다. 검사기 A 가 "skip 값이 require 값의
    접두사가 아닐 것"을 기계로 지킨다 — 나중에 누가 `approved-…` 값을 또 만들면 거기서 걸린다.
  - **방벽이 안 열린 근거**: `parked` 는 run-gates 의 `approvedSurfaces`(approved 만 본다)에 안 걸린다.
    인증·결제·권한·동시성 스펙을 parked 로 미루면 그 표면 코드의 편집이 차단된다 — 미루기는
    도망이 아니라 막다른 길이다. 검사기 C 가 이걸 실제 게이트 실행으로 확인한다.
  - **안 고친 것**: signal 의 `planned/` 폴더와 낡은 참조 3곳. signal 은 다른 에이전트가 작업 중이라
    킷만 고쳤다(사용자 지시 2026-08-30). 옛 폴더는 비재귀 스캔이라 그대로 둬도 동작한다.
  - **남은 것**: `ingestion-ranking.md` 가 활성화됐는데 `review.md` 가 약속한 별도 리뷰는 없었다.
    사용자 판단으로 넘어간다(그 프로젝트는 이 레포에서 안 고친다). 하네스 관점의 교훈만 취했다.

  - **승격 전 기록** (원 항목명은 `status: approved-deferred 정식 도입`, 2026-08-02 개설):
    - 당시: planned/ 파킹 관례를 spec 스킬에 명문화하고 프로즈로 굴렸다. 근거는 1회 관찰뿐이었다.
    - 트리거로 적어 뒀던 것: "planned/ 파킹이 혼란스럽거나(복귀 누락·중복) 2번째 프로젝트에서 또 필요해지면".
    - 2026-08-11: "합의됐으나 활성 계약 아님"과 "승인됐고 활성인데 구현 착수 전"이 같은 것인지부터
      정해야 한다고 적었다.
    - 2026-08-15 정정: 둘은 다른 것이었고 **후자는 status 를 늘리지 않고 풀렸다.** `spec-coverage` 를
      completion 게이트로 분류해 턴 차단에서 뺀 것으로 충분했다 — 없던 건 상태가 아니라 **그 상태에서
      턴을 끝낼 권리**였다. 그래서 이번에 도입한 값은 하나(`parked`)뿐이다.

- [ ] **판정(qa-classifier) → 마크 사이가 비어 있다** (2026-08-06)
  - 지금: 분류기가 실패를 spec/design/impl 로 귀속해 "이 노드를 draft 로 내려라"까지 알려주지만,
    **그 마크를 실제 파일에 새기는 단계는 아무도 강제하지 않는다.** graph.mjs 주석은 "분류기가 mark 만 찍는다"고
    적혀 있으나 찍는 주체는 본체이고, 안 찍어도 아무 신호가 없다.
  - 관찰된 실패(이번 세션): 분류기가 spec-level·design-level 을 정확히 판정했는데 마크를 안 찍었다.
    HANDOFF 는 옛 프론티어(`review`)를 그대로 들고 있었고, 그 상태에서 재작업 순서를 **손으로 추론**했다
    (CLAUDE.md 가 명시적으로 금지하는 것). 결과는 거꾸로 된 순서 — 하류(implement)부터 하자는 안.
    마크를 찍자마자 전파가 `spec, design` 을 프론티어로 뽑아냈다. 기계가 하면 맞고 손으로 하면 틀린다.
  - 보류 이유: 1회 관찰. 사람이 승인해야 하는 마크(스펙·디자인 거부)라 자동화 자체가 답은 아니다.
  - **승격 트리거**: 판정 후 마크 누락이 2번째로 관찰되면 → graph-stop 이 "분류기가 돈 흔적은 있는데
    어느 노드도 새로 dirty 가 되지 않은 턴"을 감지해 경고한다. 또는 분류기 출력 형식을
    "마크 대상 파일 경로 + 바꿀 프론트매터 값"으로 고정해 실행을 기계적으로 만든다.
  - 근거: 이번 세션 review 사인오프 실패 처리 과정.

- [x] **정상 절차를 밟는 중에 게이트가 턴 자체를 막는다** (2026-08-06 신설 → 2026-08-11 3번째 관찰 → **2026-08-15 반영**)
  → 아래 "졸업"으로 옮길 항목. 반영 내용은 거기에 적었다. (원문은 근거로 남겨 둔다)
  - 공통 사실: `graph-stop.mjs:266-269` 는 게이트 실패가 남으면 **무조건 `exit 2`** 로 턴을 막는다.
    그런데 그 실패를 푸는 유일한 길이 **사용자 승인**이거나 **다음 세션의 구현**일 때가 있다 —
    둘 다 턴을 끝내야 도달한다. 그동안 질문도 세션 정리(wrap-up)도 차단 메시지에 걸린다.
  - 관찰 ① (2026-08-06, design): `design-rules.md` 를 `status: draft` 로 내리면 그래프는
    "디자인 국면으로 돌아가라"고 처방하는데 `design/BEFORE_UI` 게이트는 같은 상태를 위반으로 본다.
    값싼 회피는 **임의로 `approved` 로 되돌리는 것** — 게이트가 막으려던 바로 그 일이 게이트 때문에 일어난다.
    이때는 회피하지 않고 차단 메시지를 무시하며 절차를 지켰다.
  - 관찰 ② (2026-08-10, spec): `content-selection.md` 를 승인 직후 `approved` 로 바꾸자 spec-coverage 가
    INV 9개의 테스트를 요구해 **wrap-up 조차 못 하는 상태**가 됐다. 되돌렸고, 그래서 그 파일은
    **승인이 끝났는데 draft** 로 남았다(문서가 사실과 다르다).
  - 관찰 ③ (2026-08-11, 같은 파일): 회고 세션 중에 다시 `approved` 로 바꿨다가 또 되돌렸다.
    9개를 다 닫으려면 그중 INV-O2 의 화면 절반이 **시안→checkpoint 승인**을 거쳐야 하는데
    그건 턴을 끝내야 가능하다. 스펙 파일 주석에 "이 줄은 **구현 세션의 첫 행동**으로만 바꾼다"를 박아
    회피했다 — 규율로 막은 것이지 구조가 고쳐진 게 아니다.
  - **승격 트리거**: 충족(3회 + 실제 회피 2회). 방향 두 가지를 함께 본다:
    ① graph-stop 이 **"그래프가 지금 처방한 상태에서 비롯된 게이트 실패"는 차단이 아니라 안내로 낮춘다.**
       판정 재료는 이미 있다 — HANDOFF 의 노드 상태와 게이트 카테고리(`design/BEFORE_UI`·`spec-coverage`)를
       맞춰보면 된다. 그 외 실패(fsd·security·tsc)는 지금처럼 차단.
    ② 또는 상태를 하나 늘려(`approved` 지만 아직 구현 착수 전) spec-coverage 가 그 스펙을 아직 안 물게 한다.
    graph.mjs·graph-stop·gates 는 전부 보호 파일 — 제안만.
  - 근거: 2026-08-06 design 국면 전체, 2026-08-10·08-11 스펙 승인 직후.

- [x] **포매터가 프론트매터 `status` 를 접어 게이트를 조용히 무력화한다** (2026-08-06 → 2026-09-02 졸업)
  → **v3.2 의 `gates/lib/read-spec.mjs` 가 승격안 ②를 실행했다.** 값을 못 찾으면 이제 조용히
    draft 로 떨어지지 않고 파일 이름과 함께 경고가 찍힌다. 실측:
    ```
    status:
      approved            ← 접힌 값
    → draft   ⚠ status 필드가 없다 → draft 로 본다
    ```
    **문구는 한 줄 더 고칠 것이 남았다** — 접힌 경우 필드는 있고 값만 비어 있으므로
    "status 값이 비어 있다(다음 줄로 접혔을 수 있다)"가 정확하다. `gates/lib/read-spec.mjs` 의
    `problems.push("status 필드가 없다 → draft 로 본다")` 한 줄이다(보호 파일 — 사용자 반영).
    승격안 ①(YAML 파싱 수준으로 올리기)은 **하지 않는다.** 접혀도 판정은 여전히 draft 이고,
    그게 안전한 방향이다 — 승인 안 된 것으로 읽는 쪽이 사고를 안 낸다. 필요한 것은 값을
    읽었느냐를 사람이 알 수 있게 하는 것뿐이었고 그건 됐다.
  - **아래는 졸업 전 기록이다(경위 보존).**
  - **2번째 관찰(2026-08-31)**: 접힘이 아니라 **중복 키**로 났지만 원인은 같다 —
    `ingestion-ranking.md` 아래쪽의 `surfaces: [concurrency]` 를 위에 넣은 `surfaces: []` 가 덮었다.
    실측으로 확인한 것: `status` 는 `.test()` 라 **한 줄이라도** approved 면 승인으로 읽고(순서 무관,
    방벽이 열리는 방향), `surfaces` 는 `.match()` 라 **첫 줄만** 읽는다. 두 키가 서로 반대 규칙이고
    어느 줄을 읽었는지는 아무 데도 안 찍힌다.
  - **반영(패치 대기)**: `gates/run-gates.mjs` 에 `frontmatter/DUPLICATE_KEY` — 같은 키가 두 줄이면
    막는다. 판정을 바꾸는 게 아니라 "어느 줄을 읽었는지 모르는 상태"를 거부한다(YAML 사양에서도
    중복 키는 오류다). 패치·검사기:
    `docs/references/pending-patches/2026-08-31-frontmatter-guards-and-profile.md` ·
    `scripts/check-frontmatter-guards.mjs`(붙기 전 2/5, 후 5/5).
  - **안 닫는 이유**: 원래 증상인 **값 접힘**(`status:` 다음 줄에 값이 오는 형태)은 중복 키가 아니라
    그대로 남는다. 아래 승격안 ②("status 를 못 찾았다"와 "approved 가 아니다"를 다른 메시지로)는
    여전히 미결이다.

  - 지금: 게이트(`spec-coverage.mjs:35`, `run-gates.mjs` designApproved, `graph-stop`)는
    `^\s*status:\s*approved\b` 로 판정한다. 그런데 `status: approved  # 긴 주석...` 처럼 같은 줄이
    길어지면 **포매터(prettier)가 값을 다음 줄로 내린다**:
    ```yaml
    status:
      approved # 주석
    ```
    이러면 YAML 로는 여전히 approved 인데 **게이트는 draft 로 읽는다.** 반대 방향(draft 인데 approved 로
    읽히는 것)이 아니라 다행이지만, "승인했는데 계속 막힌다"는 진단하기 어려운 상태가 된다.
  - 관찰: `content-safety.md` 를 approved 로 되돌리려는데 이미 접혀 있었다. 값을 짧은 한 줄로 두고
    이력을 아래 주석으로 분리 + 파일에 경고 주석을 남겨 회피.
  - 보류 이유: 1회 관찰이고 회피가 값싸다(값 줄을 짧게 유지).
  - **승격 트리거**: 2번째로 관찰되거나 다른 프로젝트에서 재현되면 →
    ① 게이트 정규식을 YAML 파싱 수준으로 올리거나(값이 다음 줄에 와도 읽게),
    ② 최소한 **"status 를 못 찾았다"와 "status 가 approved 가 아니다"를 다른 메시지로** 구분해
       원인이 포매팅인지 승인 누락인지 즉시 보이게. (gates 보호 — 제안만)
  - 근거: 이번 세션. spec 승인 복귀 과정에서 관찰.

- [ ] **protect-secrets 훅이 `.env.example` 까지 막는다** (2026-08-09)
  - 지금: `protect-secrets.mjs:12` 의 정규식 `\.env(\.[\w.-]+)?\b` 이 `.env.example` 에도 걸린다.
    읽기 명령(`grep`·`cat` 등)이 같이 있으면 차단이라, **값이 하나도 없는 견본 파일**을 못 읽는다.
    `.gitignore` 는 `!.env.example` 로 일부러 추적하는 파일인데 훅과 서로 반대 말을 한다.
    같은 이유로 `git check-ignore ... | grep` 같은 무해한 파이프라인도 막힌다.
  - 관찰: signal 의 `.env.example` 을 만들고 추적 여부를 확인하려다 두 번 막힘. 회피는
    파이프라인에서 파일명을 빼고 `git status` 로 대신 확인 — 값싸다.
  - 보류 이유: **보안 훅을 느슨하게 하는 변경**이라 값싼 회피가 있는 동안은 손대지 않는다.
    과차단은 안전한 방향의 실패다(NO_INNERHTML 을 보수 유지한 것과 같은 판단).
  - **승격 트리거**: 2번째 프로젝트에서 재현되거나, 회피가 안 되는 자리(예: `.env.example` 을
    읽어야 하는 스킬·스크립트)가 생기면 → 정규식에 견본 파일 예외
    (`.env.example`·`.env.sample`·`.env.template` 는 통과). **차단 자체는 유지하고 예외만 좁게.**
  - 근거: 이번 세션. hooks 보호 — 제안만.

- [ ] **risk-surface 스캔 범위를 `projects/*/scripts/` 로 확장** (2026-08-14)
  - 지금: `src/**` 와 `supabase/**.sql` 만 훑는다. 프로젝트 스크립트는 밖이다.
  - 보류 이유: 지금 뚫린 자리는 없다(signal 의 DB 쓰기는 전부 `src/features/ingestion` 안이고 이미 잡힌다).
    범위를 넓히면 `check-env.mjs` 의 `"예전 형식 service_role JWT"` 같은 **설명 문자열**이 auth 로 걸려
    예외 주석이 늘어난다 — 예외가 늘면 ⚠ 경고가 무뎌진다.
  - **승격 트리거**: 결제·인증·권한 로직이 `scripts/` 에 실제로 쓰이면, 또는 문자열 오탐을 걸러낼
    규칙(따옴표 안 토큰 제외 등)이 정리되면 → scanFiles 에 `scripts/` 추가.
  - 근거: 2026-08-14 risk-surface 신설 시 실측으로 범위 결정.
  - **2026-09-02 관찰(범위가 아니라 그 범위가 어디에도 안 적혀 있다는 것)**: v3.2 에서 위험 표면
    검사 프로브를 `src/shared/lib/policy.sql` 로 짰는데 게이트가 그 파일을 안 훑어 "0개 파일"로
    통과했고, 검사가 그걸 "방벽이 열렸다"로 읽을 뻔했다. **어느 확장자를 훑는지가 게이트 코드
    안에만 있어서** 검사를 쓰는 쪽이 틀린 가정을 했다.
    → 범위 확장과 별개로, **스캔 대상(경로·확장자)을 `docs/references/docs-contract.md` 에
      등재하는 것**이 먼저다. 규약이 코드에만 있으면 그 코드를 안 읽은 사람이 매번 틀린다.

- [ ] **사인오프의 리뷰어 요구가 diff 가 아니라 프로젝트 전체를 본다** (2026-08-30)
  - 지금: `require_reviewer` 판정은 `risk-surface` 게이트가 **프로젝트 전체 소스**에서 찾은 표면을 쓴다.
    signal 에는 `CRON_SECRET` 을 비교하는 `Bearer` 코드와 RLS 정책이 상주하므로, 카피 한 글자를 고친
    diff 로도 `reviewers:` 에 `security-reviewer` 가 없으면 review 가 clean 이 되지 않는다.
  - 이번에 한 것(싼 절반): 감지 위치를 `ℹ [risk-surface/AT]` 줄로 내보내고 사인오프 안내에 찍는다.
    무엇을 봐야 하는지는 알려 주되 **요구 자체는 안 줄였다.**
  - 보류 이유(C안 = 감지 범위를 이번 diff 로 좁히기): 느슨하게 만드는 변경은 놓친 것이 소리를 안 낸다.
    지금 요구는 과할 뿐 틀리지 않고, 과한 요구는 리뷰어를 한 번 더 부르는 비용으로 끝난다.
    반대로 범위를 좁혀서 놓치면 보안 표면이 리뷰 없이 지나가고 그 사실은 아무 데도 안 남는다.
  - **승격 트리거**: 표면과 무관한 diff 에서 `security-reviewer` 파견이 실제로 두 번 강제되면 →
    감지를 `git diff` 범위로 좁히되, **상주 표면은 별도로 "이 프로젝트에 이런 표면이 있다"고
    매 턴 신고**해서 좁힌 것이 사라지지 않게 한다(좁힘과 신고를 같이 넣지 않으면 승격하지 않는다).
  - 근거: 2026-08-30 하네스 점검. `docs/references/walkthrough.md` 의 "그런데 왜 security-reviewer 를
    요구하나" 절이 이 현상을 그대로 적고 있다.

- [ ] **신고만으로 넘어가는 자리 — 스펙·디자인 승인** (2026-08-14)
  - 관찰: 게이트가 "일한 결과"가 아니라 "일했다는 신고"를 검사하면, 신고를 만드는 게 제일 싼 길이 된다.
    review 사인오프가 그랬다 — graph-stop 이 basis 해시를 화면에 찍어주니 리뷰를 안 돌리고 두 줄만 적어도
    통과했다. 2026-08-14 에 `reviewers:` 기록 + 감지 표면 대조로 좁혔다.
  - 남은 같은 모양: **스펙·design-rules 의 `status: draft → approved` 는 파일 한 줄 수정**이고,
    사용자가 실제로 승인했는지는 어디에도 안 남는다. 강제는 CLAUDE.md 문구뿐
    (`git show 89517dc:docs/ARCHITECTURE.md` 의 Unverified 절에도 이미 적혀 있었다 — 그 파일은
    2026-08-30 에 지웠다). deploy 마커도 같은 모양이지만
    배포 안 하고 적으면 사이트가 안 열려 현실이 대조해 준다.
  - 보류 이유: **대조할 다른 출처가 없다.** "사용자가 승인했다"를 기계가 볼 방법이 마땅치 않다.
    문구를 강화하는 것은 답이 아니다(V3에서 걷어낸 것과 같은 종류).
  - **승격 트리거**: 승인 없이 approved 로 넘어간 사례가 1회라도 관찰되면, 또는 승인 대화의 흔적을
    파일로 남길 값싼 방법이 나오면(예: 승인 시 사용자 문장 인용을 frontmatter 이력에 요구) → 그때 설계.
  - 근거: 2026-08-14 review 사인오프 대조 작업 중 같은 패턴을 셋 발견(review·deploy·승인).
  - **2026-08-31 트리거 충족 — 절반만 반영했다.** signal2 를 열면서 다른 프로젝트의
    `workspace/review.md`(`status: passed` · basis `b853ea0606f5` · reviewers, 720줄)와 `deploy.md` 가
    **파일 복사로 통째로 따라 들어왔다.** 그때 막은 건 basis 불일치인데, basis 는 graph-stop 이 매 턴
    화면에 찍어주는 값이라 베끼면 통과한다.
  - **반영(패치 대기)**: `gates/run-gates.mjs` 에 `signoff/FOREIGN_MARKER` — 사인오프 마커
    frontmatter 의 `project:` 가 파일이 놓인 디렉터리의 프로젝트 이름과 다르거나 없으면 막는다.
    **이 항목이 미뤄져 있던 이유("대조할 다른 출처가 없다")가 여기엔 해당하지 않는다 — 출처가 경로다.**
    패치·검사기: `docs/references/pending-patches/2026-08-31-frontmatter-guards-and-profile.md` ·
    `scripts/check-frontmatter-guards.mjs`. 문서 갱신: CLAUDE.md 사인오프 절 · graph-engine.md.
  - **남은 것(이 항목을 안 닫는 이유)**: 잡은 건 **이사**뿐이다. 같은 프로젝트 안에서 리뷰를 안 돌리고
    마커를 지어내는 것, 그리고 스펙·design-rules 의 `status: draft → approved` 는 그대로다.
    그 둘은 여전히 대조할 출처가 없다.

- [ ] **워킹 스켈레톤만 있는데 프론티어가 `review` 를 가리킨다** (2026-08-31)
  - 관찰: signal2 스캐폴딩 직후 브리핑이 `◆ 지금 작업할 노드(프론티어): review` 라고 말했다.
    `src/` 는 3줄짜리 워킹 스켈레톤이고 깊은 리뷰를 돌릴 코드가 없다. `implement`·`qa` 는
    "산출물이 있고 게이트가 통과하면 clean" 이라 스캐폴딩 산출물만으로 둘 다 clean 이 됐고,
    하류인 `review` 가 유일한 dirty 로 남아 프론티어가 됐다.
  - 왜 위험한가: CLAUDE.md 는 "프론티어가 다음 할 일의 1차 근거"라고 못 박는데, 이번엔 PROGRESS 의
    "다음 할 일"이 **프론티어를 믿지 말라고 적어서** 회피했다. 문서 두 개가 서로 반대를 말하면
    다음 세션은 둘 중 하나를 고르게 된다 — 그게 "임의로 정하지 마라"가 막으려던 것이다.
  - 보류 이유: 고치는 방향이 전부 **느슨하게 만드는 쪽**이다. "리뷰할 만한 코드인가"를 기계가
    판정해야 하는데(예: 활성 스펙이 하나도 없으면 review 를 프론티어에서 뺀다), 좁혔다가 놓치면
    보안 표면이 리뷰 없이 지나가고 그 사실은 아무 데도 안 남는다. 1회 관찰이고 회피가 값쌌다.
  - **승격 트리거**: 빈 골격에 리뷰어를 실제로 파견한 세션이 나오거나, 2번째 프로젝트에서 같은
    상태가 관찰되면 → 그때 설계. 좁히는 안을 낼 때는 **좁힌 만큼을 매 턴 신고**하는 것과 함께 낸다
    (위 "사인오프의 리뷰어 요구가 프로젝트 전체를 본다" 항목과 같은 조건).
  - 근거: 2026-08-31 signal2 골격 완성 세션. `projects/signal2/workspace/PROGRESS.md` "다음 할 일".

- [ ] **사용자가 지시한 깊이를 내가 읽었는지 확인할 자리가 없다** (2026-09-01)
  - 관찰: 같은 세션에서 반대 방향으로 두 번 어긋났다.
    ① **너무 깊게** — "페이지 하단에 이전이슈, 다음이슈로 넘어가는 버튼" 한 줄을 피드 구조 전체를
    뒤집는 일로 읽고 `design-rules` 를 draft 로 내리고 `--mark` 를 찍고 시안을 그렸다. 사용자가
    "상세페이지에서만"이라고 정정하자 **뒤집을 결정이 하나도 없는 일**이 됐고 시안 한 장을 버렸다.
    ② **너무 얕게** — "키워드를 메인 상단에 보여줄거야"는 만들라는 지시였는데 "이미 1-B 로 잡아뒀다"고
    답하고 그 턴에 안 만들었다. 다음 턴에 사용자가 "왜 안뜨는거지?"라고 물었다.
  - **①은 물어보긴 했는데 질문이 틀렸다.** `AskUserQuestion` 3지선다에 실제 정답("글 하나씩 —
    상세 화면 안")이 선택지로 있었는데, **"어느 화면에 붙나"와 "한 칸이 무엇이냐"를 한 질문에 섞어서**
    미리보기 그림이 답을 다른 뜻으로 만들었다. 축을 하나만 물었으면("이 버튼이 메인이냐 상세냐")
    한 번에 끝났다.
  - 왜 위험한가: CLAUDE.md 「표면별 판단」은 **선언 → 동의 → 시작**을 요구하는데, 이번에 선언은
    새 작업을 시작할 때만 했고 승인 뒤 구현에 들어갈 때는 건너뛰었다. 그 틈으로 카드 키워드 칩 줄이
    확인 없이 들어왔다(시안 QA 에서 "1-A 구현에는 없던 줄"이라고 뒤늦게 알렸다).
  - 보류 이유(사용자 판단 2026-09-01): **관찰 1회**다. 그리고 고치는 방향 후보 둘 다 대가가 있다 —
    "질문은 축 하나만"은 질문 수를 늘려 왕복이 잦아지고, "미루는 것도 명시 동의"는 사소한 순서 조정까지
    확인받게 되어 사용자가 미룰 권리를 오히려 좁힌다. 더 쌓아 보고 판단한다.
  - **승격 트리거**: 같은 모양이 2번째로 관찰되면 — **깊이를 잘못 읽어 산출물을 버렸거나(①),
    사용자가 지시한 것을 안 만들고 넘어간(②) 사례**. 둘 중 어느 쪽이든 1회 더면 반영한다.
  - 근거: 2026-09-01 signal2 피드 구현 세션. 사용자 질문 "그게 잘 동작했나?" 로 드러났다.

- [ ] **브라우저 검증을 한 화면당 한 번으로 묶기** (2026-09-02)
  - 관찰: signal2 리뷰 세션에서 `browser_evaluate` 를 **45회**, `browser_navigate` 를 28회 불렀다.
    그중 **15회가 그냥 `/`** 로, 상태를 초기화하려고 매번 다시 이동한 것이다.
  - 쪼개진 자리의 실물:
    ① 고정 줄 높이 하나에 3회 — 스크롤을 안 해서 푸터가 화면 밖(1회) → 선택자를 잘못 잡아 `0`(2회) →
    맞춤(3회). ② 같은 대비 훑기 함수를 5회 다시 보냄. ③ 뱃지 높이·접힘·펼침·inert 를 3~4회로 나눔.
  - **비용은 생각보다 작았다 — 그래서 값싼 규칙이 아니면 반영하지 말아야 한다.** 실측:
    `browser_evaluate` 45회의 총합이 **20k토큰(세션 최대 447k의 4.5%)** 이다(보낸 코드 7k + 결과 13k).
    바이트가 아니라 **턴 수**가 비싸다 — 이 세션은 254요청이고 그중 thinking 이 124k(28%)다.
  - 왜 그래도 쌓아 두나: 45 → 12~15회로 줄일 수 있었다고 보고, 줄면 턴이 줄어 thinking 도 같이 준다.
    그리고 쪼개진 이유의 절반은 **선택자를 틀려서 다시 부른 것**이라 정확도 문제이기도 하다.
  - 보류 이유: **관찰 1회**다. 그리고 "한 번에 다 재라"는 규칙은 반대 방향 대가가 있다 —
    한 함수에 다 넣으면 하나가 던졌을 때 나머지 측정까지 통째로 날아가고, 그때 다시 쪼개게 된다.
    측정이 탐색적일수록(선택자를 모를수록) 묶는 것이 손해다.
  - **승격 트리거**: 같은 화면을 재는 데 `evaluate` 가 5회를 넘는 일이 2번째로 관찰되면 →
    `.claude/rules/` 에 "화면 하나의 검증은 한 함수에서 대비·치수·포커스·상태를 한꺼번에 반환한다.
    선택자가 불확실하면 먼저 `browser_snapshot` 으로 확인하고 그다음 한 번에 잰다" 한 절.
  - 근거: 2026-09-02 세션. 사용자 질문 "브라우저 측정을 왜 25번이나 했어?" 로 드러났다
    (실제로는 45회였다 — 내 어림이 틀렸다).

- [ ] **컨텍스트를 무엇이 채우는지 세션 중에 알 자리가 없다** (2026-09-02)
  - 관찰: 사용자가 "요즘 컨텍스트가 빨리 찬다"고 물었을 때 답할 근거가 없어서, 트랜스크립트를
    직접 파싱하는 스크립트를 그 자리에서 만들어야 했다. **그 과정에서 내 추정이 네 번 틀렸다** —
    ① 이미지를 base64 길이로 세어 스크린샷 한 장을 133k 로 봄(실제 1.6k, 픽셀로 토큰화된다)
    ② 훅 기록 전체(JSON)를 세어 주입량을 부풀림(`content` 필드만 들어간다)
    ③ 어시스턴트 자신의 출력을 앞 구간에 붙여 thinking 을 통째로 놓침(그 비용이 매 턴 등장하는
    훅에 얹혀 "터미널 이스케이프 훅이 1위"라는 틀린 결론이 나왔다)
    ④ `usage` 가 한 응답에 여러 레코드로 중복 기록되는 것을 안 걸러 출력 토큰을 2배로 봄
    (254요청이 494레코드 → 460k 가 아니라 203k).
  - 드러난 사실(3세션 일치): **내 출력이 최대 컨텍스트의 45%** 고 그중 **thinking 이 61~71%** 다.
    thinking 은 트랜스크립트에 **본문이 0자로 저장**돼서 직접은 못 세고
    `출력토큰 − 보이는 텍스트 − 도구인자` 로만 구한다.
  - 왜 위험한가: 컨텍스트가 찰수록 압축이 끼어들고, 압축은 조용히 판단 근거를 지운다.
    그런데 "무엇을 줄여야 하나"의 답이 직관과 반대다 — 파일을 적게 읽는 것보다 **왕복을 줄이는 것**이
    크고, 스크린샷은 아껴도 소용이 없다.
  - 지금: `/context` 가 현재 상태를 항목별로 보여준다. 세션 사후 분석 스크립트는 만들었지만
    스크래치패드에 있어 다음 세션에는 없다.
  - 보류 이유: 브리핑에 넣으면 매 세션 줄이 늘고(브리핑은 이미 다섯 줄을 넘본다), 정확히 세려면
    위 네 함정을 전부 피해야 해서 스크립트가 짧지 않다. 그리고 아직 **압축이 실제로 판단을 망친
    사례는 관찰되지 않았다** — 지금은 "컨텍스트가 빨리 찬다"는 체감뿐이다.
  - **승격 트리거**: 압축이 끼어든 뒤 이미 내린 결정을 다시 묻거나 뒤집은 사례가 1회 관찰되면 →
    `scripts/ctx-report.mjs` 로 정본화하고 wrap-up 이 세션 끝에 한 줄(최대 컨텍스트 · 상위 3항목)을
    PROGRESS 에 남긴다. 브리핑에는 안 넣는다.
  - 근거: 2026-09-02 세션. 사용자 질문 "정말 유효하게 컨텍스트를 잡아먹는 건 뭔데?" 로 드러났다.

- [ ] **리뷰어 파견 흔적을 파일로 남기기** (2026-08-14)
  - 지금: `workspace/review.md` 의 `reviewers:` 목록이 감지된 위험 표면과 대조된다. 이름을 안 적으면 막힌다.
  - 남은 틈: **이름을 적는 것과 실제로 파견하는 것은 다르다.** 서브에이전트 호출은 대화에서 일어나고
    레포에 흔적이 없어, 파일만 보는 게이트는 거기까지 못 본다.
  - 보류 이유: 지금도 난이도는 올랐다(해시 베끼기 → 없는 리뷰 결과를 지어내기). 더 조이면 리뷰어 출력을
    파일로 남기는 규약이 필요한데, 그건 리뷰 절차 자체를 바꾸는 일이다.
  - **승격 트리거**: `reviewers:` 에 적혔는데 실제 지적이 하나도 없는 사인오프가 반복되면 →
    리뷰어 출력을 `workspace/reviews/<날짜>-<리뷰어>.md` 로 저장하고 게이트가 파일 존재 + basis 를 확인.
  - 근거: `git show 89517dc:docs/stages/07-review.md` 의 Unverified 절에 같은 내용을 기록
    (그 파일은 2026-08-30 에 지웠다 — 같은 내용이 `docs/v2/stages/06-review.md` 에도 있다).

- [ ] **`docs/v2/diagrams/04-layers.html` 을 브라우저로 눈으로 확인** (2026-08-31)
  - 렌더는 됐고 showcase 검증도 오류 0·경고 0 이지만 실제로 열어보지는 않았다(나머지 세 장도 같다).
  - 보류 이유: 기계 검증이 통과했고 급하지 않다.
  - **승격 트리거**: 이 그림을 남에게 보여주거나 문서를 손볼 때.
  - 출처: signal2 BACKLOG 에 있던 것을 2026-09-02 에 여기로 옮겼다 — 대상이 킷 문서(docs/v2)라
    프로젝트 백로그가 아니다.

- [x] **브리핑 순서 가드가 오탐 + 한글 낱말이 죽어 있었다** (2026-09-02 발견·같은 날 닫힘)
  - 랩업으로 "다음 할 일"에 **화면 먼저, Supabase 는 그 뒤**라고 원칙대로 적었더니 경고가 떴다.
    원인 둘: ① `또는|→|then` 으로 잘라 첫 조각만 봤는데 구분자가 없는 문장에서는 첫 조각이 곧
    문장 전체라 **둘이 같이 언급되기만 하면** 순서와 무관하게 떴다 ② `dataRe` 가 `\b…\b` 로
    감싸여 있어 **`스키마`·`마이그레이션`·`데이터 계층` 세 낱말이 절대 안 맞았다** — `\b` 는
    ASCII 낱말 경계라 한글 앞뒤에서 성립하지 않는다. 영문(`DB`·`Supabase`)만 작동하고 있었다.
  - 닫은 방법: 첫 조각 대신 **등장 위치로 비교**(`dataAt < uiAt`)하고, 한글은 경계 없이 찾는다.
    합성 문장 9개로 양방향 확인(9/9) — 틀린 순서는 잡히고 맞는 순서는 침묵한다.
  - 남는 교훈: **원칙을 지킨 문장을 원칙 위반이라고 말하는 검사는 무시하는 법부터 가르친다.**
    그리고 ②는 검사를 쓰지 않았으면 계속 몰랐다 — 죽은 가지는 조용해서 통과처럼 보인다.
    (LESSONS 2026-09-02 "검사가 안 돈 것과 통과한 것은 겉이 같다"의 3번째 사례)

- [x] **v3.2 retro — LESSONS 5건 + 버전 정의 한 줄** (2026-09-02 닫힘)
  - 교훈 5건 중 **셋은 기록만**(이미 기계화됐거나 과적합), **둘만 하네스를 고쳤다**.
  - 반영: `docs/LESSONS.md` 교훈 5건 + v3.2 정의 · `CLAUDE.md`/`AGENTS.md` 두 벌에 패치 검사
    한 문장(+2)과 시각 방향 블록 압축(−4, 순 −2 · 200→198) · `scripts/briefing.mjs` 미커밋 경고 ·
    `gates/lib/read-spec.mjs` 접힘 문구.
  - `docs/LESSONS.md` 는 보호 파일이라 retro 절차로만 갱신한다. 넣을 것:
    1. **산문을 기계가 긁으면 숨은 스키마가 된다.** 형식을 문서에 등재해도 산문 속 파싱은
       frontmatter 파싱보다 오독 표면이 넓다. (근거: 스펙 status 를 DECISIONS 결정 줄로
       빼려다 접었다 — `\b` 가 하이픈을 단어 경계로 봐 `approved-…` 를 approved 로 읽은
       2026-08-02 사고가 같은 형식에서 재발할 수 있었다)
    2. **검사가 아무것도 안 한 상태와 검사가 실패한 상태는 겉이 같다.** 위반을 일부러 심어
       잡히는 것을 봐야 구분된다. (근거: 위험 표면 패턴을 `.sql` 에 심었더니 게이트가 그
       파일을 안 훑어 "0개 파일"로 통과했고, 그것을 '방벽이 열렸다'로 읽을 뻔했다)
    3. **파생값을 버릴지 정할 때는 재계산의 실패 방향을 본다.** 안전한 쪽으로 틀리면 버려도
       되고, 위험한 쪽으로 틀릴 수 있으면 사람이 비준한 기록이라 남긴다. (근거: `surfaces` 를
       "재계산 가능한 파생값"이라며 workspace 로 보내려던 판정을 뒤집었다 — 과잉 표기하면
       방벽이 조용히 열린다)
    4. **검사의 그물은 각도가 다른 것으로 둘 이상 둔다.** 하나만으로는 자기가 무엇을 놓치는지
       못 본다. (근거: 문자열 검사가 놓친 킷 스크립트 참조를 경로 검사가 잡았고, 경로 검사가
       놓친 맨 파일명을 보강한 문자열 검사가 잡았다)
    5. **커밋 안 된 작업 트리 위에서 새 작업을 시작하지 않는다.** 남의 미완 작업과 내 diff 가
       한 덩어리가 된다. (근거: v3.2 착수 시 미커밋 192개가 있었고 그중 둘이 이번 작업이
       고칠 바로 그 게이트 파일이었다)
  - 킷 문서에 버전 정의 한 줄도 추가한다(사용자 지시):
    **"이 버전부터 프로젝트 이전은 `projects/<이름>/docs/` 복사 + `docs-contract.md` 전달로 성립한다."**
  - 시점: v3.2 패치 적용 → 사인오프 → merge → 태그 다음.

- [ ] **docs/v2 의 줄 참조가 낡아 있다 (2026-09-02 38건 → 2026-09-03 49건)** (2026-09-02)
  - **2026-09-03 재측정: 49건으로 늘었다.** v3.3 이 `graph.mjs`·`gates/graph-stop.mjs`·
    `gates/run-gates.mjs` 를 고치면서 그 파일을 가리키던 `docs/v2` 참조가 또 밀렸다.
    **"게이트를 고칠 때마다 기계적으로 재발한다"가 네 번째로 확인된 셈이다**(08-16·08-17·08-30 에 이어).
    여전히 전부 `docs/v2/**` 안이고, v3.3 이 새로 쓴 문서에는 줄 번호 참조가 하나도 없다
    (그래서 이 숫자는 늘기만 하고 v3.3 문서가 보태지는 않았다).
  - `node scripts/check-doc-refs.mjs` → 참조 336개 중 의심 49건. 가장 많은 곳은
    `stages/04-implement.md`(11) · `stages/06-review.md`(7) · `stages/05-qa.md`(7) 다.
  - v3.2 작업이 민 것은 아니었다 — 그때는 대상 파일들을 안 건드렸다.
  - 그중 2건(`ARCHITECTURE.md:91` · `findings.md:44`)은 `CLAUDE.md#L103` 을 가리키는데,
    **애초에 엉뚱한 줄이었다.** 참조가 말하는 GLOSSARY 지시는 그때도 L189 에 있었다.
    v3.2 가 CLAUDE.md 에 10줄을 더하면서 L103 이 빈 줄이 돼 검사에 드러났을 뿐이다.
  - 보류 이유: `docs/v2/**` 는 v2 시절 기준으로 참인 역사 기록이라 손대지 않기로 했다
    (2026-09-02 사용자 판정). 재매핑을 시험 삼아 돌렸더니 의심이 38→43 으로 **늘었다** —
    문서의 줄 번호가 기준 판본과 애초에 안 맞아서, 틀어진 번호를 기준으로 옮긴 탓이다
    (`remap-doc-refs.mjs` 머리말이 경고하는 바로 그 경우). 되돌렸다.
  - **승격 트리거**: docs/v2 를 다시 쓰기로 하거나, 그 문서의 링크를 실제로 따라가야 할 때 →
    `--base` 를 참조가 맞았던 커밋으로 잡아 재매핑하거나, 줄 참조를 앵커 인용으로 바꾼다.

- [ ] **간격 짝수 px 강제를 게이트로 승격** (2026-07-23, wama 에서 옮겨 옴)
  - 규칙 자체는 프로젝트 지식이라 `projects/wama/docs/design/design-rules.md` 「간격」 절에 남아 있다:
    공통 레이아웃은 spacing 토큰(`--space-*`), 일회성은 짝수 px 허용(홀수 금지, 1px 헤어라인 제외).
  - 여기 있는 것은 **승격 기록뿐**이다 — "게이트로 만들자"는 킷 이야기다.
  - 지금: 사람이 지킨다. 리뷰어가 3회 반복 지적한 뒤 규칙과 현실을 맞췄고 홀수 1건을 고쳤다.
  - 보류 이유: 홀수 px 는 CSS 어디에나 나올 수 있어(테두리·그림자·transform) 과차단이 쉽다.
    무엇을 재는지(패딩·마진·gap 만인지)를 먼저 정해야 한다.
  - **승격 트리거**: 홀수 px 가 다시 들어오면 → run-gates 에 CSS 간격 속성만 보는 검사 추가.
  - 출처: wama DECISIONS 2026-07-23 항목에 섞여 있던 것을 2026-09-02 에 여기로 옮겼다.

- [ ] **design-rules 「방향」 절을 게이트로 올릴지** (2026-09-03, v3.3 C단계)
  - 지금: 계약 문서 3절에 등재만 했고 **강제하는 게이트는 없다.** 절이 없으면 에이전트가
    「디자인 방향 빈칸」으로 보고한다. `signal2` 의 design-rules 에 이 절이 지금 없다.
  - 보류 이유: `design-rules.md` 는 `design/page-designer` 의 산출물이라 절을 더하면 해시가
    바뀌어 design→implement→qa→review 가 전부 dirty 가 되고 사인오프가 무효화된다. 이미
    승인된 방향을 받아적는 문서 추가 때문에 리뷰를 다시 돌리는 건 비용이 안 맞는다.
  - **승격 트리거**: signal2 의 design 노드가 어차피 dirty 가 되는 작업이 오면 그때 절을 채운다.
    그 뒤에도 빈 프로젝트가 나오면 게이트로 올린다(BEFORE_UI 옆, 차단이 아니라 경고로).
  - 근거: 2026-09-03. 화면 작업이 없어 빈칸이 실제로 문제를 일으킨 적은 아직 없다.

- [ ] **패치 조합표에 「막혔을 때 빠져나오는 길」이 없다** (2026-09-03, v3.3 B단계에서 실제로 겪음)
  - 지금: 조합표는 각 중간 상태에서 **무슨 일이 일어나는지**만 적는다. 위험한 상태를 순서로
    제거하는 것까지는 하는데, **그 상태에 빠졌을 때 어떻게 나오는지**를 안 적는다.
  - 관찰: B단계 패치 셋 중 `graph.mjs` 만 빠진 상태가 실제로 났다. 조합표가 예측한 대로 턴이
    막혔는데, **예측하지 못한 것은 그 상태가 세션 전체를 반복에 가둔다는 것**이었다. Stop 훅이
    매 턴 exit 2 를 내고, 푸는 유일한 길은 보호 파일 편집이라 모델이 할 수 없다. 열 턴 넘게
    같은 차단 메시지만 오갔다. "막는 것은 새 기능 추가뿐"이라고 안내했는데 그것도 틀렸다.
  - 두 번째 관찰(같은 세션): `--quick` 을 안 가른 탓에 **모든 편집이 막혔다.** PostToolUse 훅이
    편집마다 `run-gates --quick` 을 돌리는데 보류는 저절로 사라지지 않아서, 보류를 닫아 푸는
    것조차 편집이라 같이 막혔다. 게이트가 막으려던 일이 게이트 때문에 일어나는 계열의 4번째다
    (2026-08-06·08-10·08-11 에 이어).
  - **승격 트리거**: 다음 다중 파일 패치를 낼 때 → 조합표 서식에 「막혔을 때 빠져나오는 길」 행을
    필수로 넣는다. 지금은 그 패치 문서에만 손으로 적어 뒀다.
  - 같이 볼 것: 편집 훅이 도는 자리(`--quick`)를 검사가 안 밟으면, 패치가 편집을 막는 것을
    아무도 못 잡는다. `check-pending-kind` 에 A4 로 추가했지만 이건 이 패치 하나에만 붙은 검사다.

- [ ] **Codex 미러 범위 재정의** (2026-09-03, v3.3 0단계에서 갈라져 나옴)
  - 지금: 치환 사전이 `.claude` ↔ `.Codex` 인데 **`.Codex/` 폴더가 이 레포에 없다.** 실제 미러 폴더는
    `.agents/` 이고 거기엔 `skills/` 만 있다 — `.claude/rules/`(7파일)와 `.claude/agents/`(7파일)는
    짝이 없다. 그래서 `AGENTS.md` 는 Codex 에게 "레이어별 상세 규칙은 `.Codex/rules/` 가 자동 로드된다"고
    말하는데 그 폴더가 없다.
  - 보류 이유(사용자 결정, 2026-09-03): Codex 를 계속 쓸지가 먼저다. 안 쓸 거면 미러 자체가 짐이다.
  - **승격 트리거**: v3.3 완료 후 사용자가 정한다 → 셋 중 택일.
    ① 치환 교정(`.claude` → `.agents`) ② `rules/` 도 미러링(관리 대상 +7파일) ③ 미러 축소(또는 폐기).
  - 근거: 2026-09-03. 0단계 동기화 검사를 짜면서 드러났다. 검사는 기존 관례를 그대로 따랐으므로
    이 결정이 바뀌면 `scripts/check-mirror-sync.mjs` 의 치환 사전도 같이 고친다.

- [ ] **두 벌 동기화 검사를 run-gates 에 편입할지** (2026-09-03 · 2026-09-04 패치 발행) — **패치는 나왔다. 적용만 남음**
  - **2026-09-04 회고에서 트리거 발동("다음 하네스 사이클")** → 패치를 냈다:
    `docs/references/pending-patches/2026-09-04-mirror-into-gates.md`.
    붙기 전 `check-mirror-sync` 5/6(D 실패) · 붙은 뒤 6/6 이 판정 기준이다.
    만들다 결함 하나가 드러났다 — 배선 프로브(D)가 `run-gates` 를 부르는데 게이트가 검사기를
    통째로 부르면 서로를 부르며 끝나지 않는다. `--repo-only` 경로를 따로 내서 끊었다.
  - 지금: `scripts/check-mirror-sync.mjs` 를 **손으로** 돌린다. 검사 자체는 붙었고(5/5) 어긋남도 고쳤다.
    남은 것은 "언제 도느냐"뿐이다.
  - 보류 이유(계획 승인 시 사용자 결정): v3.3 이 A~C 단계에서 `CLAUDE.md`·`AGENTS.md`·스킬 두 벌을
    계속 고치는 중이라, 지금 게이트로 올리면 작업 중간 상태마다 걸린다.
  - **2026-09-03 C단계에서 판단함 — 편입한다. 단 조건이 하나 붙는다.**
    **전체 실행 전용이어야 하고 `--quick`(편집 훅)에는 넣지 않는다.** 안 그러면 `CLAUDE.md` 를
    고치는 순간 두 벌이 어긋난 상태가 되고, 그걸 푸는 유일한 길인 `AGENTS.md` 편집이 같은
    게이트에 막힌다 — 게이트가 막으려던 일이 게이트 때문에 일어난다. 같은 함정을 B단계에서
    보류 신고로 한 번 밟았다(보류를 닫는 편집이 보류 때문에 막혔다).
    패치는 아직 안 냈다 — 게이트 파일이라 사용자 반영이 필요하고, C단계 범위 밖이다.
  - **승격 트리거**: 다음 하네스 사이클 → run-gates 전체 실행에 카테고리 추가(제안만).
  - **왜 트리거를 굳이 적어 두나**: 수동 도구는 아무도 안 알려준다. `check-doc-refs` 가 같은 성질이라
    "게이트로는 안 만들었다"고 적어 둔 채로 참조가 세 번 더 밀렸다. 손으로 돌리는 검사는
    돌릴 사람이 그 검사를 기억하고 있을 때만 검사다.
  - 근거: 2026-09-03 v3.3 0단계. 검사 신설과 어긋남 복구는 같은 날 반영됐고, 자동 실행만 보류됐다.

- [ ] **사이클 기준선 — 사이클 시작 시점의 기존 게이트 실패는 신규 위반으로 세지 않는다** (2026-09-03 보류 → 2026-09-04 등재)
  - 지금: 없다. 게이트 실패는 언제 생긴 것이든 똑같이 턴을 막는다.
  - 보류 이유(2026-09-03, v3.3 계획): 그때는 게이트가 전부 통과 중이라 "신규 위반 0 = 전체 0"
    이었고, 실패가 하나도 없는 상태에서 만들면 그 장치가 작동하는지 확인할 실물이 없었다.
    "위반 0건이라는 보고와 검사가 아예 안 돌았다는 것은 겉이 같다"(LESSONS 2026-09-02).
  - **이 보류가 v3.3 계획 문서 안에만 있었다.** 백로그에 없어서 트리거를 아무도 안 들고
    있었고, 2026-09-04 intake 계획에서 다시 필요해졌을 때 프롬프트 문서를 뒤져서 찾았다.
  - **승격 트리거**: 게이트가 실제로 실패하는 상태에서 사이클을 열어야 할 일이 생기면.
    만들 때의 조건은 그대로다 — 위반을 일부러 심어 기준선이 그것을 걸러 내는 것을 보는
    항목이 검사에 있어야 한다. 그 항목을 못 만들면 만들지 않는다.
  - **아래 항목과 헷갈리지 말 것.** 들여온 프로젝트의 코드가 게이트를 통과 못 하는 문제는
    기준선으로 풀지 않는다(그 항목의 첫 줄에 이유를 적었다).

- [ ] **프로젝트 단위 스캔 범위 선언 — 규약을 안 지키는 코드가 들어오면 레포 전체가 막힌다** (2026-09-04, intake 사이클에서 갈라져 나옴)
  - **기준선(위 항목)은 이 문제의 해법이 아니다.** 기준선은 사이클마다 스냅샷을 다시 뜨므로
    들여온 위반이 영구 면제되고, 줄어드는 경로가 생기지 않는다. 필요한 것은 "이 프로젝트는
    아직 게이트 시야 밖"이라고 **선언하고, 그 선언이 만료되는** 장치다.
  - 지금: 게이트는 `src/` 가 있는 모든 프로젝트의 트리 전체를 매번 본다. 편집한 파일만 보는
    게 아니다. risk-surface 는 스펙 커버가 없으면 error 이고, BEFORE_UI 는 UI 파일이 있기만
    하면 발동하며, 이 둘은 어디서 나든 턴을 막는 카테고리다. 그래서 규약을 안 지키는 코드가
    `projects/<이름>/src/` 에 들어오는 순간 **다른 프로젝트 작업까지 전부 멈춘다.**
  - 지금의 회피: intake v1 이 `src/` 를 아예 만들지 않는다(문서와 과정 기록만 들여온다).
    대가는 들여온 코드의 위험 표면을 아무도 안 보는 것이고, 리포트에 그렇게 적는다.
  - **승격 트리거**: 들여온 프로젝트의 코드를 실제로 `src/` 로 옮기려 할 때. 그 사이클의
    선행 조건이다 — 장치 없이 옮기면 첫 편집에서 레포가 잠긴다.
  - 근거: 2026-09-04 intake 계획. `study-mate` 를 실물로 대조해 판정했다.

- [ ] **하네스 참조 패턴 목록이 세 벌이다** (2026-09-04, intake 사이클에서 갈라져 나옴)
  - 지금: "프로젝트 문서에 있으면 안 되는 하네스 참조"의 정규식 목록이 `gates/run-gates.mjs` ·
    `scripts/check-handover-rehearsal.mjs` · `scripts/check-intake.mjs` 세 곳에 각각 있다.
  - 보류 이유: 하나로 모으려면 도는 검사 둘을 고쳐야 하는데, 그 둘은 지금 통과 중이고
    이번 사이클의 대상이 아니다. 그리고 어긋나도 **조용하지는 않다** — 들여오기 절차가
    프로젝트 단위 판정 바로 뒤에 전 프로젝트 리허설을 돌리므로, 목록이 갈라지면 뒤엣것이 잡는다.
  - **승격 트리거**: 셋 중 하나의 목록을 실제로 고칠 일이 생기면 → 그때 `scripts/lib/` 로
    모으고 셋이 같은 것을 읽게 한다(`run-gates` 는 보호 파일이라 패치로).
  - 근거: 2026-09-04. 사본을 하나 더 늘린 것은 이번 작업이다 — 늘리면서 등재한다.

- [ ] **판정 기준이 바뀌면 산출물은 그대로라 그래프가 안 움직인다** (2026-09-04, 불변식 범위 패치에서 겪음)
  - 지금: 노드 상태를 내리는 단계는 **dirty → clean 한 방향뿐**이다. 이미 clean 인 노드에 새 게이트
    실패가 생겨도 다시 dirty 가 되지 않는다. 보통은 문제가 안 된다 — 실패는 대개 산출물이 바뀔 때
    같이 생기고, 산출물이 바뀌면 해시로 잡히기 때문이다.
  - **그런데 게이트를 고치면 산출물은 그대로인 채 판정만 바뀐다.** 2026-09-04 에 불변식 커버 판정을
    고쳤더니 `spec-coverage` 가 실패를 넷 신고했는데 `spec` 은 계속 clean 이었고, 프론티어는
    `review` 를 가리켰다 — 승인된 계약의 불변식 넷이 검증되지 않았는데 검증 단계를 지났다고
    말하는 상태다. 손으로 `--mark spec` 을 찍어서 고쳤다.
  - **승격 트리거**: 게이트 판정을 고치는 패치가 또 나오면 → 그 패치의 판정 항목에 "이 변경으로
    새로 생기는 실패가 노드를 다시 dirty 로 만드는가"를 넣거나, release 단계에 반대 방향(clean 인데
    자기 카테고리 실패가 있으면 rework)을 추가한다. 후자는 보호 파일 패치다.
  - 지금의 대처: 게이트를 고친 사람이 `--mark` 를 손으로 찍는다. 잊으면 그래프가 거짓말을 한다.
  - 근거: 2026-09-04. `spec` 이 clean · `spec-coverage` 실패 4건이 동시에 성립했다.

## 졸업 (반영됨 → LESSONS.md로)

- [x] **활성 프로젝트가 아닌 곳의 보류가 매 턴을 막는다** (2026-09-04 발견 → 같은 날 반영, 사용자 적용)
  - 무슨 일이 났나: `projects/study-mate/` 를 들여온 뒤 그 프로젝트의 보류 2건 때문에 **모든 턴이
    막혔다.** 활성 프로젝트는 `signal2` 였고, 보류는 원래 턴을 막지 않는 종류인데도 막혔다.
  - 원인은 두 코드가 범위를 다르게 본다는 것이다. `run-gates` 는 `workspace/PENDING.md` 가 있는
    **모든 프로젝트**의 보류를 실패로 신고한다. `graph-stop` 의 에러 파서는 **활성 프로젝트 경로가
    아닌 줄을 건너뛴다**(다른 프로젝트 에러는 이 그래프와 무관하다는 판단이고, 그 자체로는 옳다).
    그래서 실패는 있는데 파싱 결과가 0건이 되고, 차단 판정이 "에러를 하나도 못 파싱했으면 막는다"
    (모르면 막는 쪽)로 떨어진다. 낮춤 규칙은 파싱된 카테고리에만 적용되므로 닿지 못한다.
  - **빠져나오는 길이 사용자 답변뿐인데 그 답을 받으려면 턴이 끝나야 한다.** 게이트가 막으려던 일이
    게이트 때문에 일어나는 계열의 5번째다(2026-08-06·08-10·08-11·09-03).
  - 고칠 때 고른 것 — 방향 셋을 놓고 ①로 갔다(어느 쪽이든 보호 파일 패치다):
    ① 파서가 다른 프로젝트의 에러를 버리지 말고 "다른 프로젝트 것"으로 표시해 낮춤 대상에 넣는다
    ② 차단 판정에서 "파싱 0건"과 "낮출 수 있는 것만 남았다"를 가른다
    ③ `run-gates` 가 활성 프로젝트의 보류만 신고한다(범위를 맞추는 쪽. 다만 다른 프로젝트의 보류가
       아예 안 보이게 되는 대가가 있다)
  - 근거: 2026-09-04. 실패 줄 둘이 전부 `projects/study-mate/` 였고 `graph-stop` 이 "이번 차단의
    이유는 파싱 불가"라고 찍었다.
  - **닫힘(2026-09-04).** 방향 ①로 갔다 — 파서는 그대로 두고, 차단 판정에만 쓰는 목록을 따로
    만들어 `GATE_KIND` 에 있는 카테고리만 낮춘다. 목록에 없는 것(fsd·security·tsc·test·
    risk-surface)은 지금처럼 어디서 나든 막는다.
  - 판정: `node scripts/check-foreign-gate-errors.mjs` — 붙기 전 3/4(A 실패) → 붙은 뒤 **4/4**.
    D 가 패치를 적용한 사본에서 미리 확인해 주고, B 가 과통과(전부 통과시키는 고장)를 막는다.
  - 회피로 밖에 내놨던 study-mate 의 보류 둘은 `## 열린 항목` 으로 되돌렸다. 되돌려도 턴이
    안 막히는 것이 패치가 붙었다는 신호다.
  - 교훈은 docs/LESSONS.md 2026-09-04 항목으로 갔다(신고 범위와 판정 범위가 다르면 그 사이에
    낀 것은 판정을 못 받는다).


- [x] **CLAUDE.md↔AGENTS.md · .claude/skills/↔.agents/skills/ 두 벌 동기화 검사** (2026-09-02 발견 →
      2026-09-03 반영, v3.3 0단계) → **`scripts/check-mirror-sync.mjs` 신설 + 어긋난 세 파일 복구.**
      보류 이유였던 "두 벌이 완전한 사본인지 먼저 정해야 한다"는 실측으로 풀렸다 — 미러 쪽에 고유
      내용이 하나도 없었고, 다른 줄은 전부 정본의 **옛 판본**이었다. 그래서 허용 차이는 치환 사전
      두 줄(`CLAUDE.md`↔`AGENTS.md`, `.claude`↔`.Codex`)로 끝나고, 그 밖의 차이는 전부 어긋남이다.
      **실측된 어긋남 34건**: `AGENTS.md` 가 「부분 적용 조합」 조항(2026-08-30 교훈) 4줄과 `--mark`
      자식 지정 3줄을 통째로 빠뜨렸고, 사인오프 기록의 `project: <프로젝트명>`(기계가 읽는 필드)도
      없었다. `.agents/skills/spec` 은 폐기된 `docs/specs/planned/` 관례를 아직 가르치고 있었고
      `status: parked` 체계 전체가 없었다. `.agents/skills/retro` 는 폐기된 "40줄" 기준을 들고 있었다.
      **Codex 쪽은 옛 하네스로 돌고 있었던 셈이다.**
      검사 설계에서 배운 것 둘: ① 줄 번호를 나란히 비교하면 삽입 하나에 뒤가 전부 밀려 한 블록 누락이
      수십 건으로 불어난다 — 최장 공통 부분수열로 실제 추가·삭제만 뽑아야 사람이 읽는다.
      ② 각도가 다른 검사 둘이 필요하다 — 내용 대조는 **치환을 빠뜨린 자리를 못 잡는다**(내용이 같으니까).
      치환 누락 검사를 따로 두자 실물 하나가 잡혔다(`AGENTS.md:115` 의 `.claude/rules/`).
      자동 실행은 위 미결 항목으로 갈라 뒀다.

- [x] **보호가 하나도 안 실린 자리에서 세션을 열 수 있다** (2026-08-15 발견 → 2026-08-17 반영, 사용자 적용) →
      **`dev/.claude/settings.json` + `dev/.claude/hooks/protect-kit.mjs` 신설. 이 킷(webdeb-colab-kit-ADAPTIVE)
      경로만 겨냥해서 막는다.**
      상위 폴더(`dev/`)에서 세션을 열고 킷을 작업하면 `gates/`·`graph.mjs`·`.claude/hooks/` 를 전부 그냥
      편집할 수 있었다 — 훅이 잘못 쓰인 게 아니라 `webdeb-colab-kit-ADAPTIVE/.claude/settings.json` 자체가
      안 읽힌 것이다(훅 등록도 permissions 도 거기 있어 한꺼번에 없는 상태가 됨). 2026-08-16 에 실제 피해로
      드러났다 — `6ad0198 "리팩토링"` 커밋이 무방비 세션에서 나갔고, Stop 훅이 안 돌아 HANDOFF 가 옛
      해시를 든 채 `review` 가 `status: passed` 로 남았다. **리뷰받은 코드와 배포하려던 코드가 다른 상태로
      deploy 직전까지 가 있었다** — 훅이 다시 도는 세션이 되고서야 basis 불일치로 잡혔다. 2번째 관찰도
      같은 세션 계열에서 났다(README 경고가 킷 폴더 안에 있어 상위 폴더에서 열면 안 보임).
      후보 둘 중 사용자 레벨 절대경로 deny(킷을 복사해도 경로가 안 따라와 이 킷 성격과 어긋남)를
      버리고 `dev/.claude/settings.json` 을 선택했다. Claude Code 는 세션을 연 그 폴더의 settings.json 만
      읽고 상위로 안 거슬러 올라가므로, 이 설정은 **세션을 정확히 `dev/` 에서 열었을 때만** 적용된다 —
      다른 폴더(`study-mate/` 등)에서 정상적으로 세션을 여는 작업에는 영향이 없다. 훅 자체도 경로/명령에
      `webdeb-colab-kit-ADAPTIVE` 이름이 있을 때만 판정해서, 이름이 거의 같은 `webdev-colab-kit-GRAPH`
      나 다른 프로젝트는 건드리지 않는다.
      검증: `scripts/check-dev-kit-protection.mjs` — 훅을 자식 프로세스로 실제 실행시켜 6가지 경우
      (이 킷의 gates·hooks·graph.mjs 는 막힘 / 이 킷의 무관한 소스 파일·다른 킷·무관한 프로젝트는 안 막힘)
      전부 확인.

- [x] **`rm -r <디렉터리>` 가 protect-files 를 빠져나간다** (2026-08-15 발견 → 2026-08-17 반영, 사용자 적용) →
      **`.claude/hooks/protect-files.mjs` 에 `rm -r <경로>` 의 인자를 보호 목록과 직접 대조하는
      `rmRecursiveDirHit` 추가.**
      `protect-files` 는 보호 **경로 문자열**이 명령에 그대로 있어야 잡는데, `rm -r .claude/` 에는
      `.claude/hooks/`·`.claude/settings.json` 같은 하위 파일명이 안 보여서 안 걸렸다. 패치 검증 중
      **더 큰 구멍도 같이 드러났다** — `rm -r gates`(끝 슬래시 없이)도 안 걸렸다. PROTECTED 항목이
      `"gates/"` 처럼 슬래시를 포함해 저장돼 있어서, 슬래시 없이 쓰면 문자열 자체가 안 맞았다.
      명령을 토큰으로 갈라 `rm` 의 인자가 PROTECTED 항목 자신이거나 그 조상 디렉터리인지 대조하는
      방식으로 바꿨다 — `node_modules` 같은 무관한 재귀 삭제는 과차단 없이 그대로 통과한다.
      `-f` 조합(`rm -rf`)은 범위 밖으로 남겼다 — 그건 `block-danger` 가 별도로 잡는다(2026-08-16 반영).
      검증: `scripts/check-rm-dir-protection.mjs` — `.claude`·`.claude/`·`docs`·`gates` 통째 삭제는
      막히고 `node_modules`·무관 경로는 통과하는지 6가지 케이스로 확인. 패치 전 실행 결과 4건이 전부
      exit 0(안 막힘)이었고, 패치 후 전부 exit 2로 바뀐 것을 실측.

- [x] **예외 만료가 원칙의 1/4만 기계화돼 있다** (2026-08-17 발견 → 같은 날 반영, 사용자 적용) →
      **경고 문장이 만료 여부를 스스로 밝히게 했다. 판정은 안 바뀌었다.**
      CLAUDE.md 는 "예외는 영구가 아니다 — 전제가 깨지면 게이트가 만료시킨다"고 선언하는데,
      실제 만료 검사는 표면 4개(`auth`·`payment`·`authz`·`concurrency`) 중 **`authz` 하나**뿐이고
      그 근거도 `supabase/**.sql` 의 `create policy` 뿐이었다(`writePolicyIn`). signal 에서 실측:
      정책 3개가 전부 `for select` 라 만료가 안 걸리는데, 실제 쓰기 경로는 스스로 "RLS 를 우회한다"고
      적어 둔 secret 키 클라이언트이고 인가 판정은 라우트의 `Bearer` 비교 한 줄이었다 — **인가 규칙이
      DB 밖에 있으면 만료 기계가 구조적으로 못 본다.**
      ① `gates/run-gates.mjs` 에 `EXPIRY_RULES` 선언(표면 → "무엇이 생기면 풀리는가"). ② EXEMPT 경고에
      해당 표면이 목록에 있으면 "…가 생기면 이 예외는 자동으로 만료된다", 없으면 "이 표면에는 만료 검사가
      없다 — 사람이 지켜봐야 한다"를 덧붙였다.
      보류한 것: `authz` 만료 근거에 코드 쪽 쓰기 경로(secret/service_role 클라이언트)를 추가하는 안 —
      signal 이 즉시 만료돼 `/spec` 을 요구받는데, 서버에서 secret 키로 쓰는 게 정상인 수집기·크론마다
      스펙이 강제된다. 사용자가 낸 예외를 규칙으로 덮는 쪽이라 판단만 남겼다.
      **재검토 트리거**: 만료 없는 표면의 예외가 전제가 깨진 채로 살아 있는 사례가 1회라도 관찰되거나,
      인가 규칙이 DB 밖에 있는 프로젝트가 둘째로 나오면 → 코드 경로 추가를 다시 검토.
      검사: `node scripts/check-exempt-expiry.mjs` — 만료 규칙 목록을 게이트 소스에서 직접 읽어 실제
      경고와 대조한다(베끼지 않는다). 픽스처로 양방향 확인함: 패치 전 실패 · 후 통과 · 문구를 하드코딩한
      변이도 실패.

- [x] **basis 불일치 메시지가 아는 것보다 많이 말한다** (2026-08-17 발견 → 같은 날 반영, 사용자 적용) →
      **"구현이 바뀌었으니" → "산출물이 바뀌었으니(주석·공백도 해시에 들어간다)"로 1줄 수정.**
      해시는 `src/**` 의 원문 바이트라 주석·공백 변경도 불일치를 만드는데, 기계가 아는 것은
      "산출물이 바뀌었다"까지다. 2026-08-17 에 실제로 갈렸다 — 바뀐 것은 `risk-surface-exempt` 주석
      3줄이고 핸들러는 그대로였다. diff 를 안 열었으면 리뷰어에게 "구현이 바뀌었다"는 틀린 전제를
      넘길 뻔했다. 같은 계열: 아래 "검사 안 됨과 통과를 구별한다" — 기계가 아는 범위를 문장이 넘지
      않게 하는 것이 이 킷이 반복해 고쳐 온 종류다.
      검사: `node scripts/check-basis-message.mjs` — 양방향 확인함. **한계: 소스 문자열 대조다.**
      그 문장을 실제로 출력시키려면 product~qa 를 전부 clean 으로 만든 픽스처 레포가 필요해서
      (tsconfig·package.json·통과하는 테스트 포함) 문구 한 줄에 비해 비싸 거기서 멈췄다.

- [x] **검사가 안 돌아도 "게이트 통과"가 찍힌다** (2026-08-16 발견 → 같은 날 반영) →
      **안 돈 검사를 전용 게이트 카테고리로 만들어 그래프가 받게 했다.**
      발견 경로: `node_modules` 가 없어 tsc·vitest 가 실행조차 못 한 실패를 조사하다, 그 반대 방향에
      더 위험한 구멍이 있는 걸 봤다 — `tsconfig.json` 이 없으면 tsc 를, `scripts.test` 가 없으면 테스트를
      **조용히 건너뛴다.** 노드 판정은 "그 카테고리 에러가 0건인가" 하나뿐이라(`gateBlocked`),
      안 돌아서 0건인 것과 다 통과해서 0건인 것이 구별되지 않는다. 실측: 설정 없는 임시 프로젝트를 두고
      돌리면 `게이트 통과 (139개 파일, 3개 프로젝트)` 가 그대로 찍혔다.
      ① `run-gates` 가 `tsc-notrun/NO_TSCONFIG` · `test-notrun/NO_TEST_SCRIPT` 를 낸다.
      ② 두 카테고리를 `implement`·`qa` 의 `clean_when.gate` 에 넣어 노드가 clean 이 안 되게 했다.
      ③ `GATE_KIND` 에 completion 으로 등록해 **턴은 막지 않는다** — 설정을 붙일지 n/a 로 선언할지는
      사용자 결정이고 물어보려면 턴이 끝나야 한다. ④ `graph-stop` 의 낮춤 조건에 owner 가 `n/a` 인 경우를
      더했다(이미 "해당 없음"을 결정한 노드의 검사로 턴을 막을 이유가 없다).
      ⑤ 통과 메시지가 무엇을 돌렸는지 말한다: `· tsc 2/2 · test 2/2`.
      실측 검증: 상류를 clean 으로 만든 임시 프로젝트에서 설정이 없으면 implement·qa 가 dirty 로 남고
      (그때 난 에러는 notrun 둘뿐), tsconfig·test 스크립트를 붙이면 둘 다 clean 이 되고 프론티어가 review 로 갔다.

- [x] **보호 파일 목록이 Windows 에서 대부분 안 걸린다** (2026-08-16 발견 → 같은 날 반영) →
      **경로 구분자를 맞춰 비교하게 고쳤다.**
      `protect-files.mjs` 가 `target.includes("gates/")` 로 판정하는데 Windows 의 `tool_input.file_path` 는
      `...\gates\run-gates.mjs` 라 안 맞았다. 보호 5개 중 슬래시가 든 4개(`docs/LESSONS.md`,
      `.claude/settings.json`, `.claude/hooks/`, `gates/`)가 Edit/Write 에서 전부 죽어 있었고,
      슬래시가 없는 `graph.mjs` 만 살아 있었다. Bash 경로(`bashWritesTo`)는 사용자가 슬래시로 쳐서 무사했다.
      발견 경로: `graph.mjs` 편집이 막히는데 같은 세션에서 `gates/run-gates.mjs` 편집은 다섯 번 다 통과한 것이
      이상해서 훅을 읽었다. **훅이 있다는 사실이 훅이 돈다는 뜻은 아니다** — 보호는 실측으로 확인해야 한다.

- [x] **정상 절차 중에 게이트가 턴 자체를 막는다** (2026-08-06·08-10·08-11 3회 + 회피 2회 → 2026-08-15 반영) →
      **노드 상태에 `rework` 를 추가하고, 턴 차단을 게이트 성격별로 갈랐다.**
      ① `graph.mjs` 에 `GATE_KIND` 신설 — `spec-coverage` 는 completion(끝내기 전에 있어야 함),
      `design`(BEFORE_UI)은 precondition(시작하기 전에 승인받아야 함). 그 밖(fsd·security·tsc·test·risk-surface)은
      목록에 없고 무조건 차단이다. ② `graph-stop` 이 exit 2 직전에 남은 에러를 카테고리로 본다 —
      completion 은 owner 가 dirty/rework 면, precondition 은 owner 가 **rework** 일 때만 안내로 낮춘다.
      ③ `rework` 는 "clean 이던 노드를 `--mark` 했다"로 파생한다. 별도 기록을 안 만든 게 핵심이다 —
      판별 재료가 이미 상태에 있었고, 그래서 **한 번도 승인 안 받은 노드는 rework 가 될 수 없다**
      (새 프로젝트에서 BEFORE_UI 가 뚫리는 걸 이걸로 막았다. 처음 설계한 "노드가 dirty 면 낮춘다"는
      정확히 그 구멍을 뚫는 안이었다). ④ 재작업 중 파일 편집은 rework 를 dirty 로 풀지 않는다 —
      안 그러면 재작업하는 행위가 거부 사실을 지운다.
      **낮춰도 강제력은 남는다**: 노드는 dirty/rework 그대로, 하류도 그대로 차단, 프론티어도 그대로.
      허용되는 건 턴 종료 하나뿐이고 매 턴 `⚠ [graph/EXPECTED]` 로 찍힌다.
      검증: 픽스처 6종(완료형은 안 막음 · 항상차단 1건 섞이면 막음 · 승인 이력 없는 design 은 막음 ·
      rework 면 안 막음 · 재승인하면 clean 으로 풀림 · 사유 없는 mark 는 지적) + `propagate --selftest` 4종.
      **LESSONS 후보(사용자 확인 필요)**: "게이트가 처방한 상태를 게이트가 위반으로 보면, 값싼 회피는
      게이트가 막으려던 바로 그 일이 된다 — 강제력이 두 겹일 때 하나는 덜어낸다."

- [x] **"테스트를 썼다"에 변이 확인이 포함되지 않는다** (2026-08-06 → 2026-08-11 승격) →
      `.claude/rules/tdd.md` "green 이 무엇을 증명하는가" 절. 트리거였던 "2번째 지적"을 훌쩍 넘겨
      **4세션 연속** 나왔다(8/06 `safeSourceUrl`·배선 0건 / 8/08 `width="0"`·`MAX_RATIO`·`</div><article` /
      8/09 주석의 `INV-C5` / 8/10 `server-env` 정규식·`/api/ingest` 인가 0건·부재만 확인·`toContain`).
      tdd.md 에 "알리바이"라는 말이 **이미 있었는데도** 재발했다는 것이 반영 방식을 정했다 — 이름이 아니라
      **무력화해서 빨간불을 본다**는 절차를 적었다. 2안(test-auditor 프롬프트에 "어떤 변이가 이걸
      깨뜨리는가" 필수 출력)은 이걸로도 반복되면 그때. LESSONS "green 은 변이로만 증명된다".

- [x] **spec-coverage 가 주석 속 INV 문자열을 테스트로 센다** (2026-08-10 → 2026-08-11 반영) →
      `gates/spec-coverage.mjs` 가 세기 전에 주석을 걷어낸다. 관찰: `INV-C5` 만 게이트가 안 물었는데
      통과가 아니라 못 본 것이었다 — 몇 달 전 다른 목적으로 쓴 **주석**에 그 글자가 있었다.
      이 게이트는 알리바이 테스트를 막는 장치인데 스스로 알리바이를 만들 수 있으면 "커버리지 통과"가
      아무것도 뜻하지 않는다. 반영 후 변이로 확인(주석에만 있으면 exit 2, 설명문에 있으면 통과).

- [x] **마이그레이션의 조건 없는 파괴적 문장** (2026-08-10 → 2026-08-11 반영) →
      3층으로 나눠 막았다. ① `gates/run-gates.mjs` 의 `security/SQL_UNCONDITIONAL_WRITE` —
      주석과 `$$…$$` 함수 본문을 공백으로 지운 뒤 **문장이 `update`/`delete from`/`truncate` 로 시작하고
      `where` 가 없는 것**만 잡는다(처음 쓴 낱말 기반 정규식은 `for update` 정책 절·트리거 정의로
      **오탐 3건**을 냈다. 실제 SQL 11파일 0건 · 견본 10/10 확인, 예외 표시는 `-- gate:allow-unconditional`).
      ② `.claude/rules/supabase.md` 마이그레이션 절 + `paths` 에 `projects/*/supabase/**`
      (그 전엔 마이그레이션을 쓰는 동안 이 규칙 파일이 **아예 안 떴다**).
      ③ `scripts/apply-migrations.mjs` 가 이름 없이 전체를 적용하던 기본값을 `--all` 명시 요구로.
      **①이 1차 방벽이다** — 새 DB·리셋에서는 `--all` 이 실제로 필요하고 그때 파일이 안전해야 한다.
      LESSONS "마이그레이션 파일에는 다시 돌려도 같은 결과인 것만 담는다".

- [x] **spec 템플릿 킷 정본화** (2026-08-02) → docs/references/spec-template.md 신설 + scaffold.mjs 복사 배선.
      LESSONS "spec 템플릿은 킷 정본으로".
- [x] **frontmatter status 오매칭 → 줄 시작 앵커** (2026-08-02) → gates/spec-coverage.mjs·graph-stop.mjs
      `^\s*status:\s*approved\b`(m). LESSONS "frontmatter status 판정은 줄 시작 앵커로".
- [x] **NO_INNERHTML 주석 오매칭 → 승격 안 함(보수 유지)** (2026-08-02) → 보안 게이트는 과차단이 안전.
      "위험 API는 코드·주석에 이름조차 안 쓴다" 규율로 대응. 위 LESSONS 항목에 근거 병기.
- [x] **스펙 승인 단위 = 구현 가능 단위로 하나씩 + planned/ 파킹** (2026-08-02) → spec SKILL "승인 단위·보류" 절.
      LESSONS "스펙은 구현 가능 단위로 하나씩 approved, 보류는 planned/". (approved-deferred 정식 status 는 위 미결로 잔류)
- [x] **run-gates.mjs `designApproved()` status 정규식 줄 앵커** (2026-08-02) → frontmatter 블록을 떼고
      `^\s*status:\s*approved\b`(m). 졸업 항목 "frontmatter status 판정은 줄 시작 앵커로" 의 빠진 사본을 메움.
      (반영 중 `existsSync` 단축평가가 깨져 파일 부재 시 ENOENT 로 죽는 상태를 거쳤고, 이후 수정됨)
- [x] **design/BEFORE_UI 트리거에 src/app 라우트 페이지 포함** (2026-08-02) → `isNextProject()`(next.config.* 존재)로
      판정한 Next 프로젝트에만 `src/app/**/page.*` 를 화면으로 봄. 라우트 화면 1장은 워킹 스켈레톤으로 예외.
      nextjs-fsd 프로파일 "게이트 함의 4" 갱신. LESSONS "게이트가 스택을 가정할 때는 판정하고 분기한다".
