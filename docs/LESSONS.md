# LESSONS.md — 승인된 교훈 (retro 절차로만 갱신 — 훅이 직접 수정을 차단함)

형식: 날짜 | 패턴 | 근거 | 반영 위치 (permissions / 게이트 / rules / CLAUDE.md)

## 2026-07-24 — RLS 격리는 구조 검증만으론 불충분

- 증상: 정책 표현식(academy_id=current_academy_id())이 스펙과 일치해 "검증 통과"로 봤으나, security definer
  함수의 `set search_path=public`이 pg_temp 섀도잉을 못 막아 격리가 실제로는 우회 가능했다.
- 교훈: (1) 모든 security definer 함수는 `set search_path=''` + 스키마 완전 한정. (2) RLS/인가는 정책이 "있다"가
  아니라 두 테넌트 세션으로 "새지 않는다"를 행동으로 증명해야 한다. (3) 프리미티브 함수(current_academy_id 등)는
  적대적 입력(temp table 섀도잉)까지 테스트.

## 2026-08-02 — 보호 파일 훅의 리다이렉트 탐지가 개행을 안 막아 오탐

- 증상: protect-files.mjs의 리다이렉트 대상 정규식 `[^'"|&;]*`이 개행을 제외하지 않아,
  `> 파일<개행>node gates/…`처럼 다음 줄의 gates/까지 삼켜 정당한 읽기/실행 명령을 차단.
- 교훈: bash 명령 파싱 정규식은 문장 경계(개행 포함)에서 멈춰야 한다. 문자셋에 `\n\r`을 넣어
  리다이렉트 대상이 줄을 안 넘게. 근거: 사인오프 데모 명령 1회 오탐(;·&&로 이으면 통과).
- 반영 위치: .claude/hooks/protect-files.mjs (게이트/훅 계열).

## 2026-08-02 — kickoff은 인터뷰보다 파일 착수가 먼저다

- 증상: 새 프로젝트 시작에서 PRODUCT.md를 만들기 전에 채팅으로 목적·기능을 먼저 물었다.
  사용자가 "플랜은 파일로 주고받으며 채우기로 했잖아"라고 교정.
- 교훈: 첫 행동은 "이름 + 한 줄 정의"를 받아 PRODUCT.md를 만드는 것. 그 뒤 모든 질문은
  파일 편집(변경분 표시)으로. 이름 모르면 '이름과 한 줄'만 물어 즉시 생성(인터뷰 아니라 파일 착수).
- 근거: 이번 세션 1회 — 구조적 빈칸(파일-우선이 순서로만 암시, 금지로 안 박혀 있었음).
- 반영 위치: .claude/skills/kickoff/SKILL.md 금지 절 (③ skill).

## 2026-08-02 — 기술 스택은 혼자 정해 설치하지 않는다 (문서로 합의)

- 증상: setup에서 scaffold가 만든 스택(Vite·바닐라)을 동의 없이 npm install. 프레임워크 선택이
  조용히 굳음. 사용자가 "동의 구하는 과정이 없다"고 지적.
- 교훈: 스캐폴딩·설치 전 tech-stack.md(draft)로 전제/선택을 분리 제시 → 사용자 수락(approved)
  후에만 설치. 전제(TS·FSD·빌드·테스트)만 안 묻는다. CLAUDE.md "구현 전 요약→동의"의 스택 버전.
- 근거: 1회 — 구조적 빈칸(setup에 스택 합의 게이트가 없었음).
- 반영 위치: .claude/skills/setup/SKILL.md 5단계 (③ skill).

## 2026-08-02 — 검증된 아키텍처는 프로파일로 학습·축적한다

- 증상: Next+FSD 구조를 프로젝트 로컬 문서에만 적어, 다음 프로젝트면 재발명해야 하는 상태.
  사용자가 "아키텍처가 하나씩 학습돼 쌓이면 좋겠다".
- 교훈: docs/references/architectures/에 프로파일 한 파일씩(구조·게이트 함의·기본 libs).
  setup이 여기서 고르게 제시, 새 검증 구조는 프로파일로 추가(재발명 금지).
- 근거: 1회 — 구조적 빈칸(재사용 지식 축적 자리가 없었음).
- 반영 위치: docs/references/architectures/{vite-fsd,nextjs-fsd}.md + setup SKILL.md 포인터 (③ docs+skill).

## 2026-08-02 — spec 템플릿은 킷 정본으로 (프로젝트 사본 재발명 금지)

- 증상: spec 스킬이 projects/<이름>/docs/specs/\_TEMPLATE.md 를 참조하는데 킷에 정본이 없어, wama 사본을
  빌려 씀. wama 없었으면 형식을 맨땅에 지어내 프로젝트마다 스펙 형식이 흔들릴 뻔.
- 교훈: 정본을 docs/references/spec-template.md 에 두고 스캐폴딩이 새 프로젝트로 복사. 스펙 쓰다 발견한
  빈칸은 정본을 두껍게 한다(modeling-checklist 와 같은 안내 문서 축). 프로젝트 쌓일수록 정본이 보강되는 게 정상.
- 근거: 1회 — 구조적 빈칸(정본·스캐폴딩 연결 부재). 반영 위치: docs/references/spec-template.md + scaffold.mjs + spec/retro SKILL (③ docs+skill).

## 2026-08-02 — frontmatter status 판정은 줄 시작 앵커로 (주석 토큰 오매칭)

- 증상: spec-coverage·graph-stop 이 `status:\s*approved` 로 frontmatter 전체를 봐, draft 스펙의 주석
  "…status: approved 로 바꾼다" 를 approved 로 오인 → 파킹 스펙의 INV 테스트를 요구(게이트 red).
  NO_INNERHTML 도 같은 오매칭 계열이나, 그건 보안 게이트라 보수적 유지가 옳아 승격 안 함(코드·주석에
  위험 API 이름조차 안 쓰는 규율 유지).
- 교훈: 구조화된 필드(frontmatter status)는 정규식을 줄 시작 앵커(`^\s*status:\s*approved\b`, m플래그)로
  값만 본다. 보안 게이트는 반대로 보수적(과차단)이 안전 — 두 종류를 구분.
- 근거: frontmatter 오매칭 1회(정규식 계열은 2회) — 구조적 버그. 반영 위치: gates/spec-coverage.mjs·graph-stop.mjs (② 게이트).

## 2026-08-02 — 스펙은 구현 가능 단위로 하나씩 approved, 보류는 planned/

- 증상: 인프라(Supabase) 의존 스펙과 오프라인 가능 스펙을 한꺼번에 approved → spec-coverage 가 두 스펙의
  INV 전부에 테스트를 요구(all-or-nothing), Supabase 없이 못 닫는 의무 발생. 게다가 spec 노드가 dirty 면
  하류 해시가 null 이 돼 완성된 슬라이스(content-safety)의 리뷰 사인오프조차 막힘.
- 교훈: 구현 가능한 단위로 쪼개 준비된 것부터 하나씩 approved. 합의됐으나 미준비인 스펙은 docs/specs/planned/
  에 draft 로 파킹(spec 글롭 비재귀라 안 막음), 준비 시 docs/specs/ 로 옮기고 approved.
- 근거: 1회 — 구조적 빈칸("합의됐으나 보류" 상태가 그래프에 없음). 반영 위치: .claude/skills/spec/SKILL.md
  "승인 단위·보류" 절 (③ skill). 미래: status: approved-deferred 정식 도입은 백로그(2회 관찰 시 승격).

## 2026-08-02 — 서브에이전트 tools 화이트리스트는 MCP 도구를 자동으로 물려주지 않는다

- 증상: style-scout 은 "Playwright MCP 연결 시에만 위임" 이 전제인 에이전트인데 tools 가
  `Read, Glob, Grep` 뿐이라, MCP 를 등록하고 재시작해도 브라우저를 못 엶. 정찰을 본체가 대신 수행 —
  에이전트가 존재만 하고 일을 못 하는데, 위임해보기 전에는 안 드러남.
- 교훈: 역할이 성립하는 데 필요한 도구를 tools 에 다 적는다(최소셋 ≠ 적을수록 좋음). MCP 는
  `mcp__<서버>__<도구>` 풀네임으로 명시해야 하고, 부모 세션 연결은 상속되지 않는다. description 에
  전제를 쓰는 것과 실제 권한을 주는 것은 별개 — 전제만 쓰면 빈 껍데기가 된다. 본문에 "도구가 안 보이면
  시도하지 말고 미연결로 보고" 를 넣어 실패를 조용하지 않게 만든다. tools 는 화이트리스트라
  금지 목록은 적을 필요가 없고, 허용 도구의 사용 범위는 본문 산문으로 좁힌다.
- 근거: 1회 — 구조적 빈칸(선언과 권한의 불일치). 반영 위치: .claude/skills/setup/SKILL.md 생성 판정 기준
  - .claude/agents/style-scout.md (③ skill+에이전트).

## 2026-08-02 — 시안은 대비를 실측해야 한다 (안 재면 미달인 채로 승인된다)

- 증상: 레퍼런스에서 가져온 메타색이 우리 카드 위에서 2.95:1, 이후 "더 흐리게" 요청을 반영하다
  태그 칩이 4.23:1. 둘 다 눈으로는 멀쩡해 보였고 본체가 computed style 로 훑어서야 드러남.
  절차에 검증 자리가 없어 안 쟀으면 그대로 승인될 뻔했다.
- 교훈: 시안 제출 전에 본체가 브라우저로 열어 전 요소 대비를 실측한다(drafter 보고를 신뢰하지 않는다).
  레퍼런스 색은 배경이 바뀌면 그대로 미달되고, "흐릿하게" 류 요청은 곧장 4.5:1 하한에 부딪힌다 —
  그때는 색이 아니라 굵기·부피 같은 다른 수단으로 푼다. 실측값은 design-rules.md 에 남겨
  다음 화면이 하한을 물려받게 한다. Playwright 는 file: 을 막으므로 http 로 서빙해야 잰다.
- 근거: 2회(메타색·태그칩) — 구조적 빈칸(검증 국면 부재). 반영 위치: .claude/rules/design-drafting.md
  시안 루프 3단계 + scripts/preview.mjs 신설 (③ rules+도구).

## 2026-08-02 — 게이트가 스택을 가정할 때는, 숨기지 말고 판정하고 분기한다

- 증상: design/BEFORE_UI 가 "화면 = src/pages·src/widgets" 가정을 코드에 숨긴 채 박아둠. Vite 만
  있을 땐 맞았지만 Next(App Router) 를 들이자 화면이 src/app 으로 옮겨가 **게이트가 조용히 통과**
  (막아야 할 걸 안 막는 상태). 고치면서 반대로 src/app 규칙을 전역에 걸 뻔했고, 그러면 Vite
  프로젝트가 근거 없는 예외("라우트 1장은 스켈레톤")를 뒤집어쓴다. 양쪽 다 에러가 안 나서 안 보인다.
- 교훈: 전역 게이트 + 스택마다 다른 사실(화면 위치·진입점·부트스트랩)이 만나는 자리가 가장 조용히
  깨진다. 스택별로 갈리는 규칙은 설정 파일 존재로 **먼저 판정하고 분기**한다(폴더 이름 추측 금지).
  분기 기본값은 "스택 전용 규칙 미적용" — 새 스택이 들어와도 잘못 막지는 않게. 그 대가로 생기는
  '못 막는 구멍'은 프로파일 문서의 게이트 함의에 반드시 적는다(안 적으면 다음 세션이 통과를
  안전으로 읽는다). 예외에는 어느 스택 사정인지 주석을 남긴다.
- 근거: 1회 — 구조적 빈칸(전역 판정 코드에 스택 가정이 암묵적으로 들어감). 새 스택이 추가될 때마다
  재발하는 종류. 반영 위치: docs/references/architectures/README.md 신설(정본+체크리스트)
  - .claude/skills/setup/SKILL.md 2곳 포인터 + nextjs-fsd.md 게이트 함의 4 (③ docs+skill).

## 2026-08-10 — green 은 변이로만 증명된다 (알리바이 테스트 4세션 연속)

- 증상: 통과하는 테스트가 아무것도 붙들고 있지 않은 경우가 네 세션 연속 나왔다. `safeSourceUrl` 의 정리
  로직을 통째로 지워도 통과 · 배선 테스트 0건(가드 호출을 지워도 전 스위트 green) · `width="0" height="300"`
  은 비율 가드가 대신 잡고 있어 정수 검사를 풀어도 통과 · `MAX_RATIO` 10→100 도 전부 통과(상수 미고정) ·
  `/<\/div><article/` 는 사이에 div 를 끼워도 통과 · `server-env.test.ts` 는 소스를 정규식으로 훑어 주석
  두 줄만 지워도 빨간불 · `/api/ingest` 인가 테스트 0건 · `source_excerpt` 는 부재만 확인 ·
  `toContain("원문 제목")` 은 자리를 고정 못 함. 게이트가 만든 것도 하나 있었다 — 몇 달 전 주석에 남은
  `INV-C5` 글자가 새 스펙의 불변식을 조용히 면제했다.
- 교훈: **red 를 봤다는 것과 그 테스트가 위반을 잡는다는 것은 다르다.** 불변식 테스트는 강제 지점을
  무력화해 빨간불을 본 뒤에야 green 으로 친다. 부재만 확인하는 단언은 절반이고, 문자열 매칭은 자리를
  고정하지 못하며, 소스를 정규식으로 훑는 것은 함수를 부르지 않으므로 테스트가 아니다. 변이를 만들 때
  타입 단언(`as never`)으로 바꾸는 건 동작이 같아 변이가 아니다.
- 근거: 4세션 연속(2026-08-06·08·09·10), 리뷰어 지적 3회 + 자체 발견 5회. tdd.md 에 "알리바이지 검증이
  아니다"라는 말이 **이미 있었는데도** 재발한 것이 반영 방식을 정했다 — 이름이 아니라 절차를 적었다.
- 반영 위치: .claude/rules/tdd.md "green 이 무엇을 증명하는가" (③ rules) +
  gates/spec-coverage.mjs 주석 제외 (② 게이트).

## 2026-08-10 — 유닛이 다 green 인 것과 동작하는 것은 다르다

- 증상: 유닛 250개가 전부 green 인 상태에서 실행 한 번이 잡은 것들 — 서버 키 둘을 한 스키마로 묶어
  `ANTHROPIC_API_KEY` 하나가 없을 때 수집 라우트 500(내가 만든 INV-S2 위반) · 요약 0건·태그 0건·본문 0건
  (더미가 "잘 되는 모습"만 보여주고 있었다) · "아래는 출처에서 가져온 원문입니다" 아래가 빈칸 ·
  재수집이 직전 주기에 추출한 본문을 덮어씀(고친 뒤 9→42건) · CSS 파일만 404 · explorer 결함 3건.
- 교훈: 그래프의 `qa` 는 오프라인 유닛이고 `review` 는 사인오프다 — **"한 번 돌려봤나"를 묻는 국면이
  어디에도 없었다.** 외부(네트워크·DB·모델)에 붙거나 화면에 나오는 기능은 리뷰 파견 전에 실제로 한 번
  돌리고, 사인오프 마커에 실행 근거 한 줄(명령 + 결과 숫자)을 남긴다. 안 돌렸으면 안 돌렸다고 적는다 —
  적을 게 없는 것과 안 적은 것은 다르다.
- 근거: 6회(2026-08-06·09·10). 게이트로는 못 올린다 — 실행은 정적 검사로 확인할 수 없다.
- 반영 위치: .claude/rules/tdd.md (paths 에 `projects/*/workspace/review.md` 추가해 마커를 쓰는 순간
  뜨게 함) (③ rules).

## 2026-08-10 — 마이그레이션 파일에는 다시 돌려도 같은 결과인 것만 담는다

- 증상: 요약 기준이 바뀌어 기존 요약을 비우는 `update public.item set summary = null` 을 `0003` 에 넣었다.
  `scripts/apply-migrations.mjs` 는 이름을 안 주면 폴더의 `.sql` 을 전부 다시 적용했다 — 다음에 `0004` 를
  추가하고 습관대로 돌리는 순간 요약 92건이 사라지고 전액 재과금될 상태였다. 리뷰어가 잡았다.
- 교훈: **"한 번만 도는 것"과 "여러 번 돌아도 되는 것"을 한 파일에 섞지 않는다.** 일회성 데이터 정정은
  파일에서 빼고 손으로 한 번 실행한 뒤 주석으로만 남긴다. 도구 기본값을 바꾸는 것만으로는 부족하다 —
  새 DB·리셋에서는 전부 적용이 실제로 필요하고, 그때 파괴적 문장이 있으면 똑같이 터진다.
  **파일이 다시 적용해도 안전한 것이 1차 방벽이고 도구 기본값은 2차다.**
- 근거: 1회. 구조적 빈칸 — 위험의 근원이 킷 도구의 기본값이었고, `rules/supabase.md` 의 `paths` 가
  `src/shared/api/**` 뿐이라 **마이그레이션을 쓰는 동안 그 규칙 파일이 아예 안 떴다**(RLS·키 규칙까지 같이).
  프로젝트가 넣은 방어 테스트는 `body0003` 한 파일만 봐서 다음 파일에는 안 붙는다.
- 반영 위치: .claude/rules/supabase.md 마이그레이션 절 + paths 확장 (③ rules) ·
  scripts/apply-migrations.mjs `--all` 명시 요구 (도구) · gates/run-gates.mjs SQL_UNCONDITIONAL_WRITE (② 게이트).

## 2026-08-10 — 항목마다 유료 호출이 붙는 기능은 설계 전에 숫자를 잰다

- 증상: 실측이 설계를 세 번 바꿨다. ① "본문을 번역해줘" → 원문 본문이 있는 항목이 11% 뿐이고 건당
  요약의 5~6배라, 이미 부르는 호출에 필드 하나를 더하는 제목 번역으로 교체(추가 비용 거의 0).
  ② 주제 밖 글 한 건이 13,099 토큰으로 나머지 평균 2,496 의 5배 — 품질 사안인 줄 알았던 "주제 밖 버리기"가
  비용에서도 제일 큰 한 방이었고, 그래서 판정을 요약 앞·제목만으로(약 270 토큰) 당겼다.
  ③ 초안대로면 번역만 하는 항목이 269→1,845 토큰(7배) — 코드에 이미 있던 조건별 조립 구조를 내 초안이
  무시하고 있었다.
- 교훈: 스펙 템플릿의 작성 전 체크는 위험 표면(결제·인증·시변)과 데이터 모델만 물었다 — **항목마다 외부
  유료 호출이 붙는 기능의 단가·적용 비율을 물을 자리가 없었다.** 셋을 잰다: 적용되는 항목의 비율 ·
  한 건 단가 · 제일 비싼 한 건. **평균만 보면 이 셋 중 아무것도 안 보인다.**
- 근거: 3회(2026-08-10, 두 세션). 구조적 빈칸 — 물어볼 국면 부재.
- 반영 위치: docs/references/spec-template.md 작성 전 체크 3번 (정본 — 안내 문서 축).

## 2026-08-15 — 강제력이 두 겹이면 하나는 덜어낸다

- 증상: 그래프가 "디자인으로 돌아가라"고 처방한 상태를 `design/BEFORE_UI` 게이트가 위반으로 봤다.
  푸는 유일한 길인 **사용자 승인**은 턴을 끝내야 도달하는데, 그 게이트가 턴을 끝내지 못하게 막았다.
  같은 모양이 스펙에서도 났다 — 승인 직후 `spec-coverage` 가 INV 9개의 테스트를 요구해 **wrap-up 조차
  못 하는 상태**가 됐다. 3회 관찰(08-06 design · 08-10·08-11 spec), 그중 2회는 회피했다.
- 교훈: **게이트가 막으려던 바로 그 일이 게이트 때문에 일어난다** — 값싼 회피가 "임의로 approved 로
  되돌리기"였다. 실제로 한 번은 "승인은 끝났는데 파일은 draft"로 남겨 문서가 사실과 달라졌다.
  원인은 강제력이 두 겹이라는 것이었다: ① 노드가 dirty 인 동안 하류가 못 간다 ② 게이트 실패면 턴이 안 끝난다.
  **완료 조건**("끝내기 전에 있어야 한다")은 ①로 충분해서 ②가 중복이고, **선행 조건**("시작하기 전에
  승인받아야 한다")은 ①이 파일 쓰기를 못 막으니 ②가 유일한 저지선이다. 성격을 갈라 완료형만 덜어냈다.
- 되돌아가기와 처음부터 안 한 것을 구분해야 했다. 처음 안은 "노드가 dirty 면 낮춘다"였는데, 새 프로젝트는
  design 이 **처음부터** dirty 라 시안도 승인도 없이 UI 를 다 만들어도 통과했다 — 그 게이트가 막으려던
  경우가 정확히 그것이다. 새 기록을 만들지 않고 **"지금 clean 인 노드를 mark 하면 rework"** 로 파생시켜
  풀었다. 판별 재료는 이미 상태에 있었고, 그래서 한 번도 승인 안 받은 노드는 rework 가 될 수 없다.
- 근거: 3회 관찰 + 회피 2회. 픽스처 6종으로 확인(승인 이력 없는 design 은 여전히 차단되는 것 포함).
- 반영 위치: graph.mjs `GATE_KIND` + gates/propagate.mjs `markRework` + gates/graph-stop.mjs 차단 판정
  (② 게이트 계열). 부수 효과로 "승인 즉시 approved 로 적는다"가 가능해져 .claude/skills/spec/SKILL.md 갱신.

## 2026-08-17 — 경고가 아는 것보다 많이 말하면 읽는 사람이 잘못 믿는다

- 증상: 두 가지가 같은 모양으로 났다. ① CLAUDE.md 는 "예외는 영구가 아니다 — 전제가 깨지면
  게이트가 만료시킨다"고 선언하는데, 실제 만료 검사는 표면 4개(auth·payment·authz·concurrency) 중
  authz 하나뿐이었고 그 근거도 SQL 의 `create policy` 뿐이었다. signal 의 authz 예외는 정책이 전부
  `for select` 라 만료가 안 걸리는데 실제 쓰기 경로는 DB 밖(secret 키 클라이언트)에 있어 만료 기계가
  구조적으로 못 봤다. ② basis 불일치 메시지가 "구현이 바뀌었으니 다시 봐야 한다"고 단정했는데, 해시는
  `src/**` 원문 바이트라 주석·공백 변경도 불일치를 만든다 — 2026-08-17 에 실제로 갈렸다(바뀐 건 예외
  주석 3줄, 핸들러는 그대로). diff 를 안 열었으면 리뷰어에게 틀린 전제를 넘길 뻔했다.
- 교훈: **경고 문장은 기계가 실제로 아는 만큼만 말해야 한다.** 만료가 걸린 예외와 안 걸린 예외를
  같은 문장으로 내보내면 읽는 사람이 "이건 지켜보고 있다"고 잘못 믿는다. 판정을 바꾸지 않고 문장만
  고쳐도 이 문제는 풀린다 — 대가 0인데 미룰 이유가 없다.
- 근거: 2건, 2026-08-17 signal review 사인오프 도중 발견. 구조적 빈칸 — 만료 검사가 표면 1개만
  커버한다는 사실과 해시가 무엇을 재는지가 경고 문구에 안 드러나 있었다.
- 반영 위치: gates/run-gates.mjs `EXPIRY_RULES` + EXEMPT 경고 문구 · gates/graph-stop.mjs basis
  불일치 문구 (② 게이트 계열). 검사: scripts/check-exempt-expiry.mjs · scripts/check-basis-message.mjs.

## 2026-08-17 — 보호 훅은 세션이 어디서 열렸는지에 따라 통째로 없을 수 있다

- 증상: 상위 폴더(`dev/`)에서 세션을 열고 킷을 작업하면 `gates/`·`graph.mjs`·`.claude/hooks/`
  전부 그냥 편집됐다. 훅이 잘못 쓰인 게 아니라 킷의 `.claude/settings.json` 자체가 안 읽힌 것이다 —
  훅 등록도 permissions 도 거기 있어 한꺼번에 없는 상태가 됐다. 2026-08-16 에 실제 피해로 드러났다:
  무방비 세션에서 나간 커밋 때문에 Stop 훅이 안 돌아 `review` 가 `status: passed` 로 남았고,
  리뷰받은 코드와 배포하려던 코드가 다른 상태로 deploy 직전까지 갔다 — 훅이 다시 도는 세션이
  되고서야 basis 불일치로 겨우 잡혔다.
- 교훈: **보호는 파일에 있다는 사실만으로 안 되고, 그 파일이 실제로 읽히는 세션에서만 작동한다.**
  Claude Code 는 세션을 연 그 폴더의 settings.json 만 읽고 상위로 안 거슬러 올라간다 — 그래서
  상위 폴더에서 세션이 열리면 하위 킷의 보호가 통째로 사라지는데 아무 신호도 없다(게이트 실패처럼
  소리가 나는 종류가 아니다). 상위 폴더에도 그 킷 이름만 겨냥한 보호 층을 따로 둬야 한다 — 사용자
  레벨 절대경로 deny는 킷을 복사해 다른 프로젝트로 옮기면 경로가 안 따라와서 이 킷의 성격과
  어긋난다.
- 근거: 2회 관찰(2026-08-15·08-16) + 실제 피해 1건(2026-08-16, deploy 직전까지 감).
- 반영 위치: dev/.claude/settings.json + dev/.claude/hooks/protect-kit.mjs 신설(킷 이름이 경로/명령에
  있을 때만 판정 — 다른 킷·무관한 프로젝트는 안 막음) (② 게이트/훅 계열). 검사: scripts/check-dev-kit-protection.mjs.

## 2026-08-17 — 문자열 대조 훅은 대상이 파일명 없이 지워지면 못 본다

- 증상: `protect-files.mjs` 는 보호 경로 문자열이 명령에 그대로 있어야 잡는다. `rm -r .claude/` 에는
  `.claude/hooks/`·`.claude/settings.json` 같은 하위 파일명이 안 보여서 안 걸렸다. 고치다가 더 큰 구멍도
  같이 드러났다 — `rm -r gates`(끝 슬래시 없이)도 안 걸렸다. 보호 목록에 `"gates/"` 처럼 슬래시가 박혀
  있어서, 슬래시 없이 쓰면 문자열 자체가 안 맞았다.
- 교훈: **경로를 문자열로 대조하는 방식은 그 경로가 명령에 안 보이는 형태(부모 디렉터리 통째 삭제,
  표기 방식 차이)에서 전부 뚫린다.** 명령을 토큰으로 갈라 실제 인자를 보호 목록과 대조해야 부모
  디렉터리 삭제·슬래시 유무 둘 다 잡힌다. `node_modules` 같은 무관한 재귀 삭제까지 막으면 정상
  작업이 매번 걸리므로, 넓히는 방향이 아니라 보호 목록의 조상 디렉터리인지만 정확히 겨냥했다.
- 근거: 1회 관찰(2026-08-15 발견 → 2026-08-17 패치), 검증 중 추가로 1건 더 발견(`gates` 슬래시 누락).
- 반영 위치: .claude/hooks/protect-files.mjs `rmRecursiveDirHit` (② 게이트/훅 계열). 검사:
  scripts/check-rm-dir-protection.mjs — 패치 전 4케이스가 exit 0(안 막힘)이었고 패치 후 전부
  exit 2로 바뀐 것을 실측.

## 2026-08-31 — 게이트가 한 줄만 읽는데 파일에는 여러 줄이 있을 수 있다

- 증상: 두 가지가 같은 모양으로 났다. ① 프론트매터에 `surfaces:` 를 두 줄 두자 게이트는 위의
  빈 선언만 읽고 아래의 `[concurrency]` 를 버렸다. 같은 파일의 `status:` 는 반대로 **한 줄이라도**
  approved 면 승인으로 읽는다(`.test()` 라 순서 무관) — 승인 안 한 스펙이 승인으로 읽히는 방향이다.
  두 키가 서로 반대 규칙인데 그게 선언된 적이 없고, 어느 줄을 읽었는지도 안 찍힌다.
  ② 다른 프로젝트의 `workspace/review.md`(`status: passed` · basis · reviewers)가 파일 복사로
  따라 들어왔다. 사인오프는 "이 프로젝트의 이 코드를 봤다"는 기록인데 파일 안에 프로젝트가 없다.
- 교훈: **판정 근거를 한 줄에서 뽑는 검사는, 그 줄이 하나뿐이라는 것을 스스로 확인해야 한다.**
  중복을 만난 자리에서 조용히 하나를 고르면 사람은 자기가 쓴 줄이 읽혔다고 믿는다. 그리고
  **기록이 무엇에 붙는 것인지는 기록 안에 적혀 있어야 한다** — 안 적혀 있으면 복사해 온 것과
  제 손으로 쓴 것이 구별되지 않는다. 승인 자체를 기계가 볼 방법은 여전히 없지만, 귀속은 볼 수 있다.
- 근거: 각 1회(2026-08-31 signal2 개설 세션). 앞의 것은 백로그 "포매터가 status 를 접어 게이트를
  무력화한다"(2026-08-06)와 원인이 같아 계열 2번째다.
- 반영 위치: gates/run-gates.mjs `frontmatter/DUPLICATE_KEY` · `signoff/FOREIGN_MARKER` (② 게이트 계열).
  검사: scripts/check-frontmatter-guards.mjs — 임시 킷에서 실제로 게이트를 돌린다(붙기 전 2/5, 후 5/5).

## 2026-09-02 — v3.2: 산문을 기계가 긁으면 숨은 스키마가 된다

- 증상: 스펙의 `status` 를 frontmatter 에서 빼 `DECISIONS.md` 의 결정 줄
  (`- YYYY-MM-DD approved: <slug> — 근거`)로 옮기고 게이트가 그걸 파싱하게 하려 했다.
  목적은 "docs 폴더만 넘기면 이전이 끝난다"였고 frontmatter 를 비우는 것이 그 수단으로 보였다.
- 교훈: **형식을 문서에 등재해도 산문 속 파싱은 frontmatter 파싱보다 오독 표면이 넓다.**
  이 레포는 이미 같은 계열로 두 번 다쳤다 — `\b` 가 하이픈을 단어 경계로 봐 `approved-…` 를
  approved 로 읽은 것(2026-08-02), 중복 키로 어느 줄을 읽었는지 모르게 된 것(2026-08-31).
  결정 줄 형식에서도 "근거 한 줄"에 다른 slug 나 `parked:` 라는 낱말이 들어가면 같은 종류의
  오독이 난다. **그리고 목적을 다시 보면 frontmatter 를 비울 필요가 없었다** — 이전이 성립하려면
  값이 사라져야 하는 게 아니라 **값의 뜻이 문서로 전달되면 된다.** 계약 문서 한 장이 그 일을 한다.
- 근거: 설계 검토 1회(2026-09-02). 실행하지 않고 접었다.
- 반영 위치: docs/references/docs-contract.md(규약 목록 + "미등재 파싱 추가 금지") ·
  gates/run-gates.mjs `docs-boundary/FRONTMATTER_FIELD`(필드 화이트리스트). ② 게이트 계열.

## 2026-09-02 — v3.2: 검사가 안 돈 것과 검사가 통과한 것은 겉이 같다

- 증상: 두 번 밟았다. ① 위험 표면 검사를 확인하려고 패턴을 `src/shared/lib/policy.sql` 에 심었는데
  게이트가 그 파일을 스캔 대상에 안 넣어 "0개 파일"로 통과했고, 검사는 그것을 **"방벽이 열렸다"**
  로 읽었다 — 하마터면 "현행 게이트에 구멍 두 개"라고 보고할 뻔했다. ② 새 경계 검사가 현행 문서를
  통과하는지 보는 항목을 게이트 출력만 보게 짰는데, 그 검사가 아직 없는 상태에서 무조건 통과했다.
- 교훈: **"위반 0건"이라는 보고와 "검사가 아예 안 돌았다"는 겉으로 구별되지 않는다.**
  그래서 검사에는 **위반을 일부러 심어 잡히는 것을 보는 항목**이 있어야 한다. 심은 것이 잡히고
  지우면 다시 통과하는 것까지 봐야 그 검사가 살아 있다는 근거가 된다. 이건 게이트뿐 아니라
  패치 동봉 검사·리허설 전부에 해당한다.
- 근거: 1회 세션에 2회(2026-09-02 v3.2). 구조적 빈칸 — 검사를 쓰는 국면에 물을 자리가 없었다.
- 반영 위치: CLAUDE.md 보호 파일 조항("적용 전 실패·적용 후 통과 검사")에 한 문장 (④ 상시).
  실물: scripts/check-read-spec.mjs 의 `S` · check-docs-boundary.mjs 의 `S1`·`S2`.

## 2026-09-02 — v3.2: 파생값을 버릴지는 재계산의 실패 방향으로 정한다

- 증상: 스펙의 `surfaces`(그 스펙이 커버하는 위험 표면)를 "재계산 가능한 파생값"이라며 버려도 되는
  폴더로 보내려 했다. 판별 기준("하네스를 바꿔도 표기만 바꿔 살아남나")은 맞게 썼는데
  **재계산이 어느 방향으로 틀리는지를 안 따졌다.**
- 교훈: `status` 는 오독되면 draft 로 떨어져 게이트가 더 엄격해진다(안전). `surfaces` 는 방향이
  둘이다 — 표면을 빠뜨리면 게이트가 막아 드러나지만(안전), **과잉 표기하면 방벽이 조용히 열린다**
  (위험). 그래서 `surfaces` 는 사람이 비준한 판단이고 잃으면 안 되는 기록이었다.
  → **파생값이라는 이유만으로 버리지 않는다. "다시 만들다 틀리면 어느 쪽으로 틀리는가"를 같이 본다.**
  안전한 쪽으로 틀리면 버려도 되고, 위험한 쪽으로 틀릴 수 있으면 남긴다.
- 근거: 1회(2026-09-02). 판정을 뒤집었고 그 근거로 게이트 소스와 사고 이력을 대조했다 —
  **설계 결정은 문서만 보고 확정하지 말고 집행 코드까지 대조한 뒤 확정한다**는 것도 같이 배웠다.
- 반영 위치: docs/references/docs-contract.md 6절(판별 질문 뒤에 붙는 두 번째 질문). ③ 조건부 문서.

## 2026-09-02 — v3.2: 검사의 그물은 각도가 다른 것으로 둘

- 증상: 문자열로 하네스 참조를 찾는 검사(검사 b)가 통과한 뒤, 경로 참조를 찾는 이전 리허설(R3)이
  같은 문서에서 킷 스크립트 의존 3건을 더 잡았다. 그걸 반영해 검사 b 를 넓혔더니 이번엔 R3 가
  못 보던 것(경로 없이 파일 이름만 쓴 2건)을 검사 b 가 잡았다.
- 교훈: **그물이 하나면 자기가 무엇을 놓치는지 알 방법이 없다.** 각도가 다른 검사 둘이 서로의
  구멍을 보여준다. 다만 이건 **규칙으로 박지 않는다** — 하나로 충분한 경우까지 강제하면
  과적합이고, 몇 개를 둘지는 그때의 판단이다. 기록만 남긴다.
- 근거: 1회(2026-09-02).
- 반영 위치: 없음(의도적). 기록만.

## 2026-09-02 — v3.2: 커밋 안 된 작업 트리 위에서 새 작업을 시작하지 않는다

- 증상: v3.2 착수 직전 작업 트리에 미커밋 192개가 있었다. 활성 프로젝트 전체가 아직 추적 밖이라
  `git mv` 가 불가능했고, `gates/run-gates.mjs`·`spec-coverage.mjs` 에는 **이번 작업이 고칠 바로
  그 파일에** 남의 미커밋 변경이 얹혀 있었다. 세션 시작 브리핑은 이것을 한 줄도 알리지 않았다.
- 교훈: **기준선이 없으면 내 diff 가 남의 미완 작업과 한 덩어리가 된다.** 단계별 커밋도, "이번
  패치가 무엇을 바꿨나"를 diff 로 보는 것도 성립하지 않는다. 특히 판정 레이어(gates·graph.mjs·
  훅·설정)가 미커밋인 상태에서 낸 패치는 **적용 전/후를 비교할 기준 자체가 없다.**
- 근거: 1회(2026-09-02). 구조적 빈칸 — 브리핑이 그래프·게이트·백로그는 읽으면서 git 상태는 안 봤다.
- 반영 위치: scripts/briefing.mjs — 판정 레이어가 미커밋이면 건수와 무관하게, 그 밖이면 30건
  이상일 때 한 줄. (② 층, 0토큰·매 세션 자동)

## 2026-09-02 — v3.2 정의

**이 버전부터 프로젝트 이전은 `projects/<이름>/docs/` 복사 + `docs/references/docs-contract.md`
전달로 성립한다.** `workspace/`(HANDOFF·PROGRESS·review·deploy)는 통째로 버려도 프로젝트 지식이
소실되지 않는다. 리허설로 확인한다: `node scripts/check-handover-rehearsal.mjs`.

## 2026-09-03 — v3.3: 게이트가 막으려던 일이 게이트 때문에 일어난다 (4번째)

- 증상: 보류 항목을 게이트 실패로 내게 했더니 **모든 편집이 막혔다.** PostToolUse 훅이 편집마다
  `run-gates --quick` 을 돌리고 exit 2 면 편집을 막는데, 보류는 저절로 사라지는 종류가 아니라서
  한 건만 열려 있어도 편집이 영구히 막힌다. **보류를 닫아서 푸는 것조차 편집이라 같이 막혔다.**
  같은 세션에서 한 번 더 났다 — 패치 셋 중 하나만 빠진 상태에서 Stop 훅이 매 턴 차단했고,
  푸는 유일한 길은 보호 파일 편집이라 모델이 할 수 없었다. 열 턴 넘게 같은 메시지만 오갔다.
- 교훈: **새 신고를 게이트에 넣을 때는 그 게이트가 도는 자리를 전부 세어 본다.** 이 레포의
  게이트는 두 자리에서 돈다 — 편집마다(`--quick`)와 턴 끝(전체). 전자는 "지금 고친 코드가
  규칙을 어겼나"를 보고, 후자는 "지금 상태가 다음으로 갈 수 있나"를 본다. 성격이 다르다.
  **저절로 사라지지 않는 조건은 편집 훅에 넣으면 안 된다** — 그것을 없애는 행동이 편집이면
  빠져나올 길이 없어진다. 2026-08-06·08-10·08-11 에 이어 4번째다.
- 근거: 2026-09-03 세션에 2회. 조합표가 "턴이 막힌다"까지는 예측했지만 그 상태에서
  어떻게 나오는지를 안 적어서, 실제로 막혔을 때 세션이 반복에 갇혔다.
- 반영 위치: gates/run-gates.mjs 의 보류 블록을 `if (!QUICK)` 로 감쌈 ·
  scripts/check-pending-kind.mjs 의 A4(`--quick` 에서 신고가 없는지) ·
  다중 파일 패치 문서에 「막혔을 때 빠져나오는 길」 절 (② 게이트 계열 + ③ 조건부 문서).

## 2026-09-03 — v3.3: 검사는 그 코드가 실제로 도는 자리에서 돌려 본다

- 증상: B단계 dry run 이 `graph-stop` 과 `run-gates` **단독 실행**만 봤다. 편집 훅이 도는
  `--quick` 경로를 안 밟아서, 패치가 모든 편집을 막는다는 것을 붙인 뒤에야 알았다.
  dry run 자체는 결함 셋을 잡아낸 좋은 검사였는데도 그랬다.
- 교훈: **같은 코드라도 도는 자리가 다르면 다르게 실패한다.** 검사를 짤 때 "이 코드가
  어디서 불리는가"를 세는 것이 "이 코드가 무엇을 하는가"를 보는 것보다 먼저다.
  훅 설정(`.claude/settings.json`)이 그 목록이다.
- 근거: 1회(2026-09-03). 구조적 빈칸 — dry run 을 설계하는 국면에 호출 자리를 세는 자리가 없었다.
- 반영 위치: scripts/check-pending-kind.mjs 의 A3·A4 를 같은 코드의 반대 방향으로 둠
  (A3 만 있으면 "신고는 되는데 아무것도 못 고치는" 상태가 통과한다) (② 게이트 계열).

## 2026-09-03 — v3.3 정의

**이 버전부터 auto-decide 등급 결정은 실행을 멈추지 않으며, 사람의 판단은 provisional 규칙으로
축적된다.** 되돌리기 싼 결정은 정책 규칙을 근거로 스스로 내리고 로그에 남기며, 되돌리기 비싼
결정은 보류로 올라가되 실행은 계속된다. 같은 판단이 두 번 반복되면 규칙이 되고, 사용자가
뒤집지 않으면 확정된다. 사이클은 사용자 지시 하나이고 계획 승인에서 열려 종료 조건에서 닫힌다.
**세는 것은 기계가 하고 규칙 문장을 적는 것은 사이클 마감 절차가 한다** — 훅은 판단이 필요한
쓰기를 하지 않고, 처리 안 된 항목이 대장에 남으면 게이트가 실패한다(2026-09-04 검증에서 확정).

## 2026-09-04 — v3.3 검증: 판정이 옳은가와 그 판정이 불리는가는 다른 질문이다

- 승격 판정과 사이클 종료 판정이 검사 스크립트 안에만 있었다. 검사는 자기 사본을 검사했고,
  게이트도 훅도 그 판정을 부르지 않았다. 그래도 검사는 초록불이었고 리포트는 "승격 0건"이라고
  적었다 — **"세어 보니 0건"과 "아무도 안 셌다"는 겉이 같다.**
- 종료 조건의 상한도 같은 모양이었다. 문서에 `상한(N)` 이라고 적혀 있고 N 에 숫자가 없었고,
  숫자 5 는 다른 파일 산문에만 있었고, 훅은 상한을 아예 안 봤다.
- 교훈: **검사에 항목을 두 종류로 나눠 둔다.** 하나는 판정 함수를 픽스처로 부르는 항목,
  다른 하나는 실제 진입점(게이트·훅)을 돌려 결과가 나오는지 보는 항목. 앞엣것만 있으면
  배선이 통째로 빠져도 통과한다.
- 근거: 2회(2026-09-03 편집 훅 경로 누락, 2026-09-04 판정 미배선). 구조적 빈칸.
- 반영 위치: `docs/references/policy/check-where-it-runs.md` 로 승격(provisional).
  `scripts/check-cycle-policy.mjs` 의 S1/C(판정)와 B/K(배선)가 그 구조다 (② 게이트 계열).

## 2026-09-04 — 파싱조차 안 되는 훅은 '할 일이 없던 훅'과 겉이 같다

- 보호 파일 패치를 손으로 붙이는 과정에서 `graph-stop.mjs` 의 닫는 `});` 한 줄이 빠졌다.
  파일 전체가 SyntaxError 라 Stop 훅이 매 턴 죽었다 — HANDOFF 갱신도 차단 판정도 통째로
  빠진 상태였는데, 화면에는 아무 줄도 안 나왔다.
- 그 상태를 잡으라고 만든 검사(`check-cycle-policy` 의 K)는 "게이트에 배선이 안 됐다"고만
  말했다. **원인이 다른 두 상태가 같은 실패 메시지로 수렴하면 검사가 있어도 못 고친다.**
- 교훈: 훅이 '막을 줄 아는가'·'등록됐는가' 위에 **'파싱은 되는가'** 를 얹는다. 앞의 둘이
  통과해도 파일이 안 읽히면 아무 일도 안 일어난다.
- 근거: 1회(2026-09-04). 구조적 빈칸 — 킷 자신의 진입점이 살아 있는지 보는 자리가 없었다.
- 반영 위치: `scripts/check-hooks.mjs` ③ 파싱 검사(설정에서 대상을 뽑고, 깨진 파일을 심어
  잡히는 것까지 본다). 패치 문서의 "붙은 뒤 확인" 맨 앞에 `node --check` 두 줄 (② 게이트 계열).

## 2026-09-04 — `git add -A` 는 사용자가 지금 붙이는 중인 보호 파일을 쓸어담는다

- 보호 파일은 사용자가 직접 적용한다. 그런데 그 적용이 진행되는 동안 내가 `git add -A` 로
  커밋했더니, 절반만 붙은(문법이 깨진) `graph-stop.mjs` 가 그대로 커밋에 들어갔다.
- 교훈: 규칙을 하나 더 만들지 않는다. **커밋 전에 검사를 돌리면 이 계열은 저절로 막힌다** —
  깨진 파일은 검사가 잡고, 잡히면 커밋을 안 하게 된다. 위 항목의 파싱 검사가 그 자리다.
- 근거: 1회(2026-09-04, 커밋 5f3ad13). 판단의 실수에 가깝지만 재발 조건이 도메인 무관하다 —
  보호 파일 패치를 넘긴 턴에는 언제나 성립한다.
- 반영 위치: 규칙 아님. 위 파싱 검사로 흡수 (② 게이트 계열).

## 2026-09-04 — 실제 파일을 심었다 되돌리는 프로브는 끊기면 손상을 남기고, 그 손상을 스스로 보존한다

- 두 벌 동기화 검사에 "게이트가 이 판정을 부르는가"를 보는 배선 프로브를 넣으면서,
  진짜 `AGENTS.md` 에 한 줄 심고 게이트를 돌린 뒤 `finally` 에서 되돌리게 짰다.
  게이트가 두 번 도는 15초 동안 창을 닫거나 Ctrl+C 를 누르면 되돌리기가 안 돈다.
- **더 나쁜 것은 그다음이다.** 다음 실행이 더럽혀진 파일을 '원본'으로 읽어서, 심고 되돌린 뒤
  "복원=true" 라고 보고하면서 손상을 그대로 보존한다. 사용자에게는 붙이기 전에도 붙은 뒤에도
  4/6 이 나왔고, 패치가 안 붙은 것처럼 보였다 — 실제로 깨진 것은 검사 쪽이었다.
- 교훈: **프로브는 임시 레포에서 돌린다.** 되돌리기로 안전을 보장하려 하지 말고, 되돌릴
  것이 없게 만든다. `run-gates` 는 `ROOT` 를 `cwd` 로 잡으므로 `cwd` 만 픽스처로 주면 된다
  (`projects/<이름>/src` 를 하나 넣어야 게이트가 일찍 안 끝난다).
- 근거: 1회(2026-09-04). 구조적 빈칸 — 이 파일은 스스로 "프로브는 전부 임시 디렉터리에서
  돈다"고 적어 뒀는데, 새로 넣은 항목이 그 불변식을 깼고 아무도 대조하지 않았다.
- 반영 위치: `scripts/check-mirror-sync.mjs` 의 D 를 픽스처 기반으로 재작성.
  실행 시간도 15초 → 0.3초가 됐다 (② 게이트 계열).

## 2026-09-04 — 게이트가 막으려던 일이 게이트 때문에 일어난다 (5번째) — 신고 범위와 판정 범위가 다르면

- 증상: 프로젝트 하나를 문서로 들여왔더니 **다른 프로젝트의 모든 턴이 막혔다.** 들여온
  프로젝트의 보류 2건이 원인이었는데, 보류는 원래 턴을 막지 않는 종류다.
- 원인은 두 코드가 범위를 다르게 본다는 것이었다. `run-gates` 는 `PENDING.md` 가 있는
  **모든 프로젝트**의 보류를 신고하고, `graph-stop` 의 파서는 **활성 프로젝트 경로가 아닌
  줄을 건너뛴다**(그 자체로는 옳다 — 다른 프로젝트 에러가 이 그래프의 판정에 섞이면 안 된다).
  그래서 "실패는 있는데 파싱 0건"이 되고, 차단 판정이 "못 파싱했으면 막는다"로 떨어졌다.
  **턴을 안 막게 해 주는 낮춤 규칙은 파싱된 카테고리에만 적용되므로 닿지 못했다.**
- 교훈: **신고하는 쪽과 판정하는 쪽의 범위가 다르면, 그 사이에 낀 것은 판정을 못 받는다.**
  판정을 못 받은 것이 "모르면 막는 쪽"으로 떨어지는 설계라면 그 순간 빠져나올 길이 없어진다.
  낮춤 규칙 같은 안전장치를 만들 때는 **그 규칙이 닿지 못하는 입력이 있는지**를 같이 본다.
  범위가 다른 것 자체는 고칠 필요가 없었다 — 판정하는 쪽에 "다른 범위의 것"이라는 자리를
  하나 만들어 주면 됐다. 2026-08-06·08-10·08-11·09-03 에 이어 5번째다.
- 검사를 짤 때 한 번 헛짚었다: 첫 픽스처가 활성 프로젝트에도 산출물 폴더를 둬서 그쪽에서
  에러가 났고, 그러면 파싱 결과가 0건이 아니라 **버그가 재현되지 않는다.** 검사는 통과했고
  결함은 그대로 있었다. **재현 조건을 정확히 짚어야 그 검사가 무언가를 본다.**
- 근거: 1회(2026-09-04, study-mate 들여오기 직후). 실패 줄 둘이 전부 다른 프로젝트 것이었다.
- 반영 위치: `gates/graph-stop.mjs` 의 `parseForeignErrors` + 차단 판정 두 자리(사용자 적용) ·
  `scripts/check-foreign-gate-errors.mjs` (A~D) (② 게이트 계열).

## 2026-09-04 — 저장소에 프로젝트가 둘이 되자 방벽 하나가 이미 열려 있었다

- 증상: 새 프로젝트의 인증 스펙을 `approved` 로 올렸다. 불변식이 넷 있고 테스트는 하나도
  없었다. **그런데 추적성 게이트가 통과했다.**
- 원인: 게이트가 `projects/*/src` 와 `projects/*/tests` 의 테스트를 **한 덩어리 문자열로 합친 뒤**
  INV id 가 그 안에 있는지 본다. **INV id 는 스펙 파일 안에서만 고유하다** — 프로젝트가 다르면
  같은 id(`INV-A1` …)를 쓰는 것이 정상이다. 다른 프로젝트의 `INV-A1`~`A4` 테스트가
  새 프로젝트의 동명 불변식을 전부 덮었다.
- 교훈: **식별자의 유효 범위와 판정의 범위가 다르면 판정이 조용히 헐거워진다.** 이름으로 찾는
  판정을 짤 때는 "이 이름이 어디까지 고유한가"를 먼저 정한다. 여기서는 스펙 파일 안까지였는데
  판정은 저장소 전체를 봤다.
- **프로젝트가 하나뿐일 때는 드러날 수 없는 종류다.** 이 저장소는 v3.2 에서 "이전 = docs 복사"를
  목표로 삼으면서 프로젝트가 여럿인 것을 전제로 만들어졌는데, 실제로 둘이 된 적이 없어서
  이런 판정들이 한 번도 시험되지 않았다. 같은 날 같은 계열로 셋을 더 찾았다 — 스캐폴딩이 만드는
  스펙 템플릿이 경계 검사에 걸리는 것, 들여오기 판정이 국면을 못 가르는 것, 사이클 검사가
  "후보 0건"을 실패로 세는 것.
- 근거: 1회(2026-09-04). 실측 — 새 프로젝트의 `INV-A1` 을 참조하는 테스트가 다른 프로젝트
  파일 하나뿐이었다.
- 반영 위치: `gates/spec-coverage.mjs` 의 프로젝트별 대조(사용자 적용) ·
  `scripts/check-inv-scope.mjs` (A~C) (② 게이트 계열).

## 2026-09-04 — 여러 줄을 바꾸는 패치는 남길 줄을 문맥으로 같이 보여 준다 (6번째)

- 증상: 위 패치를 붙이는 중에 **바로 위의 정의 둘이 함께 지워졌다.** 게이트가 실행 중에 죽었고
  (`ReferenceError`), 그 순간부터 **모든 턴이 막혔다.** 푸는 유일한 길이 보호 파일 편집이라
  모델은 할 수 없고, 같은 차단 메시지가 스무 턴 넘게 오갔다.
- 원인은 패치 문서의 모양이다. "찾을 것"을 바꿀 블록의 첫 줄부터 적었는데, 그 위에 붙어 있던
  `const testFiles = …` 와 `stripComments` 가 시각적으로 한 덩어리라 같이 선택됐다.
- 교훈: **여러 줄을 바꾸는 자리는 "찾을 것"에 바로 위·아래 한 줄씩을 문맥으로 같이 보여 주고,
  그 줄들은 남긴다고 명시한다.** 그리고 조합표의 「막혔을 때 빠져나오는 길」에 **"반쯤 붙어서
  게이트가 죽으면 모든 턴이 막힌다"**를 적는다 — 되돌리는 명령만 적어 두는 것으로는 부족하다.
  게이트가 막으려던 일이 게이트 때문에 일어나는 계열의 6번째다
  (2026-08-06 · 08-10 · 08-11 · 09-03 · 09-04).
- 근거: 1회(2026-09-04). 스무 턴 넘게 같은 차단 메시지만 오갔다.
- 반영 위치: `docs/references/pending-patches/2026-09-04-inv-scope.md` 머리말에 실제로 밟은
  함정과 다음 문서에서 고칠 것을 적었다 (③ 조건부 문서).

## 2026-09-06 — 변이가 갈래를 묶으면 「잡혔다」가 거짓이 된다

- **변이 하나는 강제 장치 하나만 무력화한다.** 갈래 여럿을 한 변이로 묶어 빼면 그중 하나만
  붙들려 있어도 빨간불이 나고 판정은 "잡혔다"가 된다 — 나머지는 아무도 안 붙드는데 숫자는
  만점이다. 프로필 가시성의 ②③ 을 함께 빼는 변이 하나였고, ③ 을 붙드는 검사가 0개인데
  47/47 전부 초록이었다. 이름표도 갈래마다 따로 단다(같으면 어느 갈래가 깨졌는지 못 가른다).
  → 규칙: `.claude/rules/tdd.md` 「green 이 무엇을 증명하는가」
- **성능·정책 측정의 준비물은 「참이 되는 경로」가 아니라 「끝까지 가는 경로」로 만든다.**
  인덱스 효과를 재려고 만든 프로필 400개가 전부 호스트라 첫 갈래에서 단락됐고, 12ms → 11ms
  로 "차이 없음"이 나왔다. 어느 갈래에도 안 걸리는 프로필로 다시 재니 1103ms → 9ms 였다.
- **`explain` 은 `security definer` 함수 안을 안 보여 준다.** 플래너가 인라인하지 않으므로
  별도 계획이고, 바깥 계획에는 함수 호출 한 줄만 나온다. 그래서 「인덱스가 안 쓰인다」가
  계획으로는 안 보이고 시간으로만 보인다. 부분 인덱스도 이 자리에서는 안 쓰인다 — 부분
  조건이 불투명한 함수 안에 있으면 플래너가 그것을 추론하지 못한다.
- **경로를 문자열로 다루면 표기가 바뀌는 날 조용히 어긋난다.** 같은 뿌리에서 세 번 나왔다 —
  보호 판정이 `gates/` 를 부분 문자열로 찾아 패치 폴더 이름(`…-retro-gates/`)이 걸렸고,
  훅 파일명 뽑기가 `split("/").pop()` 이라 따옴표 친 절대 경로에서 깨졌고(훅 셋이 멀쩡히
  도는데 "등록 안 됨"으로 보고했다), 2026-08-16 에는 백슬래시 경로를 못 맞춰 보호 5개 중
  4개가 죽어 있었다. 판정 전에 정규화하고 **경계**를 본다(부분 문자열이 아니라 경로 조각).
- **시나리오 번호가 겹치면 커버 확인이 두 계약을 뭉갠다.** 한쪽 검사를 지워도 "그 번호 검사가
  있다"는 결과가 그대로 나온다. 기계는 `INV-` 만 세므로 끝까지 모른다 — 리뷰어 둘이 독립적으로
  지적해서 잡혔다. → `docs/references/docs-contract.md` 2절에 등재, 게이트는 이 패치에 있다.
- **검사가 틀린 방향으로 시끄러운 것도 판정을 막는다.** 조용히 꺼진 것보다는 낫지만, "훅이
  안 돈다"는 오보를 믿으면 멀쩡한 배선을 뜯게 된다. 검사 자신이 무엇을 파싱하는지 바뀌면
  그 검사의 프로브도 같이 바뀌어야 한다.

---

## 2026-09-15 — 검사 자신이 상대를 굶기면 상대가 고장 난 것처럼 보인다

- **같은 프로세스에서 서버를 띄워 놓고 `spawnSync` 로 자식을 부르면 그 서버는 대답하지 못한다.**
  이벤트 루프가 자식이 끝날 때까지 멈춰 있어서다. probe 계약 테스트가 이 모양이었고, 결과는
  열일곱 항목 중 열이 "페이지를 열지 못했다"로 실패하고 일곱은 **빈손으로 통과**였다 —
  페이지가 안 떴으니 "못 한 동작"도 없어서 그 항목이 초록으로 나온 것이다.
  자식을 부르는 동안 이쪽이 살아 있어야 하면 `spawn` + `await` 로 부른다.
- **없는 것을 확인하는 항목은 빈손 통과를 같이 막아야 한다.** "실패 표시가 없다"만 보면
  아무 일도 안 일어난 실행이 가장 깨끗해 보인다. 무엇이 실제로 일어났다는 증거(위 사례에서는
  `HTTP 200`)를 같은 항목에 넣는다.
- **관측 도구의 종료 코드는 판정이 아니다.** `scripts/probe.mjs` 는 콘솔 에러·404·예외를
  잔뜩 찍고도 0 으로 끝난다. 0이 아니게 만들면 부르는 자리가 그것을 게이트로 쓰기 시작하고,
  그러면 "수시로 가볍게 확인하는 도구"가 통과 의례가 된다.
- **심은 위반이 "다른 이유로" 잡히면 그 항목은 아무것도 증명하지 않는다.** 콘솔 인자를 풀어
  적는지 보려고 객체를 심었는데, Chromium 이 만드는 미리보기가 얕게나마 그 값을 보여 줘서
  인자를 안 푸는 코드로도 통과했다(`m.text()` 실측: `"{code: OBJ-CODE, deep: Object}"`).
  한 겹 안쪽 값으로 바꾸니 그제야 잡혔다. **심은 뒤에 빼 보고 빨간불이 나는 것까지 봐야
  심은 것이다.**

---

## 2026-09-15 — 등록부에 이름이 오른 것과 그 검사가 도는 것은 다르다

- **"어느 검사가 실제로 도는가"는 파일을 고친 상태로 게이트를 돌려 세면 된다.** 대상 파일에
  빈 줄 하나를 덧붙이고 `node scripts/check-registry.mjs --changed` 의 마지막 줄을 본다.
  `바뀐 파일로 돌린 on-change N건: <이름>` 이 그 턴에 실제로 돈 목록이다. 확인이 끝나면
  `git checkout -- <파일>` 로 되돌린다. 코드를 읽어서 추론하는 것보다 빠르고, 무엇보다
  **틀릴 수가 없다.**
- **그렇게 재 보니 `check-report.mjs` 는 한 번도 안 돌고 있었다.** `@check-role: standing` 으로
  선언돼 있고 등록부도 통과했는데, 호출처로 인정된 것은 `SKILL.md` 안에 그 파일 이름이
  적혀 있다는 사실뿐이었다. 문서에 이름이 적힌 것과 기계가 부르는 것은 다르다 — 등록부가
  스킬 트리를 호출처로 세는 한, 스킬 문서에 이름만 적으면 `standing` 선언이 공짜로 통과한다.
  (같은 파일 주석에 "문서는 여기 없다"고 적혀 있었는데 스킬 트리가 그 예외였다)
- **미루기는 손대기 전까지만 유효하게 만든다.** `pending` 으로 표시한 부품을 고치는 턴에는
  게이트가 막고 확정을 요구한다. 안 그러면 "곧 만든다"는 문장을 단 채로 그 부품이 계속
  바뀌고, 계약 테스트는 영영 안 생긴다.
- **git 은 통째로 새로 생긴 디렉터리를 한 줄로 접어서 준다** (`?? .claude/skills/새스킬/`).
  바뀐 파일 목록을 그대로 비교하면 **새로 추가된 것이 영영 안 걸린다** — 정작 확인이 제일
  필요한 순간이 그때다. 끝이 `/` 인 줄은 접힌 디렉터리로 보고 펴서 비교한다.

---

## 2026-09-16 — 규칙이 문서에 있다는 것과 그 규칙이 지켜진다는 것은 다르다

- **분기 규칙은 검사로 만든다.** `CLAUDE.md` 에 「만들 물건이 이미 동작하는 코드로 존재하면
  kickoff 이 아니라 intake 다」가 적혀 있었는데, 그 분기를 묻는 장치가 없어서 좋아요 스펙을
  없는 표 이름으로 새로 썼다. 다섯 불변식이 전부 초기 스키마에 이미 있었고, 「개수를 세는가
  쌓는가」라는 결정도 그 파일 주석이 같은 근거로 이미 내려놓은 것이었다.
  **문서에만 있는 분기는 지나가도 아무 데도 안 남는다** — 지켜졌는지 아무도 셀 수 없다.
- **스펙을 쓰기 전에 그 스펙의 고유명사로 레포를 먼저 검색한다.** 표·열·함수·타입 이름이면
  된다. 있으면 쓰던 것을 읽어서 고정하는 일이고(intake), 없으면 새로 만드는 일이다(kickoff).
  검색 결과를 **있음/없음으로 기록**하면 「안 찾아봤다」와 「찾아봤는데 없었다」가 구별된다.
- **구현이 있다는 것과 그것이 계약이라는 것은 다르다.** 좋아요의 제약·정책·계산 함수는 다
  있었는데 그것을 확인하는 테스트가 하나도 없었다. 검사가 없으면 다음 사람이 「정렬이 느리다」는
  이유로 파생 열을 더해도 아무것도 빨갛게 되지 않는다.
- **개입 횟수 판정에는 전제가 있다.** 「작업 하나가 사람 개입 두 번으로 끝난다」는 스펙이
  맞다는 전제 위에 서 있다. 스펙이 코드와 어긋나면 방향 결정이 다시 열리고 개입은 얼마든지
  늘어난다 — 세 번째 개입이 승인 쪽이 아니라 **방향 쪽**에서 나왔다.

---

## 2026-09-16 — 검사를 붙인 것과 그 검사가 한 번이라도 돈 것은 겉이 같다

- **환경이 있어야 도는 검사는 게이트의 「실행 강제」 밖에 있다.** 게이트가 돌리는 것은
  `vitest run`(단위)이고 `npm run test:integration` 은 로컬 DB 가 있어야 해서 안 부른다.
  그 틈으로 좋아요 통합 테스트 열한 개가 **한 번도 안 돈 채 커밋까지 갔다.**
- **안 돈 검사는 「없는 검사」보다 나쁘다** — 파일이 있으니 다음 사람이 덮여 있다고 읽는다.
  실제로 그 파일은 한 항목도 실행 가능한 상태가 아니었다(도우미가 한글 이름을 이메일
  앞부분에 그대로 넣어 `beforeAll` 에서 전부 죽었다). 열한 개 전부 죽는 상태와
  열한 개 전부 통과하는 상태는 **돌려 보기 전까지 겉이 같다.**
- **도우미가 한 종류의 입력만 받아 온 것은 「지금까지 통과했다」가 숨긴다.** 통합 테스트
  열일곱 파일이 전부 ASCII 이름을 써서, 한글 이름을 처음 쓴 파일이 그 제약을 처음 밟았다.
  여러 파일이 공유하는 도우미는 **쓰이지 않은 입력 모양**이 곧 안 밟은 지뢰다.
- **환경이 꺼져 있다는 것은 물어봐야 알 수 있다.** Docker 가 꺼진 것은 실패 메시지로
  안 나온다 — 검사를 안 돌렸으니 메시지 자체가 없다. 「돌렸다」의 근거는 실행 명령과
  출력이지 파일의 존재가 아니다.

## 2026-09-17 — 회귀를 고치다가 요청 밖으로 나간다

- **이번 diff 가 만든 회귀는 diff 이전 동작으로 되돌리는 것까지가 범위다.** 그 과정에서
  드러난 기존 결함은 백로그로 보내고, 근본 수정은 별도 요청으로 받는다.
- **가르는 기준은 「diff 이전에도 그랬나」다.** 좋아요가 조회수를 클릭마다 부풀린 사례에서,
  「그리는 코드가 조회수를 올린다」는 구조는 그 전부터 있었다. 이 diff 가 만든 것은 그것을
  **반복 가능하게** 한 것뿐이다 — 되돌릴 대상은 그 반복이지 구조가 아니었다.
  구별하지 않았더니 스펙 하나와 기능 하나가 요청 밖에서 만들어졌다.
- **선택지를 올릴 때 「지금 안 고친다」를 빼지 마라.** 고치는 방법 셋만 올리면 고르는 사람에게
  범위를 좁힐 자리가 없다. 실제로 그렇게 올렸고, 그래서 범위를 좁히는 선택이 아예 논의되지
  않았다.
- **요청 밖으로 나간 것은 목록을 고쳐서 덮지 않는다.** 시작 시점의 항목 목록은 얼어 있고,
  나간 작업은 목록에 없는 이름으로 기록해 기계가 「요청 밖」으로 세게 둔다. 목록에 넣는
  순간 안 나간 것이 되어 버려서, 나갔다는 사실 자체가 어디에도 안 남는다.

## 2026-09-17 — 옆 파일 하나를 레포의 관행으로 착각했다

- **한 파일의 스타일은 근거가 아니다.** 하루 전에 생긴 파일 하나가 식별자를 한글로 썼고,
  그 파일을 이어 작업하면서 새 파일 넷에 같은 방식을 76개 더 퍼뜨렸다. 레포의 나머지
  40여 개 파일은 전부 ASCII 였다 — 세는 데 1분 걸리는 것을 안 세고 눈에 보이는 것을 따랐다.
- **규칙은 이미 있었는데 적용 범위를 좁게 읽었다.** CLAUDE.md 의 "코드에서 쓸 고유명사는
  원어 유지"를 문서 작성 규칙으로만 읽었다. 문서에 쓸 이름을 번역하지 않는 규칙이
  코드에 쓸 이름을 번역해도 된다는 뜻이 될 수는 없다.
- **증상을 고치면 원인이 다음 날 다시 나온다.** 붙여넣은 줄이 깨졌을 때 한글 이름을 없애는
  대신 ASCII 별칭을 하나 더 export 했다. 원인이 그대로 남아 있었고, 그래서 같은 뿌리의
  사고가 사흘에 세 번 났다(통합 테스트 열한 개 사망 · 붙여넣기 깨짐 · 치환 누락으로 게이트 실패).

