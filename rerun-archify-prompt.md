# Phase A 재실행 — 2차 (독립 생성 + 대조)

**왜 다시 하는가**: 1차 결과물([docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) 외 27파일, 커밋 `80660a1`·`7ba317b`)은
인용 221건이 전수 검사를 통과했지만, **검사가 없던 층에서 결함이 나왔다.** 재작성 자체가 목적이 아니라,
같은 입력으로 독립 생성한 2차와 **대조해서 갈리는 지점을 찾는 것**이 목적이다.
갈리는 지점 = 레포에서 나온 사실이 아니라 모델 해석이 들어간 자리다.

**1차를 덮어쓰지 않는다.** 2차는 `docs/v2/` 에 만들고, 대조 후 사람이 최종본을 정한다.

---

## 1차에서 무엇이 새는지 (조항이 없어서 생긴 사고)

| # | 사고 | 왜 안 잡혔나 | 새 조항 |
|---|---|---|---|
| 1 | 인용 링크 203건이 렌더러에서 전부 깨짐 (`docs/stages/scripts/briefing.mjs` 로 해석) | 문서를 한 번도 렌더링·검사하지 않았다 | §R2 링크 기계 검사 |
| 2 | 라이프사이클 그림에서 `거부(status: draft)` 상태가 통째로 빠짐 | 레이아웃 검증을 통과시키려고 뺐고, 뺐다는 사실이 어디에도 안 남았다 | §R3 생략 원장 |
| 3 | 프롬프트가 지시한 문서 이름 6개 중 5개가 이 레포에 없음 (`SPEC.md`·`PLAN.md`·`_cross-cutting.md`·`GLOSSARY.md`·"sealed"·"gate-mode") | 프롬프트의 고유명사를 사실로 취급했다 | §R1 고유명사 검증 |
| 4 | `validate 9/9 통과` 를 품질 근거처럼 보고할 뻔함 | 통과가 무엇을 보장하는지 구분이 없었다 | §R4 검증 축 분리 |
| 5 | 자가검증("다시 확인했습니다")이 근거로 통과됨 | 중단 신호 조항이 *누가* 근거를 대는지 안 정했다 | §R5 자가검증 금지 |
| 6 | 추출 스크립트가 한국어 조사를 파일명으로 읽어 가짜 결함 3건 생성 | 스크립트 출력 자체를 의심하는 단계가 없었다 | §R6 추출기 역감사 |

---

## 원칙 (1차와 동일 — 바꾸지 않는다)

| 표기 | 의미 |
|---|---|
| `[CONFIRMED: path:line]` | 파일에 그렇게 쓰여 있음 |
| `[INFERRED]` | 읽고 추론함. Phase B 검증 대상 |

**등급은 이 둘뿐이다.** `[INFERRED]` 가 많은 게 정상이고 정직한 상태다.

---

## 새 조항 (§R1~§R6) — 모든 단계에 상시 적용

### §R1 고유명사 검증
이 프롬프트에 등장하는 **모든 파일명·디렉터리명·개념 이름은 후보일 뿐이다.**
그리기 전에 레포에서 존재를 확인하라. 없으면:
- 그리지 마라. `[INFERRED]` 로도 채우지 마라.
- **없다는 사실 자체를 findings 항목으로 기록하라.**

### §R2 링크 기계 검사
모든 인용은 **문서 파일 위치 기준 상대경로**로 쓴다.
산출물로 `scripts/check-docs.mjs` 를 만들어라 — 문서의 모든 링크에 대해
① 대상 파일 존재 ② 줄 번호가 파일 범위 내 ③ 렌더러 기준으로 해석되는 경로인지 를 검사한다.
**각 문서 제출 시 이 스크립트의 실행 출력을 함께 보고한다.** 출력 없이 "링크 확인함"은 무효다.

### §R3 생략 원장
노드 수 제한·레이아웃 검증·가독성 때문에 그림에서 **뺀 요소는 전부 기록한다.**
- 각 그림 JSON 의 `cards` 에 "그림에 없는 것" 카드를 필수로 넣는다.
- 별도로 `docs/v2/omissions.md` 에 표로 모은다: `그림 | 뺀 것 | 이유 | 어디에 글로 남았나`.
- **제약 때문에 뺀 것이 문서에서도 사라지면 그건 왜곡이다.**

### §R4 검증 축 분리
`archify validate` 통과는 **레이아웃 통과일 뿐 내용 정확성이 아니다.**
보고할 때 두 축을 분리한다:
- 레이아웃: `9/9 checks, 0 errors` (기계)
- 내용: 노드·엣지가 레포 사실과 맞는지 — **표본 검사 결과로 따로 보고** (아래 §R5)

### §R5 자가검증 금지
"다시 확인했습니다 / 검토했습니다"는 근거가 아니다. 근거로 인정하는 것은 둘뿐이다:
1. 실행한 명령과 그 출력
2. 사용자가 표본을 직접 확인한 결과

각 산출물 제출 시 **표본 5건의 인용을 골라 실제 줄 내용을 출력해 보여준다** (`sed -n '<n>p' <file>`).

### §R6 추출기 역감사
`scripts/extract-harness.mjs` 는 이미 존재한다(결정론적이라 다시 짜도 같은 결과).
다시 짜지 말고 **적대적으로 읽어라**: 이 파서가 틀릴 수 있는 입력이 무엇인가?
최소한 다음을 점검하고 결과를 보고한다 —
한글 조사·전각 문자·글롭·`$변수`·심볼릭 링크·BOM·CRLF·프론트매터 없는 파일·여러 줄 값.
발견하면 스크립트를 고치고, 고친 뒤 재실행한 출력으로 2차를 만든다.

---

## A-0. 준비

```bash
git checkout -b docs/architecture-map-v2
mkdir -p docs/v2/{reference,stages,diagrams}
```

**1차 산출물을 읽지 마라.** `docs/ARCHITECTURE.md` · `docs/findings.md` · `docs/stages/` · `docs/diagrams/` ·
`docs/reference/` 는 이번 실행의 입력이 아니다. 대조는 사람이 A-8 에서 한다.
(읽으면 앵커링되어 대조가 무의미해진다 — 이 프롬프트는 **새 세션에서 실행**하는 것을 전제한다.)

읽어도 되는 것: 레포의 실제 하네스 — `CLAUDE.md`, `graph.mjs`, `gates/`, `scripts/`, `.claude/`, `projects/`.

---

## A-1. 인벤토리 (기존 스크립트 + 역감사)

1. §R6 에 따라 `scripts/extract-harness.mjs` 를 적대적으로 읽고 취약 입력을 점검·보고한다.
2. 출력 경로만 `docs/v2/reference/` 로 바꿔 실행한다 (스크립트에 경로 인자를 추가하거나 복사본을 쓴다 — 1차 산출물을 덮지 않는다).
3. 생성된 마크다운은 전부 `Generated by scripts/extract-harness.mjs. Do not edit by hand.` 로 시작해야 한다.
4. **먼저 레포 구조와 파일 타입별 실물 예시를 보여주고 멈춘다.** 승인 후 진행.

---

## A-2. 정합성 검사 → `docs/v2/findings.md`

1차와 같은 7항목을 검사한다. 추론·추측 금지, 수정 금지 — 목록만.

- 아무것도 참조하지 않는 스킬·서브에이전트·커맨드
- 어떤 단계도 쓰지 않는데 읽는다고 선언된 문서 (유령 참조)
- 쓰이지만 아무도 읽지 않는 문서 (고아)
- 존재하지 않는 파일을 가리키는 훅
- 아무도 세팅하지 않는 상태를 참조하는 게이트 조건
- 설명이 30단어 미만인 스킬 (약한 트리거)
- 같은 요청에 둘 이상이 발화할 만큼 겹치는 스킬 설명

추가 요구:
- **심각도를 매긴다면 그 기준을 먼저 한 문장으로 정의하고, 항목마다 그 기준의 어느 조건에 걸리는지 적어라.**
  (1차는 기준 없이 높음/중간/낮음을 매겼다 — 대조 때 갈릴 1순위다)
- 각 항목은 **기계로 재현 가능한 확인 명령**을 함께 적는다. 재현 불가능하면 `[INFERRED]` 로 표시한다.

---

## A-3. archify

이미 설치돼 있다(`~/code/archify`, `~/.claude/skills/archify` 정션). `node bin/archify.mjs doctor` 로 확인만.

렌더러 제약(1차에서 실측한 것 — 사실이므로 전달한다):
- workflow 컬럼 X 좌표 고정: `[88, 220, 300, 430, 500, 625]`. 같은 레인의 인접 노드는 **28px 이상** 떨어져야 하고, 노드 기본 폭은 92px 다.
- lifecycle: main 레인은 `phaseXs [94,248,402,556,710]`, event/outcome 밴드는 `[402,556,710]`. **blocked·recovery 류 레인은 한 밴드를 공유**하므로 같은 col 을 쓰면 충돌한다.
- dataflow: `rowYs` 5행 고정. 같은 스테이지 쌍의 흐름이 corridor 를 공유하면 `ambiguous-corridor` 가 난다 → `route: "vertical-channel"` + `channelX` 로 분리.
- architecture: `pos`/`size` 자유 배치라 제일 다루기 쉽다.

---

## A-4. 워크플로우 그림 → `docs/v2/diagrams/00-workflow.workflow.json`

```
Use archify to create a workflow diagram of this harness's main execution
path, based on docs/v2/reference/.

Constraints:
- 8 to 12 nodes maximum. If you have more, group them — do not add edges.
- One primary happy path, clearly dominant.
- Mark every gate explicitly as a distinct node shape or lane.
- Put supporting detail in node cards, not in additional edges.

Ground rules:
- Every node must correspond to something in docs/v2/reference/.
- Mark each node [CONFIRMED: path:line] or [INFERRED].
- 노드 라벨의 정본 용어는 graph.mjs 의 노드명이다. (§R1 — GLOSSARY.md 는 이 레포에 없다)
- §R3: 제약 때문에 뺀 노드·엣지·라벨을 전부 기록한다.
- §R4: validate 결과와 내용 정확성을 분리해 보고한다.
- §R5: 노드 태그 5개를 골라 인용한 줄의 실제 내용을 출력한다.
```

---

## A-5. 보조 그림 (한 번에 하나씩, `docs/v2/diagrams/`)

각 그림마다 §R3·§R4·§R5 를 동일하게 적용한다.

1. **문서 흐름** `03-doc-flow.dataflow.json` — 노드는 이 레포에 **실재하는** 문서만(§R1).
   어느 단계가 쓰고 어느 단계가 읽는지, 승인 프론트매터·해시 검증 지점을 구분해 표시.
   A-2 의 고아·유령을 시각적으로 구별한다.
2. **게이트 상태** `02-gate.lifecycle.json` — 상태·전이·복구 경로·종료 상태를
   `graph.mjs` 와 `gates/` 에서만 도출한다. **거부(승인 취소)처럼 사람이 일으키는 전이도 상태로 넣을지
   먼저 판단하고, 빼기로 했다면 §R3 원장에 남긴다.**
3. **계층 구성** `04-layers.architecture.json` — CLAUDE.md·rules·skills·subagents·hooks 5층.
   항상 로드 vs 조건부를 구분하고, 조건부 층마다 **로드 트리거의 근거 파일:줄**을 태그에 넣는다.

---

## A-6. 단계 문서 → `docs/v2/stages/NN-<name>.md`

워크플로우 노드 하나 = 문서 하나. 템플릿은 1차와 동일:

```
# NN. <Stage name>
## Purpose            한 문장: 왜 존재하고 없으면 무엇이 깨지나
## Entry condition    들어가려면 무엇이 참이어야 하나 + 강제하는 파일:줄
## What it does       번호 매긴 단계
## Skills and tools   | name | when | evidence |   ← [CONFIRMED: path:line] 또는 [INFERRED]
## Documents read     | document | purpose | required? |
## Documents written  | document | ownership | consumed by |
## Gate               조건 · 통과 경로 · 실패 경로
## Failure path       완료 못 할 때 제어가 어디로 가나
## Exit condition     넘어가려면 무엇이 참이어야 하나
## Unverified         파일에서 확인 못 한 것. 완성돼 보이려고 비워두지 마라
```

- **한 장을 먼저 쓰고 멈춘다. 절대 일괄 생성하지 않는다.**
- 각 장 제출 시 `check-docs.mjs` 출력(§R2)과 인용 표본 5건(§R5)을 함께 낸다.

---

## A-7. 진입점 → `docs/v2/ARCHITECTURE.md`

150줄 이내. 워크플로우 그림 링크, 하네스가 푸는 문제 한 문단, 단계 표,
다른 그림 링크(각 한 줄), `Known gaps`(findings 상위), `Unverified claims`(단계 문서의 `[INFERRED]` 전량),
그리고 **`Omissions`(§R3 원장 요약)**.

---

## A-8. 대조 (이번 실행의 핵심 — 여기서 값어치가 나온다)

1차와 2차를 기계로 비교하고 `docs/v2/diff-report.md` 를 쓴다.

비교 축 4개:
| 축 | 무엇을 비교 | 갈리면 의미 |
|---|---|---|
| 인용 집합 | 두 결과가 인용한 `파일:줄` 의 합집합/차집합 | 한쪽에만 있는 인용 = 근거 선택이 자의적이었던 자리 |
| findings | 항목 ID·내용·심각도 | 한쪽에만 있는 항목 = 놓쳤거나 오탐. 심각도 불일치 = 기준 부재 |
| 그림 노드·엣지 | 노드 집합, 뺀 것 원장 | 다르면 "무엇이 중요한가"가 해석이었다는 뜻 |
| 단계 경계 | 문서 개수·이름·경계 | 다르면 단계 구분이 임의적이었다는 뜻 |

- **갈린 항목만** 근거를 다시 확인해 어느 쪽이 맞는지 판정한다(둘 다 틀릴 수도 있다).
- 일치한 항목은 재확인하지 않는다 — 독립 생성 2회가 같은 결론을 냈다는 게 근거다.
- 최종본을 어느 쪽으로 확정할지는 **사용자가 정한다.** 임의로 병합하지 않는다.

---

## 중단 신호

에이전트가 **"이 단계는 아마 이렇게 동작할 것"** 이라고 매끄럽게 쓰기 시작하면 멈춘다.
의심되면:

```
Re-read the last document you wrote. For every claim, give me the exact file
path and line number that supports it, and print the actual content of those
lines. If you cannot produce one, move that claim to the Unverified section.
```

§R5 에 따라, 이 요청의 답으로 "확인했습니다"만 오면 그것도 실패다 — **출력이 없으면 근거가 아니다.**

---

## 완료 기준

- [ ] §R6 추출기 역감사 결과가 보고됨 (취약 입력 목록 + 고쳤으면 무엇을)
- [ ] `docs/v2/reference/` 생성, `scripts/check-docs.mjs` 가 링크 0건 실패로 통과
- [ ] `docs/v2/findings.md` — 심각도 기준이 먼저 정의되고, 항목마다 재현 명령이 붙음
- [ ] 그림 4장이 `docs/v2/diagrams/` 에 생성 + `docs/v2/omissions.md` 원장 존재
- [ ] 단계 문서 전부, 각 장이 표본 5건 인용 출력과 함께 제출됨
- [ ] `docs/v2/ARCHITECTURE.md` 150줄 이내
- [ ] **`docs/v2/diff-report.md` — 1차와 갈린 지점 목록** ← 이번 실행의 실질 산출물
